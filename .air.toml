root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/mentat"
  cmd = "go build -o ./tmp/mentat ./cmd/mentat"
  delay = 1000
  exclude_dir = ["tmp", "ui", "node_modules", ".git", ".agentrc"]
  exclude_regex = ["_test.go"]
  include_ext = ["go", "sql"]
  kill_delay = "0s"
  send_interrupt = false
  stop_on_error = true

[build.args]
  bin = ["serve", "--dev", "--port", "8090"]

[log]
  time = false

[misc]
  clean_on_exit = true
