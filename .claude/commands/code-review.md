Interactive code review — run quality checks, present findings with per-finding actions (accept, fix now, create task) using AskUserQuestion structured dialogs.

Read the full skill instructions at `.claude/skills/code-review.md` and follow them exactly.

Arguments: $ARGUMENTS

If no scope is specified, review uncommitted changes.
