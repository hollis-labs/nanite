Interactive drift resolution — scan portfolio for configuration drift, present findings with auto-fix options using AskUserQuestion structured dialogs, and execute selected fixes.

Read the full skill instructions at `.claude/skills/drift-resolve.md` and follow them exactly.

Arguments: $ARGUMENTS

If no project is specified, scan all projects in repos.yaml.
