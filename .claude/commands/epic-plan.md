Interactive epic planning — show an epic's sprints with execution strategy options, dependency previews, and sprint activation using AskUserQuestion structured dialogs.

Read the full skill instructions at `.claude/skills/epic-plan.md` and follow them exactly.

Arguments: $ARGUMENTS

Requires an epic_id argument. Use --project to override the default project.
