Interactive git cleanup — inventory worktrees and stale branches, cross-reference with Volon, present findings with per-item actions using AskUserQuestion structured dialogs.

Read the full skill instructions at `.claude/skills/git-cleanup.md` and follow them exactly.

Arguments: $ARGUMENTS

If no project is specified, scan all projects in repos.yaml.
