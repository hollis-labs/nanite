Run the git-status-all skill: read `config/repos.yaml` to get all managed project paths, then for each project run `git rev-parse --abbrev-ref HEAD`, `git status --porcelain | wc -l`, `git log --oneline @{u}..HEAD 2>/dev/null | wc -l`, and `git log -1 --format="%cr"`. Present results as a markdown table with columns: Project, Branch, Dirty Files, Unpushed Commits, Last Commit. After the table, highlight any projects with dirty files > 0, unpushed > 0, or branch != main.
