Run the pcc-refresh-all skill. For each project in `config/repos.yaml` that has a `.agentrc/pcc/global/` directory, refresh the PCC files from source, then sync to Cortex via MCP. Use `--projects <id1,id2>` to limit to specific projects.

$ARGUMENTS
