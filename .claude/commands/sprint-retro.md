Interactive sprint retrospective — analyze velocity, present keep/drop/try items for voting using AskUserQuestion structured dialogs, create tasks for adopted experiments.

Read the full skill instructions at `.claude/skills/sprint-retro.md` and follow them exactly.

Arguments: $ARGUMENTS

Requires a sprint_id argument. Use --project to override the default project.
