Run the standup skill. Generate a standup report from the last 24 hours (or `--hours N`) covering: completed tasks (from Volon via `volon_tasks_list`), in-progress tasks, blockers, and git commits across all managed projects. Use `--format full` for detailed output with commit messages and file changes.

$ARGUMENTS
