Interactive batch triage of unassigned tasks — present tasks with per-task options (assign, defer, archive) using AskUserQuestion structured dialogs, then execute bulk operations.

Read the full skill instructions at `.claude/skills/task-triage.md` and follow them exactly.

Arguments: $ARGUMENTS

If no project is specified, default to project_id="mentat".
