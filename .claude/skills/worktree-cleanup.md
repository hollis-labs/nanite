# Worktree Cleanup

**This skill has been superseded by `/git-cleanup`.** Use `/git-cleanup` instead.

The `/git-cleanup` skill provides everything this skill did, plus:
- Agent branch detection and deletion
- Volon task status cross-referencing
- Stale branch detection (7+ day inactivity)
- Remote reference pruning
- Comprehensive safety invariants

See `.claude/skills/git-cleanup.md` for the full procedure.
