name: Content Writer
slug: content-writer
profile_slug: content-writer
lifecycle_class: template
provider: anthropic
# Left blank on purpose (follow-up sweep to CW-20260817, see
# loom-curator.yaml for the full rationale) — store.ResolveProviderAndModel
# resolves an empty instance Model to the runtime default at request time.
# The previous pinned "claude-sonnet-4-20250514" snapshot was retired by
# Anthropic and every wake turn 404'd until cleared.
model: ""
runtime_kind: api
launch_source_type: task_template_run
metadata:
  scope: glyph-brief-or-draft
  one_piece_per_run: "true"
