# Remove `tool_permissions`/`CheckPermission` entirely; collapse all agent-tool-permission surfaces to `agent_tools`-only

**Status:** implemented
**Depends on:** `TASKS/adhoc/01-eliminate-file-based-agent-runtime.md` (landed and validated 2026-08-19 — see `TASKS/INDEX.md`'s "Ad hoc tasks" section and that task's own Work Log)
**Touches:** `internal/toolclient/permissions.go`, `internal/toolclient/broker.go`, `internal/service/agent_permissions.go` (delete), `internal/service/tool.go` (`SelectForAgent`), `internal/service/tool_execution_rules.go` (`enforceExecutionRulesViaAgentTools`), `internal/api/tools.go` (`handleListAgentTools`), `internal/store` (`agent_profiles.tool_permissions` column — see step on schema below)

**Scope note (2026-08-19, Orchestrator, post-`01` landing):** `01`'s own implementation already removed surface 4 (`internal/api/agent_tools.go`'s `requireRealAgentToolsTarget`) in full — it had no independent logic beyond the now-deleted `agent.IsFileBasedID` check, so it was correctly swept as part of `01`'s own call-site cleanup rather than left for this task. `01` also already left `internal/service/agent_permissions.go`'s `newFileAgentPermissionResolver` stubbed to a permanent no-op (unwired from `container.go`, no longer callable) but did **not** delete it, per its own task file's explicit instruction to leave full deletion to this task. This task's remaining scope is therefore: surfaces 1-3 below, deleting `agent_permissions.go` and the `tool_permissions`/`CheckPermission` machinery itself, and the schema question in step 6. Step 4 below (re-examining `requireRealAgentToolsTarget`) is done — nothing left to do there.

## Context

Discovered and explained (imprecisely, then corrected) during this session's own Phases 2-5 wrap-up: `tool_permissions` is a JSON column on `agent_profiles` (`toolclient.ToolPermissions{AllowList, DenyList, MaxCallsPerTurn, ...}`, checked via `ToolClient.CheckPermission`) — a **deny-only** model where an empty allow-list means everything is allowed. `agent_tools` (migration `116_known_tools_and_agent_tools.sql`, Phase 1 #04) is the newer, **opt-in** model — a tool is allowed only if a grant row exists. The two coexisted because file-based agents (no real `agent_profiles` row reachable the normal way) needed *some* permission mechanism, and `tool_permissions` was it.

Once `TASKS/adhoc/01-eliminate-file-based-agent-runtime.md` lands, there is no longer any population that needs `tool_permissions` — every agent is a real DB row reachable via `store.GetAgent`, so `agent_tools` covers 100% of agents, not "most of them, plus a legacy fallback for the rest."

### The four surfaces that currently branch on `dbAgent := store.GetAgent(agentID)` succeeding-or-failing to decide `agent_tools` vs. legacy `tool_permissions`

1. `internal/service/tool.go`'s `SelectForAgent` (fixed to prefer `agent_tools` in Phase 4 `#05`) — still has a legacy fallback branch for when `GetAgent` fails.
2. `internal/service/tool_execution_rules.go`'s `enforceExecutionRulesViaAgentTools` (Phase 5 `#01`) — same shape.
3. `internal/api/tools.go`'s `handleListAgentTools` (Phase 5 `#10`) — same shape.
4. `internal/api/agent_tools.go`'s `requireRealAgentToolsTarget` (Phase 5 `#01`) — doesn't fall back, it hard-*rejects* grant/revoke calls against an agent `GetAgent` can't find, with a 400. Once `01` lands, `GetAgent` should never fail for a real agent again, so this guard becomes dead weight (though verify — it may still be legitimate as a "does this agent ID exist at all" check, just no longer needing the file-based-specific framing).

Also delete `internal/service/agent_permissions.go`'s `newFileAgentPermissionResolver` in full — it's a `toolclient.PermissionResolver` built exclusively for file-based IDs (gated on `agent.IsFileBasedID`), unreachable dead code once `01` lands. Find and remove wherever it's wired into `ToolClient`'s construction.

## What to do

1. Confirm `01` has actually landed and verify its own "Done means" bar holds (a quick `grep -rn "IsFileBasedID" --include="*.go" internal/agent internal/service internal/api internal/messaging` should return nothing) before starting — if it doesn't, stop, this task isn't safe to run yet.
2. Delete `internal/service/agent_permissions.go` (`newFileAgentPermissionResolver`) and its wiring into `ToolClient`.
3. Remove the legacy-fallback branch from all three of surfaces 1-3 above — each should become unconditionally `agent_tools`-based, no `dbAgent`-succeeds-or-fails split left (since it always succeeds now).
4. Re-examine `requireRealAgentToolsTarget` (`internal/api/agent_tools.go:158`) — decide whether it should be deleted outright (if `GetAgent` failing is now a genuine "agent doesn't exist" 404 case handled elsewhere) or simplified (kept as a not-found check, but with the file-based-specific comment/framing removed). Document your reasoning either way.
5. Remove `toolclient.CheckPermission`/`GetPermissions`/`ToolPermissions`/`ParsePermissions` and the `tool_permissions` JSON parsing path from `internal/toolclient/` if nothing else references them after steps 2-4 — confirm via grep before deleting, don't assume.
6. **Schema**: decide whether to actually `DROP COLUMN tool_permissions` from `agent_profiles` in this task, or leave the column in place (dead, unread) for now and drop it in a later, separate migration once you're confident nothing — including any external tooling, export/import paths, or admin UI — still reads or writes it. Recommendation: leave the column in place for this task (safer, reversible) unless your own investigation turns up a clean, low-risk path to drop it now; note your decision and reasoning in the Work Log either way. Also check for a `toolPermissions:` YAML frontmatter field on any remaining agent-file-adjacent code paths and remove references there too.
7. Live-verify against the real deployed `nanite-api-service`: create a fresh test agent, confirm zero tools allowed before any `agent_tools` grant (not "everything allowed," which was the old deny-only default), grant one tool, confirm exactly `{granted tool} + always_included` is allowed via all three read/enforcement surfaces. Clean up the test agent after.

## Done means

- Zero remaining references to `tool_permissions`/`CheckPermission`/`GetPermissions`/`ToolPermissions`/`newFileAgentPermissionResolver` in non-test Go code (confirm via grep).
- All three permission-surfaces (`SelectForAgent`, `enforceExecutionRulesViaAgentTools`, `handleListAgentTools`) are unconditionally `agent_tools`-based, no legacy branch remains.
- `requireRealAgentToolsTarget` either removed or simplified per step 4, with reasoning documented.
- `go build ./cmd/nanite/`, `go vet ./...` (no new findings beyond the 2 pre-existing `container.go` ones), `go test ./...` all green.
- Live dogfeed per step 7, documented in the Work Log.

## Out of scope

- Anything already covered by `TASKS/adhoc/01-eliminate-file-based-agent-runtime.md` — this task assumes that one is done.
- The broader agent-roster audit/cull — `TASKS/phase-2/07-audit-agent-roster.md`.

## Work log

**Pre-flight (per this task's own step 1).** `grep -rn "IsFileBasedID" --include="*.go" internal/agent internal/service internal/api internal/messaging` returned only comment/docstring references (`internal/agent/convert_test.go`'s "this was removed" note, `internal/service/agent_permissions.go`'s own doc comment explaining why it's a no-op) and `internal/service/skill.go`'s three hits, which are a *different*, unrelated `skill.IsFileBasedID` (the skills subsystem's own independent file-based concept, explicitly out of scope per `01`'s own Work Log) — zero live agent-runtime `agent.IsFileBasedID` call sites. `01`'s "Done means" bar holds; safe to proceed.

**1. `internal/service/agent_permissions.go` deleted outright.** The stubbed `newFileAgentPermissionResolver` (permanent `return nil`, unwired from `container.go` by `01`) is gone; `container.go`'s neighboring comment block (~line 487) rewritten to explain the full removal rather than "left to adhoc/02."

**2/3. Surfaces 1-3 collapsed to unconditional `agent_tools`.**
- `internal/service/tool.go`'s `SelectForAgent`: the `if dbAgent != nil { filterToolsByAgentTools } else { filterToolsByPermissions }` split is gone — `filterToolsByAgentTools(ctx, s.agentToolsStore(), agentID, allTools)` now runs unconditionally, keyed on `agentID` directly rather than gated on a successful `dbAgent` lookup (its own internal `st.ListAgentToolNames` call degrades to "no grants" — i.e. fail-closed — for a genuinely unknown ID, which is the correct outcome, not the old permissive fallback). `filterToolsByPermissions` (the whole function) deleted.
- `internal/service/tool_execution_rules.go`'s `enforceExecutionRules`: the `if s.store.GetAgent(agentID) succeeds { agent_tools path } else { legacy Tools-allowlist + ToolPermissions.CheckPermission }` split is gone. The redundant second `s.store.GetAgent` lookup is also gone — since `agentServiceImpl.Get()` (used by the first lookup) is now a pure `s.agents.GetAgent(id)` passthrough post-`01`, it and `s.store.GetAgent` are equivalent, so a single successful lookup is enough before calling `enforceExecutionRulesViaAgentTools` unconditionally. `encoding/json` and `toolclient` imports dropped (both were only used by the deleted legacy branch).
- `internal/api/tools.go`'s `handleListAgentTools`: the `dbAgent bool` split and the `else { perms := a.Services.ToolClient.GetPermissions(agentID); ... }` branch are gone — `granted`/`always` maps are now built unconditionally (empty maps, i.e. `allowed:false` for everything, when `a.Services.Store` is nil or the agentID doesn't resolve).

**4. `requireRealAgentToolsTarget`** — confirmed via the Scope note and a direct read of `internal/api/agent_tools.go`: `01` already deleted it in full (its own comment block at the old call site documents this). Nothing left to do.

**5. `toolclient.CheckPermission`/`GetPermissions`/`ParsePermissions`/`ToolPermissions`/`PermissionResolver`/`DefaultMaxCallsPerTurn` deleted from `internal/toolclient/permissions.go` and `broker.go`** — but this required more than a blind delete-and-see-what-breaks. Following this task's own step-5 instruction ("confirm via grep before deleting, don't assume"), a grep for real call sites beyond the three named surfaces found two the task's Context section didn't name:
  - `internal/toolclient/broker.go`'s own `SelectToolsAsProvider`, `CallTool`, and `HandleRequestToolsForAgent` all called `tb.CheckPermission(agentID, ...)` **internally**, inside the `toolclient` package itself, not just from the three outer service/api surfaces.
  - Tracing `CallTool`'s actual callers found it is the **sole** execution-time gate for two real production paths that never call `enforceExecutionRulesForTool` first: `internal/service/chat_reflex_dispatch.go`'s `task_execute` dispatch (line ~329) and `internal/service/workflow_step_executor.go`'s tool/LLM step execution (lines ~110, ~239) — both call `ToolService.Execute` → `callTransport` → `s.toolClient.CallTool` directly. Deleting `CheckPermission` from `CallTool` with no replacement would have left these two paths with **zero** tool-permission enforcement at all — a real security regression, not a legacy-cleanup no-op.
  - Given the task's own title ("collapse **all** agent-tool-permission surfaces to `agent_tools`-only") and its `Touches` list explicitly naming `broker.go`, the correct move was to replace, not just delete: added `ToolClient.isToolGrantedToAgent(ctx, agentID, toolName) bool` (agent_tools + `known_tools.always_included`, nil-`Store`-permissive/fail-closed-once-wired, mirroring `filterToolsByAgentTools`/`enforceExecutionRulesViaAgentTools`'s own precedent) and swapped it in at `CallTool` (the load-bearing backstop, doc comment rewritten to name the two callers that depend on it) and `HandleRequestToolsForAgent` (which gained a `ctx context.Context` first parameter — zero production callers existed to migrate, only tests, confirmed via grep before the signature change). `SelectToolsAsProvider`'s internal per-tool check was **removed with no replacement** (not swapped) — its own sole caller, `SelectForAgent`, already applies the same `filterToolsByAgentTools` unconditionally right after it returns (per items 2/3 above), so the internal check was pure redundant DB-query overhead once the caller-side split was removed, not a coverage gap.
  - Wrote three new tests in `internal/toolclient/agent_tools_permission_test.go` (`TestCallTool_AgentToolsDeniesUngrantedTool`, `TestCallTool_FreshAgentZeroToolsAllowedBeforeAnyGrant`, `TestHandleRequestToolsForAgent_AgentToolsDeniesUngrantedInnerName`) as the direct replacement for the deleted `internal/service/tool_permissions_render_test.go`'s `TestCallTool_DefenseInDepthDeniesBypassedTool` — same load-bearing assertion (a bypassed/ungranted tool is denied at the deepest layer regardless of what selection-time filtering did), new mechanism.
  - `MatchPattern`/`ArgsContainEscalationPattern`/`valueContainsEscalation` kept in `permissions.go` — both still have real, independent live callers (`known_tools_backfill.go`'s pattern matching for `MatchPattern`; `broker.go`'s traversal-arg safety net for `ArgsContainEscalationPattern`) unrelated to the `tool_permissions` JSON mechanism.

**Additional real reference found and reconciled (also per step 5's grep instruction):** `internal/service/known_tools_backfill.go`'s `legacyGrantCandidates` — the one-time-per-agent boot backfill that seeds a brand-new agent's *initial* `agent_tools` grants from its `tools`/`role_tools`/`tool_permissions` columns — called `toolclient.ParsePermissions(agent.ToolPermissions)`/`.CheckPermission(name)` to narrow its pattern-matched candidate set. Live-DB check (`~/.local/share/nanite/workspaces/default/main.db`, read-only copy) confirmed all 35 existing agents already have a row in `agent_tools_legacy_backfill` (the one-time-run marker), so this narrowing step has been inert for every *existing* agent since long before this task — it would only fire for a brand-new agent created going forward. Since `tool_permissions` is retired everywhere else, silently keeping this one backfill path as the last live consumer of a `deny_list` would be inconsistent, not conservative — removed the narrowing stage entirely; `legacyGrantCandidates` now returns the raw tools/role_tools pattern-matched set unnarrowed. Regression test `TestBackfillAgentToolsFromLegacyColumns_DenyListExcludesTool` (asserted the old, now-wrong behavior) replaced with `TestBackfillAgentToolsFromLegacyColumns_ToolPermissionsNoLongerNarrows` (asserts the deny_list is ignored).

**6. Schema decision: column kept in place, not dropped.** Followed the task's own recommendation. Reasoning, beyond "safer/reversible":
  - Live-DB check found 14 of 35 real seeded agents (`worker`, `researcher`, `analyst`, `file-backend`, `backend`, `background-job`, `code-auditor`, `reviewer`, `system-architect`, `project-manager`, `task-planner`, `orchestrator`, `conductor`, `loom-curator`) still carry non-trivial, real historical `tool_permissions` JSON — dropping the column would destroy that data with no way to recover it, for zero functional gain (nothing reads it either way once this task lands).
  - The column has a live, ongoing writer that is out of scope to also retire here: the REST agent create/update DTOs (`internal/api/agents.go`, `internal/api/agent_builder.go`, `internal/api/types.go`) still accept/return `tool_permissions` as a plain passthrough field — dropping the column would be a breaking API/schema change requiring its own dedicated task (updating those DTOs, a migration, and — per this repo's schema-migration convention — testing against a real backed-up DB copy), not a drive-by inside this one.
  - `agent_profiles.tool_permissions` is also read by an entirely separate, still-live mechanism unrelated to `tool_permissions`/`CheckPermission` enforcement: `internal/service/role_cascade.go`'s `AgentOverrideConfig`/`RoleOverrideConfig` decode it as a raw `map[string]any` into the role→agent→task override cascade's `Permissions` field, which flows into `ctxpkg.SlotPermissions` — a context-broker slot-content concern (permission *visibility* in the assembled prompt, one of the six invariants `internal/context/INVARIANTS.md` documents), not access control. This is a legitimate, independent consumer of the column's raw JSON value that this task does not touch (out of scope — it isn't part of the `tool_permissions`/`CheckPermission` machinery named for removal) and that dropping the column would also break.
  - Given all three of the above, "leave the column in place, dead, unread for access control" was both the recommended and, on investigation, the clearly correct call — nothing turned up a clean, low-risk path to drop it now.
  - What *did* change: everything that used to derive meaning from the column's content for tool-selection/execution purposes now treats it as inert. `internal/agent`'s `Definition.ToProfile()` always writes `"{}"` now (frontmatter can no longer populate it — see next item). `internal/agentvalidation/validation.go`'s malformed-JSON/empty-allow_list/glob-pattern/"permissive+no-MCP-servers" validation and warnings for this column were removed in full (`isPermissionsPermissive` deleted) — validating or warning about the shape of a column nothing reads for access control anymore was validating dead data, not protecting an operator from anything real; `validateGlobPattern` itself is kept (still exercised by the live v2 `tools` column). `internal/runtime/agent/sandbox_content_claude.go`'s `BuildAgentContext` no longer renders a "## Tool Permissions" section into a launched CLI agent's boot content — continuing to present that JSON as if it reflected real access control would have been actively misleading, not merely dead weight. `internal/store/agents.go`'s `ToolPermissions`/`Tools` field doc comments rewritten to describe the actual current state (inert vs. still-read-by-the-backfill, respectively) instead of the stale "read at every SelectForAgent call" claim.
  - Also swept, per step 6's explicit instruction: the `toolPermissions:` YAML frontmatter field. `internal/agent/parser.go`'s `Definition.ToolPermissions *AgentToolPermissions` field and the `AgentToolPermissions` struct itself deleted; `internal/agent/managed_files.go`'s `managedFileFrontmatter.ToolPermissions` field (both the read-back-into-frontmatter struct field and its `WriteManagedAgentProfile` marshal line) deleted — a managed agent `.md` file written back out after an API edit no longer echoes a `toolPermissions:` block.
  - `internal/api/agent_tools.go`'s `handleGrantAgentTool` also lost its `matchingDenyPattern`/409-Conflict pre-flight check: that guard existed *specifically* to protect against the deeper `SelectToolsAsProvider`/`CallTool` `tool_permissions.deny_list` backstop silently vetoing a fresh grant (`TASKS/phase-4/05`'s Work Log item 3) — since that backstop is now agent_tools-based (item 5 above), there is no deeper mechanism left for a stale `deny_list` to conflict with, so the guard's entire justification is gone. `matchingDenyPattern` deleted. Replacement test `TestHandleGrantAgentTool_SucceedsDespiteStaleDenyList` asserts the same stale-deny_list setup now succeeds (201) instead of 409.

**Test fallout** (compile/behavior breaks from removing `ToolPermissions`/`CheckPermission`/`ParsePermissions`/`PermissionResolver`/`AgentToolPermissions`, found via `go vet ./...` + `go test ./...`, not by grep alone):
- `internal/toolclient/permissions_test.go`, `internal/toolclient/broker_file_agent_test.go` — deleted in full (every test in both files exercised only the removed `ToolPermissions`/`ParsePermissions`/`GetPermissions`/`PermissionResolver` symbols).
- `internal/toolclient/broker_permissions_test.go` — the three direct `ToolPermissions{}.CheckPermission(...)` tests deleted; `TestSelectToolsAsProvider_BuiltinFilteredThroughPermissions` renamed/rewritten to `TestSelectToolsAsProvider_BuiltinsPassThrough` (asserts pass-through, not a permission check that no longer happens there); `HandleRequestToolsForAgent` call sites updated for the new `ctx` parameter.
- `internal/service/tool_permissions_render_test.go` — deleted in full (every test exercised the removed `PermissionResolver`/`ToolPermissions` path at the service layer); its one security-critical assertion (`TestCallTool_DefenseInDepthDeniesBypassedTool`) has a direct replacement at the toolclient layer (see item 5 above — arguably the more precise location for it, since that's literally where the check now lives).
- `internal/service/known_tools_backfill_test.go` — `TestBackfillAgentToolsFromLegacyColumns_DenyListExcludesTool` replaced (see above).
- `internal/agentvalidation/validation_test.go` — `TestMalformedToolPermissionsJSON`, `TestEmptyAllowList`, `TestNoMCPServersPermissivePermissions`, `TestInvalidGlobPattern`, `TestEmptyPermissionsWithMCPServers`, `TestDenyListWithInvalidGlob` replaced by a single `TestToolPermissionsNotValidated` (asserts malformed `tool_permissions` JSON no longer produces any error/warning); `TestPrefixGlobIsValid` rewritten to exercise `validateGlobPattern` via the still-live `tools` column instead (a new `TestToolsInvalidGlobPattern` added alongside it for negative-case coverage, since removing the `tool_permissions`-based glob tests would otherwise have left `validateGlobPattern` with zero direct test coverage at all).
- `internal/agent/permissions_test.go` — `TestParseMD_ToolPermissions`, `TestDefinition_ToProfile_PrefersToolPermissionsFrontmatter`, `TestDefinition_ToProfile_FallsBackToToolsAllowList` deleted (frontmatter field gone).
- `internal/agent/convert_test.go` — `TestDefinition_ToProfile`'s "tool permissions derived from tools" assertion block replaced with a straight `p.ToolPermissions == "{}"` check (matches `TestDefinition_ToProfile_Minimal`'s existing, unchanged assertion).
- `internal/api/tools_test.go` — `TestHandleListAgentTools_FileBasedAgentUnchanged` (asserted the now-deleted legacy-fallback behavior for an unresolvable agentID) replaced with `TestHandleListAgentTools_UnknownAgentDeniesAll` (asserts fail-closed: every tool `allowed:false`).
- `internal/api/agent_tools_test.go` — `TestHandleGrantAgentTool_RejectsStaleDenyListMatch` replaced with `TestHandleGrantAgentTool_SucceedsDespiteStaleDenyList` (see item 6 above).

**Left deliberately untouched, with reasoning:**
- `internal/store/migration_063_populate_role_prompts_test.go` — still references `toolclient.ToolPermissions`/`CheckPermission`/`MatchPattern` in prose comments and reimplements a minimal local matcher (by design — the `store` package cannot import `toolclient`, an existing constraint predating this task). It asserts the literal `tool_permissions` JSON seeded by migration 063 for the `analyst` role (`{"deny_list":["*"]}`) hasn't drifted — a legitimate migration/seed-data regression test independent of whether anything downstream enforces that JSON, so left as-is; nothing in it references a removed Go symbol.
- `internal/service/role_cascade.go` (`AgentOverrideConfig`/`RoleOverrideConfig` decoding `tool_permissions` into the slot-visibility `Permissions` cascade) — a distinct, still-live mechanism explained under item 6 above; not part of this task's scope.
- `internal/api/agents.go`/`internal/api/agent_builder.go`/`internal/api/types.go`'s plain CRUD passthrough of the `tool_permissions` field on the agent create/update REST DTOs — legitimate given the schema decision to keep the column; breaking that API contract would be its own task.

**Live verification (task step 7 / Done-means bullet 5).** Built the binary directly from this worktree (`go build -o .../nanite-scratch ./cmd/nanite/`), copied `~/.local/share/nanite/workspaces/default/main.db` to a scratch file (never touching the original or the live `nanite-api-service`), and booted the scratch binary against the copy on a spare port (8099):
- `POST /api/agents` (fresh test agent, no `tool_permissions`/`tools`/`agent_tools` set) → created successfully with `tool_permissions:"{}"` (confirms `Definition.ToProfile`'s ingest-time write path only applies to file-discovered agents, but the REST-create path's own default is also `"{}"` — consistent).
- `GET /api/agents/{id}/tools` **before any grant** → 307 catalog tools, exactly 2 unique names `allowed:true` (`tool_describe`, `tool_list` — the `known_tools.always_included` escape hatch), zero others — **not** "everything allowed," confirming the deny-only `tool_permissions` default-permit behavior this task retires is really gone.
- Looked up `dev_read`'s real `known_tools.id` (read-only `sqlite3` query against the same scratch DB file while the server ran, standard SQLite concurrent-reader behavior) and granted it via `POST /api/agents/{id}/tools`.
- `GET /api/agents/{id}/tools` **after the grant** (surface 3, `handleListAgentTools`) → exactly `{dev_read, tool_describe, tool_list}` allowed, nothing else — `{granted} + always_included`, exactly as the Done-means bar specifies.
- For surfaces 1/2 (`SelectForAgent`, `enforceExecutionRulesViaAgentTools`), which aren't directly HTTP-exposed with per-agent granularity outside the real chat/LLM loop, wrote a throwaway `internal/service/zz_live_verify_task02_test.go` (env-var-gated, skipped unless `NANITE_LIVE_VERIFY_DB`/`NANITE_LIVE_VERIFY_AGENT` are set, never committed, deleted immediately after use) that opened the **same** copied-production-DB file directly via `store.New` and called the real, unexported production functions against the real grant state the HTTP call above had just written:
  - `SelectForAgent(ctx, session, agentID, "read a file", "", 0)` → returned exactly `[dev_read, request_tools]` (`request_tools` being the always_included escape hatch synthesized outside the registered-builtins loop — didn't register it as a builtin in this harness, so it's the one escape-hatch tool visible here instead of `tool_describe`/`tool_list`); a registered-but-ungranted `dev_write` builtin was correctly absent.
  - `enforceExecutionRules(ctx, agentID, "dev_read")` → allowed. `enforceExecutionRules(ctx, agentID, "dev_write")` → denied, reason `"tool not in agent's allowed tool set"`. `enforceExecutionRules(ctx, agentID, "tool_describe")` → allowed (always_included).
- Cleaned up: `DELETE /api/agents/{id}` (confirmed 404 on a follow-up `GET`), killed the scratch server process, deleted the scratch DB file/binary/log and the throwaway test file. The real deployed `nanite-api-service` was never started, stopped, or touched.

**Baseline (final, worktree HEAD).**
- `go build ./cmd/nanite/` — pass.
- `go vet ./...` — pass except the same 2 pre-existing `internal/service/container.go` `stopReaper`/`stopRuntimeReaper` "possible context leak" findings documented in prior tasks' Work Logs (confirmed same line numbers/function names, unrelated to this change).
- `go test ./... -count=1` — all packages pass, including a fresh (non-cached) targeted re-run of every package touched by this task (`internal/service`, `internal/toolclient`, `internal/api`, `internal/agent`, `internal/agentvalidation`, `internal/store`, `internal/runtime/agent`).
- Final grep sweep: `grep -rln "tool_permissions\|CheckPermission\|GetPermissions\|ToolPermissions\|newFileAgentPermissionResolver" --include="*.go" . | grep -v "_test.go"` still matches files, but every hit is either (a) a historical/explanatory comment, or (b) the still-live, still-kept `store.AgentProfile.ToolPermissions` string field name (the schema-decision column) and its legitimate CRUD/cascade/seed-data consumers named above. A stricter sweep for the actual removed symbols (`toolclient.ToolPermissions`/`.CheckPermission(`/`.GetPermissions(`/`.PermissionResolver`) confirmed zero live-code hits, comments only.

No escalations. Everything found during investigation (the `broker.go` internal `CheckPermission` call sites, the `known_tools_backfill.go` narrowing dependency, the `agent_tools.go` deny-list-conflict guard, the misleading sandbox boot-content section) was squarely within this task's own stated scope (`Touches` list explicitly names `broker.go`; the task title says "collapse **all** agent-tool-permission surfaces") once actually traced — none of it contradicted another active task or required a stop, per step 5's own "confirm via grep before deleting, don't assume" instruction and the operating principle that finding a bigger real job than it looked means doing the full job, not stopping to ask.
