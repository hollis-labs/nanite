# Collapse the three `ENVELOPE_DATA` marker-extraction implementations into one

**Phase:** 2 — Telemetry, consumer cleanup, worked example (`TASKS/harness-reactive-self-tools`)
**Status:** reviewed
**Depends on:** none. Not required for `04-render-card-construction.md`'s render_card path to work — the three consumers below are already marker-agnostic and pick up a correctly-formatted marker regardless of which code produced it (see `04`'s own Context for why). This task is a separable DRY cleanup the design doc flags, not a functional prerequisite for anything else in this batch. Can land before, after, or in parallel with the rest of Phase 1/2.
**Touches:** `internal/service/chat_tool_executor.go` + `internal/service/chat_generate.go` (`captureEnvelopeData`, `internal/service/chat_generate.go:3049`), `internal/api/tools_call.go` (`extractEnvelopeMarker`, `internal/api/tools_call.go:129`), `internal/mcpserver/handlers.go` (`convertEnvelopeMarkers`, `internal/mcpserver/handlers.go:30`) — a new shared location for the collapsed function (package TBD, see step 1).

## Context

`docs/engineering/architecture/11-harness-reactive-self-tools.md`'s "The import-cycle constraint..." section, closing paragraphs: *"today, three independent consumers... each hand-scan for the same marker — the exact 'duplicated by necessity, not oversight' shape the taxonomy doc already diagnosed for `dispatch_to_agent`'s three call sites... This design's fix is the same shape as [reflex-taxonomy] `03`'s: collapse the three independent interpretations into one shared function... called from each door that structurally has to exist."*

Confirmed directly against this checkout (not assumed) before writing this task — all three are real, independently-implemented marker scans:

- `internal/service/chat_generate.go:3049`, `captureEnvelopeData(result string, pending []string) []string` — index-based (`strings.Index`) extraction, called from `internal/service/chat_tool_executor.go:701` and `:813`.
- `internal/api/tools_call.go:121-129`, `extractEnvelopeMarker(result *mcp.ToolResult) string` — its own constant pair (`envelopeMarkerOpen`/`envelopeMarkerClose`) and its own extraction logic, structurally similar to `captureEnvelopeData` but a separate implementation, called at `internal/api/tools_call.go:101`.
- `internal/mcpserver/handlers.go:27-32`, `convertEnvelopeMarkers(text string) string` — a third, again-separate implementation, using its own local `startTag`/`endTag` constants.

**Unlike `dispatch_to_agent`'s three call sites** (which the parent taxonomy doc collapsed into one shared decision *engine*, `internal/agent/reflexes/resolve.go`'s `Resolve()`, called from all three), these three marker-extraction functions have no import-cycle reason to stay separate — none of `internal/service`, `internal/api`, or `internal/mcpserver` has a constrained-import problem calling a shared helper the way `internal/mcp`/`internal/selftools` do. The duplication here is plain code duplication, not an enforced-by-the-import-graph necessity. That makes this a lower-risk, more mechanical DRY task than `03`'s reflex-decision-engine collapse was — find where all three packages can safely import a new shared function without creating a cycle (check each of `internal/service`, `internal/api`, `internal/mcpserver`'s own existing import graph first — do not assume; a shared home under `internal/chat` (this project's own envelope-types home per this project's `CLAUDE.md`) is a reasonable starting guess but must be confirmed, not assumed).

## What to do

1. **Locate a shared home.** Confirm (via `go list -deps` or direct inspection of each file's own imports) that `internal/service`, `internal/api`, and `internal/mcpserver` can all import a single target package without a cycle. `internal/chat/envelope.go` (already the source of truth for envelope *types* per this project's own `CLAUDE.md`) is the natural first candidate — verify, don't assume, since `internal/chat`'s own import graph may or may not already reach all three callers cleanly.
2. **Extract one shared function**, e.g. `func ExtractEnvelopeMarker(text string) (json string, ok bool)`, matching the exact marker delimiters all three existing implementations already agree on (`<!--ENVELOPE_DATA:` / `:ENVELOPE_DATA-->`) and the exact extraction semantics the majority of the three already share (confirm all three actually agree on single-vs-multiple-marker handling, trimming, and malformed-marker behavior before assuming they're interchangeable — if they've silently diverged, document the divergence in this file's Work Log and pick one behavior deliberately rather than accidentally standardizing on whichever you happened to port first).
3. **Rewire all three real call sites** (`chat_tool_executor.go`/`chat_generate.go:captureEnvelopeData`'s callers, `tools_call.go:101`, `mcpserver/handlers.go`'s caller) to use the new shared function instead of their own local implementation. Delete the three now-dead local implementations and their now-dead local marker-delimiter constants.
4. **No behavior change** is the goal — if step 2 found a real divergence between the three (see above), that's the one place this task is allowed to change observable behavior, and it must be called out explicitly and deliberately in the Work Log, not buried in a generic "refactor" description.

## Done means

- One shared marker-extraction function exists in a location all three callers can import without a cycle; the three original hand-scan implementations are deleted, not left dead alongside the new one.
- Every existing test that exercised any of the three original functions (directly or via its caller) still passes unchanged, proving this is a behavior-preserving refactor (or, if step 2 found a real divergence, the Work Log documents exactly what changed and why, and any test asserting the old divergent behavior is updated deliberately, not silently).
- A regression test proves the shared function works correctly when called from all three original call sites' actual code paths (not just the extracted function in isolation).
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- Your shared-home package choice (step 1) is documented in this file's Work Log.

## Work log

**Re-verified the three consumers against this checkout before touching anything** (per the task's own instruction to re-confirm, not trust the Context section's snapshot as still-current) — all three were still real, independently-implemented, and unchanged since the task file was written: `internal/service/chat_generate.go:3049` `captureEnvelopeData`, `internal/api/tools_call.go:121-129/129` `extractEnvelopeMarker` + its `envelopeMarkerOpen`/`envelopeMarkerClose` consts, `internal/mcpserver/handlers.go:27-32` `convertEnvelopeMarkers` + its local `startTag`/`endTag` consts. Also checked `internal/mcp/general_tools.go`'s `envelopeMarkerRE` (a fourth marker-shaped regex) and `internal/selftools/render_card_marker.go`'s `EmbedRenderCardMarker` (the task-04 marker *builder*, not an extractor) — confirmed both are out of scope: the former is a security-redaction mechanism in a package (`internal/mcp`) that per the architecture doc cannot import the shared home anyway, the latter is the producer side of the same marker, not a fourth duplicate consumer.

**Step 1 — shared home.** Confirmed via `go list -deps` (not assumed): `internal/chat`'s own transitive dep closure contains none of `internal/service`, `internal/api`, `internal/mcpserver` (zero-match grep on all three) — so `internal/chat` importing any of them, now or later, is not itself a live risk today, and more importantly `internal/service` and `internal/api` already import `internal/chat` today (confirmed by both `go list -deps` and by `chat` already being an unaliased import in `chat_tool_executor.go`, `chat_generate.go`, and `tools_call.go`). `internal/mcpserver` did not previously import `internal/chat`; since `internal/chat`'s dep closure doesn't reach back into `internal/mcpserver`, adding that import is safe and was added. `internal/chat/envelope.go` — the project's own documented envelope-types source of truth per this repo's `CLAUDE.md` — was confirmed as the right *package* (not necessarily the same file): it already houses `ParseEnvelopes`, the downstream consumer of the `nanite-envelope` fenced blocks `convertEnvelopeMarkers` produces, so the marker-extraction primitive and its eventual consumer now live in the same package. Added the new function to a sibling file, `internal/chat/envelope_marker.go`, rather than inlining into the existing `envelope.go`, matching this package's existing convention of many small topic-scoped files (`envelope_handler.go`, `envelope_response.go`, etc. all already sit alongside `envelope.go`).

**Step 2 — shared function, and the real divergence found.** Checked all three originals' single-vs-multi-marker, trimming, and malformed-marker behavior directly against the code (not assumed) before writing anything:
- Trimming: none of the three trimmed the extracted payload. No divergence.
- Malformed marker (opening delimiter with no closing delimiter anywhere later in the text): all three treat it as "not found" and do not scan past it looking for a second, well-formed marker in the same text/block. No divergence.
- Single-vs-multiple marker handling: **this is the one real, structural divergence.** `captureEnvelopeData` and `extractEnvelopeMarker` are pure *extraction* functions — each finds and returns only the *first* well-formed marker in its input (`extractEnvelopeMarker` additionally iterates `*mcp.ToolResult` content blocks, but still returns on the first block/marker it finds). `convertEnvelopeMarkers` is a *transformation* function with a different job entirely — it loops and replaces *every* well-formed marker in the text with a `` ```nanite-envelope `` fenced block, returning the modified text, not a JSON payload. These are not interchangeable calling shapes; collapsing them onto a single `func ExtractEnvelopeMarker(text string) (json string, ok bool)` (the task's own suggested signature) alone would have left `convertEnvelopeMarkers` unable to do its actual job (replace-all, not read-one).
  **Resolution, deliberate:** built one shared low-level scan (`findEnvelopeMarker`, unexported) returning the payload plus the full marker's byte range, then exposed two functions on top of it in `internal/chat/envelope_marker.go`: `ExtractEnvelopeMarker(text) (json string, ok bool)` — the single-marker read, used by `captureEnvelopeData` and `extractEnvelopeMarker` — and `ReplaceEnvelopeMarkers(text string, replace func(payload string) string) string` — the replace-every-occurrence transform, used by `convertEnvelopeMarkers`. Both share the identical delimiter constants and identical malformed-marker handling (delegated to the same `findEnvelopeMarker`), so the actual duplicated *scan* logic is fully collapsed to one place; the two thin public wrappers reflect the two genuinely different *jobs* callers need, not a re-duplication. No observable behavior changed for any of the three original callers — this is documented here per the task's own instruction because it's a real divergence between the three, not a "no divergence found" case, even though the resolution required no test assertions to change.

**Step 3 — rewiring.** All three call sites (`chat_tool_executor.go:701`'s `captureEnvelopeData` call, `tools_call.go:101`'s `extractEnvelopeMarker` call, `mcpserver/server.go:213`'s `convertEnvelopeMarkers` call) were left calling the exact same local function names/signatures they always did — a deliberate choice, not an oversight: each of the three local functions retains a real, non-duplicated, call-site-specific job on top of the now-shared scan (`captureEnvelopeData` appends to a caller-owned pending slice; `extractEnvelopeMarker` iterates a `*mcp.ToolResult`'s content blocks; `convertEnvelopeMarkers` supplies the fenced-block replacement template). Each of the three function *bodies* was rewritten to delegate its actual delimiter scan to `chat.ExtractEnvelopeMarker`/`chat.ReplaceEnvelopeMarkers` instead of re-implementing `strings.Index` scanning locally, and each local delimiter-constant pair (`captureEnvelopeData`'s inline literals, `tools_call.go`'s `envelopeMarkerOpen`/`envelopeMarkerClose`, `handlers.go`'s `startTag`/`endTag`) was deleted outright — the constants now live once, unexported, in `internal/chat/envelope_marker.go`. `strings` became an unused import in `internal/api/tools_call.go` after the rewrite and was removed. This reading of "delete the three now-dead local implementations" (Done-means bullet 1) is: the *implementations* — the actual duplicated scan logic and delimiter constants — are gone and not left dead; the three call-site function *names* are not themselves "dead" (they're still live, still called, still doing real call-site-specific work) so they were kept rather than force-renamed, which also meant zero changes were needed to any pre-existing test file, and every comment elsewhere in the codebase that names `captureEnvelopeData`/`extractEnvelopeMarker`/`convertEnvelopeMarkers` by name (`internal/plugin/envelope_validator.go`, `internal/selftools/render_card_marker.go`, `tools_call.go`'s own `handleSelfToolCall` doc comment) stayed accurate with no edits required.

**Step 4 — no behavior change confirmed.** Every pre-existing test exercising any of the three originals passes unchanged: `internal/service`'s `TestCaptureEnvelopeData`, `internal/api`'s `TestExtractEnvelopeMarker` (all four subtests) and task-04's `TestExtractEnvelopeMarker_RoundTripsRenderCardReactionPayload` (the marker-round-trip regression test that call explicitly said not to break — confirmed passing, unedited), and `internal/mcpserver`'s `TestConvertEnvelopeMarkers`/`_NoMarkers`/`_Multiple`. None needed any edits.

**Regression tests added** (Done-means bullet 3 — proving the shared function through all three original call sites' real code paths, not just in isolation):
- `internal/chat/envelope_marker_test.go` (new file) — isolation-level unit tests for `ExtractEnvelopeMarker`/`ReplaceEnvelopeMarkers` directly: no-marker, well-formed, malformed-ignored, multiple-returns-first-only, no-trimming, replace-no-markers, replace-every-occurrence, and replace-stops-at-malformed-marker.
- `internal/service/chat_test.go`: added `TestCaptureEnvelopeData_UsesSharedExtractor` — calls the real `captureEnvelopeData` call-site function (not the shared function directly) with a malformed marker (confirms it's dropped) and a sequence of calls accumulating onto one `pending` slice, mirroring `chat_tool_executor.go`'s actual per-tool-call-in-a-loop usage.
- `internal/api/tools_call_test.go`: added `TestExtractEnvelopeMarker_MalformedMarkerAndMultiBlock` — calls the real `extractEnvelopeMarker(result *mcp.ToolResult)` call-site function with a first content block carrying a malformed marker and a second carrying a well-formed one, confirming the per-block iteration correctly skips the malformed block and finds the later well-formed one.
- `internal/mcpserver/server_test.go`: added `TestConvertEnvelopeMarkers_MalformedMarkerHaltsLoop` — calls the real `convertEnvelopeMarkers` call-site function with a malformed leading marker, confirming the replace-loop halts (matching this function's exact pre-collapse behavior).

One correction made mid-work: my first draft of the two malformed-marker-halts-the-loop test cases (in `envelope_marker_test.go` and `server_test.go`) used input containing a second, later, well-formed marker's closing delimiter — which the algorithm (correctly, by design) treats as *satisfying* the first marker's closing delimiter (greedy match to the next `:ENVELOPE_DATA-->` anywhere later in the text), so the marker wasn't actually malformed and both tests initially failed. Fixed by using input with no closing delimiter anywhere in the string at all. Caught by running the actual test suite rather than assuming the hand-written expectation was correct — worth noting since it's exactly the kind of assumption-not-verified mistake this whole task was about avoiding in the *production* code.

**Verification:**
- `go build ./cmd/nanite/` — pass.
- `go build ./...` — pass.
- `go vet ./...` — one pre-existing failure in `internal/service/container.go` (`stopReaper`/`stopRuntimeReaper` "not used on all paths" possible-context-leak warnings) confirmed via `git status`/`git diff --stat` to be **completely untouched by this task's diff** — not introduced by this change. `go vet` on each of the four touched packages individually (`internal/chat`, `internal/service`, `internal/api`, `internal/mcpserver`) is clean.
- `go test ./...` (forced with `-count=1`, not relying on cache) — all packages pass, including the four touched ones. Confirmed via `go test -json` that every specific test named above (both pre-existing and newly added) individually reports `"Action":"pass"`.
- `gofmt -l` clean on every new/touched file except `internal/service/chat_test.go`, which has one pre-existing misaligned-comment gofmt drift on an unrelated line (`CountSessionToolCalls` stub method, line ~329) that predates this diff — confirmed via `git diff --stat` showing 41 insertions/0 deletions for that file (a pure append, nothing pre-existing was touched or reformatted). Left as-is; out of scope for this task.

**Deviations from the task file's own suggested approach:** the task's Context/What-to-do sections suggest a single `ExtractEnvelopeMarker(text) (json, ok)` signature as "the" shared function; actually landed with two exported functions (`ExtractEnvelopeMarker` plus `ReplaceEnvelopeMarkers`) sharing one unexported scan, because `convertEnvelopeMarkers`'s real job (replace-all) cannot be expressed through a single-marker-read signature without either losing behavior or forcing that caller to re-implement its own loop around the shared read (which would have left real scan-adjacent logic, specifically the "how do I find the next marker after this one" iteration, still duplicated). This is flagged explicitly per What-to-do step 4's instruction to call out any place a real divergence changed the shape of the work, even though it did not change any caller's *observable* behavior.

**Independently re-verified by the Orchestrator before merge:** re-read all three production diffs (`chat_generate.go`, `tools_call.go`, `handlers.go`) directly — confirmed each is a clean, mechanical delegation to the shared function with no observable behavior change, `go build ./...`/`go vet ./...`/`go test ./...` re-run clean in this worktree, and confirmed the three new test-file diffs are purely additive (0 deletions across `chat_test.go`/`tools_call_test.go`/`server_test.go`).

No escalations. No blockers.

## Review notes

**2026-08-20 — PASS.** Fresh reviewer (no shared context with the worker), full Phase 2 review covering `05`/`06`/`07` together. Confirmed real, not just claimed: diffed every pre-collapse original (`git show bce8426f:<path>`) against its new caller byte-for-byte — `captureEnvelopeData`'s untrimmed first-marker-only semantics, `extractEnvelopeMarker`'s malformed-marker fall-through-to-next-block behavior, and `convertEnvelopeMarkers`' full-text replace-loop semantics all preserved exactly. The two-function split (`ExtractEnvelopeMarker` single-read vs. `ReplaceEnvelopeMarkers` replace-all) confirmed genuinely necessary, not unnecessary complication — the two single-read call sites and the one replace-all call site have real, different jobs on top of one shared internal scan. Confirmed task `04`'s regression test (`TestExtractEnvelopeMarker_RoundTripsRenderCardReactionPayload`) is present and unmodified by this diff. Also independently verified the new `internal/mcpserver` → `internal/chat` import edge this task introduces doesn't create a cycle (`internal/chat` has zero transitive dependency back on `internal/mcpserver`).
