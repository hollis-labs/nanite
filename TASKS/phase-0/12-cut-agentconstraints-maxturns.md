# Cut AgentConstraints.MaxTurns and the chat-loop-budget-soft-warning signal

**Phase:** 0
**Status:** not-started
**Depends on:** `11-cut-strategy-planner` — not a hard functional dependency (this item's cut is independently well-defined), but both tasks edit `internal/service/chat_generate.go` inside the same function at nearby line ranges, and `11`'s removal changes what value feeds this item's target mechanism. Land `11` first; do not dispatch these two as parallel workers. See Context for the full reasoning.
**Touches:** `internal/chat/engine.go` (`AgentConstraints.MaxTurns` field, ~line 32), `internal/agent/parser.go` (mirror `AgentConstraints.MaxTurns` field, ~line 140 — kept in sync with the runtime struct per that file's own doc comment), `internal/agentvalidation/validation.go` (`numericConstraintKeys["max_turns"]` + the `max_turns`-specific `-1` validation branch, ~lines 137-166), `internal/service/chat_loop_state.go` (`defaultMaxTurns` const, `c.MaxTurns` handling in `resolveIterationLimits`, `checkSoftMaxTurnsWarning`, `softMaxTurnsWarningFired` field, the `CW-20260504-0001` doc-comment block — see What To Do for what to keep vs. cut), `internal/service/chat_generate.go` (the `checkSoftMaxTurnsWarning`/`emitChatLoopBudgetSoftWarning` call site, ~lines 807-814), `internal/service/chat_loop_budget_soft_warning.go` (delete file), `internal/service/chat_loop_budget_soft_warning_test.go` (delete file), `internal/envelope/contracts_test.go` (`knownTypes`/`examplePayloads` entries for `chat-loop-budget-soft-warning`, ~lines 47, 234), `internal/service/plugin_envelope_wrap_test.go` (~line 43-49, uses this envelope type as its `DevModeOnly` example — needs a different example type), `ui/src/components/chat/envelopes/ChatLoopBudgetSoftWarningCard.tsx` (delete), `scripts/generate-plugin-imports.mjs` (remove the `chat-loop-budget-soft-warning` entry from `CORE_OVERRIDES`, ~lines 39-42), `ui/src/generated/plugin-envelopes.ts` (regenerate via `node scripts/generate-plugin-imports.mjs` after the manifest edit below), **plus, in the separate sibling repo `libs/go-envelopes`** (checked out locally at `../../libs/go-envelopes` relative to this repo, referenced via a `replace` directive in `go.mod`; real GitHub repo `github.com/hollis-labs/go-envelopes`): `manifest/envelopes.yaml` (remove the `chat-loop-budget-soft-warning` entry, ~line 132-135) and `manifest/schemas/chat-loop-budget-soft-warning.schema.json` (delete).

**Do NOT touch** (verified real, hard stoppers that must survive this cut untouched): `chat.AgentConstraints.RunawayFailCap` (`internal/chat/engine.go:42`, default 10, `chat_loop_state.go:78` `defaultRunawayFailCap`, enforced at `shouldStop()` Layer 1, `chat_loop_state.go:446`); `chat.AgentConstraints.HardCeiling` (`engine.go:33`, default 200, `chat_loop_state.go:62` `defaultHardCeiling`, enforced at `shouldStop()` Layer 3, `chat_loop_state.go:458`); `chat.AgentConstraints.IdleTimeoutSeconds` (`engine.go:43`, default 900s / 300s for subagents, enforced at `shouldStop()` Layer 2, `chat_loop_state.go:452`); the natural `end_turn` stop path (elsewhere in `chat_generate.go`, unrelated to `iterationLimits`). Also do not touch `internal/service/chat_generate_diag.go`'s `diagLogLoopStart` (~line 126, logs `ls.resolvedMaxTurns()` alongside `hardCeiling`/`runawayFailCap`/etc. as general diagnostics — it's not strategy- or soft-warning-specific and can keep calling `resolvedMaxTurns()` regardless of what this cut does to that method's internals).

## Context

TASKS.md Phase 0 item 12: "**`AgentConstraints.MaxTurns`**, and the `chat-loop-budget-soft-warning` signal that depends on it. Same reasoning as #11."

`docs/architecture-decision-log-2026-08-17.md` §12 ("Harness — turn loop, CLI/API routing, tool lazy-load, run-another-agent unification"):

> **`AgentConstraints.MaxTurns` (default 75, soft/telemetry-only) — cut.** A second, separate "soft budget" from the already-cut strategy-planner one, same anti-pattern (fires a warning, stops nothing). Real stoppers stay as-is: `RunawayFailCap` (10, hard), `HardCeiling` (200, hard — this is already the generous non-strategic iteration backstop asked about earlier, no new mechanism needed), `IdleTimeoutSeconds`, natural `end_turn`.

`docs/engineering/architecture/04-harness.md`: "Two redundant 'soft' turn-budget mechanisms are cut: the strategy planner's `MaxTurns` and, separately, `AgentConstraints.MaxTurns` (a second, independent soft budget that also only fired a warning and never stopped anything). The real, hard stoppers stay as-is: `RunawayFailCap` (consecutive tool failures), `HardCeiling` (an absolute iteration backstop...), `IdleTimeoutSeconds`, and natural `end_turn`."

### Verified against real code (2026-08-18)

**`chat.AgentConstraints.MaxTurns` is confirmed already soft/non-gating**, matching the decision log exactly. `internal/service/chat_loop_state.go:29-59` (comment, commit `CW-20260504-0001`): "`defaultMaxTurns` and `Strategy.MaxTurns` are now SOFT HINTS, not hard terminators. The chat loop terminates on actual pathology — `runawayFailCap`..., `idleTimeout`..., `hardCeiling`... — or on the agent's natural `end_turn` stop signal. Hitting `maxTurns` no longer kills the loop." `shouldStop()` (`chat_loop_state.go:444-472`) never checks `maxTurns`. The only remaining consumer of `ls.limits.maxTurns` is `checkSoftMaxTurnsWarning()` (`chat_loop_state.go:474-495`), which fires the `chat-loop-budget-soft-warning` envelope (dev-mode-gated SSE + always-on log line) exactly once per generation and does not stop the loop. The default is **75** (`defaultMaxTurns = 75`, `chat_loop_state.go:61`), not the "25" a stale comment elsewhere in the same file claims (`chat.go` doc comment on `AgentConstraints.MaxTurns` says "0=default(25)" — that comment is stale relative to the real constant; not in scope to fix here, just noting it so the worker doesn't get confused chasing the wrong default value).

**Real coupling with item 11, requiring serialization, not parallelization**: `ls.limits.maxTurns` is currently written from *two* places — `resolveIterationLimits(constraints)` seeds it from `chat.AgentConstraints.MaxTurns` (this item's target), and then, later in the same turn, `applyStrategyToLimits(ls, turnStrategy)` in `internal/service/chat_strategy.go` **overwrites** it with the strategy planner's own `MaxTurns` (item 11's target) — see `chat_generate.go:778`, which runs *after* `newLoopState` (`chat_generate.go:696`) but *before* the loop's `checkSoftMaxTurnsWarning` check (`chat_generate.go:812-814`). Both item 11 and this item edit `internal/service/chat_generate.go`, in the same function (`generateResponse`), at nearby-but-distinct line ranges (item 11: ~762-778; item 12: ~807-814). Per `EXECUTION-PROCESS.md`'s parallelization-safety rule ("If there's any doubt, serialize them"): **do not dispatch items 11 and 12 as parallel workers.** Run item 11 first (it's the one whose removal changes what value feeds into this item's `checkSoftMaxTurnsWarning` — after item 11 lands, `ls.limits.maxTurns` is sourced purely from `AgentConstraints.MaxTurns`/default, which is exactly this item's target, so landing 11 first makes 12's diff cleaner and easier to review), then run item 12 against the post-11 tree. If both must be scheduled together for throughput reasons, run them serially in the same worker session rather than as two parallel worktree dispatches.

**The `chat-loop-budget-soft-warning` envelope type lives in a separate git repository, not this one.** Per this project's `CLAUDE.md`: "Manifest (source of truth): the external `github.com/hollis-labs/go-envelopes` module's embedded `manifest/envelopes.yaml`... It is no longer vendored as `config/envelopes.yaml`." Verified: `go.mod` has `replace github.com/hollis-labs/go-envelopes => ../../libs/go-envelopes`, a local checkout of a real separate GitHub repo (`git remote -v` in that directory shows `git@github.com:hollis-labs/go-envelopes.git`). The manifest entry (`libs/go-envelopes/manifest/envelopes.yaml:132-135`) reads: "Chat-loop soft budget warning. Emitted once per generation when the loop crosses **the strategy planner's** soft max_turns budget without terminating. Signal-only; loop continues. Frontend component is intentionally omitted." (Component wiring instead lives host-side, in this repo's `scripts/generate-plugin-imports.mjs` `CORE_OVERRIDES` map — a deliberate exception documented in that file's header for host-only renderers.) Fully removing this envelope type means editing files in **two repositories**. Confirm with the operator/orchestrator how commits to `libs/go-envelopes` should be made (separate PR against that repo, vs. direct local commit) before assuming a same-session edit-and-commit there is acceptable — this is a process question, not a code question, so it isn't answered by this task file.

**No doc/reality mismatch found for the core claim** — `AgentConstraints.MaxTurns` really is soft/telemetry-only today, exactly as described. The two things worth flagging (both already captured above, not blockers): the cross-repo envelope-manifest dependency, and the stale "default(25)" doc comment.

## What to do

1. Remove `MaxTurns int` from `chat.AgentConstraints` (`internal/chat/engine.go:32`) and its `json:"max_turns"` tag. Update the surrounding doc comment (lines 13-29) so it no longer lists `MaxTurns` among the "surviving fields."
2. Remove the mirrored `MaxTurns int` field from `agent.AgentConstraints` (`internal/agent/parser.go:140`) — this is the YAML-frontmatter-parsing struct kept in sync with `internal/chat.AgentConstraints`'s shape by hand (per that file's own doc comment, ~line 116-131). Leave the *other* `Definition.MaxTurns int` field at `internal/agent/parser.go:49` (`yaml:"maxTurns"`, top-level, not inside `Constraints`) alone — **this is a separate, already-unrelated dead field** (confirmed: `ToProfile()` in `internal/agent/convert.go` never reads `d.MaxTurns`, only `d.Constraints`, which is a different struct with its own nested `maxTurns` key under `constraints:`). It's not part of this cut's scope; flag it to the Orchestrator as a candidate for a future, separate small cleanup rather than folding it in here.
3. In `internal/agentvalidation/validation.go` (~lines 137-166), remove `"max_turns": true` from the `numericConstraintKeys` map and the `else if k == "max_turns"` branch that allows `-1`. After removal, an incoming `constraints.max_turns` key falls through to the `default:` case (line ~169, "constraints: unknown key %q - ignored", a warning not an error) — confirm that's the desired behavior (a profile still carrying a stale `max_turns` key from before this cut should warn, not hard-fail validation).
4. In `internal/service/chat_loop_state.go`:
   - Remove the `c.MaxTurns > 0` / `c.MaxTurns == -1` branch in `resolveIterationLimits` (~lines 371-375).
   - Remove `checkSoftMaxTurnsWarning()` (~lines 474-495) and the `softMaxTurnsWarningFired` field (~lines 194-198) on `loopState`.
   - Rewrite the `CW-20260504-0001` comment block (~lines 29-59) — it currently explains the soft-warning mechanism in detail; since the mechanism is gone, trim it to a short note that `maxTurns`/`hardCeiling` used to have a separate soft-warning layer that was removed here, pointing at `RunawayFailCap`/`HardCeiling`/`IdleTimeoutSeconds`/`end_turn` as the real terminators (don't just delete the comment silently — it explains *why* `shouldStop()` looks the way it does, which is still true and still worth documenting).
   - **Judgment call, don't guess past it**: once `AgentConstraints.MaxTurns` and `checkSoftMaxTurnsWarning` are both gone, `iterationLimits.maxTurns`, `defaultMaxTurns` (=75), and `resolvedMaxTurns()` have no remaining *behavioral* consumer — their only reader left would be `chat_generate_diag.go`'s `diagLogLoopStart` (general diagnostics) and any inspector/`TurnSnapshot.MaxTurns` display code. Decide whether to (a) leave `maxTurns`/`resolvedMaxTurns()` in place purely as an inert diagnostic value (simplest, lowest blast radius), or (b) remove `maxTurns` from `iterationLimits` entirely and have diagnostics report `hardCeiling` directly instead. Prefer (a) unless (b) is clearly trivial once you're in the code — this task's scope is "cut `AgentConstraints.MaxTurns` and the soft-warning signal," not "redesign `iterationLimits`." If genuinely unsure which the Orchestrator wants, escalate rather than guessing.
5. In `internal/service/chat_generate.go`, remove the `checkSoftMaxTurnsWarning`/`emitChatLoopBudgetSoftWarning` call block (~lines 807-814, including its `CW-20260504-0001` comment).
6. Delete `internal/service/chat_loop_budget_soft_warning.go` and `internal/service/chat_loop_budget_soft_warning_test.go` in full (`chatLoopBudgetSoftWarningEnvelopeType`, `chatLoopBudgetSoftWarningPayload`, `emitChatLoopBudgetSoftWarning`, and the `(s *chatServiceImpl) devModeEnabled()`/`envDevModeOn()` helpers also defined in that same file). Verified: within `internal/service`, both helpers are only called from this file — there is a separate, unrelated same-named `envDevModeOn()`/`devModeEnabled()` pair in `internal/api/skills.go` (a different package, its own independent copy), so deleting the `internal/service` copies has no effect on the API package's dev-mode gating.
7. Update `internal/envelope/contracts_test.go`: remove `"chat-loop-budget-soft-warning"` from the `knownTypes` slice (~line 47) and its entry from `examplePayloads` (~line 234).
8. Update `internal/service/plugin_envelope_wrap_test.go` (~line 43-49): it uses `chat-loop-budget-soft-warning` as its example of a `DevModeOnly`-gated envelope. Swap in a different existing `DevModeOnly` envelope type if one exists, or otherwise adjust the test so it still exercises the `DevModeOnly` code path without referencing the deleted type.
9. In the sibling repo checked out at `../../libs/go-envelopes`: remove the `chat-loop-budget-soft-warning` entry from `manifest/envelopes.yaml` (~lines 132-135) and delete `manifest/schemas/chat-loop-budget-soft-warning.schema.json`. Confirm with the Orchestrator how to land this change (separate commit/PR in that repo) before treating it as done.
10. In `scripts/generate-plugin-imports.mjs`, remove the `'chat-loop-budget-soft-warning': {...}` entry from `CORE_OVERRIDES` (~lines 39-42).
11. Delete `ui/src/components/chat/envelopes/ChatLoopBudgetSoftWarningCard.tsx`.
12. Regenerate `ui/src/generated/plugin-envelopes.ts` via `node scripts/generate-plugin-imports.mjs` (after step 9 lands in the sibling repo — the generator reads that repo's manifest) and confirm `node scripts/generate-plugin-imports.mjs --check` passes clean.
13. Run `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...`, and `cd ui && npm run build` (or project frontend typecheck/build).

## Done means

- `chat.AgentConstraints` and `agent.AgentConstraints` no longer have a `MaxTurns` field.
- `internal/agentvalidation`'s numeric-constraint validation no longer recognizes `max_turns` as a known key (falls through to the existing "unknown key — ignored" warning path).
- `checkSoftMaxTurnsWarning`/`softMaxTurnsWarningFired`/`emitChatLoopBudgetSoftWarning`/`chatLoopBudgetSoftWarningEnvelopeType` no longer exist anywhere in the Go codebase.
- The `chat-loop-budget-soft-warning` envelope type is removed from the `go-envelopes` manifest (in the sibling repo), from `CORE_OVERRIDES`, from `internal/envelope/contracts_test.go`, and the `ChatLoopBudgetSoftWarningCard.tsx` component file is deleted; `ui/src/generated/plugin-envelopes.ts` regenerates clean with no reference to it.
- `RunawayFailCap`, `HardCeiling`, `IdleTimeoutSeconds`, and natural `end_turn` termination are all still present and untouched — a runaway-tool-failure session and an idle session both still terminate correctly (dogfeed or targeted test, don't just trust that the diff "looks like" it left them alone).
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...`, and the frontend build all pass.
- If the sibling-repo edit (`libs/go-envelopes`) couldn't be completed in this session (e.g. it needs its own PR/review process), that's flagged explicitly in the Work Log — this task isn't fully "done" until that repo's manifest and schema file are actually updated, even if the nanite-side changes land first.

## Work log
<Worker fills this in as it goes: what was actually done, any deviation from plan and why, anything escalated.>

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
