# Cut legacy rename-artifact tables (`agent_messages_legacy_089`, `todos_legacy_d1`)

**Phase:** 0
**Status:** implemented
**Depends on:** `09-adopt-goose-migrations`
**Touches:** `internal/store/migrations/090_agent_messages_subagent_result_kind.sql` (renumbered from `089_...` by `09-adopt-goose-migrations`'s duplicate-`051`-prefix resolution — the migration's content and the `agent_messages_legacy_089` table name it produces are unchanged, only the file's version number shifted; see that task's Work Log), `internal/store/migrations/043_scope_project_reminders_pins_todos.sql` (unaffected by the renumber), a new migration (goose-format, per `09-adopt-goose-migrations`'s adopted mechanism) that drops `agent_messages_legacy_089` and `todos_legacy_d1`; no Go code references either table (verified — see Context)

## Context

TASKS.md Phase 0 item 19: "`agent_messages_legacy_089`/`todos_legacy_d1` — after #9 (goose) lands." Decision log §16 ("Confirmed dead, cut"): "`agent_messages_legacy_089`/`todos_legacy_d1` — safely droppable once goose (§13) replaces the swallowed-error idempotency mechanism that's been keeping them around as a side effect." `docs/engineering/architecture/05-storage-and-migrations.md`'s "Confirmed dead, cut" section repeats the same finding.

**Verified origin of both tables** — they are rename artifacts from SQLite's "no in-place `ALTER TABLE ... ALTER COLUMN`/CHECK-widening" limitation, produced by the rename→recreate→copy migration pattern:

- `agent_messages_legacy_089` is created by `internal/store/migrations/090_agent_messages_subagent_result_kind.sql` (renumbered from `089_...sql`, see the Touches line above), which widens `agent_messages.kind`'s CHECK constraint to add `'subagent_result'`. Line 27: `ALTER TABLE agent_messages RENAME TO agent_messages_legacy_089;`, followed by `CREATE TABLE IF NOT EXISTS agent_messages (...)` with the widened CHECK, then `INSERT OR IGNORE INTO agent_messages ... SELECT ... FROM agent_messages_legacy_089`. The migration's own comment block is explicit: "`agent_messages_legacy_089` is intentionally NOT dropped, so subsequent runs find it for the rename-swallow + INSERT-OR-IGNORE no-op path."
- `todos_legacy_d1` is created by `internal/store/migrations/043_scope_project_reminders_pins_todos.sql`, which swaps the `todos.scope` CHECK enum (drops `workspace`, adds `turn`) and adds `project_id`. Line 64: `ALTER TABLE todos RENAME TO todos_legacy_d1;`, same rename→recreate→copy shape, same "intentionally NOT dropped ... small fixed cost preserved only to make this migration safely re-runnable" comment.

Both migrations' comments spell out **why** the legacy table is kept: with no `schema_migrations` ledger (the exact gap `09-adopt-goose-migrations` closes — see decision log §13, `docs/engineering/architecture/05-storage-and-migrations.md`), every migration file re-executes in full on every boot. The `RENAME TO` statement's second-boot failure ("already another table") is swallowed by the migration runner (`internal/store/store.go`, the `migrate:skip-if-column-exists` mechanism), which only works because the legacy table still exists to rename onto. Decision log §13 flags this exact structural pattern — "the identical structural pattern in `043`/`089` remains unguarded" — as the still-live instance of the failure class that caused a prior production crash-loop (`e2273f8`) elsewhere. `09-adopt-goose-migrations` replaces the swallowed-error idempotency mechanism with goose's real version ledger, which is what makes these two legacy tables droppable: once migrations only run once (tracked in `goose_db_version` or equivalent), the rename-swallow trick this code depends on is no longer how re-run safety is achieved, and the legacy tables lose their only reason to exist.

**Confirmed zero live consumers**: `grep -rn "agent_messages_legacy_089\|todos_legacy_d1" --include="*.go" .` returns no matches anywhere in the Go codebase — no store method, no query, no test reads or writes either table. They are pure byproducts of the migration mechanism itself, not application data.

**Hard dependency on `09-adopt-goose-migrations` — why this must not run first**: dropping these tables while the swallowed-rename idempotency mechanism is still the live migration model would break `089`/`043` on the very next boot after this change lands (elsewhere, not on this repo checkout, but on any environment where migrations still re-run from scratch) — the `RENAME TO agent_messages_legacy_089` / `RENAME TO todos_legacy_d1` statements in those two migration files would then attempt to rename onto tables that no longer exist to fail-and-swallow against, changing their error shape and potentially surfacing as a real failure instead of the currently-swallowed "already another table" case. This task must land strictly after goose is adopted and 089/043 (or their goose-migrated equivalents) are re-expressed as one-time, ledger-tracked migrations that no longer depend on the legacy table surviving.

## What to do

1. Confirm `09-adopt-goose-migrations` has landed and merged first — do not start this task otherwise.
2. Under the new goose-based migration mechanism, add a migration that drops both `agent_messages_legacy_089` and `todos_legacy_d1` (`DROP TABLE IF EXISTS <name>` is the safe form, consistent with existing drop precedent in this codebase, e.g. `internal/store/migrations/018_rename_a2a_messages.sql`'s `DROP TABLE IF EXISTS a2a_messages`).
3. Check whether `09-adopt-goose-migrations`'s own work re-expresses the historical migration set (including `043`/`089`) as goose-format `.sql` files with `-- +goose Up`/`-- +goose Down` annotations, or whether it layers goose on top of the existing `internal/store/migrations/*.sql` files unchanged. Either way, this task's new drop-migration must run *after* 089/043 in migration order (by whatever ordering goose uses — versioned filenames) so the legacy tables exist to be dropped, and must not be a no-op replay hazard under goose's real up/down tracking.
4. If goose's adoption changed how `089`/`043` themselves are expressed (e.g. collapsed to a single migration since there is no more production data to preserve via the rename-copy dance), coordinate with what `09-adopt-goose-migrations` actually did rather than assuming the current file shapes described in Context are still literally present — re-verify before writing the drop migration.
5. Verify no Go code references either table before landing (re-run the grep in Context — should still be empty).
6. Test against a real copy of the backed-up database (`~/.local/share/nanite/workspaces/default/backups/`), not just an empty fixture, per `EXECUTION-PROCESS.md`'s migration-testing requirement — confirm the drop succeeds cleanly on a database that actually has both legacy tables populated (from real historical migration runs) and that the live `agent_messages`/`todos` tables are untouched.

## Done means

- `agent_messages_legacy_089` and `todos_legacy_d1` no longer exist after migrations run on a fresh boot.
- The live `agent_messages` and `todos` tables are unaffected — same schema, same row counts, before and after.
- Migration is idempotent under goose's real ledger (runs once, tracked, not swallowed-error-safe by accident).
- Tested against a real backed-up database copy, not just an empty fixture.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- No Go code references either legacy table (re-confirmed after landing).

## Work log

**Precondition check.** `09-adopt-goose-migrations` is marked `implemented` in its own task file and `TASKS/INDEX.md`. Re-verified its "What to do" step 4 in light of that: `agent_messages_legacy_089` is now produced by `internal/store/migrations/090_agent_messages_subagent_result_kind.sql` (renumbered from `089_...` by 09's duplicate-`051`-prefix resolution — content/table name unchanged, only the version number shifted), and `todos_legacy_d1` is still produced by `043_scope_project_reminders_pins_todos.sql` (unaffected by the renumber, exactly as this task file's own header already anticipated). `go.mod` carries `github.com/pressly/goose/v3 v3.27.3`, `internal/store/store.go`'s `migrate()` is goose-based (`goose.NewProvider` + `provider.Up(ctx)`), and `legacyMigrationCutoverVersion = 94` is the fixed boundary — confirmed the new migration must be numbered above 94 to run for real (not silently seeded-as-applied) under the pre-existing-database cutover path.

**Environment discrepancy found and resolved (worth flagging, not a design escalation).** This worker was dispatched into an isolated git worktree (per `EXECUTION-PROCESS.md`'s parallel-dispatch rule). `09-adopt-goose-migrations`'s actual work — the goose conversion described above — exists only as **uncommitted working-tree state in the shared checkout** (`/Users/chrispian/dev/hollis-labs/apps/nanite`), not as a real git commit; `git log`/`git show HEAD:...` in this worktree (which shares the same `.git` and is at the same `df71e71` HEAD as the shared checkout) confirm the old, pre-goose `store.go`/93-file migration set is what's actually committed. Since a `git worktree` only materializes committed content, this worktree started with none of 09's work. Resolved by copying the shared checkout's current (uncommitted) `internal/store/` tree, `go.mod`, `go.sum`, `internal/chat/universal_rules.go`, and `internal/api/loom_curator_wake.go` into this worktree — exactly the file set 09's own Work Log lists as touched, verified via `diff -rq` before copying. Confirmed via the same diff that two *other* uncommitted, unrelated changes also exist in the shared checkout (an `internal/api/agent_boot_plans.go`/`_test.go` removal and an `internal/plugin/builtin/sessionstats` removal, presumably other in-flight Phase 0 tasks run directly against the shared checkout) — these were deliberately **not** pulled into this worktree; they're out of scope for `19` and pulling them in would conflate unrelated tasks into this branch. Flagging for the Orchestrator: `09-adopt-goose-migrations` (and apparently other Phase 0 work) needs a real commit on `main` before any more worktree-isolated workers are dispatched against it, or every subsequent worker will hit this same gap. Not logged as a formal `TASKS/ESCALATIONS.md` entry (a git/dispatch-plumbing hiccup, not a `TASKS.md` decision question), but explicitly documented here per this task's own instruction to note deviations.

**Adjacent discovery, surfaced not fixed.** While inspecting the real backup DB's schema ahead of writing the drop migration, found that migrations `090`/`043`'s `CREATE INDEX IF NOT EXISTS`/`CREATE TRIGGER IF NOT EXISTS` statements targeting the *live* `agent_messages`/`todos` tables were silent no-ops on every real boot — the index/trigger names were already claimed by objects `RENAME TO` carried forward onto the legacy tables. Live `agent_messages` has only `idx_agent_messages_kind_unread`; live `todos` has only `idx_todos_project` and no `updated_at` trigger (mitigated at the application layer — `internal/store/todos.go` sets `updated_at` explicitly on every UPDATE path). This is a real but pre-existing, adjacent bug — not caused by and not fixed by this task, since fixing it would change the live tables' schema, which this task's own Done-means criteria ("same schema... before and after") doesn't authorize. Logged as a full heads-up entry in `TASKS/ESCALATIONS.md` ("Item 19: live `agent_messages`/`todos` are silently missing indexes...") with the concrete index/trigger definitions a follow-up task would need, rather than silently fixing or silently dropping it.

**Implementation.** Added `internal/store/migrations/095_drop_legacy_rename_tables.sql` (goose format, first version above `legacyMigrationCutoverVersion`, so it always applies for real — never seeded as already-applied by the pre-existing-database cutover path). `Up`: `DROP TABLE IF EXISTS agent_messages_legacy_089` / `DROP TABLE IF EXISTS todos_legacy_d1` — the same safe form as `018_rename_a2a_messages.sql`'s `DROP TABLE IF EXISTS a2a_messages` precedent the task file calls out. `Down`: a real, non-empty rollback (per `09-adopt-goose-migrations`'s Work Log: "New migrations going forward are expected to carry real, tested Down sections") that recreates both tables, indexes, and (for `todos_legacy_d1`) the `trg_todos_updated_at` trigger with the exact schema captured directly from the real backup DB via `sqlite3 <db> ".schema <table>"` — structure only, not data (DROP is inherently lossy and neither table has a live writer to replay from).

**Re-verified zero Go call sites** (`grep -rn "agent_messages_legacy_089\|todos_legacy_d1" --include="*.go" .`): only `internal/store/migration_skip_recreate_test.go`'s own comments/assertions (updated below) — no store method, query, or non-test reference anywhere. Clean per the task's step 5 and Done-means criteria.

**Test suite updated**, not just patched to compile:
- `TestMigrateDoesNotReRecreateLegacyRenameMigrations` (`migration_skip_recreate_test.go`): flipped its `todos_legacy_d1`-must-still-exist assertion to `todos_legacy_d1`-must-be-gone (it previously documented "stays in place until 19 drops it" — that's now true in the other direction), updated the surrounding comments to describe the post-095 world instead of leaving them stale.
- Added `TestMigrateDropsLegacyRenameTables` (same file): the concrete regression for this task's Done-means criteria in one place — both legacy tables absent after a full migration run (via `newTestStore`, which always boots through every embedded migration from empty), live `agent_messages`/`todos` present with their real post-090/post-043 columns and zero rows on a fresh store, a seeded row in each survives a second `s.migrate()` (simulated restart, confirmed via `assertGooseHasNothingPending` beforehand) untouched, and neither legacy table reappears.
- Both pass: `go test ./internal/store/... ` — `ok`.

**Real-DB verification** (per `EXECUTION-PROCESS.md`'s migration-testing requirement and this task's own step 6/Done-means bullet). Copied `~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726` (37 MB, confirmed via `ls` — this is the exact filename referenced by `09-adopt-goose-migrations`'s own Work Log) to a scratch working copy; verified via `md5` that the original backup file is byte-identical before and after this task's work (`90a8ba59c739beedf7a99ad753be3bc3`, unchanged). Confirmed via `sqlite3` directly against the working copy, before running any Go code: `agent_messages_legacy_089` = 7 rows, `todos_legacy_d1` = 26 rows, `agent_messages` = 13 rows, `todos` = 42 rows — matching `09-adopt-goose-migrations`'s own recorded real-DB counts exactly, confirming this is the same real, populated backup, not an empty fixture, and that both legacy tables genuinely have data in them (not just schema) going into the drop.

Ran an ad-hoc, not-committed test (`internal/store/zz_realdb_verify_19_test.go`, deleted after use — same throwaway pattern `09-adopt-goose-migrations` used for its own real-DB check, hardcodes a session-local scratch path so it can't be a permanent test) that opened the working copy via the real `store.New()` cutover path (pre-existing database, no `goose_db_version` yet — the actual code path a real upgrade takes) and confirmed:
- Both legacy tables gone after the cutover boot.
- Live `agent_messages` (13 rows) / `todos` (42 rows) — byte-identical counts to before, schema intact (post-090 `kind`/`channel` columns and post-043 `project_id` column both still queryable).
- A second boot (close + reopen `store.New()` again against the same file, simulating a restart) is a clean no-op: legacy tables stay gone, row counts unchanged.

Independently re-confirmed all of the above directly via `sqlite3` against the mutated working copy (no Go involved): `sqlite_master` query for either legacy table name returns zero rows; `goose_db_version` ledger's max applied version is `95`; live row counts still `13`/`42`.

**Baseline checks**: `go build ./cmd/nanite/` — ok. `go vet ./...` — one finding, `internal/service/container.go`'s `stopReaper`/`stopRuntimeReaper` possible context leak — confirmed pre-existing and unrelated (this is the exact same finding `09-adopt-goose-migrations`'s own Work Log recorded and ruled out of scope; untouched by this task).

`go test ./...` — full suite result: 2 package failures (`internal/envelope`'s `TestEnvelopeSchemas_AllTypesHaveSchemas`, `internal/mcp`'s `TestNaniteToolDescribe_ShowCardExamplesValidateAgainstSchemas`), both `envelope type "question-form" has no schema` — everything else, including `internal/store` (this task's own package, `ok`), passes. Root-caused, not just noticed: `question-form` is fully absent from the shared `go-envelopes` library's manifest (`grep -rln question-form .claude/libs/go-envelopes/` matches only `CHANGELOG.md`; `manifest/envelopes.yaml` has no `question-form` entry, `manifest/schemas/` has no `question-form.schema.json`), while nanite's own Go code (`internal/mcp/self_tools*.go`, `internal/envelope/contracts_test.go`, `internal/service/chat_generate.go`, `internal/api/*envelopes*_test.go`) still references it — confirmed via `git status --short` that none of those packages have any changes in this worktree (this task never touched them). This is Phase 0 item `13-cut-question-form` (still `not-started` in `TASKS/INDEX.md` as of this writing) caught mid-flight: `.claude/libs/go-envelopes` is a shared, mutable local-replace target (`go.mod`'s `replace github.com/hollis-labs/go-envelopes => ../../libs/go-envelopes`) that every worktree resolves to the *same* directory outside git-worktree isolation, so another in-progress worker's edits to the shared library are visible here even though this task never touched envelopes/MCP code. Not a regression from this task — logged as a process note in `TASKS/ESCALATIONS.md` (shared, non-isolated dependency directory, same class of gap as the `TASKS/` uncommitted-09 issue above, affecting any worker's full-suite run until `13` finishes) rather than treated as this task's own failure.

**Done means, checked against this task's own criteria:**
- `agent_messages_legacy_089`/`todos_legacy_d1` no longer exist after migrations run on a fresh boot — confirmed (`TestMigrateDropsLegacyRenameTables`, real-DB verification).
- Live `agent_messages`/`todos` unaffected, same schema/row counts before and after — confirmed (real-DB row counts identical; schema columns re-verified queryable).
- Idempotent under goose's real ledger (runs once, tracked) — confirmed (`assertGooseHasNothingPending` before the second `s.migrate()`/second boot in both the unit test and the real-DB check; migration 095 is version 95, above `legacyMigrationCutoverVersion`, so it's a real, tracked, one-time application, not swallowed-error-safe by accident).
- Tested against a real backed-up database copy, not just an empty fixture — confirmed (row counts matching 09's own recorded real-DB state).
- `go build`/`go vet`/`go test` pass — confirmed (vet's one finding is pre-existing/unrelated, as above).
- No Go code references either legacy table, re-confirmed after landing — confirmed (grep clean outside this task's own updated test file).

No genuine stop-and-escalate condition was hit for the core task decision itself (dropping the two tables was never in question — `TASKS.md`/decision log §16/the architecture doc all agree, and this task's own re-verification found nothing contradicting that). The environment/dispatch gap and the adjacent index/trigger bug are both documented above and in `TASKS/ESCALATIONS.md` as heads-ups, per `EXECUTION-PROCESS.md`'s "surface, don't silently drop or silently fix out-of-scope things" default.


## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
