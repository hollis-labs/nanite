# Retire the in-app `workspaces` table and the filesystem `NANITE_WORKSPACE` mechanism

**Phase:** 0
**Status:** implemented
**Depends on:** none
**Touches:** `internal/store/migrations/001_schema.sql`-defined `workspaces` table (new drop migration), `internal/store/workspaces.go` (Workspace CRUD only — NOT the `Project`/`ListProjects`/etc. functions in the same file), `internal/api/workspaces.go` (workspace CRUD handlers only), `internal/api/api.go` (route removal, `/api/workspaces` CRUD only), `internal/api/sessions.go`, `internal/api/search.go` (`workspace_id` query param + `ResolveWorkspaceID` calls), `sessions.workspace_id` column (new migration), `projects.workspace_id` column + FK (new migration — **do not drop the `projects` table**, see Context), `workspace_role_trust` table (**full removal — operator-confirmed, see Context**), `internal/store/trust.go` (`ResolveTrust` — remove the override-layer call, revert to unconditional base-tier resolution), `internal/subagent/service.go:772`, `internal/mcp/self_tools_panels.go:168` (call-site updates for `ResolveTrust`'s simplified signature), `internal/api/trust.go` (remove the `workspace_role_trust` REST surface), `ui/src/components/settings/WorkspaceProjectManager.tsx` (delete or reduce to project-only manager), `ui/src/components/settings/RoleTrustPanel.tsx` (delete — workspace-scoped trust UI has nothing left to manage), `ui/src/lib/api.ts`/`ui/src/lib/types.ts` (workspace CRUD + trust-scoping client removal), doc-comment cleanup in `cmd/nanite/serve_autostart.go`, `cmd/nanite/path.go`, `internal/config/layout.go`, `cmd/nanite/plugin_cmd.go` (NANITE_WORKSPACE references — comments only, see Context)

## Context

TASKS.md Phase 0 item 20: "Retire the in-app `workspaces` table and the filesystem-level `NANITE_WORKSPACE` multi-instance mechanism (keep `NANITE_DB_PATH`)." Decision log §15 ("Two 'workspace' concepts — both retired, for different reasons"), architecture doc `05-storage-and-migrations.md`'s "'Workspace' — both concepts retired" section, and `GLOSSARY.md`'s **Instance**/**"Workspace"** entries all describe the same two-concept split. **This task required real disambiguation work before writing "What to do" — the naive reading of decision log §15 ("`projects` (nested under it) goes with it") is wrong and would break a real, kept mechanism if followed literally. Read this section in full before touching anything.**

### Concept 1 — the in-app `workspaces` table (UI session-grouping): real, 1 row, safe to retire — but `projects` does NOT go with it

`workspaces` is defined in `internal/store/migrations/001_schema.sql`. Confirmed 1 real row in practice (migration `087_consolidate_personal_workspace.sql`'s own commit message: consolidated a second "personal" workspace down to the sole "default" one, with 22 real sessions migrated, zero projects/workflows at that workspace). `internal/store/workspaces.go` has full `Workspace` CRUD (`ListWorkspaces`/`GetWorkspace`/`CreateWorkspace`/`UpdateWorkspace`/`DeleteWorkspace`/`ResolveWorkspaceID`), exposed via `internal/api/workspaces.go` + 5 real REST routes, with a real, mounted settings-page component (`ui/src/components/settings/WorkspaceProjectManager.tsx`). This part is a clean, low-risk cut.

**However — decision log §15 says "`projects` (nested under it) goes with it," and this is where the task prompt's disambiguation warning turns out to be load-bearing.** Verified directly against the schema (`internal/store/migrations/001_schema.sql`):

```sql
CREATE TABLE projects (
    id TEXT PRIMARY KEY,
    workspace_id TEXT NOT NULL REFERENCES workspaces(id),
    ...
);

CREATE TABLE agent_projects (
    agent_id TEXT NOT NULL,
    project_id TEXT NOT NULL REFERENCES projects(id),
    ...
);
```

**There is exactly one `projects` table in this schema — not two.** It is simultaneously:
1. The UI-grouping table nested under `workspaces` (`internal/store/workspaces.go`'s `Project`/`ListProjects(workspaceID)`/`CreateProject`/etc., exposed via `/api/workspaces/{wid}/projects` in `internal/api/workspaces.go`), **and**
2. The FK target of `agent_projects` — the real, live Agent Construction scope-reference mechanism architecture doc `01-agent-construction.md` explicitly describes as kept: *"`agent_projects` (already real, already FK'd once fixed) is the existing precedent [for scope]."* Confirmed live: `internal/store/agent_projects.go` (`ListAgentProjects`/`ListProjectAgents`/`AddAgentProject`/`RemoveAgentProject`, all joining directly against `projects`), exposed via real REST routes in `internal/api/agents.go` + `internal/api/api.go` (`GET/POST /api/agents/{id}/projects`, `DELETE /api/agents/{id}/projects/{projectId}`, `GET /api/projects/{id}/agents`).

**These are not two different `projects` concepts that happen to share a name — they are the literal same table serving both roles.** Decision log §15's "`projects` goes with it" is talking about the UI-grouping *role* of this table, but taking it literally (`DROP TABLE projects`) would delete the FK target `agent_projects` depends on and break the kept Agent Construction scope mechanism outright. **This is the real finding this task's disambiguation instruction was pointing at.**

**Resolution used for this task file** (a design call within what the docs already settled, not a new architecture decision — flag to the Orchestrator if this reasoning doesn't hold up under implementation): keep the `projects` table and `agent_projects` fully intact; only remove the `workspace_id NOT NULL REFERENCES workspaces(id)` column/FK from `projects` (drop the column, since there's no more `workspaces` table for it to reference) and the workspace-nested REST surface (`/api/workspaces/{wid}/projects/*`) — NOT the project-CRUD logic itself, which likely needs to be re-exposed as flat `/api/projects` routes (or reuse the `/api/agents/{id}/projects` + a still-needed `GET/POST/PUT/DELETE /api/projects` for the settings-page project manager, which the frontend `WorkspaceProjectManager.tsx`/sidebar project UI still needs — verify what the sidebar `ScopeSelector.tsx`/`NewProjectDialog.tsx` actually call before removing any project-CRUD route). **If this restructuring isn't mechanically obvious once you're in the code, escalate — don't guess past it; this is exactly the kind of design call `EXECUTION-PROCESS.md` wants surfaced, not silently resolved differently by two different workers.**

### Concept 1b — `workspace_role_trust`: a third, real, live consumer of `workspace_id` the decision log never mentions

Verified while tracing `workspace_id` usage: `workspace_role_trust` (`internal/store/migrations/035_role_trust_model.sql`, seeded by `036_role_trust_seed_dogfood.sql`) is a **live trust-tier gating mechanism** — `internal/store/trust.go`'s `ResolveTrust(ctx, workspaceID, agentProfileID)` is called from `internal/subagent/service.go:772` (real subagent-dispatch approval-bypass gating) and `internal/mcp/self_tools_panels.go:168`. `internal/dispatch/trust.go`'s `TrustResolver` interface doc comment: *"resolves the effective trust tier for a (workspace, agent) pair."* REST surface: `GET/POST/DELETE /api/workspaces/{workspace_id}/roles/{agent_profile_id}/trust` in `internal/api/trust.go`, with a real settings panel (`ui/src/components/settings/RoleTrustPanel.tsx`).

**Neither decision log §15 nor TASKS.md item 20 mentions `workspace_role_trust`** — flagged to the operator during planning as needing an explicit call. **Operator decision, 2026-08-18 — final: full removal, not a collapse-to-global.** Drop the `workspace_role_trust` table entirely, remove the `ResolveTrust` call sites at `internal/subagent/service.go:772` and `internal/mcp/self_tools_panels.go:168` (along with the override-layer logic in `internal/store/trust.go` they call into), remove the `/api/workspaces/{workspace_id}/roles/{agent_profile_id}/trust` REST surface (`internal/api/trust.go`), and delete `RoleTrustPanel.tsx`. Trust resolution reverts to unconditional base-tier resolution with no override layer — whatever `ResolveTrust` fell back to when no override row existed becomes the only behavior, not a workspace-scoped or global-scoped override table.

### Concept 2 — filesystem-level `NANITE_WORKSPACE`: lives in an external, separate repo — not directly cuttable from within `apps/nanite`

Grepped the entire repo for `NANITE_WORKSPACE`: it appears **only in doc comments** (`cmd/nanite/serve_autostart.go:190`, `cmd/nanite/path.go:23`, `internal/config/layout.go:20`, `cmd/nanite/plugin_cmd.go:155`) — **Nanite's own Go code never reads this env var directly.** It's honored implicitly by `github.com/hollis-labs/go-apppaths` (`paths.Resolve(brand.ID, ...)` in `internal/config/layout.go`), which derives `<APPNAME>_WORKSPACE` from the app's brand prefix as part of its own path-resolution API. **`go-apppaths` lives in a separate git repository** (`/Users/chrispian/dev/hollis-labs/libs/go-apppaths`, confirmed via `git rev-parse --show-toplevel` — it is not a subdirectory of this repo, it's a versioned Go module dependency, `github.com/hollis-labs/go-apppaths v0.1.0` in `go.mod`), **shared across Nanite, Tesseract, and (per `path.go`'s own comment) Torque**. The workspace-selection API (`Layout.SelectWorkspace`/`ResolveWorkspace`/`Workspaces()`, backed by an `active_workspace` pointer file under `StateDir`) is defined entirely in that external module's `paths/workspace.go`.

**Confirmed Nanite-side usage is zero**: `grep -rn "SelectWorkspace\|ResolveWorkspace\|\.Workspaces()\|WithWorkspace"` across the whole `apps/nanite` tree returns nothing — Nanite never calls any of go-apppaths' workspace-switching API. It only passively inherits the env-var-driven path resolution because that's how `paths.Resolve` works. **This means there is no Nanite-side code to delete for "the mechanism" itself — it was never built here.** What *is* real and in-scope for this repo: the doc comments in the 4 files above describe `NANITE_WORKSPACE` as if it's a supported, notable Nanite feature (e.g. `path.go`'s doc comment for the `nanite path` CLI subcommand: *"Resolution honors every override the running daemon would see — the NANITE_DB_PATH / NANITE_WORKSPACE env vars..."*). That's accurate today (the env var genuinely does still work, inherited from go-apppaths) but should be reworded to stop presenting it as a first-class Nanite feature, since Nanite is retiring its conceptual support for multi-instance switching.

**Confirmed decision log's claim of "zero automated usage in the test suite"**: `grep -rn "NANITE_WORKSPACE" --include="*_test.go" .` returns zero matches.

**Scope boundary for this task** (flag prominently when reporting): actually removing `<APPNAME>_WORKSPACE` support from `go-apppaths` would be a cross-repo change affecting Tesseract and Torque too — clearly out of scope for a Nanite-only Phase-0 task and not something this task file can direct a worker to do. If the operator wants the library-level capability gone too, that's a separate, cross-portfolio decision to raise explicitly — this task only covers what's actually addressable inside `apps/nanite`: doc-comment cleanup (stop describing it as a supported mechanism) and confirming (already done, above) that nothing here builds on top of it.

### A third, unrelated "workspace" — do NOT touch

While tracing `workspace` mentions, found a **third, distinct** usage that must be left alone: `WorkspacesRoot` (`internal/runtime/agent/deps.go`, `internal/service/agent_deps.go`, `internal/launcher/launcher.go`) is the on-disk root (`~/.nanite/workspaces/`, the pre-XDG dotdir convention, separate from go-apppaths' `~/.local/share/nanite/workspaces/default/`) where **CLI-launched agent subprocess boot directories get materialized** — completely unrelated to both the DB `workspaces` table and the `NANITE_WORKSPACE` env var. `GLOSSARY.md` and the decision log only document two "workspace" collisions (Instance vs. in-app table); this is a third one this task's investigation surfaced independently. Do not remove or rename `WorkspacesRoot` as part of this task — it's live, unrelated infrastructure for agent boot-dir materialization.

## What to do

1. **In-app `workspaces` table**: new migration dropping `workspaces`. Remove `Workspace`-only CRUD from `internal/store/workspaces.go` (keep `Project`/`ListProjects`/etc., see step 2) and `ResolveWorkspaceID`. Remove the 5 workspace-CRUD routes from `internal/api/api.go` and their handlers from `internal/api/workspaces.go`. Update `internal/api/sessions.go`/`internal/api/search.go` to drop the `workspace_id` query param and `ResolveWorkspaceID` call (list/search sessions without workspace scoping — there's only ever been one). Drop `sessions.workspace_id` column (new migration).
2. **`projects`/`agent_projects`**: **keep both tables.** Drop only `projects.workspace_id`'s column+FK (new migration). Re-derive whatever project-listing REST surface the frontend actually needs as workspace-less routes (verify real frontend callers — `ScopeSelector.tsx`, `NewProjectDialog.tsx`, `WorkspaceProjectManager.tsx`, `CreateProjectModal.tsx` — before deciding the final route shape; don't assume). Leave `agent_projects`, `internal/store/agent_projects.go`, and the `/api/agents/{id}/projects`/`/api/projects/{id}/agents` routes completely untouched.
3. **`workspace_role_trust`**: drop the table entirely (new migration). Remove the `ResolveTrust` call sites (`internal/subagent/service.go:772`, `internal/mcp/self_tools_panels.go:168`) and the override-layer logic in `internal/store/trust.go` they call into. Remove the REST surface in `internal/api/trust.go`. Trust resolution reverts to unconditional base-tier resolution — confirm what that base-tier fallback actually is (read `ResolveTrust`'s current implementation for what it returns when no override row exists) and make that the permanent, only behavior. This is operator-confirmed as of 2026-08-18 — not a design call left to the worker.
4. **Frontend**: remove/trim `WorkspaceProjectManager.tsx` per step 2's resolution (it may become a project-only manager rather than being deleted outright — check what it actually renders first). Delete `RoleTrustPanel.tsx` entirely — with `workspace_role_trust` fully removed, there's nothing left for this panel to manage. Remove workspace CRUD and trust-scoping client functions from `ui/src/lib/api.ts`/`types.ts`.
5. **`NANITE_WORKSPACE` doc comments**: reword the 4 comment sites (`cmd/nanite/serve_autostart.go:190`, `cmd/nanite/path.go:23`, `internal/config/layout.go:20`, `cmd/nanite/plugin_cmd.go:155`) to stop presenting `NANITE_WORKSPACE` as a supported Nanite feature, while staying factually accurate that go-apppaths still honors it underneath (don't claim it no longer works — it does, Nanite just isn't building anything on top of it). Do not touch `NANITE_DB_PATH` references — that mechanism is explicitly kept.
6. **Do not touch** `WorkspacesRoot` (`internal/runtime/agent/deps.go`, `internal/service/agent_deps.go`, `internal/launcher/launcher.go`) or anything under `internal/plugin/`/`internal/toolclient/tool_knowledge.go` that uses "workspace" to mean something else (e.g. `hadron_workspace_get`, the Hadron plugin's own unrelated concept) — verify any file you're about to edit is actually about one of the two concepts in scope before changing it.

## Done means

- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- `cd ui && npm run build` passes.
- Fresh-boot migration produces a DB with no `workspaces` table, no `sessions.workspace_id`/`projects.workspace_id` columns; `projects` and `agent_projects` tables still exist with their existing rows intact.
- Tested against a real copy of the backed-up database (`~/.local/share/nanite/workspaces/default/backups/`) — confirm the migration correctly re-points/preserves existing `projects` rows (there's real data here per migration `087`'s own note) and doesn't silently drop them.
- Agent-scope project assignment (`/api/agents/{id}/projects`) still works end-to-end after the migration — this is the concrete regression check for the disambiguation finding above.
- `workspace_role_trust` no longer exists; subagent-dispatch trust gating (`internal/subagent/service.go:772`) and `internal/mcp/self_tools_panels.go:168` compile and run against the simplified, unconditional base-tier `ResolveTrust` — verify with a real subagent dispatch that this doesn't silently break approval-bypass gating (confirm what the new unconditional behavior actually is and that it's intentional, not just "whatever didn't crash").
- `NANITE_DB_PATH` continues to work exactly as before (untouched).
- Settings page no longer shows a workspace switcher/manager (or shows a simplified project-only manager per step 2's resolution) and no longer shows a Role Trust panel.

## Work log

Implemented 2026-08-18. Before writing any migration, built a fresh fully-migrated store and dumped the real current schema via `sqlite_master`/`PRAGMA table_info` in a throwaway test (deleted before commit) rather than trusting old migration files — highest existing migration was confirmed 105, so new migrations are 106-109.

**Step 1 — in-app `workspaces` table**: new migration `106_drop_sessions_workspace_id.sql` drops `idx_sessions_workspace` then `sessions.workspace_id`; `109_drop_workspaces_table.sql` drops the `workspaces` table outright (Down recreates it + reseeds the single `default` row). Removed `Workspace` struct + CRUD (`ListWorkspaces`/`GetWorkspace`/`CreateWorkspace`/`UpdateWorkspace`/`DeleteWorkspace`/`ResolveWorkspaceID`) from `internal/store/workspaces.go`, which was renamed to `internal/store/projects.go` (`git mv`) since only the kept `Project` CRUD remains in it. Removed the 5 workspace-CRUD routes + workspace-nested project routes from `internal/api/api.go`; `internal/api/workspaces.go` renamed to `internal/api/projects.go` (`git mv`), now containing only the flat `handleListProjects`/`handleCreateProject`/`handleUpdateProject`/`handleDeleteProject`. `internal/api/sessions.go`/`search.go`/`shell.go`/`autocomplete.go` dropped `ResolveWorkspaceID`/`workspace_id` query-param logic. `internal/store/seed.go`'s idempotency gate switched from `SELECT COUNT(*) FROM workspaces` to `SELECT COUNT(*) FROM user_settings`.

**Step 2 — `projects`/`agent_projects`**: both tables kept fully intact; only `projects.workspace_id` column+FK dropped (migration `107_drop_projects_workspace_id.sql`). Route-shape verified (not assumed) against real frontend callers — `ScopeSelector.tsx`, `NewProjectDialog.tsx`, `WorkspaceProjectManager.tsx` (renamed to `ProjectManager.tsx`), `CreateProjectModal.tsx` — all called `listProjects(workspaceId)`/`createProject(workspaceId, data)` with `workspaceId` as a required-but-meaningless single-workspace param; resolved to flat `GET/POST /api/projects`, `PUT/DELETE /api/projects/{pid}` per the task's own suggested resolution. This was mechanically obvious once in the code, so no escalation was needed on the step-2 "design call" flagged in Context. `agent_projects`, `internal/store/agent_projects.go`, and the `/api/agents/{id}/projects`/`/api/projects/{id}/agents` routes left untouched except for `ListAgentProjects`'s SQL/Scan dropping the now-gone `p.workspace_id` column (required — the column no longer exists).

**Step 3 — `workspace_role_trust`**: full removal per operator-confirmed 2026-08-18 decision (not reopened). Migration `108_drop_workspace_role_trust.sql` drops the table (Down recreates table+index, no data restoration by design). `internal/store/trust.go`'s `ResolveTrust` simplified to `(ctx, agentProfileID string) (dispatch.TrustTier, error)` — queries only `agent_profiles.default_trust_tier`, falls back to `TrustNormal` on any miss (confirmed this is the existing base-tier default, not a new behavior). Removed `WorkspaceRoleTrust` type, `PromoteRoleInWorkspace`, `DemoteRoleInWorkspace`, `ListWorkspaceRoleTrust`. Updated call sites at `internal/subagent/service.go:772` and `internal/mcp/self_tools_panels.go:168` (via `internal/mcp/tool_ctx.go`'s simplified `CallerProfileFromContext`). Deleted `internal/api/trust.go` entirely (REST surface) and its 3 routes.

**Step 4 — frontend**: deleted `RoleTrustPanel.tsx` (confirmed zero mount points — was never wired into `SettingsPage.tsx`). `WorkspaceProjectManager.tsx` replaced by `ProjectManager.tsx` (renamed component, flat `listProjects()`, no `activeWorkspaceId`). Removed workspace CRUD + trust-scoping client functions from `ui/src/lib/api.ts`; removed `Workspace`/`WorkspaceRoleTrustOverride`/`TrustTier` types and `workspace_id` fields from `ui/src/lib/types.ts`. Removed `activeWorkspaceId` state from `useAppStore.ts` and every consuming component (`AppShell.tsx`, `LeftSidebar.tsx`, `ScopeSelector.tsx`, `NewProjectDialog.tsx`, `StartSurfaceDialog.tsx`, `CommandPalette.tsx`, `NavRail.tsx`, `SearchModal.tsx`, `AgentProfileManager.tsx`, `SessionInfoWidget.tsx`, `useKeyboardShortcuts.ts`, `AgentBuilderWizard.tsx`, `CreateProjectModal.tsx`).

**Step 5 — `NANITE_WORKSPACE` doc comments**: reworded the 4 sites (`cmd/nanite/serve_autostart.go`, `cmd/nanite/path.go`, `internal/config/layout.go`, `cmd/nanite/plugin_cmd.go`) to stop presenting it as a supported Nanite feature while staying accurate that go-apppaths still honors it underneath. `NANITE_DB_PATH` untouched.

**Step 6 — do-not-touch list respected**: `WorkspacesRoot` (agent boot-dir materialization in `internal/runtime/agent/deps.go`/`internal/service/agent_deps.go`/`internal/launcher/launcher.go`) and the Hadron plugin's `hadron_workspace_get` were left alone — confirmed both are unrelated "workspace" usages before touching anything nearby.

**Bug incidentally fixed**: `internal/service/durable_wake.go`'s `wakeSkipReason` had a `workspaceID == ""` check that always fired for any durable-agent instance with no attached session — silently masking that the CW-20260817 "process-class rewakeable while active" fix's own logic path was unreachable in exactly that case. Removing the dead `WorkspaceID` threading (it was never load-bearing outside this check) fixes this as a side effect. `internal/api/loom_curator_wake.go`'s `loomCuratorWakeWorkspaceID = "default"` hardcoded workaround, which existed specifically to route around this bug, was removed.

**Pre-existing bug found but NOT fixed (out of scope)**: while writing the end-to-end regression test for `/api/agents/{id}/projects` (`internal/api/agent_project_scope_e2e_test.go`), discovered that the sibling reverse-lookup route `GET /api/projects/{id}/agents` (`handleListProjectAgents` → `store.ListProjectAgents`) throws `"SQL logic error: ambiguous column name: created_at"`. Root cause: `store.agentColumns` (`internal/store/agents.go`) selects bare `created_at`/`updated_at`, and `ListProjectAgents`'s query JOINs `agent_profiles` with `agent_projects`, which has its own `created_at` column (`migrations/001_schema.sql`). Confirmed via `git diff HEAD -- internal/store/agent_projects.go` that this function and `agentColumns` are both untouched by this task's diff (my only edit in that file was `ListAgentProjects`, an unrelated function), and confirmed via `git log` that this predates the session entirely. Left unfixed per the task's explicit instruction to keep `agent_projects` fully intact and to avoid unrelated scope creep; the e2e test exercises everything the task's "Done means" actually requires (`/api/agents/{id}/projects`, not the reverse route) and documents the bug in a comment instead of asserting on it. Worth a follow-up task.

**Verification performed**:
- `go build ./cmd/nanite/`, `go build ./...`, `go test ./...` all pass.
- `go vet ./...` shows only one pre-existing, unrelated finding (`internal/service/container.go:1186/1206/1257`, "stopReaper function is not used on all paths") — confirmed via a temporary git worktree checked out at unmodified HEAD that this exists independent of this task's changes.
- `cd ui && npm run build` and `npx vitest run` (181/181 tests, 22/22 files) pass.
- Migration tested against a real copy of the production backup DB (`~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726`, copied to scratchpad before testing): all 344 real sessions preserved after migration; `workspaces` and `workspace_role_trust` tables dropped; `sessions.workspace_id`/`projects.workspace_id` columns dropped; `projects`/`agent_projects` tables intact and queryable with their existing rows; re-opening the migrated DB is idempotent (throwaway test, deleted before commit).
- Agent-scope project assignment end-to-end: new permanent test `internal/api/agent_project_scope_e2e_test.go` (`TestAgentProjectScope_EndToEnd`, `TestProjectsAPI_FlatCRUD_EndToEnd`) exercises the real HTTP mux — create project via flat `POST /api/projects`, assign via `POST /api/agents/{id}/projects`, list via `GET /api/agents/{id}/projects`, unassign via `DELETE .../projects/{projectId}`, confirm removal — plus full flat-CRUD coverage (`GET`/`PUT`/`DELETE /api/projects/...`). Both pass.
- Trust-gating end-to-end: new permanent test `internal/subagent/trust_real_store_test.go` (`TestSpawn_RealStoreTrustResolver_EndToEnd`) wires the real `*store.Store` (matching production's `container.go:1169` wiring) as the trust resolver and verifies all 4 tiers — untrusted refuses outright, trusted bypasses approval, normal requires approval, unknown-profile-ID falls back to `TrustNormal` — end-to-end through `subagent.Service.Spawn`. All 4 subtests pass.

No genuine escalation was hit; the step-2 route-shape question resolved mechanically once real frontend callers were inspected, matching the task file's own permitted resolution path. No `TASKS/ESCALATIONS.md` entry added.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
