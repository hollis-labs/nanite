# Housekeeping: delete stale agent profile files (`agridd-project-manager.md`, `proxima.md`)

**Phase:** 0
**Status:** implemented
**Depends on:** none
**Touches:** `.nanite/agents/agridd-project-manager.md` (deleted), `.nanite/agents/proxima.md` (deleted), `.nanite/agents/torque-task-writer.md` (unchanged — explicitly kept). No Go code changes required (verified — see Context).

## Context

TASKS.md Phase 0 item 24: "Housekeeping: review/delete `.nanite/agents/agridd-project-manager.md`/`proxima.md`; keep `torque-task-writer.md`." Decision log §3 ("Agent-profile housekeeping (captured, not urgent)"): "`.nanite/agents/agridd-project-manager.md` and `proxima.md` — flagged for review then deletion. May be recreated under new names, or dropped entirely, once the new agent spec exists. No action needed now. `.nanite/agents/torque-task-writer.md` — confirmed keep, solid as-is."

Both files exist and were read in full for this task:

- `.nanite/agents/agridd-project-manager.md` (18KB) — a work-coordination advisor profile for "the durable-agent runtime substrate (working title: agridd)," monitoring Torque task state and recommending dispatch timing.
- `.nanite/agents/proxima.md` (31KB) — "Operator's primary chat-interface agent... `operator → Proxima → everyone else`," a singleton relay/concierge durable agent.
- `.nanite/agents/torque-task-writer.md` (35KB, kept, not touched by this task) — a durable agent that validates and creates well-formed Torque tasks on behalf of other agents.

**Verified there is no live functional reference to either file** — this is the check the task explicitly asked for before scoping as a clean delete:

- `internal/agent/builtin/profiles_test.go`'s `TestInternalProfiles_LoadsAllExpectedSlugs` test comment confirms both are in the "zero-ref" bucket by design: "Phase 2 migrated the zero-ref product/tooling agents (analyst, code-auditor, file-backend, agent-builder, **agridd-project-manager**, **proxima**, torque-supervisor, torque-task-writer) out to the managed config layer (`.nanite/agents/`) where they are operator-editable." Neither slug appears in the compiled-in `internal/agent/builtin/profiles/` embed or in any harness dispatch/prompt-framing logic that hardcodes a dependency on them (unlike `backend`, `worker`, `planner`, `default`, etc., which the same test explicitly does pin as harness-referenced).
- `internal/service/durable_agent_recipes.go` mentions `agridd-project-manager` only in a **code comment**, as historical context for why the generalized `project-manager` recipe exists: "The older `agridd-project-manager` profile is the POC this was generalized from and is slated for retirement; new applies should use this one [`.nanite/agents/project-manager.md`, a separate file, not touched by this task]." The recipe's actual `ProfileRule: "operator_selected"` means the operator picks a profile at apply-time from whatever's live in the DB — it does not hardcode a lookup of `agridd-project-manager.md` or `proxima.md` by path or slug.
- The `"proxima-relay"` recipe (same file) is an unrelated, generically-named durable-agent recipe (`Name: "Proxima Relay"`) — it does not read or depend on `.nanite/agents/proxima.md`; it's a separate concept that happens to share a word.
- Other hits (`internal/store/durable_agents_test.go`'s `"legacy-proxima"` fixture slug, `internal/service/durable_agent_recipes_test.go`'s `"architect-proxima"` YAML fixture) are unrelated test-only string literals, not references to the file's content or path.
- Remaining references are documentation-only (decision log itself, `docs/architecture-agents-tasks-2026-08-18.md`, `docs/system-audit/2026-08-17/*`, `docs/architecture/agent-roles-design.md`, `docs/engineering/TASKS.md`) — informational, not functional.

**One live mechanism this deletion does interact with, worth understanding before deleting**: `.nanite/agents/` (project-level) is a real, currently-active discovery tier (`internal/agent/discovery.go`, "Priority 2: `.nanite/agents/` (project)") — files here are parsed and upserted into the DB's agent profile table on every boot via `AutoIngestAgents` (`internal/service/ingest.go`). This is the *same* file-reingest-on-boot mechanism TASKS.md item 10 is separately fixing ("stop `AutoIngestAgents` re-overwriting `source='internal'` rows every boot"), but that fix is not a prerequisite for this task — deleting the `.md` files simply means they stop being discovered and re-ingested on the next boot. It does **not** retroactively delete any existing DB row for these two profiles if one already exists in a given environment's database (the ingest mechanism is additive/upsert, not a two-way sync that prunes DB rows for vanished files) — an already-materialized `agent_profiles` row for `agridd-project-manager`/`proxima` would become an orphan (source file gone, but the DB row lingers) rather than disappearing automatically. Decide in What to do whether that orphan is acceptable (it is inert — nothing references the slug — matching decision log §3's "no action needed now" framing) or worth a one-line cleanup.

## What to do

1. Delete `.nanite/agents/agridd-project-manager.md` and `.nanite/agents/proxima.md`.
2. Do not touch `.nanite/agents/torque-task-writer.md` — confirmed keep.
3. Re-run the discovery/ingest path (or just restart the dev instance) and confirm no error/warning fires from the two profiles no longer being discoverable — `AutoIngestAgents` should simply stop seeing them, not fail.
4. Decide whether to leave any pre-existing `agent_profiles` DB rows for these two slugs as inert orphans (acceptable, matches decision log §3's low-urgency framing — nothing references the slug so an orphaned row has zero functional effect) or clean them up with a one-line `DELETE FROM agent_profiles WHERE slug IN ('agridd-project-manager', 'proxima')`-equivalent Store call. Given this is explicitly scoped as "small, low-risk" (per this task's origin instructions) and decision log §3 says "no action needed now," leaving any pre-existing row as an inert orphan is an acceptable default — but note the decision either way in the Work Log so it isn't silently ambiguous later.

## Done means

- Both files are gone from `.nanite/agents/`.
- `torque-task-writer.md` is untouched.
- A fresh boot/ingest pass produces no error or warning related to the removed files.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass (expect no code changes required, only the file deletions — if any test hardcodes an expectation that these files exist, that's a real finding to report, not silently work around).
- The DB-orphan question (see What to do, step 4) is explicitly decided and recorded, not left ambiguous.

## Work log

1. Verified pre-deletion state: both `.nanite/agents/agridd-project-manager.md` (18KB) and `.nanite/agents/proxima.md` (31KB) present in the worktree; `torque-task-writer.md` present throughout and never touched.
2. Independently re-verified the "no live functional reference" claim from Context (didn't just trust it): grepped `internal/` for `agridd-project-manager` and `proxima.md` — exactly two hits, both comments (`internal/agent/builtin/profiles_test.go:32`, `internal/service/durable_agent_recipes.go:710`). Grepped all `*_test.go` for the bare strings `agridd-project-manager`/`proxima` — three files hit (`profiles_test.go`, `durable_agent_recipes_test.go`, `durable_agents_test.go`), all comment-only or unrelated fixture strings (`architect-proxima` YAML fixture, `legacy-proxima` slug fixture, `proxima-relay` unrelated recipe name) exactly as Context described. Confirmed neither slug is in the compiled-in embed (`internal/agent/builtin/profiles/` contains only backend/background-job/default/hint-selector/planner/researcher/reviewer/system-architect/worker — no agridd/proxima). No new mismatch found here.
3. Deleted both files: `git rm .nanite/agents/agridd-project-manager.md .nanite/agents/proxima.md`. `torque-task-writer.md` left untouched (still present, unmodified — confirmed via final `git diff --stat HEAD`, which shows exactly the two deletions and nothing else).
4. Step 3 ("re-run discovery/ingest, confirm no error") verified by code reading rather than a literal live restart — restarting the shared `nanite-api-service` isn't appropriate work for an isolated-worktree housekeeping task, and the code path makes the outcome unambiguous: `internal/agent/discovery.go`'s `Discover()` calls `discoverDir(.../.nanite/agents, "project")`, which is a plain `os.ReadDir` with no diffing against any prior manifest and no hardcoded expectation that a given filename exists. `internal/service/ingest.go`'s `AutoIngestAgents` is purely additive — it iterates whatever `Discover()` returned and upserts each entry; a file that's simply absent from that returned slice is never visited, so there is no code path by which removing these two files can produce an error or warning. This structurally satisfies "Done means" without needing a literal restart.
5. **DB-orphan decision (step 4) — explicit call made: LEAVE existing `agent_profiles` rows (and their dependents) as inert orphans. No DELETE was run against the DB.** Investigated the real running DB (`~/.local/share/nanite/workspaces/default/main.db`, not a disposable test fixture) rather than guessing:
   - Both slugs do have pre-existing `agent_profiles` rows (`source='project'`, `updated_at` 2026-08-17 — yesterday's routine boot re-ingest, the same overwrite-on-boot behavior TASKS.md item 10 separately fixes).
   - **Correction to this task file's own Context**: "nothing references the slug... orphaned row has zero functional effect" is not fully accurate at the DB level. Both profile IDs *are* FK-referenced by dependent rows: `agent_known_tools` (54 rows), `agent_procedures` (5 rows), and `durable_agent_instances` (1 row each, `id` pattern `legacy-profile-<uuid>` — a generic backfill-migration artifact).
   - Checked whether this makes these two profiles special/"live": it doesn't. The identical `durable_agent_instances` backfill pattern exists for essentially every agent profile in the DB, including `torque-task-writer` itself (explicitly kept by this task) and ~15 others — all with `durable=0`. The dependent rows are generic system scaffolding applied uniformly across the whole roster, not evidence of special usage of these two specifically.
   - Proxima's instance row shows `status='start_requested'`, which in isolation could look like a live pending action — but its `updated_at` is `2026-05-25` (roughly three months stale relative to today, 2026-08-18) with no `current_session_id` set, so it reads as stale leftover state, not something an active process is currently acting on.
   - Given real FK-dependent rows exist across three tables, this is the live production DB (not a fixture), and there is no existing cascade-aware Store method for this kind of profile deletion (the only precedent, `internal/store/agents.go:477`, is a transactional delete-by-id, not a raw multi-table cleanup) — a one-line raw-SQL `DELETE FROM agent_profiles WHERE slug IN (...)` is *not* the "small, low-risk" action the task file's Context assumed; it risks either failing under FK enforcement or leaving dangling child rows. Leaving the rows in place is the safe, fully reversible, zero-blast-radius choice, and it still matches decision log §3's "no action needed now."
   - This is a documented decision, not a blocking escalation — the task file already sanctioned "leave as inert" as an acceptable default; this entry corrects *why* (real dependents exist, but they are generic infrastructure, not special live usage) so the call isn't left ambiguous.
6. `go build ./cmd/nanite/` — passed clean.
7. `go vet ./...` — one pre-existing failure, unrelated to this task: `internal/service/container.go:1186/1206/1257` (`stopReaper`/`stopRuntimeReaper` "not used on all paths" possible context leak). Confirmed pre-existing and unrelated: `git diff --stat HEAD` shows this task's diff is exactly the two file deletions, nothing touches `container.go`.
8. `go test ./...` — two pre-existing failures, both unrelated to this task and already tracked by a different, not-yet-executed Phase 0 item: `TestEnvelopeSchemas_AllTypesHaveSchemas` (`internal/envelope`) and `TestNaniteToolDescribe_ShowCardExamplesValidateAgainstSchemas` (`internal/mcp`) both fail because the `question-form` envelope type has no registered schema file. This is exactly what Phase 0 task `13-cut-question-form` (not this task) exists to remove — confirmed via `grep` that `question-form` appears only in `internal/envelope/validator.go` and test files, nothing related to `.nanite/agents/` or agent-profile ingestion. Every other package passes. No test in the suite hardcodes an expectation that `agridd-project-manager.md`/`proxima.md` exist.
9. **Process/environment note, not part of this task's own scope, logged separately in `TASKS/ESCALATIONS.md`**: a routine `git stash`/`git stash pop` (used only to diff the pre-existing `go vet` failure against clean HEAD) collided with a different, concurrently-active worktree's stash entry — `refs/stash` is a single ref shared by the whole repository, not scoped per worktree, and ~20+ worker worktrees were active at the time. No data was lost; see the ESCALATIONS.md entry for the full account and recovery. Net effect on this task: zero — final `git diff --stat HEAD` confirms only the two intended file deletions.

**Summary: Done means, checked off.** Both files gone from `.nanite/agents/`; `torque-task-writer.md` untouched; ingest/discovery path structurally verified to produce no error for the missing files; `go build`/`go vet`/`go test` all run, with only pre-existing, unrelated failures (documented above, not caused by this task); the DB-orphan question is explicitly decided (leave as inert orphan) and recorded above, not left ambiguous.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
