# Cut `ClassifySessionIntent`/`ScoreIntent`/`sessions.intent`; make Glass-4 universal

**Phase:** 0
**Status:** implemented
**Depends on:** none (see Context for why — and a real file-overlap coordination note with `21-cut-modes`, not a dependency)
**Touches:** `internal/service/session_intent.go` (deleted), `internal/service/session_intent_test.go` (deleted), `internal/api/sessions.go` (session-creation handler, ~lines 86-103 — remove the classify-and-set block), `internal/store/sessions.go` (`Session.Intent *string` field ~line 56, `GetSessionIntent`/`SetSessionIntent` ~lines 370-410, scan-population sites ~lines 295 and 579), `internal/store/migrations/052_session_intent.sql` (read-only reference — the column this removes), a new migration dropping `sessions.intent` (CHECK-constrained column, requires SQLite rename-recreate-copy or goose equivalent), `internal/service/handoff_glass4.go` (`IsLongRunning`, ~lines 157-165 — deleted), `internal/service/chat_generate.go` (two `IsLongRunning`-gated blocks, ~lines 2363-2368 and ~2627-2632 for the proactive stash call; ~lines 2426-2431 and ~2670-2676 for the post-compaction inject call — remove the gates, make both calls unconditional), `internal/store/sessions_intent_test.go` (deleted or rewritten), `internal/store/migration_052_verify_test.go` (deleted or rewritten).

## Context

TASKS.md Phase 0 item 28: "`ClassifySessionIntent`/`ScoreIntent`/`sessions.intent`, alongside making Glass-4 universal (remove the `IsLongRunning` gate so every session gets it, then delete the now-unused classifier)."

Decision log §23 ("Glass-4 handoff — made universal; the intent classifier is cut entirely") and `docs/engineering/architecture/06-session-lifecycle-and-recovery.md`'s "Handoffs, scratchpad, and Glass-4" section are the governing sources:

> "Glass-4 ... becomes universal — every session gets it, not just ones that scored above a threshold. Motivation: not an observed context-size problem (none seen in practice), but general, low-cost value regardless of session type. **The entire session-intent classification mechanism is cut alongside this** (`ClassifySessionIntent`/`ScoreIntent`, `internal/service/session_intent.go`, the `sessions.intent` column, `IsLongRunning`). Verified it has exactly one real consumer — gating Glass-4 — which is now moot. Reinforcing evidence this was the right call, not just a simplification: two of the classifier's weighted signals (`HasSessionMode`, `HasWorkspace`) already depend on mechanisms cut earlier in this same review (modes; the in-app `workspaces` concept), so the classifier would have silently degraded regardless."

**Verified end-to-end, matches the decision log exactly:**

- `internal/service/session_intent.go` defines `IntentSignals` (with `HasSessionMode`/`SessionModeSlug` populated from a `*store.Mode`, and `HasWorkspace` populated from `sess.WorkspaceID != ""`), `ScoreIntent` (additive weighted scorer — `+3` for `HasSessionMode` when the mode slug is one of `work`/`plan`/`planning`/`research`/`build`/`code`; `+1` for `HasWorkspace`), `ClassifySessionIntent` (buckets the score into `long-running`/`per-turn`/`ephemeral`), and `SignalsFromSession` (the assembler, taking `*store.Mode` as a parameter).
- **Exactly one real caller**: `internal/api/sessions.go`'s session-creation handler (~lines 86-103) — `service.SignalsFromSession(sess, agent, sessionMode)` → `service.ClassifySessionIntent(...)` → `a.Services.Store.SetSessionIntent(sess.ID, intent)`. No other call site anywhere in the codebase (verified via grep for `ClassifySessionIntent`/`ScoreIntent` — only `session_intent.go` itself, `session_intent_test.go`, and this one handler).
- **`IsLongRunning`** (`internal/service/handoff_glass4.go` ~line 161): `func IsLongRunning(sess *store.Session) bool { if sess == nil || sess.Intent == nil { return false }; return *sess.Intent == store.SessionIntentLongRunning }`. Its own doc comment: "Used as the gate for both the proactive Glass-4 stash hook and the post-compaction inject — per-turn / ephemeral / unclassified sessions never carry a Glass-4 handoff."
- **Exactly two call sites, both in `internal/service/chat_generate.go`**, each gating two things:
  - `longRunning := IsLongRunning(sess)` gates a call to `s.ensureGlass4HandoffPreCompact(ctx, sess, chatMessages, ch)` (the proactive stash write) in both `recoverFromContextOverflow`-shaped code (~line 2363) and `enforceBudgetOrCompact` (~line 2627).
  - The same `longRunning` var also gates `InjectGlass4HandoffSlot(s.store, result.Window, sessionID, ch)` (the post-compaction inject) a few lines later in each function (~2428 and ~2672).
  - **Note**: in the first of these two functions, `longRunning` (via `!longRunning`) *also* currently gates whether the legacy P7 stash writer (`pipeline.StashWriter = storeStashWriter{s: s.store}`) is wired — but P7 itself is being cut entirely by a separate task (TASKS.md item 27, decision log §22: "cut the extraction step only; the scratchpad tool and Glass-4 both stay untouched"). Once P7's extraction step is gone, this `if !longRunning { pipeline.StashWriter = ... }` line disappears on its own — don't treat it as this task's problem to solve if `27-cut-p7-handoff-stash-snapshot` (or equivalent) lands first; if it hasn't landed yet when this task runs, removing the `IsLongRunning` gate here means `pipeline.StashWriter` would need to be set unconditionally-false (never wired) rather than conditionally, since after this task every session is "long-running" in Glass-4 terms. Coordinate or escalate if both tasks are in flight on this file simultaneously.
- **`sessions.intent` column**: added by `internal/store/migrations/052_session_intent.sql` — `ALTER TABLE sessions ADD COLUMN intent TEXT NULL CHECK (intent IS NULL OR intent IN ('long-running', 'per-turn', 'ephemeral'))`. A `CHECK`-constrained column requires the same SQLite rename-recreate-copy dance (or a goose equivalent, if `09-adopt-goose-migrations` has landed) as other constrained-column removals in this codebase — this is not a plain `DROP COLUMN`.
- **No frontend surface**: checked `ui/src/lib/types.ts` and related components — the only `intent` fields exposed to the UI are `BrokerDecision.intent`/`InspectorBrokerDecision.intent` (the **Tool Broker**'s per-selection intent string, an unrelated system — see `22-remove-skill-and-tool-broker-abstractions`'s Context section for the same naming trap). `sessions.intent` is never rendered or read by the frontend. No frontend changes needed.
- **Naming trap to flag for the worker**: `internal/context/handoff.go`'s `HandoffPayload.SessionIntent` (a free-text narrative field inside the Glass-4 self-authored payload, e.g. `"long-running session; deterministic handoff fallback..."` in `handoff_glass4_fallback.go`) is a **completely different field** from `store.Session.Intent`/`sessions.intent` — same word, unrelated system (one is a classifier enum on the session row, the other is prose content inside a handoff envelope). Do not touch `HandoffPayload.SessionIntent` or its call sites; it stays as-is.

### Dependency reasoning on `21-cut-modes` / `20-retire-workspaces-and-instance-mechanism` (explicitly asked for)

This task **does not need** `21-cut-modes` or `20-retire-workspaces-and-instance-mechanism` to land first. The reinforcing argument in decision log §23 (`HasSessionMode`/`HasWorkspace` "already depend on mechanisms cut earlier in this same review") is *supporting evidence the cut is correct*, not a technical prerequisite — this task deletes `session_intent.go` wholesale, so it doesn't matter whether `store.Mode`/workspace fields still resolve correctly by the time this lands; the whole scoring mechanism that reads them is going away regardless, not being preserved in degraded form.

**However, there is a real file-overlap risk worth flagging, not a dependency**: `internal/api/sessions.go`'s session-creation handler (the block this task removes) currently calls `a.Services.Store.GetSessionMode(sess.ID)` to obtain the `*store.Mode` passed into `SignalsFromSession`. If `21-cut-modes` lands *first* and removes `store.Mode`/`GetSessionMode` from the codebase before this task starts, `internal/api/sessions.go` will already fail to compile at that call site — which is fine, because this task's own worker will be removing that exact block anyway (verify it's already broken/removed rather than re-introducing it). If this task lands *first*, the classify-and-set block (including the `GetSessionMode` call) disappears entirely, and `21-cut-modes`'s worker won't find anything session-intent-related left to touch in this file. **Both tasks touch the same ~20-line region of `internal/api/sessions.go`.** Per `EXECUTION-PROCESS.md`'s parallelization-safety check ("For every pair of tasks you're considering running in parallel, explicitly check they don't touch overlapping files... If there's any doubt, serialize them"), **this task and `21-cut-modes` must not run in parallel** — the Orchestrator should serialize them (order doesn't matter, but not simultaneously) to avoid a merge collision on the same lines.

## What to do

1. Delete `internal/service/session_intent.go` and `internal/service/session_intent_test.go`.
2. In `internal/api/sessions.go`, remove the classify-and-set block (~lines 86-103: the `GetSessionMode` call, `SignalsFromSession`, `ClassifySessionIntent`, `SetSessionIntent`, and the `sess.Intent = &intentVal` assignment). Confirm nothing else in this handler depended on `sessionMode`/`agent` being fetched only for this purpose — check whether `agent` (`a.Services.Store.GetAgent(agentID)`) is used elsewhere in the handler before removing that fetch too.
3. In `internal/service/handoff_glass4.go`, delete `IsLongRunning`.
4. In `internal/service/chat_generate.go`, remove all four `IsLongRunning`/`longRunning`-gated conditionals: make `ensureGlass4HandoffPreCompact` and `InjectGlass4HandoffSlot` calls unconditional in both functions. Handle the P7 `StashWriter` interaction per the Context note above (coordinate with `27-*` if in flight, or resolve directly if not).
5. In `internal/store/sessions.go`, remove `Session.Intent *string`, `GetSessionIntent`, `SetSessionIntent`, `SessionIntentLongRunning`/`SessionIntentPerTurn`/`SessionIntentEphemeral` constants, and the two scan-population sites (~lines 295, 579).
6. Add a migration dropping the `sessions.intent` column — this is a CHECK-constrained column, so it needs the rename-recreate-copy pattern (see `internal/store/migrations/073_agent_profiles_multi_agent.sql` or `082_durable_agent_stopped_status.sql` for precedent shape) unless `09-adopt-goose-migrations` has landed, in which case use whatever mechanism that task establishes for constrained-column drops.
7. Remove or rewrite `internal/store/sessions_intent_test.go` and `internal/store/migration_052_verify_test.go` — they test the mechanism being deleted.
8. Test the migration against a real copy of the backed-up database (`~/.local/share/nanite/workspaces/default/backups/`), confirming existing `sessions.intent` values are handled cleanly by the drop (no orphaned CHECK-violating state) and no other column data is lost.

## Done means

- `internal/service/session_intent.go` no longer exists.
- `sessions.intent` column no longer exists.
- `IsLongRunning` no longer exists; Glass-4's proactive stash and post-compaction inject fire unconditionally for every session (verified with a real chat session, not just a passing test — confirm a plain, non-pinned, no-mode, no-workspace session still gets a Glass-4 handoff written and re-injected across a real compaction event).
- `HandoffPayload.SessionIntent` (the unrelated prose field) is untouched.
- The P7 `StashWriter`-gating interaction (Context note) is resolved cleanly — either coordinated with the P7 cut task or handled directly, not left half-gated.
- Tested against a real backed-up database copy for the column-drop migration.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- This task's execution was serialized against `21-cut-modes` (not run in parallel) — flagged to the Orchestrator if that wasn't already reflected in the parallelization plan.

## Work log

Confirmed task 27 (P7 cut) had already landed: grepped for `StashWriter`/`storeStashWriter` across `internal/service/*.go` and `internal/context/*.go` — zero hits. The Context note's "coordinate or escalate" branch was moot as the brief said; nothing to resolve there.

Implemented per "What to do", 1:1:

1. Deleted `internal/service/session_intent.go` and `internal/service/session_intent_test.go` (`git rm`).
2. `internal/api/sessions.go`: removed the classify-and-set block (the `GetSessionMode`/`SignalsFromSession`/`ClassifySessionIntent`/`SetSessionIntent`/`sess.Intent = &intentVal` block, originally lines ~85-104). Confirmed `agent`/`sessionMode` were scoped only to that `if` block and used nowhere else in `handleCreateSession`. `service`/`slog` imports stay — both used elsewhere in the file (compaction handler, error checks).
3. `internal/service/handoff_glass4.go`: deleted `IsLongRunning`. `store` import still used elsewhere in the file (`UpsertHandoffStash`, `ErrHandoffStashNotFound`).
4. `internal/service/chat_generate.go`: removed all four gates. Actual line numbers had drifted from the brief's estimates (~2310/2363 and ~2562/2600 vs. the brief's ~2363/2426 and ~2627/2670) — confirmed by reading current file state directly rather than trusting the stale line numbers, per instructions. Both `ensureGlass4HandoffPreCompact` and `InjectGlass4HandoffSlot` calls are now unconditional in `recoverFromContextOverflow` and `enforceBudgetOrCompact`. The `longRunning` local var is gone from both functions; `sess, _ := s.store.GetSession(sessionID)` stays (still needed by `ensureGlass4HandoffPreCompact`'s signature).
5. `internal/store/sessions.go`: removed `Session.Intent`, `GetSessionIntent`, `SetSessionIntent`, `SessionIntentLongRunning`/`PerTurn`/`Ephemeral`, `validSessionIntent`. Found **three** scan-population sites, not two as the brief estimated (`ListSessions`, `GetSession`, and `GetSessionByShortCode` — the brief's "~lines 295 and 579" only accounted for the first two; `GetSessionByShortCode` was a third, independent scan/hydrate block that also selected `intent`). Fixed all three: dropped `sess.intent`/`intent` from each SELECT and Scan, removed the `intent sql.NullString` locals, and dropped the `intent` parameter from the shared `hydrateSessionOptionalFields` helper (used by `ListSessions`/`GetSession`; `GetSessionByShortCode` hydrates inline and needed its own local fix). Verified with `grep -n "Intent\|intent" internal/store/sessions.go` → zero hits afterward.
6. **Mechanism correction (worker step 7 — decision stands, rationale corrected):** the task file states the CHECK-constrained column "needs the rename-recreate-copy pattern... this is not a plain DROP COLUMN," citing pre-goose migrations 074/083 (CHECK-*widening*, a different operation) as precedent. Verified directly against the SQLite version this codebase actually embeds (`modernc.org/sqlite v1.54.0` → `SQLITE_VERSION = "3.53.3"`, confirmed in `lib/sqlite.go`) with a live round-trip probe (create table with the exact `sessions.intent` CHECK clause, insert rows, `ALTER TABLE ... DROP COLUMN intent`, re-`ADD COLUMN` with the same CHECK, confirm the constraint still rejects an out-of-enum value): modern SQLite drops a CHECK constraint that references only the column being dropped, along with the column, natively — no rebuild required. This matches the immediate predecessor task's own precedent (migration 102, also post-goose, also a Phase 0 column-cut: plain `ALTER TABLE ... DROP COLUMN` + a real Down). Used that mechanism instead of a full rename-recreate-copy rebuild — simpler, less risk of index/trigger drift, and directly precedented. Full reasoning is in the new migration file's own header comment.
   - Added `internal/store/migrations/103_drop_session_intent.sql` (103 was the next free number — reverified `ls internal/store/migrations/` immediately before writing, still topped at 102). Real, tested Up (`DROP COLUMN`) and Down (`ADD COLUMN ... CHECK (...)`, structure-only per 102's precedent — DROP COLUMN is inherently lossy and nothing read `sessions.intent` back into a durable log).
7. Deleted `internal/store/sessions_intent_test.go` (tests the deleted mechanism entirely — no rewrite target). Deleted `internal/store/migration_053_verify_test.go` (migration renumbered 052→053 by `09-adopt-goose-migrations`; the file tested migration 053's Up-side column/CHECK presence, which is now moot on a fresh DB post-103). Replaced with `internal/store/migration_103_drop_session_intent_test.go`, modeled directly on `migration_102_drop_session_compaction_test.go`'s shape (fresh-migrate-drops-column + idempotent-re-migrate test, plus a DownTo/Up round-trip test) — the Down-round-trip test also folds in the old 053 test's raw-SQL CHECK-constraint-enforcement probe (now exercised on the Down-recreated column, since a fresh DB no longer has it at all).
8. Migration tested against a real copy of the backed-up DB (`~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726`, copied with its `-shm`/`-wal` siblings to a scratch dir). This backup predates goose (no `goose_db_version` table) with schema already through migration ~09x territory (has the CHECK-constrained `intent` column, 343 sessions, 3 with `intent='per-turn'`, 340 NULL). Opened via `store.New()` (exercises `isPreGooseDatabase`/`seedLegacyLedger` then runs 095→103 for real): `sessions.intent` column gone afterward, all 343 rows preserved (`ListSessions`/`GetSession` both round-tripped cleanly on real rows), re-open is a clean no-op (`assertGooseHasNothingPending`). Verification script was a throwaway `_test.go` in the `store` package, deleted after use — not committed.

**Real chat-session verification (per Done means, not just a passing test):** deployed to the running `nanite-api-service` via `cerberus_resource_deploy` (pid cut over 96049→99735, clean boot, no crash-loop). Discovered `context_window_tokens` is not a mutable field via `PUT /api/settings` (not in `handleUpdateSettings`'s accepted-fields list) — set/restored it directly against the live DB's `user_settings` row instead (temporary value during the test only, restored to 200000 immediately after). Created a genuinely plain session (`POST /api/sessions` with only `workspace_id`; no agent override, no mode, no pin — confirmed the JSON response has no `intent` field at all now, not even `null`, since the Go field is gone) and drove three real turns through it, then dropped the window low enough to force `enforceBudgetOrCompact` to fire on a fourth turn with real accumulated conversation to compact against. Server logs and the SSE stream both confirm, for this session with **no `sessions.intent` value — the column doesn't exist** — Glass-4 fired on both sides unconditionally:
- `"handoff: pre-compaction stash captured (glass-4 deterministic fallback)"` (the `ensureGlass4HandoffPreCompact` call)
- SSE `handoff_loaded` event + `"handoff: post-compaction inject"` log line (the `InjectGlass4HandoffSlot` call)
- `handoff_stashes` table has a real persisted row for this session (`id=ded3762a-1283-4a50-ad29-45d24bc56eb7`, `payload` containing `"schema":"glass-4"` and the expected fallback `session_intent` prose field — confirming `HandoffPayload.SessionIntent`, the unrelated free-text field, is alive and untouched, exactly as the brief required)

The eventual `"Context too large after all reductions"` error later in that turn is an artifact of the deliberately-tiny 1000-token test window (unrelated to this change — the pre-existing hard-ceiling enforcer correctly refusing an impossible budget). Cleaned up afterward: restored `context_window_tokens` to 200000 (confirmed via `GET /api/settings`), archived the test session (`DELETE /api/sessions/{id}`), confirmed service still `running` with no stale-artifact flag.

**Checks:** `go build ./cmd/nanite/` — ok. `go vet ./...` — one pre-existing failure in `internal/service/container.go` (`stopReaper`/`stopRuntimeReaper` possible-context-leak lint), confirmed unrelated: that file is untouched by this change (not in `git status` diff) and the finding predates this task. `go test ./...` — all packages pass, including `internal/store`, `internal/service`, `internal/api`.

No escalations. Serialized correctly against `21-cut-modes` per the brief's own note (this task ran to completion solo before any `21` work started — the Orchestrator can now unblock `20`/`21`, both of which listed `28` as a dependency in `TASKS/INDEX.md`).

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
