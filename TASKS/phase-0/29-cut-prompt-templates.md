# Relocate compaction-disclosure content, then cut `prompt_templates`/`agent_prompt_templates`

**Phase:** 0
**Status:** not-started
**Depends on:** none — see Context for why this does *not* need `21-cut-modes` to land first, despite TASKS.md's own phrasing suggesting otherwise
**Touches:** `internal/chat/context.go` (`renderCompactionDisclosure`/`disclosureSlugForMode`/`interpolateDisclosure` — rewritten to use hardcoded content); `internal/chat/context_client.go` (`assembleAgentSlotContent`, lines 602-644 — remove the `ComposePromptForAgent` call, the former "legacy fallback" becomes the only path); `internal/chat/universal_rules.go` (stale comment reference, line 13); `internal/store/prompt_templates.go` (delete entire file); `internal/api/prompt_templates.go` (delete entire file); `internal/api/api.go` (remove 8 route registrations, lines 344-351); `internal/api/agent_builder.go` (~lines 518-525, remove `PromptTemplateIDs` handling in `capabilityOperations`); `internal/api/types.go` (line 326, remove `PromptTemplateIDs` field from `AgentBuilderCapabilitiesInput`); `internal/service/store.go` (lines 163-172, remove 10 interface method declarations); a new migration dropping `prompt_templates`/`agent_prompt_templates`; frontend: `ui/src/components/settings/PromptTemplateEditor.tsx` (delete), `ui/src/components/settings/AgentProfileManager.tsx`, `ui/src/components/settings/agents/AgentCapabilitiesPanel.tsx`, `ui/src/components/settings/SystemPromptsViewer.tsx`, `ui/src/components/settings/agents/AgentDetailView.tsx`, `ui/src/components/settings/agents/PromptDetailView.tsx`, `ui/src/components/settings/agents/AgentBuilderWizard.tsx` (remove the prompt-template assignment step), `ui/src/lib/api.ts` (remove `listPromptTemplates`/`getPromptTemplate`/`createPromptTemplate`/`updatePromptTemplate`/`deletePromptTemplate`/`listAgentTemplates`/`assignTemplateToAgent`/`removeTemplateFromAgent`, ~lines 2353-2440), `ui/src/lib/types.ts` (remove `PromptTemplate` type); tests: `internal/store/prompt_templates_test.go` (delete), `internal/store/compaction_disclosure_test.go` (rewrite against the new hardcoded content), `internal/store/migration056_idempotency_test.go` (review — tests a migration chain against a table that no longer exists post-cut), `internal/store/agents_test.go`, `internal/builders/builder_test.go`, `internal/service/chat_test.go`, `ui/src/__tests__/phase-22-agent-capabilities.test.tsx`, `ui/src/__tests__/phase-24-agent-builder-wizard.test.tsx`, `ui/src/__tests__/phase-26-agent-boot-plan.test.tsx` (review/update all for `prompt_templates`/`PromptTemplate` references)

## Context

TASKS.md Phase 0 Cuts, item 29: "`prompt_templates`/`agent_prompt_templates` — redundant with the new Role-based `system_prompt` and barely adopted in practice (one real assignment ever, added early in development before agents were properly set up). Relocate the compaction-disclosure content first: make it a single, universal, hardcoded piece of content in the compaction pipeline itself — every chat agent gets it unconditionally when relevant, not a per-agent-assignable template. The 4 existing variants were keyed by *mode* (general/code/plan/research); since modes are being cut, this collapses to one unified disclosure message rather than 4 mode-branched ones — genuinely simpler, not just relocated. Then cut the rest of the mechanism." The decision log doesn't cover `prompt_templates` as its own topic anywhere — the closest hits are the "Open question, not yet decided" note at the end of the Phase 0 Renames section ("`prompt_templates` vs. `templates` — flagged as a confusing naming collision but never explicitly decided as a rename. Add to this sweep, or leave deferred?") and §5's naming-cleanup notes, neither of which actually discusses cutting the mechanism. **This task's authority is TASKS.md alone for the cut decision itself** — cite TASKS.md, not a decision-log section that doesn't exist for this specific claim.

### Reality check #1 — the mechanism is materially bigger and more live than "barely adopted" suggests. Report this prominently.

`prompt_templates`/`agent_prompt_templates` is not just a compaction-disclosure content store. `ComposePromptForAgent` (`internal/store/prompt_templates.go:255-305`) is the **live system-prompt assembly path for the canonical `file-default` chat agent** — called from `internal/chat/context_client.go:620` inside `assembleAgentSlotContent`, which every system-prompt assembly path passes through. Migration `027_chat_role_harness_prompt.sql` is the **only** `INSERT INTO agent_prompt_templates` row across all 93 migrations (`grep -n "INSERT.*agent_prompt_templates\|AssignPromptTemplateToAgent" internal/store/migrations/*.sql` confirms this — TASKS.md's "one real assignment ever" claim is accurate on this specific point) — it assigns `blt-chat-harness-001` (the actual "You are a helpful AI assistant embedded in the Nanite chat harness..." identity prompt) to `agent_id = 'file-default'`. That one assignment is what puts real, load-bearing content in front of every `file-default` chat turn today.

There is also a real, wired frontend surface beyond that one seeded row: the Agent Builder Wizard (`ui/src/components/settings/agents/AgentBuilderWizard.tsx`) lets an operator select prompt templates when creating/editing any agent, and on save loops `for (const templateId of capabilities.prompt_template_ids ?? []) { await api.assignTemplateToAgent(agentId, { template_id: templateId }); }` (line 1757-1759) — a real call into `POST /api/agents/{id}/prompt-templates`. So while only one assignment has ever been *seeded*, the wizard makes it possible for an operator to have created more at runtime; this cut removes a working (if lightly-used) feature, not just an inert seed row. Full CRUD REST surface confirmed at `internal/api/api.go:344-351` (list/create/get/update/delete templates, plus list/assign/remove per-agent).

**Why the cut is still safe** (verified, not assumed): `assembleAgentSlotContent` already has a fallback for every agent *without* a template assignment — `if composed == "" { ...use agent.SystemPrompt... }` (`internal/chat/context_client.go:625-634`). Every agent except `file-default` already goes through this fallback today (zero other assignments exist). `file-default`'s own `agent_profiles.system_prompt` is kept in lockstep with the exact same canonical content the `chat-role-harness` template serves, via the file-source-of-truth boot sync documented in `internal/agent/builtin/profiles/default.md`'s frontmatter (PROMPT-SYNC comment: "`internal/agent/builtin/profiles/*.md` [is] the file source-of-truth for ALL internal agents — boot-time sync replaces the `source='internal'` agent_profiles row body with this file's contents on every Nanite start"). Migration `058_universal_rules_extract.sql`'s own comment confirms the same demoted body is maintained in both places by hand ("`internal/agent/builtin/profiles/default.md` is the canonical markdown source and has been demoted in parallel ... When the demoted body changes, edit `default.md` AND the SQL strings below in lockstep"). So removing `ComposePromptForAgent` and always taking the `agent.SystemPrompt` path should be functionally equivalent for `file-default` — **but this is a claim to verify against the live DB, not just trust from the comments** (see What to do, step 1).

### Reality check #2 — "since modes are being cut" is an imprecise citation; the actual dependency reasoning is independent of `21-cut-modes`

TASKS.md's rationale conflates two unrelated "mode" concepts that happen to share vocabulary. The 4 disclosure variants (`compaction-disclosure-general`/`-code`/`-plan`/`-research`, seeded by `internal/store/migrations/030_compaction_disclosure_prompts.sql`) are selected by `disclosureSlugForMode(evt.SummaryMode)` (`internal/chat/context.go:100-111`), where `SummaryMode` comes from `CompactionEvent.SummaryMode`, which is populated from `CompactionPipeline.Mode`, which is set by `classifyModeFromAgentTags(agent)`/`ClassifyCompactionMode` (`internal/service/chat_generate.go:2814-2886`) — **a heuristic that inspects `agent.Tags` for substrings like "code"/"engineer"/"plan"/"research"**. This is a completely different mechanism from the Session Mode system `21-cut-modes` removes (the `modes` table, `sessions.current_mode_id`, slash commands, `ClassifyMode`). `classifyModeFromAgentTags`/`CompactionMode` is not part of that cut at all — it continues to exist after `21-cut-modes` lands, and it's still actively used elsewhere: `p.Mode` also selects the summarizer's system prompt during real compaction (`summarySystemPrompt(p.Mode)`, `internal/context/compaction.go:271`) — **this task does not touch that**, only the disclosure-template lookup.

Given that, the actual reasoning for collapsing 4 disclosure variants into 1 doesn't route through `21-cut-modes` at all — it's simply: the 4 texts share ~90% identical structure (Compaction Notice → Preserved/Lost → Recovery: handoff stash id, `chat_search`, summary metadata) and differ mainly in which nouns get preserved/lost per domain; collapsing them into one unified message that doesn't need a mode lookup is a reasonable simplification on its own merits, independent of any other Phase 0 item. **Depends on: none** — do not block this task on `21-cut-modes`.

### Verified: the compaction-disclosure mechanism is currently unreachable in practice (separate finding, not a reason to skip step 1)

`renderCompactionDisclosure` (`internal/chat/context.go:43-69`) starts with `evt, err := s.GetLatestCompactionEvent(ctx, sessionID)`. Per decision log §20, `compaction_events` has no writer wired at either production call site yet (Phase 5 work) — meaning `evt` is `nil` today and the disclosure never actually injects in a live session. This doesn't change what to do here (the relocation should still happen so the feature is correct once §20's wiring lands), but it does mean there's no way to manually dogfeed-verify the *disclosure rendering itself* end-to-end before `compaction_events` is wired — verify it by direct unit test against a synthetic `CompactionEvent`, not by triggering a real compaction and expecting to see it in a chat session.

## What to do

### Step 1 — relocate the compaction-disclosure content (do this first)

1. Before touching anything, confirm the safety claim above against a real DB: query the live `agent_profiles` row for `file-default` and the live `prompt_templates` row `blt-chat-harness-001`, and confirm their content matches (both should reflect the post-058-demotion "slim" body). If they've drifted, escalate — don't proceed assuming they match.
2. Read the 4 seeded variants in `internal/store/migrations/030_compaction_disclosure_prompts.sql` as the source material. Design one unified disclosure message that keeps the real recovery affordances every variant already shares — handoff stash id, the `chat_search` self-tool, and the summary metadata block (window, token count, evicted/preserved counts) — without mode-branching. Exact wording is your call; preserve the substance (what's preserved vs. lost, and the two concrete recovery actions), don't just concatenate the 4 variants.
3. Add this as a hardcoded Go string (function or constant) in `internal/chat/context.go`, colocated with `renderCompactionDisclosure` — do not place it in `internal/context/` (that package is being actively modified by `27-cut-p7-scratchpad-snapshot`; keeping this change scoped to the chat/prompt-assembly layer avoids an unnecessary file-overlap with that task).
4. Rewrite `renderCompactionDisclosure` to interpolate the hardcoded string directly from the `*store.CompactionEvent` fields it already has (`HandoffStashID`, `CoverageWindowStart`/`End`, `SummaryTokenCount`, `EvictedCachePointers`, `PreservedSources`) instead of calling `s.GetPromptTemplateBySlug(slug)`. Keep the freshness check (`isCompactionEventFresh`) exactly as-is — that logic is unrelated to where the content lives. Remove `disclosureSlugForMode` (no longer needed — one message, no mode lookup) and `interpolateDisclosure`'s slug-based template lookup (replace with direct formatting of the hardcoded string).
5. Do not touch `classifyModeFromAgentTags`/`ClassifyCompactionMode`/`CompactionPipeline.Mode`/`summarySystemPrompt` — those remain exactly as they are; only the disclosure template selection is being collapsed.

### Step 2 — cut the rest of the mechanism

6. Confirm step 1 is complete and `renderCompactionDisclosure` no longer calls `GetPromptTemplateBySlug` before proceeding.
7. In `internal/chat/context_client.go`'s `assembleAgentSlotContent` (lines 602-644), remove the `composed, err := s.ComposePromptForAgent(...)` call and the `if composed == "" { ...fallback... }` branch structure — the fallback body (`agent.SystemPrompt` + mode addendum) becomes the only path, unconditionally.
8. Delete `internal/store/prompt_templates.go` and `internal/api/prompt_templates.go` entirely.
9. Remove the 8 route registrations in `internal/api/api.go` (lines 344-351).
10. Remove the 10 interface method declarations from `internal/service/store.go` (lines 163-172: `ListPromptTemplates`, `GetPromptTemplate`, `GetPromptTemplateBySlug`, `CreatePromptTemplate`, `UpdatePromptTemplate`, `DeletePromptTemplate`, `ListPromptTemplatesForAgent`, `AssignPromptTemplateToAgent`, `RemovePromptTemplateFromAgent`, `ComposePromptForAgent`).
11. Remove `PromptTemplateIDs` from `AgentBuilderCapabilitiesInput` (`internal/api/types.go:326`) and its handling in `capabilityOperations` (`internal/api/agent_builder.go:518-525`).
12. Update the stale comment in `internal/chat/universal_rules.go:13` referencing `agent_prompt_templates(file-default, blt-chat-harness-001)` — that assignment no longer exists; describe the new `agent.SystemPrompt`-only path instead.
13. Determine the next available migration number (check `internal/store/migrations/` at implementation time; use `09-adopt-goose-migrations`'s format if it has landed by then). Write a migration that drops both tables: `DROP TABLE IF EXISTS agent_prompt_templates;` then `DROP TABLE IF EXISTS prompt_templates;` (drop the referencing table first — `agent_prompt_templates.template_id` has an `ON DELETE CASCADE` FK to `prompt_templates`, but dropping in dependency order avoids relying on that). Do not edit or delete the historical migrations that create/seed/mutate these tables (001, 027, 029, 030, 046, 048, 049, 050, 051, 053, 055, 056, 058) — per this codebase's established convention (see `063_drop_agent_profiles_default_mode.sql`'s "no compat shims, clean break" precedent for column drops, applied here at the table level), they keep running harmlessly every boot (create/seed a table that a later-numbered migration then drops) since there's no migration ledger yet.
14. Remove the frontend surface: delete `PromptTemplateEditor.tsx`; remove prompt-template UI/state from `AgentProfileManager.tsx`, `AgentCapabilitiesPanel.tsx`, `SystemPromptsViewer.tsx`, `AgentDetailView.tsx`, `PromptDetailView.tsx`, and the assignment step in `AgentBuilderWizard.tsx`; remove the API client functions in `ui/src/lib/api.ts` (`listPromptTemplates`/`getPromptTemplate`/`createPromptTemplate`/`updatePromptTemplate`/`deletePromptTemplate`/`listAgentTemplates`/`assignTemplateToAgent`/`removeTemplateFromAgent`) and the `PromptTemplate` type in `ui/src/lib/types.ts`.
15. Update or delete tests referencing any of the above: `internal/store/prompt_templates_test.go` (delete — tests a deleted file), `internal/store/compaction_disclosure_test.go` (rewrite to test the new hardcoded-content path from step 1 instead of the 4 seeded `prompt_templates` rows), `internal/store/migration056_idempotency_test.go` (review — if it asserts against `prompt_templates` row content post-migration-chain, it needs adjusting since the table no longer exists after this task's new drop migration runs), `internal/store/agents_test.go`, `internal/builders/builder_test.go`, `internal/service/chat_test.go`, and the three frontend test files listed in Touches.

## Done means

- `file-default` chat sessions still get the correct canonical Chat-role identity prompt, verified by manual dogfeed (start a chat session with the default agent, confirm behavior/tone matches the current `chat-role-harness` content) — not just by code inspection.
- Every other agent's system prompt is unaffected (they were already on the `agent.SystemPrompt` fallback path; verify no behavior change).
- The compaction disclosure — when `compaction_events` eventually gets wired (Phase 5, out of scope here) — renders one unified message from hardcoded Go content, with no DB lookup and no mode branching. Verified by a direct unit test against a synthetic `CompactionEvent`, since end-to-end triggering isn't possible until Phase 5's wiring lands.
- `prompt_templates` and `agent_prompt_templates` tables no longer exist after migrations run on a fresh boot.
- No references to `PromptTemplate`, `ComposePromptForAgent`, `prompt_templates`, or `agent_prompt_templates` remain in Go or TypeScript source (tests included).
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass; `cd ui && npm run build` passes.
- Migration tested against a real backed-up database copy, not just an empty fixture.

## Work log
<Worker fills this in as it goes: what was actually done, any deviation from plan and why, anything escalated.>

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
