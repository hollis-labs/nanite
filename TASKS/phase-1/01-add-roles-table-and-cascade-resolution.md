# Add `roles` table and the role → agent → task cascading override resolver

**Phase:** 1
**Status:** implemented
**Depends on:** none
**Touches:** new migration (`roles` table), `internal/store/agents.go` (`agent_profiles` read path — cascade resolution needs to consult both tables), `internal/service/agent.go` (`ResolveForSession`/`resolveForSession` — likely insertion point for cascade resolution), new `internal/store/roles.go` (CRUD), `GLOSSARY.md` (already defines Role — no rename needed, just confirm the doc matches what ships)

## Context

Architecture doc `01-agent-construction.md`: *"An Agent is built from three layers, resolved with closest-wins override — the same shape as RBAC/IAM's Role+Binding+cascading-scope pattern... `roles` — new table, the one genuinely new facet. Holds persona/identity (name, system_prompt) and optionally sensible defaults for tools/skills/permissions that a new composition can start from."* Decision log §6: *"Cascading override resolution, closest wins, confirmed direction: Role (broadest defaults) → Agent/composition (scope-specific settings) → Task/invocation (narrowest, most specific — overrides everything above it at runtime). Same resolution order as `.htaccess`/Kubernetes RBAC."*

This is the one genuinely new facet in the whole Phase 1 schema — everything else (`agents`, `agent_tools`, `agent_skills`, `agent_projects`) is either an existing table gaining columns/FKs or a new join table riding on facets that already exist. `roles` has no precedent in the current schema; this task builds it from nothing.

### What `agent_profiles` already carries that `roles` needs to make reusable

Verified against `internal/store/migrations/001_schema.sql:32-60` (plus 11 extending migrations): `agent_profiles` currently has `system_prompt`, `default_mode`, `default_model`, `default_provider`, `tool_permissions`, `tools`, `role_tools`, `role_skills`, `class` (`'advisor'` default, migration `074`), `activation_mode` (`'singleton'`/`'instance'`, same migration) all living on the single composition row — i.e. persona/identity and scope-specific settings are currently fused into one table with no way to reuse a persona across multiple scoped compositions without copy-pasting the whole row. This is the literal `.nanite/agents/*.md` sprawl problem the architecture doc names as the reason for splitting Role out.

## What to do

1. Create `roles` table: `id`, `slug` (unique), `name`, `system_prompt`, optional default-hint columns for tools/skills/permissions that a new `agents` composition can start from (mirror the shape of `agent_profiles.tools`/`role_tools`/`role_skills` as JSON-array defaults, since `02-add-agents-composition-columns.md` will add `agents.role_id` pointing here), `created_at`, `updated_at`. Do not make role-level tool/skill defaults authoritative — per architecture doc: *"Tools/skills/permissions bind at the composition (`agents`) level, not fixed by role... the actual grant is adjustable per composition/scope."*
2. Build the cascade resolver: given an `agents` row (with `role_id`) and, at invocation time, any task/dispatch-level overrides, resolve `system_prompt`/tool defaults/`class`/model selection with closest-wins semantics (role → agent → task). Confirm the natural insertion point is `internal/service/agent.go`'s `ResolveForSession`/`resolveForSession` (already the per-turn resolution seam) rather than inventing a new one.
3. `class` (`advisor`/`process`/`template`/`harness`) already exists on `agent_profiles` (migration `074`, TEXT NOT NULL DEFAULT `'advisor'`, no CHECK constraint — Go-layer validated) — per decision log §6, `class` follows the same 3-tier cascade as everything else, not a special case. Wire it into the cascade resolver rather than treating it as a fixed per-agent value; a role can supply a default `class`, a composition can override it, a task/dispatch can override again.
4. Build CRUD (`internal/store/roles.go`, REST routes) for `roles` — this is new infrastructure a worker will need alongside `09-build-assignment-ui-api.md`'s UI work, but land the store/API layer here since it's a natural extension of this task's own schema work.
5. Do **not** build a file-reingest path for `roles` — per the "database is the source of truth" principle and `08-kill-file-reingest-on-boot-pattern.md`'s scope, `roles` should be DB-authoritative from the moment it exists, with no YAML/`.md` source ever re-parsed into it on boot. `~/.nanite/roles/` (the developer-persona Claude-Code-boot convention documented in `GLOSSARY.md`) is unrelated and must not be confused with or read by this table — confirm no code accidentally wires the two together.

## Done means

- `roles` table exists, seeded with at least the roles implied by the current `.nanite/agents/*.md` corpus's distinct personas (finalized in `10-data-migrate-nanite-agents-md.md`, not necessarily seeded here).
- The cascade resolver produces the correct closest-wins result for `system_prompt`, tool/skill defaults, and `class` across role → agent → task, verified with a real test exercising all three override levels.
- `roles` CRUD (store + REST) exists and is exercised by at least one integration test.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- No code path re-reads a role definition from disk on boot; `roles` is DB-authoritative only.

## Work log

**2026-08-18 — worker report.**

**Worktree base correction (not a task-content issue, logged per worker step 7):** this worktree's branch (`worktree-agent-ac5a5042e24803a00`) was checked out at `df71e710` ("Document sync"), an ancestor 73 commits behind `phase-1-execution`'s actual tip (`505f1f8f`, "Phase 1 planning: commit corpus to isolated branch..."). `TASKS/phase-1/` didn't exist at all at that commit — the verification step in my own instructions (`ls TASKS/phase-1/`) caught this immediately. Working tree was clean and the branch had zero commits of its own ahead of `df71e710` (`git log df71e710..HEAD` empty), so `git merge --ff-only phase-1-execution` was a lossless, non-destructive fast-forward (not a hard reset) — confirmed via `git merge-base` before running it. Started real work from `505f1f8f` after the fast-forward; that's the commit SHA to check against for "branched from the right base."

**What was built:**
1. **Migration `108_add_roles_table.sql`** — creates `roles` (`id`, `slug` UNIQUE, `name`, `system_prompt`, `default_class`, `default_model`, `default_provider`, `default_tools`/`default_skills` (JSON arrays), `default_permissions` (JSON object), `created_at`, `updated_at`). No `agent_profiles.role_id` FK added — that column is explicitly `02-add-agents-composition-columns.md`'s job per this task file's own item 1 ("since `02` will add `agents.role_id` pointing here"); adding it here would have duplicated that task's stated scope. Real, tested Down (`DROP TABLE IF EXISTS roles`) — verified both directions (Up then Down) manually against a scratch SQLite file via the `sqlite3` CLI, and Up verified against a real copy of the production backup (see below).
2. **`internal/store/roles.go`** — `Role` struct + `ListRoles`/`GetRole`/`GetRoleBySlug`/`CreateRole`/`UpdateRole`/`DeleteRole`, mirroring `skills.go`'s CRUD shape. `default_class` validated at the Go layer (`validateRoleFields`), matching `agent_profiles.class`'s own no-CHECK-constraint validation approach. `internal/store/roles_test.go` covers full CRUD, default-value backfill, invalid-class rejection, and slug-uniqueness.
3. **Cascade resolver — reused existing, previously-dead infrastructure instead of writing a second merge engine.** Found `internal/agent/override` (`OverrideConfig`/`Resolve`/`ResolveWithMap`, a three-layer last-writer-wins-scalars + union-with-+/- -prefix-lists + shallow-merge-maps engine) already implementing exactly the closest-wins semantics this task needs — but with **zero non-test callers anywhere in the codebase** (confirmed by repo-wide grep). Its one intended caller, `session_agent_overrides`, was already cut in full by Phase 0 #18a (`TASKS/phase-0/18a-cut-dead-storage-and-config.md`, implemented); that task's own text flagged "verify... whether `override.Resolve`/`override.OverrideConfig` are still used elsewhere in the same cascade before removing the import" but no other layer ever picked it up, so the package sat fully dead until now. Extended it with two new scalar fields (`SystemPrompt`, `Class`) rather than building a parallel implementation, and added `internal/service/role_cascade.go` (`RoleOverrideConfig`, `AgentOverrideConfig`, `ResolveAgentCascade`) mapping `store.Role`/`store.AgentProfile` into that engine's three layers (role → agent → task).
4. **Wired into `internal/service/agent.go`'s `resolveForSession`** (the confirmed insertion point, per item 2) via a new `s.roleForProfile(ctx, profile) *store.Role` method and `applyScalarCascade`. `roleForProfile` always returns `nil` today — `agent_profiles` has no `role_id` column until `02` lands, so there is nothing to look up yet; the method exists purely as the seam `02` will complete. Only the four scalars (`system_prompt`/`class`/`model`/`provider`) are written back onto the resolved profile; `ResolveAgentCascade` still computes the full merge including `Tools`/`Skills`/`Permissions` (so that part of the cascade logic is real and tested), but that output is deliberately **not** applied to the live per-turn profile — per the architecture doc's own instruction that "tools/skills/permissions bind at the composition (agents) level, not fixed by role... the actual grant is adjustable per composition/scope," re-merging a role's tool/skill defaults into every turn's live grant would contradict that. Net effect today: `resolveForSession`'s output is byte-for-byte unchanged (role and task are both `nil` in every live call), proven by a dedicated regression test (`TestApplyScalarCascade_NoOpWhenRoleAndTaskAbsent`, `TestAgentService_ResolveForSession_CascadeIsNoOpToday`) rather than by inspection alone.
5. **Cascade tests** (`internal/service/role_cascade_test.go`): `TestResolveAgentCascade_AllThreeTiers` is the "Done means" test — a real role, an agent-composition layer, and a task-level override together, asserting closest-wins for `system_prompt` (role default flows through when agent leaves it unset), `class` (task overrides the role default), `model`/`provider` (agent overrides role), and `tools`/`skills` (list union/removal semantics — see deviation note below). Plus nil-role/nil-task passthrough tests and JSON-decode-failure-degrades-to-nil tests.
6. **REST CRUD**: `internal/api/roles.go` (`GET/POST /api/roles`, `GET/PUT/DELETE /api/roles/{id}`), request types in `internal/api/types.go` (`CreateRoleRequest`/`UpdateRoleRequest`), routes registered in `api.go` next to Skills. `internal/api/roles_test.go`'s `TestRolesCRUD_EndToEnd` is a full HTTP-level integration test (create → list → get → get-404 → update-partial → delete → get-404-after-delete) against a real migrated store, mirroring `api_test.go`'s existing `TestCreateAndListSessions` pattern; `TestCreateRole_InvalidClassRejected` covers the validation-error path.
7. **GLOSSARY.md** — confirmed unchanged. Its existing **Role** entry ("the reusable persona/behavior template an Agent is built from: name, system prompt, and optional defaults") already matches exactly what shipped; no edit needed, per the task file's own touch-list caveat.
8. **`internal/store/agents.go`** — not substantively touched, deviating from the Touches list's "agent_profiles read path — cascade resolution needs to consult both tables." There is genuinely nothing to join yet: `agent_profiles.role_id` doesn't exist until `02` lands, so `GetAgent`/`GetAgentBySlug` have no column to consult. The "consult both tables" seam lives in `agent.go`'s `roleForProfile` stub instead, ready for `02` to complete without any further change to `agents.go`'s read path shape.
9. **No file-reingest path added for `roles`** — confirmed by construction: `internal/store/roles.go` has no `Discover`/`Load`/file-parsing code at all, only DB CRUD. `08-kill-file-reingest-on-boot-pattern.md`'s own item 4 (negative verification) can point at this Work Log entry.
10. **No seed rows added.** The task's own Done-means marks this "not necessarily seeded here" (deferred to `10-data-migrate-nanite-agents-md.md`), and `10` is out of scope for Phase 1 per the operator decision already recorded in `TASKS/phase-1/10-data-migrate-nanite-agents-md.md` and the `505f1f8f` commit message. `roles` ships as an empty, DB-authoritative table — no personas were data-migrated into it.

**Real-backup migration test:** copied `~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726` to a scratch path and opened it through `store.New` (runs the real goose migration chain, including 106). Confirmed: `roles` table created, `CreateRole`/`GetRole` round-trip against the migrated backup, and pre-existing data survived untouched (`agent_profiles`=28 rows, `sessions`=343 rows, both readable post-migration). The throwaway test file used to drive this (`zz_manual_backup_migration_test.go`, gated behind an env var so it's a no-op in normal CI) was deleted before the final commit — it is not part of the shipped diff.

**Verification:** `go build ./cmd/nanite/`, `go vet ./...` (only the two pre-existing, unrelated `stopReaper`/`stopRuntimeReaper` findings in `internal/service/container.go`, confirmed present on `phase-1-execution` before this task's changes via `git stash`), and `go test ./...` (full repo) all pass. `gofmt -l` clean on every touched/added file.

**Deviations summary (all logged above, restated for scan-ability):** (a) worktree base fast-forward correction; (b) did not add `agent_profiles.role_id` (task 02's scope, per this file's own text); (c) reused/extended a previously fully-dead package (`internal/agent/override`) instead of writing new merge logic — a positive discovery, not a scope change; (d) tools/skills/permissions cascade computed and tested but not applied to the live per-turn profile, per the architecture doc's own "binds at composition level" instruction; (e) `agents.go` left untouched since there's no `role_id` column yet to join on; (f) no seed rows, per the task's own "not necessarily seeded here" allowance and `10`'s out-of-scope status.

Nothing here required escalation — the one real ambiguity (how to "wire" a cascade whose role-side FK doesn't exist yet) was resolved by building the general-purpose resolver now and gating its live effect behind a documented, tested stub, which is exactly the kind of forward-compatible seam `02`'s own Depends-on note anticipates.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
