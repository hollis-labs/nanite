# Add `agents` composition columns: `role_id`, `model_id`, `instance_mode` resolution, `runtime_kind`

**Phase:** 1
**Status:** implemented
**Depends on:** `01-add-roles-table-and-cascade-resolution.md` (`role_id` FKs against `roles`), `06-fix-models-table-sync-target.md` should land first or in the same change (`model_id` FKs against `models`, which is only meaningful once the sync gap is fixed — see Context)
**Touches:** new migration (`agent_profiles` ALTER — `role_id`, `model_id`, `runtime_kind` columns; `activation_mode` resolution decision, see Context), `internal/store/agents.go`, `internal/service/durable_wake.go` (`wakeSkipReason` — the real consumer of whatever `instance_mode` resolution this task settles on)

## Context

Architecture doc `01-agent-construction.md` lists the target composition columns on `agents` (not renamed — stays `agent_profiles` under the hood, per decision log §6: *"`agents` is not renamed... every existing `agent_id` FK... keeps its current meaning"*): `role_id (FK -> roles)`, `consumer_id (FK -> consumers, nullable)`, `model_id (FK -> models)`, `instance_mode`, `runtime_kind`, `class`, `enabled`.

**`consumer_id` is a separate task** (`03-add-consumers-table.md`) since it depends on a new `consumers` table with no schema precedent. **`class` already exists** (migration `074`, wired into the cascade resolver by `01`). This task is the remaining three: `role_id`, `model_id`, `runtime_kind` — plus a real design decision on `instance_mode` this task must make explicit, not silently pick.

### `activation_mode` already exists and is NOT what `instance_mode`'s real consumer reads — a genuine design choice, not a rename

Verified directly: `agent_profiles.activation_mode` (migration `074_agent_profiles_multi_agent.sql:27`) is TEXT NOT NULL DEFAULT `'singleton'`, Go-layer-validated (`internal/store/agents.go:131-134`) to exactly two values — `'singleton'` or `'instance'` — not the three (singleton/fresh-per-wake/concurrent) the decision log implies for `instance_mode`. Meanwhile, the actual behavior decision log §4 calls "no explicit field exists today" — `wakeSkipReason` (`internal/service/durable_wake.go:325-338`) — switches on `durable_agent_instances.status` + `durable_agent_instances.lifecycle_class`, **not** `agent_profiles.activation_mode` at all. So today's schema has a 2-value column that isn't consulted by the logic the architecture doc describes, and separately a real hardcoded-per-`lifecycle_class` behavior in `wakeSkipReason` with no column backing it.

**This task must decide and document, not silently default**: (a) extend `activation_mode` to a real 3-value enum (singleton/fresh-per-wake/concurrent) and rewire `wakeSkipReason` to read it, retiring the implicit per-`lifecycle_class` switch, or (b) introduce a genuinely new `instance_mode` column and deprecate `activation_mode`. Option (a) reuses existing (if under-scoped) infrastructure; option (b) avoids overloading a column whose current 2-value meaning may already be relied on elsewhere — grep every `activation_mode` read site before choosing, and record the choice and reasoning in this file's Work Log.

### `runtime_kind` — the column, not the routing wiring

This task adds the typed `runtime_kind` column (`cli` | `api`) to `agent_profiles`. **Wiring it as the actual thing that decides CLI-vs-API routing is Phase 2's job**, not this task's — do not touch `chat.IsCLIProvider`/`NormalizeCLIProvider`/`shouldUsePTY`/`agent_deps.go`'s `stripRegistryPrefix` here. This task's scope ends at "the column exists, is populated with a correct value per agent, and nothing reads it yet for routing decisions."

## What to do

1. Add `role_id TEXT REFERENCES roles(id)` to `agent_profiles` (new migration). Nullable during transition (existing rows have no role yet until `10-data-migrate-nanite-agents-md.md` backfills), non-null enforced at the application layer once migration completes — do not add a NOT NULL constraint that would break existing rows before the data migration runs.
2. Add `model_id TEXT REFERENCES models(id)` to `agent_profiles`. Depends on `06`'s fix landing first (or in the same change) — a `model_id` FK against a `models` table whose rows aren't kept in sync with `models.dev` is a real-looking but practically-useless reference.
3. Add `runtime_kind TEXT CHECK (runtime_kind IN ('cli','api'))` to `agent_profiles`, backfilled per-agent from whatever `chat.IsCLIProvider`/`NormalizeCLIProvider` would currently infer from that agent's configured provider/model — this backfill is what makes Phase 2's later wiring safe (a correct value already exists for every agent before Phase 2 starts reading it for routing).
4. Resolve the `activation_mode`/`instance_mode` design question above; implement the chosen option, including rewiring `wakeSkipReason` if option (a) is chosen.
5. Wire `role_id`/`model_id`/`class`/`runtime_kind` (once Phase 2 makes the last one live) into `01`'s cascade resolver — a composition's own `role_id` supplies role-level defaults; the composition's own columns override at the agent level; do not build a second, parallel resolution path.

## Done means

- `agent_profiles.role_id`/`model_id`/`runtime_kind` exist, correctly backfilled for every existing row (verified against the real `.nanite/agents/*.md`-derived rows and the seeded builtins).
- The `activation_mode`/`instance_mode` design decision is made, documented in this file's Work Log with the reasoning, and implemented — `wakeSkipReason` reads whichever column is now authoritative.
- No routing behavior changes as a result of this task — `runtime_kind` is populated but not yet consulted by anything (confirmed by grep: zero new read sites for `runtime_kind` outside this task's own backfill logic and tests).
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- Tested against a real copy of the backed-up database, not just an empty fixture.

## Work log

**2026-08-18 — worker report.**

**Worktree base correction (not a task-content issue, logged per worker step 7):** this worktree's branch (`worktree-agent-ab4e264ff3d2f9186`) was checked out at `df71e710` ("Document sync"), a commit with no `TASKS/` directory at all (predates that convention) and an ancestor of `phase-1-execution`'s actual tip (`3aa5a302`, "Phase 1 Wave 1: mark reviewed, record fresh-reviewer findings"). The verification step in my own instructions (`ls TASKS/phase-1/`) caught this immediately. Working tree was clean and the branch had zero commits of its own beyond that ancestor point (`git log phase-1-execution..HEAD` empty, confirmed via `git merge-base` first), so `git merge --ff-only phase-1-execution` was a lossless, non-destructive fast-forward, not a hard reset. Started real work from `3aa5a302`; that's the commit to check against for "branched from the right base." Both declared dependencies (`01`, `06`) were already merged and marked `implemented` at that commit.

**The `activation_mode`/`instance_mode` design decision — Option (a): extend `activation_mode` to a real 3-value enum, rewire `wakeSkipReason` to read it.**

Grepped every real read/write site of `activation_mode` before choosing (per this file's own instruction): `internal/store/agents.go` (the 2-value Go-layer validation + defaulting), `internal/api/agents.go`/`agent_builder.go`/`types.go` (pure passthrough, no branching on value), `internal/agent/parser.go`/`convert.go`/`managed_files.go` (YAML frontmatter round-trip for `.nanite/agents/*.md`, also pure passthrough), and the UI (`AgentBuilderWizard.tsx` — a free-text `Input`, not even a fixed-option dropdown). Zero behavioral branches on `activation_mode`'s specific value existed anywhere — confirming the task's own claim that today's column is stored but never consulted. `wakeSkipReason` (the intended real consumer) switched on `durable_agent_instances.lifecycle_class` instead, exactly as described.

Since nothing anywhere relies on the current 2-value meaning behaviorally, option (b)'s stated risk ("avoids overloading a column whose current 2-value meaning may already be relied on elsewhere") doesn't apply here — there is no elsewhere. Chose **option (a)**: widened `validateAgentMultiAgentFields` from `singleton`/`instance` to `singleton`/`fresh-per-wake`/`concurrent`, retired `instance` as a valid value, and rewired `wakeSkipReason` (`internal/service/durable_wake.go`) to read `activation_mode` via a new `activationModeForInstance` lookup (added `GetAgent` to `durableWakeStore`) instead of its old hardcoded `lifecycle_class != process` special case. Added `wakeAllowsConcurrentActive(activationMode)` — `fresh-per-wake`/`concurrent` bypass the "wake already active" block, everything else (including empty/unrecognized) blocks, matching `singleton`'s old default behavior.

**Real-corpus finding that changed the backfill design.** Copied the real production backup (`main.db.pre-execution-backup-20260818-132726`) before writing the migration's backfill logic and found `activation_mode`'s *pre-existing* value never actually tracked real wake-skip behavior 1:1: two rows (`torque-supervisor`, class=process; `task-planner`, class=template) already carried `activation_mode='singleton'` despite their `class` exempting them from the old hardcoded block, while three others (`atlas-curator`, `loom-curator`: process; `content-writer`: template) carried `'instance'`. Since `activation_mode` was never read by any behavior before this migration, `class` — not the pre-existing `activation_mode` text — is the only column that ever carried real, live wake-skip intent. Deriving the migration's backfill from `class` (not from the old `activation_mode` value) is what actually preserves every row's current real behavior; deriving it from the old value instead (my first draft) would have silently *regressed* `torque-supervisor`/`task-planner` from rewakeable-while-active to blocked. Verified this end to end against the real backup after landing the final migration (see Verification below) — `torque-supervisor`/`task-planner` land on `fresh-per-wake`, matching their real class-driven behavior, not their stale stored value.

**A related latent bug this rewrite closes (not scope creep — a direct consequence of the task's own instruction to retire the hardcoded switch).** The old `wakeSkipReason` only ever exempted `lifecycle_class == process` from the "wake already active" block; `template`-class instances (`SessionPolicyFreshOneShot` — a fresh session per invocation, same "no reuse collision to guard against" reasoning process's `SessionPolicyFreshPerWake` already got exempted for) were never covered by the original CW-20260817 fix, leaving `content-writer` (a real, currently-`active` template-class agent per the production backup) with the exact same "wakeable exactly once" bug. Migration 111's `class IN ('process','template') -> 'fresh-per-wake'` backfill, combined with the new activation-mode-driven `wakeSkipReason`, closes this for `content-writer` as a side effect of doing the task's own rewiring correctly. Added `TestDurableWakeTemplateClassRewakeableWhileActive` as a permanent regression test for this (`internal/service/durable_wake_test.go`), analogous to the existing process-class one.

**What was built:**
1. **Migration `111_agent_profiles_composition_columns.sql`** (highest migration at start of work was `109_agent_reflex_opt_out.sql`, confirmed by listing `internal/store/migrations/` before naming). Adds `role_id TEXT REFERENCES roles(id)` and `model_id TEXT REFERENCES models(id)` (both nullable, no rows backfilled — `10-data-migrate-nanite-agents-md.md` is explicitly out of Phase 1 scope). Adds `runtime_kind TEXT CHECK (runtime_kind IS NULL OR runtime_kind IN ('cli','api'))` — a real DB-level CHECK, unlike `class`/`activation_mode`/`default_state`'s Go-layer-only enforcement, following `053_session_intent.sql`'s exact `ADD COLUMN ... CHECK (x IS NULL OR x IN (...))` precedent (smoke-tested directly against a scratch SQLite file before writing the real migration, including the DROP COLUMN sequence in Down). Backfills `runtime_kind` from `default_provider` via a SQL `CASE` mirroring `chat.IsCLIProvider` exactly (`= 'pty'` OR `LIKE 'pty-%'` OR `LIKE 'sub-%'` → `cli`, else `api`). Backfills `activation_mode` from `class` per the design decision above. Down drops the three added columns in reverse order (structure-only — the `activation_mode` value rewrite is not reverted, matching the established lossy-value-remap precedent in `105`/`109`'s own Downs).
2. **`internal/store/agents.go`**: added `RoleID`, `ModelID`, `RuntimeKind` fields to `AgentProfile`; extended `agentColumns`/`scanAgent`/`CreateAgent`/`UpdateAgent` for the three columns (nullable-FK pattern for `RoleID`/`ModelID` mirrors the existing `ConsumerID` handling); widened `validateAgentMultiAgentFields`'s `activation_mode` switch to the 3-value enum and added a `runtime_kind` switch (Go-layer validation alongside the DB CHECK, for a clean error message instead of a raw SQLite failure — matching this function's existing job for every other enum column); reordered `applyMultiAgentDefaults` to default `Class` before `ActivationMode` (needed so a caller setting `Class` and leaving `ActivationMode` empty lands on the class-correct default, not always `singleton`); added exported `DefaultActivationModeForClass(class string) string` (process/template → `fresh-per-wake`, else `singleton` — exported so `internal/api/agent_builder.go`'s draft advisor can share the same mapping, see below) and unexported `inferRuntimeKind(providerName string) string` (mirrors `chat.IsCLIProvider`; `internal/store` can't import `internal/chat` — it already imports `internal/store` — so this is a hand-kept mirror, same pattern this file's own `urnPrefix`/`generateAgentURN` already use for `internal/agent`).
3. **`internal/service/store.go`**: added `GetRole(id string) (*store.Role, error)` to the `AgentReader` interface (already implemented by `*store.Store` via task 01's `roles.go`; only test fakes needed updating — see below).
4. **`internal/service/agent.go`**: made `roleForProfile` real — was a permanent `return nil` stub left by task 01 (its own Work Log: "the seam `02` will complete"). Now looks up `s.agents.GetRole(profile.RoleID)`, returning `nil` on an unset `RoleID`, an unknown `RoleID` (dangling FK — degrades rather than erroring, matching `GetRole`'s own miss contract), or any store error. `applyScalarCascade` now also writes `cascade.ModelID` back onto the resolved profile (fifth scalar, alongside the existing four) when non-empty.
5. **`internal/service/role_cascade.go`**: added `ModelID` to `AgentOverrideConfig` (`profile.ModelID`, plain passthrough). Left `ModelID` unset in `RoleOverrideConfig` — `roles` (migration 108) has no `model_id`-equivalent hint column, so the role tier has nothing to contribute for this field today; this is documented as a real gap, not an oversight, so a future role-level default has a seam to land in without inventing a second resolution path. Deliberately did **not** add `RuntimeKind` to either override config or the cascade — per this task's own item 5 parenthetical ("once Phase 2 makes the last one live") and the "zero new read sites" Done-means criterion.
6. **`internal/agent/override/merge.go`**: added `ModelID` as a sixth last-non-empty-writer-wins scalar (alongside `Model`/`Provider`/`Description`/`SystemPrompt`/`Class`), wired into `applyLayer`.
7. **`internal/api/agent_builder.go`**: one-line, necessary deviation from this task's own Touches list (which doesn't mention `internal/api`) — the deterministic builder advisor's `Draft()` pre-filled a hardcoded `ActivationMode: "singleton"` for every new agent draft regardless of `Class`, including `lifecycle == process`. Left as-is, this would have let an operator submit a brand-new process/template-class durable agent straight into the exact bug this task's design decision exists to close. Changed to `store.DefaultActivationModeForClass(lifecycle)`, sharing the same mapping `applyMultiAgentDefaults` uses rather than a second hardcoded copy. The free-text `Input` in `AgentBuilderWizard.tsx` still lets the operator override the suggestion before submit — no UI change made or needed.
8. **`.nanite/agents/content-writer.md`, `atlas-curator.md`, `loom-curator.md`**: changed `activationMode: instance` → `activationMode: fresh-per-wake` in each file's frontmatter. Required, not optional: `internal/service/ingest.go`'s `upsertAgentDef` re-ingests these files into the DB via `CreateAgent`/`UpdateAgent` at boot, which now runs the widened validation — leaving `instance` in these files would have broken boot-time ingest for three real, currently-running agents (Loom Curator, Atlas Curator, content-writer). Confirmed via grep that no other `.nanite/agents/*.md` or `internal/agent/builtin/*` file used `activationMode: instance`.
9. **Test updates for the retired `'instance'` value**: `internal/store/agents_fu28_test.go`'s `TestListAgentsFilter` used `activation_mode='instance'` as fixture data for two rows; updated to `'fresh-per-wake'` (the assertions were purely mechanical filter-matching, unaffected in substance).
10. **New tests**: `internal/store/agents_composition_test.go` (`TestCreateAgent_RoleIDModelIDNullableRoundTrip`, `TestCreateAgent_RuntimeKindDefaultedFromProvider`, `TestCreateAgent_RuntimeKindInvalidRejected`, `TestValidateAgentMultiAgentFields_ActivationModeThreeValueEnum`, `TestDefaultActivationModeForClass`, `TestCreateAgent_ActivationModeDefaultsFromClass`); `internal/service/role_cascade_test.go` additions (`TestAgentOverrideConfig_IncludesModelID`, `TestRoleOverrideConfig_NeverSuppliesModelID`, `TestApplyScalarCascade_ModelIDPassthrough`, `TestAgentService_RoleForProfile_RealLookup`, `TestAgentService_ResolveForSession_RoleCascadeAppliesWhenRoleIDSet` — the last one is the real end-to-end proof that `roleForProfile` is no longer a permanent nil stub); `internal/service/durable_wake_test.go` additions (`TestDurableWakeTemplateClassRewakeableWhileActive`) plus fixture updates to the two existing rewake tests (`TestDurableWakeProcessClassRewakeableWhileActive` now sets `Class: "process", ActivationMode: "fresh-per-wake"` explicitly — it relied on the old `lifecycle_class`-only check before and would otherwise now fail against a `singleton`-defaulted fixture profile; `TestDurableWakeAdvisorClassStillBlockedWhileActive` asserts the `singleton` default explicitly for documentation). `internal/service/agent_test.go`/`chat_test.go`: added `GetRole` to `stubAgentReader` and `stubAgentReaderStore` (both needed it to keep satisfying `AgentReader`/`Store`).
11. **`role_id`/`model_id`/`class`/`runtime_kind` wiring into the cascade resolver (item 5)**: `role_id` — `roleForProfile` made real (above). `class` — already wired by task 01; unchanged, still correct. `model_id` — wired into `AgentOverrideConfig`/`applyScalarCascade` as a plain composition-level passthrough (no role-level default exists to cascade from; see item 5 above). `runtime_kind` — explicitly **not** wired into the cascade, per the task's own parenthetical deferring it to Phase 2, and to keep the "zero new read sites" Done-means criterion literally true.

**Deviation note on `model_id` scope.** Did not implement resolving a set `model_id` into a concrete provider/model string for a live turn (joining against the `models` table via `provider_id`). Task 06's own Done-means aspirationally mentions this ("an agent composition with a `model_id` set, resolved through the cascade, produces the correct provider/model string for a real turn") but that requirement can only be exercised once `model_id` exists, which is this task's job, not task 06's — and this task's own "What to do"/"Done means" text asks only for the column, its cascade wiring as a scalar, and correct backfill, not a new relational-lookup behavior. Flagging as a follow-up candidate for whichever task (09 or a later one) builds the real UI/routing consumer of `model_id`, rather than building it speculatively here.

**Real-backup migration test (task's own Done-means criterion, not skipped for a fixture).** Copied `~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726` to a scratch path and ran the real goose migration chain against it via a throwaway `cmd/verify02` program (built, run, and deleted before commit — not part of the shipped diff), same pattern task 01/06 used. Confirmed for all 28 real rows:
- `runtime_kind`: 0 rows with NULL/empty; all 28 land on `'api'` (none of the real seeded/file-based agents use a `pty`/`sub-` provider today).
- `activation_mode`: 0 rows with the retired `'instance'` value remaining. `atlas-curator`, `loom-curator`, `torque-supervisor` (all `class=process`) and `content-writer`, `task-planner` (both `class=template`) all land on `'fresh-per-wake'`; the remaining 23 advisor/harness rows stay `'singleton'`. This is the finding described above, confirmed end to end post-migration.
- `role_id`/`model_id`: NULL for all 28 rows, as expected (no backfill source exists yet; `10` is out of scope).
- Data integrity: `agent_profiles` = 28 rows, `sessions` = 343 rows, both unchanged from the pre-migration backup — no data loss.

**Verification.** `go build ./cmd/nanite/` — clean. `go vet ./...` — only the two pre-existing `stopReaper`/`stopRuntimeReaper` findings in `internal/service/container.go`, unrelated to this task and already flagged in tasks `01`/`06`'s own Work Logs as pre-existing on `phase-1-execution`. `go test ./...` — full repo, all packages pass; ran `internal/store`, `internal/service`, `internal/agent/...`, `internal/api` with `-count=1` (no cache) after the final round of edits. `gofmt -l` clean on every touched/added Go file (one file briefly picked up a stray Unicode right-double-quote character from a tool-call content transfer glitch while I was authoring a comment — caught by a direct byte-level scan of every touched file, not just visual inspection, and fixed before the final verification pass; confirmed no other touched file carries the same issue).

**Merge-coordination note.** This task also touches `internal/store/agents.go`, which two other in-flight Phase 1 tasks (`04-add-known-tools-and-agent-tools-fk.md`, `08-kill-file-reingest-on-boot-pattern.md`) are working on in parallel per the orchestrator's own heads-up. My diff to that file is additive (new struct fields, new SELECT/INSERT/UPDATE columns appended, one function reordered, two new small functions) and doesn't touch `04`'s or `08`'s likely surface (known-tools FK / file-reingest boot path) directly, but a real textual merge conflict is plausible given both tasks land in the same file; noting it here per the worker instructions rather than attempting to coordinate directly.

**Deviations summary (all logged above, restated for scan-ability):** (a) worktree base fast-forward correction; (b) chose option (a) for the activation_mode/instance_mode decision, with the reasoning above; (c) derived the migration's activation_mode backfill from `class`, not from the pre-existing activation_mode value, after a real-backup finding showed the latter would have caused a regression for `torque-supervisor`/`task-planner`; (d) extended the `fresh-per-wake` backfill to `template` class (not just `process`), closing a latent instance of the CW-20260817 bug for `content-writer` as a direct, disclosed consequence of retiring the hardcoded lifecycle_class switch; (e) touched `internal/api/agent_builder.go` (outside this task's stated Touches list) to stop the builder's draft advisor from reintroducing the same bug for newly-created agents; (f) updated three `.nanite/agents/*.md` files' `activationMode` value in lockstep with the schema change, required for boot-time file re-ingest to keep working; (g) did not implement `model_id` → provider/model string resolution for a live turn, flagged as a follow-up candidate rather than built speculatively.

Nothing here required escalation.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
