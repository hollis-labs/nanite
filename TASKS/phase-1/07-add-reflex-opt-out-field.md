# Add the reflex opt-out field to `agent_reflexes`

**Phase:** 1
**Status:** implemented
**Depends on:** none
**Touches:** new migration (`agent_reflexes` — new column, e.g. `opt_out_allowed`), `internal/api/reflexes.go` (CRUD/validation for the new field), `internal/agent/reflexes/` (wherever reflex applicability to a given agent is currently resolved — must consult the new field)

## Context

Architecture doc `01-agent-construction.md`: *"Reflexes at construction time — `agent_reflexes` already supports what's needed structurally: `agent_id IS NULL` = class-bound/global reflex, a specific `agent_id` = per-agent reflex. Gap being closed: a field distinguishing 'required, cannot opt out' from 'default-on, agent may opt out.'"* Decision log §4 states this the same way. Phase 3's Steering work (`dispatch_to_agent` action kind, promptrouter migration) depends on this field existing per `TASKS/INDEX.md`'s Phase 3 summary row — land this early in Phase 1, it's small and low-risk.

### Current schema, exact state

`agent_reflexes` (`internal/store/migrations/075_agent_reflexes.sql:30-51`): `id`, `agent_id TEXT REFERENCES agent_profiles(id)` (nullable — confirmed `agent_id IS NULL` = class-bound/global, per the migration's own header comment), `class_tag`, `name`, `trigger_kind`, `trigger_spec`, `action_kind` (CHECK'd to the 5 existing values), `action_spec`, `status`, `priority`, `fired_count`, `last_fired_at`, `created_at`, `created_by`. **No existing column maps to "required, cannot opt out" vs. "default-on, may opt out"** — confirmed, this needs a genuinely new column, not a repurposed existing one.

## What to do

1. Add a new column to `agent_reflexes` — e.g. `opt_out_allowed BOOLEAN NOT NULL DEFAULT true` (default permissive: existing class-bound reflexes remain opt-out-able unless explicitly marked required, preserving current behavior for every existing row). Use a real goose migration.
2. Wire whatever code path currently determines "does this reflex apply to this agent" (grep `internal/agent/reflexes` for the applicability/filtering logic — likely in the engine's trigger-evaluation entry point) to respect `opt_out_allowed`: if `false`, the reflex applies unconditionally regardless of any per-agent override; if `true` (default), an agent-level override mechanism can suppress it. Confirm whether a per-agent opt-out override mechanism already exists anywhere (e.g. an agent-level reflex-suppression list) or needs to be built as part of this task — architecture doc frames this as "the gap being closed," implying both the field and its consumption are in scope, not just the column.
3. Seed `opt_out_allowed = false` for any reflex that's genuinely safety-critical and shouldn't be individually disable-able (identify these during implementation — check the current 12 base-reflex seeds in `internal/agent/reflexes/seeds.go` for candidates; do not guess without checking).

## Done means

- `agent_reflexes.opt_out_allowed` (or equivalent) exists, correctly defaulted, and is genuinely consulted by the reflex-applicability logic — verified with a real test: a required reflex still fires for an agent that has attempted to opt out; a default-on reflex can be suppressed per-agent.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- `internal/api/reflexes.go`'s CRUD/validation surface exposes and validates the new field.

## Work log

**Worktree setup correction (not part of the task itself, logged per EXECUTION-PROCESS.md's Log integrity rule):** this worktree (`worktree-agent-ade117f7ad95d7e77`) was, at task start, branched from an old point on `main` (`df71e710`, predating even several Phase 0 tail items) rather than from `phase-1-execution`'s actual tip (`505f1f8f`, which is what the task brief's own Context said this worktree should be). `TASKS/phase-1/` and this task file did not exist on disk at the start of the session as a result. Confirmed `df71e710` is a genuine ancestor of `phase-1-execution` (`git merge-base HEAD phase-1-execution` == `df71e710` == `HEAD`, no divergent commits on this worktree's branch), so fast-forwarded via `git merge phase-1-execution --ff-only`, landing cleanly and bringing in the task corpus. Started implementation from `505f1f8f` (phase-1-execution tip) after the fast-forward.

**1. Migration.** Added `internal/store/migrations/106_agent_reflex_opt_out.sql` (goose Up/Down):
- `agent_reflexes.opt_out_allowed BOOLEAN NOT NULL DEFAULT TRUE` — default permissive, exactly as the task file's own suggested column shape.
- A backfill `UPDATE ... SET opt_out_allowed = FALSE WHERE agent_id IS NULL AND action_kind = 'halt_session' AND name IN (...)` for the three safety-critical seeds (see step 3 below) — this matters for upgrading a database that already has these rows seeded under the pre-migration schema, since `SeedBaseReflexes` never overwrites an already-existing row. Verified this backfill against a real copy of the production DB backup (`~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726`, copied to the scratchpad and opened via `store.New` to run the actual migrate() path) — confirmed all 11 pre-existing class-bound base-reflex rows in that live DB end up with the correct `opt_out_allowed` value after migration (the 3 halt_session rows → false, the 8 inject_reminder rows → true). Deleted the scratch test file used for that check before committing; it is not part of this change.
- A new join table `agent_reflex_opt_outs (agent_id, reflex_id, created_at)` — the per-agent opt-out override mechanism (see step 2). Confirmed via grep (`suppress|opt_out|optOut|OptOut|disabled_reflex|reflex_override|ReflexOverride`) that no such mechanism existed anywhere in the codebase before this task, so it needed to be built, per the task file's own instruction not to guess. Both FKs (`agent_profiles(id)`, `agent_reflexes(id)`) use `ON DELETE CASCADE` — precedented by `agent_mode_assignments.mode_id REFERENCES modes(id) ON DELETE CASCADE` (`001_schema.sql`) — so deleting either side cleans up the opt-out row automatically; no changes needed to `DeleteAgent`'s or `DeleteAgentReflex`'s existing manual-cleanup code, which I verified by test (`TestMigrate106AgentDeleteCascadesOptOuts`, `DeleteAgentReflex`'s doc comment).
- First draft wrapped the Up/Down bodies in explicit `BEGIN;`/`END;` (matching 105/075's style), which failed with "cannot start a transaction within a transaction" — those two migrations also set `-- +goose NO TRANSACTION` (needed because their table-rebuild dance toggles `PRAGMA foreign_keys`, which SQLite disallows inside a transaction); this migration doesn't need that, so it dropped the explicit BEGIN/END and let goose's own default per-migration transaction wrapping handle it (matching 103/104's simpler style instead).

**2. Applicability wiring.** `internal/store/agent_reflexes.go`:
- `AgentReflex` gained `OptOutAllowed bool` (`opt_out_allowed` column/JSON key).
- `ListAgentReflexesForAgent` (the entry point `internal/agent/reflexes/engine.go`'s `EvaluateState` calls to resolve which reflexes apply to an agent) now excludes a class-bound reflex when `opt_out_allowed = 1` AND an `agent_reflex_opt_outs` row exists for `(agentID, that reflex's id)`; a class-bound reflex with `opt_out_allowed = 0` is never excluded by this table, matching the task file's exact contract in step 2. Agent-specific reflexes (`agent_id` set) are unaffected — the opt-out question is a class-bound-reflex concern per the architecture doc's own framing.
- Added `SetAgentReflexOptOut` / `ClearAgentReflexOptOut` / `ListAgentReflexOptOuts` store functions — the concrete override mechanism. Did **not** add new HTTP routes for managing these (e.g. no `POST /api/agents/{id}/reflexes/{reflexId}/opt-out`) — the task's own Touches list scopes `internal/api/reflexes.go` to "CRUD/validation for the new field" (i.e. `opt_out_allowed` itself, which is wired — see step 4), not a management surface for the opt-out table, and Phase 3's Steering task (`dispatch_to_agent` action kind) is the stated consumer of this field; leaving the actual opt-out-request surface (self-tool vs. operator API vs. something else) for that task to decide is a deliberate scope boundary, not an oversight. The mechanism itself is real, store-callable, and tested (see step 5).
- `InsertAgentReflex`/`UpdateAgentReflex` now bind `OptOutAllowed` explicitly on every write (no implicit Go-zero-value defaulting inside these functions, since `false` is a legitimate explicit value here, not just "unset" — same reasoning that already applies to `TriggerKind`/`ActionKind` etc. in this file). Audited and updated all 4 real (non-test) call sites that construct `store.AgentReflex{}` literals (`internal/agent/reflexes/seeds.go`, `internal/agent/reflexes/loom_pilot_seeds.go`, and `internal/api/reflexes.go`'s create/patch handlers) to set the field deliberately. `ApprovePendingReflex`'s raw-SQL insert (pending-reflex review flow) intentionally omits the column from its INSERT so it falls through to the column's own `DEFAULT TRUE` — correct there since agent-proposed reflexes are never the hand-picked safety-critical seeds.

**3. Safety audit of the base seeds.** `internal/agent/reflexes/seeds.go`'s `BaseSeeds()` has **11** entries as of this commit (process: 6, advisor: 3, template: 2), not the 12 the task file states — a factual correction to the task file's own count, logged here per EXECUTION-PROCESS.md worker step 7 rather than reopening the task (the task itself — do the safety audit against whatever the real seed list is — stands unchanged). Added a `Required bool` field to `BaseReflexSeed` (zero-value `false` preserves every existing seed's current opt-out-able behavior without touching each literal) and set `Required: true` on exactly the three seeds whose `action_kind` is `halt_session`: `drift_detector_echo` (FU-13 cache-miss-echo kill switch), `task_complete_self_terminate` and `task_timeout` (template-class hard lifecycle caps). All three are genuine kill switches where an agent opting out would defeat the mechanism entirely; the other 8 seeds are all `inject_reminder` nudges an agent can legitimately outgrow or find too noisy, so they stay at the permissive default. `loom_pilot_seeds.go`'s 3 agent-specific seeds (Loom Curator/Weaver pilot) are all `inject_reminder` too — explicitly set `OptOutAllowed: true`, not safety-critical.

**4. API surface.** `internal/api/reflexes.go`: `handleCreateAgentReflex` and `handlePatchAgentReflex` both gained an `opt_out_allowed *bool` request field (pointer, matching the pointer-optional-field pattern the PATCH handler already used for its other fields) — omitted on create defaults to `true` (permissive); omitted on patch leaves the existing value untouched. `GetAgentReflex`/`ListAgentReflexesForAgent`/`ListAllAgentReflexes` all round-trip the field automatically since it's part of the `AgentReflex` struct's JSON tags. No changes needed to `validateReflexDefinition` — a bool has no invalid-enum state beyond JSON's own type enforcement, unlike `trigger_kind`/`action_kind`/`status`.

**5. Tests.**
- `internal/store/migration_106_agent_reflex_opt_out_test.go`: column DEFAULT TRUE (verified via a raw insert that omits the column, mirroring `ApprovePendingReflex`'s own column list), the opt-out table's FK/PK behavior (idempotent set, FK rejection against a nonexistent `reflex_id`), both CASCADE directions (deleting the reflex removes the opt-out row; deleting the opting-out agent removes it too, while the class-bound reflex itself survives), and the goose Down/Up round trip (`DownTo(105)` then `Up`).
- `internal/agent/reflexes/opt_out_test.go` — the task's literal Done-means test, run through the real `Engine.EvaluateState` path (not just the SQL layer): `TestReflexOptOut_RequiredFiresDespiteOptOutAttempt` (a reflex with `opt_out_allowed=false` still fires for an agent with an active opt-out row against it), `TestReflexOptOut_PermissiveCanBeSuppressedPerAgent` (a default `opt_out_allowed=true` reflex is genuinely suppressed once opted out), `TestReflexOptOut_ClearingOptOutReenables`, and `TestReflexOptOut_OnlyAppliesToTheOptingOutAgent` (scoping — a second agent of the same class untouched by another agent's opt-out). Each case uses its own freshly-inserted reflex row rather than reusing one across "before"/"after" assertions in the same test — `Engine.EvaluateState`'s `recentlyFired` 15-minute re-fire guard (`engine.go`) means a row that already fired once won't fire again moments later regardless of opt-out logic, which the first draft of this test discovered the hard way (a shared-row version returned 0 actions instead of the expected 1, purely from the guard, not a logic bug) — documented as a comment in the test file so a future editor doesn't reintroduce it.

**Verification:** `go build ./cmd/nanite/`, `go vet ./...` (pre-existing, unrelated `internal/service/container.go` reaper-leak vet warnings only — confirmed present before this change via `git stash`), and `go test ./...` all pass (0 failures across 90 tested packages).

Commit: (recorded in a follow-up commit per this codebase's established two-step pattern, e.g. `54ed763c`).

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
