# Make the project's post-compaction CLAUDE.md/AGENTS.md re-read mandatory-by-default, code-driven

**Phase:** 2
**Status:** implemented
**Depends on:** none
**Touches:** wherever the current per-agent opt-in flag for this behavior lives (not yet located — confirm during implementation, see Context), `internal/runtime/agent/bootdir.go`/`kickoff.go` (the boot-dir planting infrastructure this task does NOT change, only confirms/doesn't break)

## Context

**Read this task's distinction carefully before starting — a planning landmine flags this precisely**: *"the boot-profile catalog retirement is subtle. The mandatory post-compaction re-read exists because of a specific Claude Code CLI fact — only the boot-dir's own `CLAUDE.md` survives compaction — not because of a general 'agents should re-read project files' preference. Get this distinction right before scoping what to port forward vs. retire."*

There are genuinely **two separate things** here, confirmed against the code, and this task is only the second one:

1. **Nanite's own boot-dir `CLAUDE.md`/`AGENTS.md` planting and its survival through Claude Code's compaction** — this is **already real, shared, working infrastructure**, confirmed via `internal/runtime/agent/prompt.go:19-22`'s doc comment: *"For ModeLongLived sessions the prompt is set ONCE at PTY start... slot changes thereafter regenerate `<bootDir>/CLAUDE.md` and rely on claude's context-recovery re-read."* This is what makes Nanite's own system/workflow instructions durable across compaction — it already works, it's not catalog-specific, and **this task does not touch it**. Decision log §7 is explicit: *"already safe today, nothing to port, just something to not accidentally break when launching gets redesigned."*
2. **Whether an agent is additionally instructed to re-read the *project's own real* `CLAUDE.md`/`AGENTS.md`** (a different file — the project's, not Nanite's boot-dir's) **after compaction** — decision log §7: *"a deliberate, optional design choice layered on top (a deliberate choice, not a technical necessity)... can be made mandatory/code-driven now if wanted, low stakes either way"* and separately, architecture doc `02-agent-launching.md`: *"Post-compaction re-read of the project's real `CLAUDE.md`/`AGENTS.md` is mandatory-by-default, code-driven — not a per-agent opt-in flag."* **This is what this task actually changes.**

### The specific per-agent opt-in flag — not yet located, confirm during implementation

Research for this planning pass did not conclusively locate a specific, currently-existing per-agent flag gating instruction (2) above — grepping `internal/runtime/agent/`/`internal/agent/` for project-CLAUDE.md re-read instructions found only the boot-dir's own regeneration-and-implicit-re-read mechanism (item 1, untouched by this task). **Before implementing, a worker must first locate the actual current mechanism** (likely a boot-profile-catalog-driven prompt addendum, given the boot-profile catalog's `LaunchSpec.BootPrompt` override path documented in `prompt.go:37-44` — `resolveBootPrompt`'s `Options.BootPromptOverride`; if the current "re-read the project CLAUDE.md" instruction is only ever injected via a catalog-authored `BootPrompt`, then it may not exist as a standalone flag at all today outside the (retiring) boot-profile catalog, in which case this task's real job is *introducing* the mandatory code-driven instruction for the first time, not converting an existing opt-in flag to mandatory). Document what's actually found in this file's Work Log — this Context section states the landmine and the search starting points, not a confirmed pre-existing mechanism.

## What to do

1. Locate whatever currently controls whether an agent's boot content instructs it to re-read the project's real `CLAUDE.md`/`AGENTS.md` post-compaction. Document what's found (a real per-agent flag, or confirmation this only ever came from catalog-authored `BootPrompt` content and has no other current source).
2. Make this instruction code-driven and unconditional for every CLI-based agent's planted boot content — not a per-agent opt-in, not YAML-catalog-gated. Land this in the same shared boot-content-composition path `composeSystemPrompt`/`resolveBootPrompt` (`internal/runtime/agent/prompt.go`) already uses, so it's structurally impossible for a new agent to be missing the instruction by omission.
3. Explicitly confirm this task does **not** touch Nanite's own boot-dir `CLAUDE.md` planting/regeneration mechanism (item 1 above) — that's already correct and working; a worker should verify by reading `prompt.go`/`bootdir.go` in full before making any change, to avoid conflating the two.
4. Verify in a real session: trigger compaction on a real CLI-based agent session, confirm the agent's next turn's boot content includes the project-re-read instruction unconditionally (not dependent on any per-agent config).

## Done means

- Every CLI-based agent's boot content includes the project-CLAUDE.md/AGENTS.md-re-read instruction post-compaction, unconditionally, code-driven — no opt-in flag exists or is needed.
- Nanite's own boot-dir `CLAUDE.md` planting/regeneration mechanism is confirmed unchanged (regression check).
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- Verified end to end in a real session with a real compaction event.

## Work log

**1. What's actually controlling instruction (2) today — confirmed absence, not a pre-existing opt-in flag.**

Grepped `internal/runtime/agent/`, `internal/bootprofile/`, `internal/service/` for any existing "re-read the project's real CLAUDE.md/AGENTS.md" instruction:

- `composeSystemPrompt`/`resolveBootPrompt`/`ResolveSystemPrompt` (`internal/runtime/agent/prompt.go`) — the shared boot-content-composition path every CLI Layout (`claudeLayout`, `codexLayout`, `opencodeLayout`) and `composeBootdirParams`/`ResolveBootdirParams` (`bootdir.go`) route through — had no such instruction anywhere in `composeSystemPrompt`, `roleFraming`, or `modeFraming`.
- The boot-profile catalog's default prompt renderer, `internal/bootprofile/compiler.go`'s `renderDefaultPrompt`, also had no such instruction (it only emits an Identity block + ordered slot bodies).
- `internal/store/agents.go`'s `AgentProfile` struct has no field resembling a per-agent opt-in for this (`SystemPrompt` is the only relevant free-text field, and it's agent-authored content, not a structural flag).
- The only "re-read CLAUDE.md" text anywhere in the codebase is `internal/service/chat_boot_drive.go:280`'s `slotRereadInstruction` ("System: configuration updated. Please reread CLAUDE.md...") — but that's item 1 (Nanite's own boot-dir CLAUDE.md re-read, sent as a mid-session SendInput nudge after a slot regen), not item 2 (an instruction to re-read the *project's* real CLAUDE.md/AGENTS.md). Confirmed distinct and untouched.

**Conclusion, as the task's Context flagged as the likely outcome: there is no pre-existing per-agent opt-in flag for instruction (2) anywhere in the codebase, catalog-gated or otherwise.** This task's real job is introducing the mandatory, code-driven instruction for the first time, not converting an existing flag. No escalation needed — this matches one of the two outcomes the task file's Context section explicitly anticipated and pre-authorized.

**2. Implementation.** Added a fixed, unconditional instruction constant (`mandatoryPostCompactionRereadInstruction`) and a single append helper (`withMandatoryPostCompactionReread`) to `internal/runtime/agent/prompt.go`. Wired it into both `resolveBootPrompt` (the hook every CLI Layout's `BootPrompt()` method and `composeBootdirParams`'s `SetupParams.SystemPrompt` consult) and the exported `ResolveSystemPrompt` (used by `chat_boot_drive.go`'s `regenerateBootDirSlots` for mid-session CLAUDE.md regen) — the two entry points the task named. Appended it **after** the override-vs-composed branch in both functions, not inside `composeSystemPrompt` itself, so it lands unconditionally regardless of whether the base content came from `composeSystemPrompt` (role/profile/mode) or a boot-profile catalog's `Options.BootPromptOverride` / `LaunchSpec.BootPrompt` — satisfying "not YAML-catalog-gated" literally: a catalog-authored prompt can no longer omit it. Left `composeSystemPrompt` itself unchanged (its own unit test asserts `composeSystemPrompt(unknown-role, nil, ModeLongLived) == ""`, which would break if the instruction were folded in there instead).

**3. Confirmed item 1 (boot-dir CLAUDE.md/AGENTS.md planting/regeneration mechanism) is unchanged.** Read `bootdir.go`, `bootdir_claude.go`, `bootdir_codex.go`, `bootdir_opencode.go`, `bootdir_plant.go`, and `kickoff.go` in full before touching anything. `kickoff.go` (the `boot.md`/`composeKickoff`/`composeBootContent` first-turn-payload mechanism) is untouched entirely — zero diff. `bootdir.go`'s `Layout` interface, `Setup`/`Populate`/`RegenerateSystemPromptSlot`/`AmendEnv`/`SpawnWorkdir`/`BootMode` methods, the three per-provider `claudeInjectionSpec`/`codexInjectionSpec`/`opencodeInjectionSpec` file-set builders, and `plantInjectionSpec`'s atomic-write mechanics are all untouched — only the *content string* flowing through `resolveBootPrompt`/`ResolveSystemPrompt` (which was already the single source both `SetupParams.SystemPrompt` and every Layout's `BootPrompt()` consult) now carries one extra fixed instruction line. The delivery mechanism (plant into `<bootDir>/CLAUDE.md`, rely on claude's own context-recovery re-read of its own cwd) is exactly as documented before this task — the new instruction is content riding on top of that already-working mechanism, not a change to the mechanism itself. This is the intended relationship per the task's own step 2 ("land this in the same shared boot-content-composition path... already uses").

**4. Test updates.** Several existing tests pinned the pre-existing "override wins **verbatim**" behavior with exact-equality assertions (`TestResolveBootPrompt_Override`, `TestResolveBootPrompt_FallthroughComposes`, `TestBootPrompt_{Claude,Codex,Opencode}Layout_HonorsOverride` in `launch_spec_options_test.go`; `TestComposeBootdirParams_SystemPromptHonorsOverride`'s "override wins" subtest and `TestResolveSystemPrompt`'s "override wins verbatim" subtest in `streaming_stdio_frame_test.go`). Updated all of these to assert the override body still wins verbatim as a *prefix*, with the mandatory instruction appended after it — this is an expected, deliberate behavior change these tests were pinning against (the task explicitly requires the instruction to be unconditional even under a catalog override), not a regression. `TestResolveSystemPrompt`'s "parity with resolveBootPrompt" subtest needed no change (both functions still produce identical output for identical inputs). No other test in the repo asserted exact-match boot-prompt/system-prompt *resolved output* content — the several other hits on `SystemPrompt`/`BootPrompt` across `internal/runtime/agent/*_test.go` and `internal/service/*_test.go` were either the `store.AgentProfile.SystemPrompt` DB-row field (input, not resolved output) or used `strings.Contains`, which still passes.

**5. Baseline checks.** `go build ./cmd/nanite/` — pass. `go vet ./...` — fails with two pre-existing findings in `internal/service/container.go` (`stopReaper`/`stopRuntimeReaper` unused-on-all-paths), confirmed pre-existing and unrelated by stashing this task's diff and re-running vet (identical failure with the stash applied). `go test ./...` — all packages pass, including `internal/runtime/agent` and `internal/service`.

**6. Real-session, real-compaction verification (Done means item 4).** Did not deploy to the shared `nanite-api-service` Cerberus resource — that's a live, shared instance (confirmed running via `launchctl`/`ps`) that other in-flight work may depend on, and deploying an unmerged worktree build onto it would be an unsafe cross-worktree action for a single task's verification. Instead, verified against the real, installed `claude` CLI binary (the same one this session runs on, already authenticated) directly, mirroring Nanite's own boot-dir strategy, entirely within this worktree + the scratchpad — no shared/production resource touched:

- Wrote a throwaway `cmd/_verify03/main.go` generator (deleted before commit; `git status --short` confirmed clean after) that imports the real, modified `internal/runtime/agent` package and calls the actual exported `ResolveSystemPrompt` + `BuildCLAUDEMD` functions — i.e. exercised real, modified production code, not a hand-typed approximation — to render a real boot-dir `CLAUDE.md` into a scratch directory. Confirmed the mandatory instruction line is present verbatim in the generated file.
- Created a second scratch "project directory" with its own `CLAUDE.md` carrying a unique marker line (`PROJECT-CLAUDE-MARKER: zephyr-91427-quokka`), reachable only via `--add-dir` — standing in for "the project's own real CLAUDE.md," distinct from the boot dir.
- Drove a real, live `claude -p --input-format stream-json --output-format stream-json` subprocess (cwd = the generated boot dir, `--add-dir` = the project dir, `--tools Read`, `--permission-mode bypassPermissions`, `--model haiku`, `--no-session-persistence`, `--max-budget-usd 1.00`) through several real turns, then sent a real `/compact` control message.
- First attempt with only 1 prior turn: `/compact` returned `compact_result: failed, compact_error: "Not enough messages to compact."` — a real (if unsuccessful) compaction attempt. Added several more turns and retried.
- Second attempt: a real `compact_boundary` system event fired (`compact_result: success`, `trigger: manual`, `pre_tokens: 67992 → post_tokens: 255`) — a genuine Claude Code compaction, not a mock.
- The next turn after compaction (the first turn on the "other side" of the real compaction event) had the model, unprompted beyond the test question, **quote the mandatory post-compaction re-read instruction verbatim from its CLAUDE.md**, then **actually invoke the real `Read` tool** against the project directory's `CLAUDE.md` (the file reachable only via `--add-dir`, not the boot dir) and correctly report the marker value `PROJECT-CLAUDE-MARKER: zephyr-91427-quokka`.

This confirms, end to end, with a real running agent and a real Claude Code compaction event: (a) the instruction from this task's code change is genuinely present in real boot content produced by the modified production code; (b) item 1's already-working boot-dir-CLAUDE.md-survives-compaction mechanism, left untouched by this task, continues to deliver that content correctly across a real compaction; and (c) a live agent both recites and actually acts on the instruction (performs the real project-CLAUDE.md re-read via its Read tool) immediately after a real compaction event.

No escalation was needed — both possible outcomes flagged in the task's Context (a real per-agent flag existing, vs. no such mechanism existing outside the retiring catalog) were explicitly pre-authorized as valid findings, and the second one is what the codebase actually showed.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
