# Retire the boot-profile catalog as a standalone system

**Phase:** 2
**Status:** implemented
**Depends on:** `01-wire-runtime-kind-routing.md` (the catalog's own `pty-*` handling must not be the last live routing path once this lands); `TASKS/phase-0/18a-cut-dead-storage-and-config.md` (already cuts `agent_boot_plans`, a related-but-distinct earlier attempt at the same problem — confirm landed, do not re-cut it here)
**Touches:** `internal/bootprofile/*` (full package — `loader.go`, `profile.go`, `compiler.go`, `registry.go`, `slots.go`, `requirements.go`, `agentcontext_adapter.go`, `launchplan_bridge.go` or equivalent), `internal/config/config.go` (`BootProfileCatalogPath`/`ResolvedBootProfileCatalogPath`), `examples/boot-profiles/` (example catalog — retire alongside), `docs/boot-profile-cli-harness.md` (update or retire — **currently live-linked from this project's own root `CLAUDE.md`**, see Context), `internal/service/chat_bootprofile_resolve.go`, `internal/service/chat_boot_drive.go`

## Context

Architecture doc `02-agent-launching.md`: *"The boot-profile catalog is retired as a standalone system. It never competed with agent construction — it only ever overrode prompt content, with a real `agents` row still resolved underneath. Two pieces carry forward as first-class, DB-configurable mechanisms available to every agent... Nothing else carries forward. 'Lineage' ... is dropped entirely. Cross-app portability ... is deliberately opt-in, not a structural default."* Decision log §7 has the full reasoning, including: *"`agent_boot_plans` (previously flagged as dead/unwired code, a judgment call) is now understood in this light too — it looks like an earlier, abandoned attempt at the same 'plant items into a boot dir' problem this catalog already solves differently. Reinforces treating it as safe to cut rather than revive."* — that cut is Phase 0's job (`18a`), not this task's; confirm it landed rather than re-doing it.

### `LineageAlias`/`LineageID` — confirmed real, confirmed drop-entirely

`internal/bootprofile/profile.go:52-53` (`LineageAlias string`, `LineageID string`), consumed throughout `loader.go` (catalog keys by `LineageAlias`, required non-empty), `compiler.go` (`lineage_alias`/`lineage_id` rendered into compiled output). Per decision log §7: *"Built specifically for Tesseract to observe how an agent definition changed across versions over time — not a continuity/hand-off mechanism as originally guessed... 'we got no value out of it.' Nothing to port, nothing to reconcile against the new `agents.id` model."*

### **`docs/boot-profile-cli-harness.md` is currently live, not a stale doc — do not retire it silently**

This project's own root `CLAUDE.md` currently reads: *"Boot profiles let an operator register shared catalog YAML that surfaces as additional rows in the chat composer's provider/model dropdown... See `docs/boot-profile-cli-harness.md` for the catalog schema, end-to-end happy path..."* This CLAUDE.md reference must be rewritten (not just the underlying doc retired) as part of this task, or a developer reading the live project instructions will be pointed at a doc describing a system that no longer exists. Same caution applies to `examples/boot-profiles/`, which CLAUDE.md calls out as wired into a real smoke test (`go test ./internal/service/ -run TestBootProfileSmoke_`) — confirm that test's fate explicitly (retire it alongside the catalog, or repoint it if any of its assertions genuinely carry forward to the two ported mechanisms).

## What to do

1. Confirm `02-port-forward-dynamic-resolver.md` and `03-mandatory-post-compaction-reread.md` have landed (or land in the same batch) — those two carry forward the only pieces of this system with real value; this task should not delete anything before its replacement exists, per this project's general build-then-cut sequencing principle.
2. Delete `internal/bootprofile/*` in full once nothing references it — confirm via grep no remaining import.
3. Remove `internal/config.Config`'s `BootProfileCatalogPath` field and its resolution logic.
4. Retire `examples/boot-profiles/` and the `TestBootProfileSmoke_*` test suite, or repoint the smoke test at whatever `03`/`04` build if genuinely equivalent coverage is wanted (a worker judgment call — don't force artificial test coverage of a retired system just to preserve a test name).
5. Rewrite this project's root `CLAUDE.md` (and any other project doc referencing the boot-profile catalog by name) to describe the two carried-forward mechanisms instead of the retired catalog — do not leave the current "Boot profiles let an operator register shared catalog YAML..." paragraph pointing at a deleted system.
6. Retire or clearly supersede `docs/boot-profile-cli-harness.md` — per the same standing policy Phase 6 item 2 applies elsewhere, either delete it (its content is fully superseded by this task's replacement) or leave it with an explicit "superseded" banner if it retains real historical value. This is a small, self-contained instance of that broader policy question, not something that needs to wait for Phase 6.

## Done means

- `internal/bootprofile` package is fully deleted; `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass with no remaining references.
- `LineageAlias`/`LineageID` are gone from the codebase entirely — confirmed no lingering references anywhere, including tests.
- Root `CLAUDE.md` accurately describes the post-retirement mechanism, not the deleted catalog.
- `docs/boot-profile-cli-harness.md` is either deleted or carries an explicit superseded banner.
- Every prior boot-profile-catalog-driven launch still launches correctly via the new mechanisms (`03`/`04`) — verified in a real session, not just build/test passing.

## Work log

### Dependencies confirmed landed

`TASKS/phase-2/02-port-forward-dynamic-resolver.md` and `TASKS/phase-2/03-mandatory-post-compaction-reread.md` are both `implemented` on this branch (`agent_context_resolvers` table + `internal/runtime/agent/context_resolver.go`; `mandatoryPostCompactionRereadInstruction` in `internal/runtime/agent/prompt.go`). `TASKS/phase-0/18a-cut-dead-storage-and-config.md` is `implemented` and confirmed it cut `agent_boot_plans` already — not re-touched here.

### Correction to this task's own scope estimate — executed anyway, expanded per worker step 7

The task's own "Touches" list (`internal/bootprofile/*`, `internal/config.Config`, `examples/boot-profiles/`, `docs/boot-profile-cli-harness.md`, `chat_bootprofile_resolve.go`, `chat_boot_drive.go`) badly undersold the real coupling surface. Confirmed via repo-wide grep for `bootprofile` before starting: the boot-profile catalog was NOT a self-contained package with a few call sites — it was the load-bearing spine of an entire family of features, several of which have their own real, live UI and their own standalone CLI subcommand. Per this project's standing policy ("if the correction means the job is bigger than it looked... expand the task's scope to remove all of it — don't stop and ask") and per this same pattern already established by `18a`'s `agent_boot_plans` finding, the full cut ended up covering:

- **`internal/bootprofile/*`** (16 files) — deleted in full, as the task named.
- **`internal/launchplan/*`** (1 file, `launchplan.go`) — NOT named in the task's Touches list. Its entire public API (`RuntimeBindingForSpec`, `AgentSpecForLaunch`, `Build`) is typed on `*bootprofile.LaunchSpec`; it exists solely as the "S5 Phase F" plan-assembly bridge both the standalone launcher and the chat boot-profile path used. Zero purpose once `bootprofile.LaunchSpec` is gone. Deleted in full.
- **`internal/agentregistry/*`** (3 files) — NOT named. Its own doc comment states it wires "a FileBackedRegistrar over the boot-profile catalog root" — 100% boot-profile-catalog-rooted, zero independent value (confirmed via grep: its only consumers were `internal/launchplan`, `internal/launcher`, and the chat/container wiring that fed both). Deleted in full, including the `agentregistry.RegisterAgentSource` "agent-source directory handle" registration in `main.go` (a real, additional feature built on the same Registry object — a pre-standardization cross-app directory-handle registration matching exactly the "Tether... pre-standardization A2A-host experiment" GLOSSARY.md flags as deliberately not a structural default of Nanite's own operation).
- **`internal/launcher/*`** (5 files) — NOT named. This is the entire standalone `nanite launch <profile-id>` CLI subcommand (`cmd/nanite/launch_cmd.go`, deleted along with its orphaned `launch_cmd_test.go`), 100% boot-profile-catalog-driven (`LoadCatalog` → `CompileFromCatalog` → `ResolveRequirementsContext` → `bootOptionsFor`). Removed the `launch` case from `main.go`'s command switch and its usage string. **One real, non-bootprofile-specific helper was rescued before deletion**: `internal/launcher/launcher.go`'s `ResolveBinaryPath` (symlink-resolve a binary path) had a second, unrelated real caller in `main.go` (resolving the binary path fed into the external workflow-engine runner scripts, `internal/workflowrunner`). Moved verbatim to `cmd/nanite/main.go` as a local `resolveBinaryPath` helper rather than deleted — this is exactly the "reality doesn't match the doc" case this project's process expects a worker to catch rather than silently break a working, unrelated feature.
- **`internal/api/meta_harnesses.go`** (+ test) — NOT named. A full CRUD REST API (`GET/POST/PUT/DELETE /api/meta-harnesses`) that read/wrote boot-profile catalog YAML files directly, with a real, mounted settings-page UI panel (`ui/src/components/settings/MetaHarnessManager.tsx`, wired into `SettingsPage.tsx`'s "Harnesses" nav item). Deleted in full, frontend included.
- **`internal/api/providers.go`, `harness_v1.go`, `frontend_readiness.go`** — the actual "chat composer provider/model dropdown" and "start surface" mechanisms CLAUDE.md described. Trimmed the boot-profile-catalog rows/fields (`bootProfileProviderRow`/`bootProfileModelRow`/`bootProfileOption`/`BootProfileID` request field/`inferBootSource`'s `boot_profile` case) while leaving the DB-seed + registry-catalog dropdown merge (CW-20260526-0001) untouched.
- **`internal/service/a2a_agent_card.go`** — NOT named. `AgentCardGenerator` derived A2A skills from both the workflow registry and the boot-profile catalog (`skillFromBootProfile`); trimmed to workflow-only.
- **`internal/service/container.go`, `chat.go`, `chat_generate.go`** — NOT named except `chat_boot_drive.go`. All three had real `BootProfiles`/`AgentRegistry`/`activeSessionLaunchSpecs` wiring; removed the registry construction, the `resolveBootProfile` call in the per-turn provider-resolve path (`chat_generate.go` now resolves `session.Provider` directly — behaviorally identical for every non-bootprofile session, which per Task 01's own real-DB-backup finding is 100% of this deployment's sessions today), and narrowed `classifyNilProvider`'s doc comment (Task 01 had explicitly flagged its boot-profile-case OR-fallback as "dead code to delete once `04` lands" — the OR itself stays, for the still-live file-discovered-agent case; only the stale doc-comment reference to the now-deleted mechanism was removed).
- **`internal/service/chat_bootprofile_recovery.go`** (+ test) — NOT named. The whole crash-recovery pre-boot-hook file was boot-profile-specific per its own package comment ("boot-profile crash-recovery hooks"). Deleted in full, including `RestartAgentSession` (confirmed zero callers anywhere outside its own file/tests — not wired to any API route or `ChatService` interface method despite its doc comment's claim of being a "user-driven restart entry point"; genuinely dead even before this cut). The generic `agentBootAdapter.SetPreBootHook`/`PreBootHook` mechanism itself (`agent_deps.go`) was **kept** — it's a real, general extension point (also the documented seam for future resume-ID threading), not literally coupled to boot profiles; only its sole populator and the `container.go` wiring line installing it were removed.
- **`internal/service/durable_agent_recipes.go`** — removed one recipe-form input field (`boot_profile_id`, writing into a free-text `metadata.boot_profile_id` key with a `bootprofile:claude-smoke` placeholder) that only ever existed to feed the old system.
- **`examples/boot-profiles/`, `docs/boot-profile-cli-harness.md`, `docs/standalone-launcher.md`** — all deleted. `docs/standalone-launcher.md` (documenting `internal/launcher`) was NOT named in the task's Touches list but is fully superseded now that the feature it documents is gone.
- **Frontend**: beyond `MetaHarnessManager.tsx`/`SettingsPage.tsx`, also trimmed the "Harness" start-path tab out of `StartSurfaceDialog.tsx` (`HarnessStartPane`, `createHarness` mutation, `bootProfileId` state — the tab always showed "No boot profiles available" and could never work post-cut) and `ChatHeader.tsx`'s two `provider.startsWith("bootprofile:")` branches that used to route "Open Start from here" into that tab. `types.ts`: removed `BootProfileOption`/`MetaHarness`/`MetaHarnessInput` interfaces, `StartSurfaceCapabilitiesResponse.boot_profiles`, `HarnessCreateSessionRequest.boot_profile_id`, `StartSurfacePrefill`'s `"harness"` path member and `boot_profile_id` field, and the `"boot_profile"` literal from `SessionBootSource`/`ImmutableStartField` (the backend can no longer emit either). `api.ts`: removed the four `*MetaHarness*` client functions.

### Explicitly NOT touched — real scope boundaries, not oversights

- **`store.DurableAgentLaunchBootProfile = "boot_profile"`** (Go enum constant + its TS mirror `DURABLE_AGENT_LAUNCH_SOURCES`) — kept. This is a durable-agent `launch_source_type` history/audit tag (real historical rows may carry this value), does not import `internal/bootprofile`, and is a distinct concept from the file-catalog system this task retires. Same treatment as `18a`'s handling of adjacent "boot"-named concepts.
- **`ui/src/lib/sidebar-session.ts`'s `isBootProfileProvider`/`bootprofile:` prefix-stripping** — kept, deliberately. This is read-only, backward-compatible display/classification logic for **existing DB rows** that may still carry a `bootprofile:<id>` provider string from before this migration (real historical data this migration does not rewrite). Removing it would misclassify/mislabel any such pre-existing session in the sidebar. Its own test coverage (`phase-10-sidebar-session-presence.test.tsx`) still exercises this and was left untouched.
- **Historical/point-in-time docs** (`docs/architecture-decision-log-2026-08-17.md`, `docs/system-audit/2026-08-17/**`, `CHANGELOG.md`, `agent-workspaces/phase6-nanite/0027-handoff.md`, `docs/phase6-shared-launch-adoption.md`) — left untouched. These are explicitly-scoped historical records (the decision log's own text says it is "not internally perfect... some early framing was later corrected and the prose was never fully reconciled" and is not meant to be kept in sync going forward); rewriting them would falsify history rather than correct live guidance. Only `docs/engineering/*`, the root `CLAUDE.md`, and the two docs this task explicitly names were in scope.
- **Comment-only historical mentions** of `bootprofile.ResolveRequirements`/`bootprofile.LaunchSpec` in `internal/runtime/agent/context_resolver.go`, `internal/runtime/agent/prompt.go`, `internal/service/chat_boot_drive.go`, `internal/api/frontend_readiness.go` (explaining what Task 02's port-forward or the pre-cut legacy CLI-provider path used to mirror) — left as-is. These are accurate provenance notes matching the pattern already used throughout this codebase's own history (e.g. Task 02's own added comments), not dangling live references — confirmed via `grep -rln "internal/bootprofile\|internal/launchplan\|internal/launcher\|internal/agentregistry" --include="*.go"` that zero files retain a real Go import of any deleted package.
- **`internal/assets/framework/skills/end-of-session.md`'s `lineage_alias` mention** — left untouched. This is generic vocabulary in a shared, cross-project skill asset (the `.nanite`/`~/.nanite/skills` developer-persona-boot convention GLOSSARY.md explicitly calls out as unrelated to Nanite's own runtime agent system), not a reference to the deleted `internal/bootprofile.Profile.Identity.LineageAlias` Go struct field.
- **The real, machine-local `~/.config/nanite/config.yaml`'s `boot_profile_catalog_path` key** (outside this git-tracked worktree, on the operator's own machine — confirmed present during Task 02's own investigation) — not editable from a worktree-isolated dispatch and out of this task's file scope. It becomes an inert, silently-ignored unknown YAML key (confirmed `gopkg.in/yaml.v3`'s default unmarshal behavior per `18a`'s own established finding) — flagging here as an operator heads-up, not a code gap.

### `LineageAlias`/`LineageID`

Confirmed gone from the codebase entirely: `grep -rn "LineageAlias\|LineageID" --include="*.go"` (repo-wide, including tests) returns zero matches post-cut.

### `docs/boot-profile-cli-harness.md`

Deleted outright rather than left with a superseded banner — its content (catalog schema, crash-recovery semantics, known limitations) has no residual historical value once the whole catalog mechanism it documents is gone; the replacement mechanisms are documented fresh in root `CLAUDE.md`'s new "Agent launching (Phase 2 — boot-profile catalog retired)" section and in `docs/engineering/architecture/02-agent-launching.md`. `docs/standalone-launcher.md` was deleted for the identical reason (it documents `internal/launcher`, also fully deleted).

### Root `CLAUDE.md`

Replaced the "Boot-profile CLI harness" section with "Agent launching (Phase 2 — boot-profile catalog retired)", describing the two carried-forward mechanisms (`agent_context_resolvers` dynamic context resolvers; the mandatory post-compaction re-read) and pointing at `docs/engineering/architecture/02-agent-launching.md`. Confirmed via grep zero remaining `boot-profile`/`bootprofile`/`launcher`/`meta-harness` mentions anywhere else in the file.

### Build / vet / test

- `go build ./...` (whole repo, not just `cmd/nanite/`) — pass.
- `go vet ./...` — two pre-existing findings in `internal/service/container.go` (`stopReaper`/`stopRuntimeReaper` possible-context-leak), independently reconfirmed pre-existing via `git stash`/`git stash pop` on this task's own diff (same two functions flagged, same shape, on the unmodified base commit) — unrelated to this task, matching the same finding Tasks 01 and 02 already logged.
- `go test ./...` — full suite passes, zero failures, including every remaining package this task touched.
- Frontend: `npm install` (node_modules was empty in this worktree — installed fresh), `npx tsc --noEmit` clean, `npm run build` clean (including the envelope-manifest codegen step), `npx vitest run` — 22 test files, 180 tests, all passing (including the four test files this task edited: `phase-9-frontend-api-wiring.test.ts`, `phase-11-start-surface.test.tsx` — which also had its dedicated `"starts a boot-profile harness session"` test removed, since that code path no longer exists — `phase-24-agent-builder-wizard.test.tsx`, `phase-10-sidebar-session-presence.test.tsx` unchanged and still passing).

### Real-session verification ("Done means" item 5)

Could not deploy to the shared `nanite-api-service` Cerberus resource for the same reason Tasks 01/02/03 already documented (a live, shared instance other in-flight work may depend on; deploying an unmerged worktree build would be an unsafe cross-worktree action). Instead, built a throwaway generator (`cmd/_verify04/main.go`, deleted before commit; `git status --short` confirmed clean immediately after cleanup, matching the process reminder) that calls the real, modified, currently-shipping production functions directly — not a hand-typed approximation:

1. `runtimeagent.ResolveContextBlocks` (Task 02's mechanism) against a real `store.AgentContextResolver` row (`kind=cmd`, `run="printf '72F-and-sunny'"`) — a genuine subprocess exec, not mocked.
2. `runtimeagent.ResolveSystemPrompt(role, agentProfile, ModeLongLived, "", blocks)` — the exact function `chat_boot_drive.go`'s `regenerateBootDirSlots`/`resolveBootPrompt` call — confirmed the composed prompt contains BOTH the Task 02 `## Dynamic context: weather` block AND the Task 03 mandatory post-compaction re-read instruction, unconditionally, with zero `internal/bootprofile` involvement anywhere in the call graph (already proven at compile time by the package's deletion; this proves it functionally).
3. `runtimeagent.BuildCLAUDEMD` — planted the real resulting bytes as `CLAUDE.md` in a scratch boot dir.
4. A second scratch "project directory" with its own `CLAUDE.md` carrying a unique marker (`PROJECT-CLAUDE-MARKER-PHASE2-04: zebra-58213-falcon`), reachable only via `--add-dir`.
5. Drove a real, live `claude -p --input-format stream-json --output-format stream-json --verbose --tools Read --permission-mode bypassPermissions --model haiku` subprocess (cwd = the generated boot dir) with one real turn asking it to state the weather value and read the project's own `CLAUDE.md`.

Result, from the real subprocess's actual output: the model correctly recited **"Weather: 72F-and-sunny"** (proving Task 02's dynamic-context data reached a real, live CLI agent's context and was used) and correctly invoked the real `Read` tool against the project directory's `CLAUDE.md`, reporting the marker verbatim (proving the mandatory re-read instruction — Task 03's mechanism — is still present in real boot content generated by the post-cut code and a live agent still acts on it when relevant, reconfirming task 03's own separately-proven "fires spontaneously after a real compaction event" finding still holds now that its sibling mechanism has been fully removed). This directly satisfies "every prior boot-profile-catalog-driven launch still launches correctly via the new mechanisms" for the CLI-routed launch path — the one boot-profile-catalog launches always used (`ProviderAlias`/`pty-*` forcing CLI routing). Cleaned up both scratch directories and the throwaway generator; `git status --short` confirmed clean afterward.

The API-routed (HTTP provider) launch path was not separately live-verified with a real provider call (no network/credentials in this sandboxed dispatch) — its routing logic (`classifyNilProvider`'s `runtimeKind != "cli" && !chat.IsCLIProvider(...)` gate) was untouched by this task's diff (only its doc comment changed), and the full `go test ./...` pass includes its existing dedicated test coverage (`TestClassifyNilProvider_*`, `chat_generate_test.go`) unchanged and green.

### Commit

All changes committed in this worktree as a single commit; see the dispatch's final report for the SHA.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
