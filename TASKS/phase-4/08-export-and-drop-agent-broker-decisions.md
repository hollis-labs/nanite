# Export and drop `agent_broker_decisions`

**Phase:** 4
**Status:** implemented
**Depends on:** `02-dispatch-to-agent-reflex-action-kind-and-broker-migration.md` (this table's writer, `attemptBrokerDispatch`/`persistAgentBrokerDecision`, is retired by that task — this task can't drop the table safely until nothing writes to it anymore)
**Touches:** `internal/store/broker_decisions.go` (`InsertAgentBrokerDecision`, `ListRecentAgentBrokerDecisions`), `internal/service/chat_broker_dispatch.go` (`persistAgentBrokerDecision` — should already be gone once task 01 lands; confirm), `cmd/nanite/admin_cmd.go` (the admin CLI subcommand that dumps recent rows), `internal/store/migrations/` (new drop migration for `agent_broker_decisions`)

## Context

`TASKS/ESCALATIONS.md`'s resolved entry "Item 23: `agent_broker_decisions` deferred to Phase 3" states: *"`agent_broker_decisions` excluded from Phase 0, picked up when Phase 3 retires the Agent Broker into reflexes."* Phase 0's `23-export-and-drop-decision-tables.md` already exports and drops `broker_decisions`/`strategy_decisions` — this task does the same thing for the third table in that original TASKS.md item (item 23), which Phase 0 couldn't touch because its writer (the agent broker itself) wasn't retired until this phase. Decision log §14: *"The three now-orphaned `*_decisions` tables (`broker_decisions`, `agent_broker_decisions`, `strategy_decisions`... get their data exported, then dropped."*

### Current state, verified during planning research

- Schema: migration `058_agent_broker_decisions.sql` (goose-renumbered from `057`): `id, session_id, turn_id, user_input_hash, mode_signal, scope_tier, reflex_id, decision, reason, confidence REAL, created_at`. Append-only by convention (no update/delete helpers exist).
- Single write call site: `Store.InsertAgentBrokerDecision` (`internal/store/broker_decisions.go:53`), called only from `persistAgentBrokerDecision` (`internal/service/chat_broker_dispatch.go:448-495`), itself called from `attemptBrokerDispatch` — the exact code path `01-dispatch-to-agent-reflex-action-kind-and-broker-migration.md` deletes.
- Read consumers: `Store.ListRecentAgentBrokerDecisions` (`broker_decisions.go:97`), consumed by a real admin CLI subcommand in `cmd/nanite/admin_cmd.go` that dumps recent rows. **No frontend consumer** — the `agent_broker_decision` SSE event this table's writer also emitted has no FE handler (confirmed dead wire, intentional per the emitting code's own comment: "FE inspector card intentionally out of scope").
- Reuse Phase 0's export mechanism/format from `TASKS/phase-0/23-export-and-drop-decision-tables.md` rather than inventing a new one — same table shape category (append-only decision/reasoning log), same disposal reasoning.

## What to do

1. Confirm task 01 has landed and `persistAgentBrokerDecision`/`attemptBrokerDispatch` no longer exist — `agent_broker_decisions` should have zero live writers before proceeding.
2. Export the full historical contents of `agent_broker_decisions`, using the same export format/location Phase 0's `23-export-and-drop-decision-tables.md` established for `broker_decisions`/`strategy_decisions`.
3. Drop `agent_broker_decisions` via a new goose migration.
4. Remove `Store.InsertAgentBrokerDecision`/`ListRecentAgentBrokerDecisions` and the admin CLI subcommand that reads them (or repoint the admin CLI at the exported archive if that command is still wanted for historical lookups — pick one, document the choice).
5. Confirm the dead `agent_broker_decision` SSE-event plumbing is also removed as part of task 01's broker retirement (not this task's job to re-verify beyond a quick grep — flag if it's somehow still present).

## Done means

- `agent_broker_decisions`'s historical data is exported to the same archive location/format Phase 0 used for the other two decision tables.
- The table is dropped; a fresh migration run produces no `agent_broker_decisions` table.
- No remaining Go code references `InsertAgentBrokerDecision`/`ListRecentAgentBrokerDecisions`/the dropped table.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- Tested against a real copy of the backed-up database, not just an empty fixture.

## Work log

**2026-08-19 — worker report.**

**Step 1 pre-condition re-verified, per the task's own instruction to confirm rather than assume.** `internal/service/chat_broker_dispatch.go` no longer exists (`ls` fails, file absent). `grep -rn "persistAgentBrokerDecision\|attemptBrokerDispatch"` returns zero real call sites — only three explanatory comments (`cmd/nanite/main.go:336`, `internal/service/chat_reflex_dispatch.go:5,124`, `internal/service/chat_generate.go:631`) that reference the retired functions by name for historical context. `git log --oneline -- TASKS/phase-4/02-dispatch-to-agent-reflex-action-kind-and-broker-migration.md` confirms it landed on `main` (commit `f55bf667`, "feat(reflexes): add dispatch_to_agent action kind, retire agent broker"), and its own Work Log documents deleting `chat_broker_dispatch.go` in full (571 lines: `attemptBrokerDispatch`, `buildBrokerInput`, `persistAgentBrokerDecision`, `emitAgentBrokerDecisionSSE`, `hashUserInput`, `classifyForReflex`) and both its test files. Confirmed `agent_broker_decisions` had zero live writers: the only remaining call sites of `InsertAgentBrokerDecision`/`ListRecentAgentBrokerDecisions` were the `UsageStore` interface declaration (`internal/service/store.go`), a test-stub method (`internal/service/chat_test.go`), the store package's own tests, and the admin CLI's read path (`cmd/nanite/admin_cmd.go`) — no production write call site anywhere. Proceeded.

**Step 2 — export mechanism reused exactly, not reinvented.** Read `TASKS/phase-0/23-export-and-drop-decision-tables.md`'s own Work Log and the resulting implementation (`cmd/nanite/admin_export_decisions.go`, migration `111_drop_broker_and_strategy_decisions.sql`) in full before touching anything, per this task's brief. Added one entry to the existing `decisionTablesToExport` slice in `cmd/nanite/admin_export_decisions.go`:
```go
{table: "agent_broker_decisions", eventType: "agent_broker_decision_export", category: "agent_broker", detailColumn: "decision"}
```
`decision` (the retired broker's `broker.Decision.AgentProfile` — `""`/`"worker"`/`"planner"`/custom slug) is the closest analog to the existing `approach`/`intent` detail columns: a short, human-readable summary of what the row recorded. No other code changed in that file beyond: widening the `--force` re-run guard's `event_type IN (...)` list to include `agent_broker_decision_export`, and updating the file's header comment (it previously said "Does NOT touch agent_broker_decisions... not retired by any currently-scheduled Phase 0 task" — that was Phase 0's correct scoping at the time; now that this Phase 4 task retires the writer, updated the comment to explain the one-line extension rather than leaving a stale "does not touch" claim next to code that now does). The generic column-map export logic, the pre-export duplicate-run guard, and the post-export row-count-mismatch hard-failure (decision log §14 / task 23's "a failed export must not silently precede a successful drop" requirement) all apply unchanged — no new code path was needed for the third table, confirming task 02's own note that the export mechanism "was built to make easy" for exactly this follow-up.

**Step 3 — drop migration.** `internal/store/migrations/120_drop_agent_broker_decisions.sql` (goose format; 119 was the prior highest, landed by task 02). `DROP TABLE IF EXISTS agent_broker_decisions` in the Up; the Down recreates the table's final pre-drop shape (structure only, no data — same precedent as `110_drop_prompt_templates.sql`/`111_drop_broker_and_strategy_decisions.sql`), including both indexes from migration 058 (`idx_agent_broker_decisions_session`, `idx_agent_broker_decisions_created_at`). The migration's header documents the mandatory pre-condition (export must run first) with the same "DROP TABLE is not recoverable" warning 111 carries.

**Step 4 — admin CLI subcommand: removed in full, not repointed at the archive.** Decision, documented here as the task instructs:

- **What was removed**: `internal/store/broker_decisions.go` (the whole file — `AgentBrokerDecision` type, `InsertAgentBrokerDecision`, `ListRecentAgentBrokerDecisions`) and its test file `internal/store/broker_decisions_test.go`; the `UsageStore.InsertAgentBrokerDecision` interface method (`internal/service/store.go`) and its test stub (`internal/service/chat_test.go`); the entire `nanite admin agent-broker-decisions` subcommand and every helper it alone used (`adminAgentBrokerDecisions`, `agentBrokerDecisionsFilter`, `parseAgentBrokerWhere`, `parseAgentBrokerSince`, `applyAgentBrokerFilter`, `fetchPool`, `parseSQLiteDatetime`, `printAgentBrokerTable`, `printAgentBrokerJSON`, `emptyDash`, `truncDisplay` — confirmed via grep that none of these were used anywhere outside `admin_cmd.go`/`admin_cmd_test.go`) and its dedicated test file `cmd/nanite/admin_cmd_test.go`. `cmd/nanite/admin_cmd.go` now only contains `cmdAdmin`'s dispatch to the one remaining subcommand, `export-decision-tables`.
- **Why removal over repointing, not repointing over removal**: (1) this is the exact disposal Phase 0's `23-export-and-drop-decision-tables.md` already chose for the two sibling tables — it exported `broker_decisions`/`strategy_decisions` to `event_log` and dropped them without building any archive-reading CLI or API for the exported rows; that precedent task explicitly frames "same disposal reasoning" as the standard to reuse, and no bespoke reader was part of that reasoning. (2) The subcommand's own header comment stated its purpose precisely: "so operators can see how the deterministic agent broker is performing" — a live-system triage tool. Once task 02 retired the Agent Broker itself, there is no live performance left to triage; the data is permanently frozen historical telemetry, identical in kind to what task 23 already left as plain `event_log` rows with no dedicated reader. (3) No other code (Go or frontend) called any of the removed helpers or the `AgentBrokerDecision` type — confirmed via grep before deleting — so nothing else breaks. (4) The replacement mechanism (`dispatch_to_agent` reflex firings, per task 02) already writes richer structured `event_log` rows directly (`reflex_id`, `reflex_name`, `agent_slug`, `confidence`, `reason`, `scope_tier`, `execution_pattern`, `alternatives_considered`) — an operator investigating *current* routing behavior queries `event_log WHERE event_type='dispatch_to_agent'`; an operator investigating the retired broker's *historical* behavior queries `event_log WHERE event_type='agent_broker_decision_export'`. Both are directly queryable via `sqlite3` without any bespoke CLI, matching how task 23 left its two exported tables. (5) Rebuilding the filter/table/JSON-output plumbing (`--since`/`--where confidence<F`/`--json`) against the differently-shaped `event_log.metadata` JSON blob would be meaningfully more code than the export step itself needed, in service of a query pattern (an operator browsing dead historical telemetry from a system that no longer runs) with zero recorded demand since task 02 landed.

**Step 5 — dead SSE plumbing, quick-grep confirmation only (not re-verified beyond that, per the task's own scope).** `grep -rln "agent_broker_decision\b\|emitAgentBrokerDecisionSSE\|AgentBrokerDecisionEvent"` across all `.go`/`.ts`/`.tsx` files returns zero hits. Confirmed already fully removed by task 02, as its own Work Log states. Nothing to flag.

**Real backup DB verification (Done means' explicit requirement).** Copied `~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726` (plus its `-wal` sidecar) into an isolated scratchpad location — never touched the original backup. This particular backup snapshot pre-dates migration 111 (Phase 0's drop of `broker_decisions`/`strategy_decisions`), so it exercises the full remaining chain (108→120) in one run, including this task's new migration 120.
- Pre-export/pre-migration counts (this specific backup copy): `agent_broker_decisions`=315 rows, `broker_decisions`=1006 rows, `strategy_decisions`=411 rows, `event_log`=3836 rows.
- Built the real `nanite` binary from this worktree and ran `admin --db <scratch-db> export-decision-tables --dry-run` (correct row counts reported, zero writes), then the real export (no `--dry-run`). `event_log` gained exactly 315 `agent_broker_decision_export` rows (plus the pre-existing sibling tables' 1006 `broker_decision_export` + 411 `strategy_decision_export`, since this backup hadn't been exported before) — row-count match confirmed both by the command's own built-in post-export verification (which would have hard-failed on any mismatch) and an independent `sqlite3` count.
- Spot-checked and scripted full-file validation of all 315 `agent_broker_decision_export` metadata blobs: 100% valid, complete JSON, each reconstructing the full original row (`id`, `session_id`, `turn_id`, `user_input_hash`, `mode_signal`, `scope_tier`, `reflex_id`, `decision`, `reason`, `confidence`, `created_at`) — zero parse failures.
- Verified the `--force` guard: a second export run without `--force` correctly refused ("event_log already has 1732 exported decision row(s)").
- Applied migrations via a temporary throwaway Go harness (`cmd/tmp-migrate-runner-task08/`, calling the real `store.New()`) — built, run once against the scratch DB, then **deleted** before finishing, same "assemble, run, delete" pattern task 23's worker used for `cmd/tmp-migrate-runner-task23/`; not part of the committed diff. Post-migration: `sqlite_master` has zero tables matching `%decision%` (all three — `agent_broker_decisions`, `broker_decisions`, `strategy_decisions` — gone); `goose_db_version`'s latest entry is `120`; `event_log` unchanged at 5568 rows (matches pre-migration count exactly — the drop touches no other table); spot-checked `sessions`=344 and `messages`=1105 intact.
- `git status --short` run immediately after the verification (per `EXECUTION-PROCESS.md`'s guidance) — confirmed clean, only this task's real diff present, no stray write to any tracked file.

**Checks.** `go build ./cmd/nanite/`, `go build ./...`, `go test ./...` — all pass, full package list green. `go vet ./...` — only the pre-existing `stopReaper`/`stopRuntimeReaper` lostcancel warnings in `internal/service/container.go`, confirmed via `git status --short internal/service/container.go` (no diff — this task never touches that file) that they're unrelated, matching the same finding tasks 23 and 02 both already documented.

**Deviations from the plan.** None beyond the admin-CLI disposition decision the task explicitly asked to be made (documented above under Step 4). No scope expansion was needed — grepped confirmation showed the removed helpers had no other callers.

**Escalations.** None. No genuine ambiguity, missing-coverage, or item-vs-item conflict arose. The one real judgment call this task's own brief flagged as needing a decision (admin CLI: remove vs. repoint) was made and documented per the task's own instruction, not escalated.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
