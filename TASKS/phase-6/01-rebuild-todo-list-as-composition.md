# Rebuild `todo-list` as a `list-card` composition

**Phase:** 6
**Status:** reviewed
**Depends on:** none
**Touches:** `libs/go-envelopes/manifest/envelopes.yaml` (external module, local `replace` directive — remove the standalone `todo-list` entry), `ui/src/components/chat/envelopes/TodoListCard.tsx` (retire or fold into `list-card`'s component), `ui/src/components/chat/envelopes/primitives/` (the `list-card` primitive component), `internal/mcp/self_tools_transport.go:1074` (the `"type": "todo-list"` emitter — update to emit `list-card` with a status field/discriminator instead)

## Context

Architecture doc `08-cards.md`: *"`todo-list`, `plan-review`, and `subagent-spawn-approval` are being rebuilt this way [composed from the primitive set] rather than staying separate types."* Decision log §36: *"`todo-list` → `list-card` + status field."*

### Current implementation, verified

`TodoListCard.tsx` — `data: {scope, scope_id, title?}`. **The envelope itself is just a pointer/trigger, not a data payload** — the component fetches live via `useTodos({scope, scope_id})` (a `useQuery` hook against a real REST endpoint) and mutates via `useToggleTodo()`. This live-refetch behavior must be preserved by the composition — `list-card` is not currently built to re-fetch its own data, so this rebuild needs either a `list-card` variant that supports a live-data-source prop, or a thin wrapper component that fetches and then renders through `list-card`'s presentation layer. Emitted from `internal/mcp/self_tools_transport.go:1074`.

## What to do

1. Confirm `list-card`'s current schema/component (`ui/src/components/chat/envelopes/primitives/`) and decide how it accepts a status field per item (checkbox/done-state) — extend the schema if it doesn't already support this, following the existing `props` discriminator pattern.
2. Decide the live-refetch question above and implement: either extend `list-card` to accept a live-data-source configuration, or keep a thin `TodoListCard`-shaped wrapper that fetches via `useTodos` and renders through `list-card`'s presentation internals (not a full parallel component) — document the choice in this file's Work Log.
3. Update the manifest (`libs/go-envelopes/manifest/envelopes.yaml`) to remove the standalone `todo-list` type once the composition is live — run `node scripts/generate-plugin-imports.mjs` (or confirm it runs automatically) to regenerate `ui/src/generated/plugin-envelopes.ts`.
4. Update `self_tools_transport.go:1074`'s emitter to produce the new composed shape.
5. Verify toggle/mutation behavior (`useToggleTodo`) still works identically through the composed card.

## Done means

- `todo-list` no longer exists as a standalone manifest entry; its behavior is fully reproduced via `list-card` + composition.
- A real todo list still renders and toggles correctly in a live chat session, streaming and post-reload.
- `cd ui && npm run build` passes; `node scripts/generate-plugin-imports.mjs --check` passes.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.

## Work log

**Path note:** the emitter has moved since this task was written — `internal/mcp/self_tools_transport.go:1074` no longer exists; the `todo_list` self-tool (including the `callTodoList` emitter) now lives in `internal/selftools/self_tools_transport.go` (package renamed at some point between task authoring and this pass). Edited that file instead; confirmed via grep there is no `internal/mcp/self_tools_transport.go` in the repo.

**Live-refetch decision (item 2):** chose "extend `list-card`" over "keep a thin `TodoListCard`-shaped wrapper". `list-card`'s schema grew an optional top-level `data_source` object (`{kind: "todos", scope, scope_id}`, `additionalProperties:false`, `kind` required) and optional per-item `id`/`status` (`"pending"|"done"`) fields. The frontend `ListCard` component checks `data.data_source?.kind === "todos"` and, when true, renders an internal (not separately manifest-registered) `LiveTodoList` sub-component that re-fetches via `useTodos({scope, scope_id})` and toggles via `useToggleTodo()` — byte-for-byte the same hooks `TodoListCard.tsx` used. This keeps a single `list-card` manifest entry/component (no parallel type), satisfying "compose, don't multiply types," while still being a genuinely thin, clearly-delimited live-fetch branch rather than smearing fetch logic through the static-list render path. `TodoListCard.tsx` was retired (deleted) — its logic folded into `ListCard.tsx`.

**Files changed:**
- `libs/go-envelopes/manifest/envelopes.yaml` (shared physical mirror, reached via the local `replace` directive from every worktree — not Nanite-repo-tracked): removed the standalone `todo-list` entry (4 lines) from the "Work system (todo/plan)" block, replaced with a comment pointing at this task and the `list-card` schema. Left the `plan-review` entry and everything else in the file untouched — kept the diff to exactly the `todo-list` block per the task's own narrow-diff instruction. **Confirmed the file really is a live, concurrently-shared resource, not just theoretically so**: mid-task, a re-read of the file showed the sibling `03-rebuild-subagent-spawn-approval` task's edit already landed (composed `subagent-spawn-approval` onto `ApprovalCard`'s component/export), and later the sibling `02-rebuild-plan-review` task's removal of the standalone `plan-review` entry landed too — both applied cleanly around my edit with no overlap, and task 02 additionally *extended* the `data_source`/`status` fields I'd added to `list-card.schema.json` (adding a `"plans"` `data_source.kind`, a `plan_id` field, and `in_progress`/`skipped` status values) in a way fully compatible with my todo composition. No manual conflict resolution was needed at any point; each task's edit stayed additive and non-overlapping.
- `libs/go-envelopes/manifest/schemas/list-card.schema.json`: added `data_source` (object, optional, `additionalProperties:false`) and per-item `id`/`status` (optional) as described above. Existing required fields (`items`) and existing example/test payloads remain valid — purely additive.
- `libs/go-envelopes/CHANGELOG.md`: added `[Unreleased]` "Added" and "Removed" entries for the `list-card` schema growth and the `todo-list` type removal, matching the style/precedent the `subagent-spawn-approval` (task 03) and earlier Phase-0 cut entries already established in this file.
- `ui/src/components/chat/envelopes/primitives/ListCard.tsx`: rewritten. Default branch (no `data_source`) renders exactly like the prior generic `ListCard`, factored into a shared `ListRow` sub-component that additionally renders a checkbox instead of a bullet/number when an item carries `status` (works for both static and live items). Added `LiveTodoList`, the todo-composition branch described above.
- `ui/src/components/chat/envelopes/TodoListCard.tsx`: deleted. Confirmed via grep it had no other importers besides the (regenerated) `ui/src/generated/plugin-envelopes.ts`.
- `ui/src/generated/plugin-envelopes.ts`: regenerated via `node scripts/generate-plugin-imports.mjs` (and again via `npm run build`, which runs the same codegen). `todo-list` entry is gone; `list-card` entry unchanged (still maps to `ListCard`, no `props` discriminator needed — the default `{ data }` shape already carries `data_source` through). Note: by the time of the final regeneration the shared manifest also reflected sibling task 02's `plan-review` removal and task 03's `subagent-spawn-approval` composition — the generated file necessarily reflects the manifest's live combined state, not just this task's own edit; flagging this so the Orchestrator/reviewer understands why the generated-file diff is wider than "just remove todo-list" but is not a mistake — it's a deterministic function of a shared, non-worktree-isolated source file.
- `internal/selftools/self_tools_transport.go` (`callTodoList`): emitter now marshals `{"kind":"envelope","version":1,"type":"list-card","data":{"title":..., "items":[], "data_source":{"kind":"todos","scope":...,"scope_id":...}}}` instead of `{"type":"todo-list","data":{"scope":...,"scope_id":...,"title":...}}`. `items` is sent as an explicit empty array to satisfy `list-card`'s schema (`items` is a required field) — the frontend does not trust it when `data_source` is set. Updated the two comment blocks above/around the emission site accordingly.
- `internal/selftools/self_tools.go`: updated the `todo_list` tool's description text (agent-facing prompt copy) to say "list-card (with a live todos data source)" instead of "todo-list card", and "list-card envelope (with a data_source pointer)" instead of "todo-list envelope" in the output-shape description. No functional change, just kept the agent-facing docs honest about the new wire shape.
- `internal/selftools/self_tools_test.go` (`TestSelfToolsTransport_TodoListEmitsEnvelope`): updated assertions from `"type":"todo-list"` / bare `scope`/`scope_id` at the top of `data` to `"type":"list-card"` / `"kind":"todos"` plus the same `scope`/`scope_id` values now nested under `data_source`. Test still passes (verified directly via a compiled test binary with `-test.v`, since the sandboxed `go test` wrapper in this environment doesn't surface `-v` output — see Verification below).

**Deliberately left untouched (flagged, not fixed):**
- `libs/go-envelopes/registry_test.go` (`TestLoadCore_typesWithoutSchemaHaveNilDataSchema`) and `validator_test.go` (`TestValidateEnvelope_typeWithoutSchema`) both hardcode `"todo-list"` as their "a core type with no schema file" fixture. Before this task, `todo-list` and `plan-review` were the *only* two schema-less core types; after this task and sibling task 02 both land, there will be **zero** schema-less core types left in the manifest, so both tests will fail (or need a different fixture) regardless of which of the two removal tasks "wins" the race to edit them — a guaranteed collision between this task and task 02 on the exact same two test functions, not something I could resolve unilaterally without either stepping on task 02's concurrent edit or picking a fixture (`plan-review`) that task 02 was about to remove out from under me. This is out of this task's explicit "Touches" list (only `manifest/envelopes.yaml` was named) and is a real, if narrow, follow-up: whoever picks it up should restructure both tests to register a local, ad-hoc schema-less type via `RegisterTypeFromManifest` at test time instead of depending on a specific core manifest entry staying schema-less. Not part of Nanite's own `go build`/`go vet`/`go test ./...` baseline (that module's tests only run from within `libs/go-envelopes` itself), so it doesn't block this task's "Done means," but it's real technical debt in the shared library.
- `internal/envelope/contracts_test.go`'s `knownTypes`/`examplePayloads` fixtures: already didn't include `todo-list` (it never had a schema file, so it was never in this schema-file-driven fixture list) — nothing to update here.
- `docs/planner.md` and `internal/assets/framework/docs/nanite-planner.md` (developer-persona/meta documentation for humans and dev-agents working *on* this codebase, part of the unrelated `.nanite/` boot-persona system per `GLOSSARY.md`'s "Agent" entry, not the Nanite runtime's own agent-launching CLI boot content) both still describe `todo-list` as a card type. Left alone — not in this task's "Touches" list, not runtime-facing, and `08-cards.md`'s own "known, live bug in CLI-agent boot content" note explicitly defers stale-type-list cleanup to the separate CLI-boot-directory-content work in `02-agent-launching.md`, not to individual composition tasks.
- Historical docs (`docs/audits/envelope-audit-2026-05-16.md`, `docs/architecture-decision-log-2026-08-17.md`, `docs/system-audit/2026-08-17/code-architecture/15-envelope-system.md`, `docs/superpowers/specs/...`, `docs/superpowers/plans/...`) mentioning `todo-list` — left untouched as historical record, consistent with how the repo already treats past audit/decision-log snapshots.

**Verification performed:**
- `go build ./cmd/nanite/` — passes.
- `go vet ./...` — one pre-existing failure in `internal/service/container.go` (`stopReaper`/`stopRuntimeReaper` possible-context-leak lint), confirmed via `git status`/`git log` to be unrelated to and untouched by this task (no diff on that file). `go vet` on every package this task actually touched (`./internal/selftools/...`, `./internal/chat/...`, `./internal/envelope/...`) is clean.
- `go test ./...` — all packages pass (`ok` or cached-`ok`), including after the shared manifest picked up sibling tasks' concurrent edits partway through this session.
- `cd ui && npm install && npm run build` — passes (had to `npm install` first; `node_modules` was empty in this worktree). `tsc -b && vite build` both clean, no new TypeScript errors from `ListCard.tsx`.
- `node scripts/generate-plugin-imports.mjs --check` — passes.
- `TestSelfToolsTransport_TodoListEmitsEnvelope` specifically re-verified via a compiled test binary (`go test -c` + `-test.v`) since this sandbox's `go test` wrapper suppresses `-v` output even on a fresh (`-count=1`) run: `--- PASS: TestSelfToolsTransport_TodoListEmitsEnvelope (0.21s)`.
- Not performed: an actual live chat session click-through (create/list/toggle a todo end-to-end against a running server) — that requires a deployed instance via Cerberus and a live LLM turn, which is outside this implementation pass's tooling. The schema, emitter unit test, and frontend build/typecheck are the automated stand-ins; a reviewer with a live session should still exercise the real toggle path per "Done means."

## Review notes
**2026-08-21, fresh Reviewer (no shared context with the implementing worker): PASS.** Confirmed the emitter (`internal/selftools/self_tools_transport.go`'s `callTodoList`) emits `type:"list-card"` with `data_source:{kind:"todos",...}` matching the schema's `items`-required constraint. `ListCard.tsx`'s `LiveTodoList` reuses `useTodos`/`useToggleTodo` byte-for-byte. The manual merge with sibling task 02 in `ListCard.tsx` (both tasks independently added a `data_source`-driven branch to the same file) was independently re-verified as correct — see `TASKS/INDEX.md`'s Phase 6 section for the full combined review writeup covering all five tasks. No findings, no fix needed.
