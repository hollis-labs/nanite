# Rebuild `subagent-spawn-approval` as an `approval-card` composition (larger rebuild than `todo-list`/`plan-review`)

**Phase:** 6
**Status:** implemented
**Depends on:** none
**Touches:** `libs/go-envelopes/manifest/envelopes.yaml` (remove standalone `subagent-spawn-approval` entry, or keep the type name but change its component wiring — see What to do), `ui/src/components/chat/envelopes/SubagentSpawnApprovalCard.tsx` (retire, folding its UI into/alongside `ApprovalCard`), `internal/subagent/service.go:848` (`svc.approver.Emit(ctx, run.ParentSessionID, "subagent-spawn-approval", payload)` — the real emitter), `internal/chat/envelope_response_subagent.go` (the registered response handler — `chat.RegisterResponseHandler("subagent-spawn-approval", ...)`, `internal/service/container.go:1231`), `internal/api/sessions.go` (a conditional at ~line 818 gating recovery-related handling on `inst.EnvelopeType != "subagent-spawn-approval" && ... != "elicitation-prompt"` — must not break)

## Context

Architecture doc `08-cards.md`: *"`subagent-spawn-approval` → `approval-card` + subagent-specific data via the existing `props` discriminator mechanism (already used for `approval-card`/`proposal-card`, just not applied consistently)."* Decision log §36 same framing.

### This is a materially bigger rebuild than `04`/`05` — verified, flag this sizing explicitly

Unlike `todo-list`/`plan-review` (thin wrappers over a live re-fetch, swappable for a composed primitive with the same shape), `subagent-spawn-approval` today does **not** reuse `ApprovalCard` at all — it is its own fully custom, self-contained component (`SubagentSpawnApprovalCard.tsx`, `envelope`-shaped prop, `data: {run_id, role, prompt, mode, parent_agent_id?, timeout_seconds?, inputs_json?, risk_level?}`): a risk-level badge, collapsible prompt/advanced sections, a reason textarea, and approve/reject buttons posting via the generic typed-response mechanism. "Approval-card + subagent-specific props" means **deleting a working, materially more elaborate bespoke component and re-implementing its UI (risk badge, collapsibles, reason field) inside or alongside `ApprovalCard`** — not a shape-preserving swap like `04`/`05`. Scope and estimate this task accordingly; do not treat it as the same size as the other two composition rebuilds.

### The `props` discriminator's real current mechanics — read before designing

`manifest.yaml` declares `props: approval`/`props: proposal` only for `approval-card`/`proposal-card` — but the actual generated frontend registry does **not** use those values as-is. A host-side `CORE_OVERRIDES` map (`scripts/generate-plugin-imports.mjs:38-47`) force-overrides `props` to `"envelope"` for `approval-card`, `proposal-card`, `confirmation-card`, `subagent-spawn-approval`, and `elicitation-prompt` — because interactive cards need the *whole* envelope wrapper (to read `prior_response` for post-reload hydration), which a stripped single-field `approval`/`proposal` prop would hide. Confirmed in `EnvelopeRenderer.tsx:189-212`: the `"approval"`/`"proposal"` prop-branches exist in the switch but are **currently unreachable** from both the core registry (always overridden to `"envelope"`) and dynamic plugin registrations. **Any new composed card needing response-routing + prior_response hydration should be wired via `props: "envelope"` + a `CORE_OVERRIDES` entry, matching the pattern the 5 existing interactive cards already use — not the manifest's own `approval`/`proposal` discriminator values, which are effectively vestigial today.**

### A real call site that must not break

`internal/api/sessions.go` (~line 818) gates recovery-related envelope handling on `inst.EnvelopeType != "subagent-spawn-approval" && inst.EnvelopeType != "elicitation-prompt"` — if this task changes the emitted `type` string, this comparison (and any other string-literal `"subagent-spawn-approval"` match) must be updated in lockstep. Grep for every literal occurrence of `"subagent-spawn-approval"` before finishing, not just the emitter/handler/component sites already known.

## What to do

1. Design the risk-badge/collapsible-sections/reason-field UI as an extension of `ApprovalCard` (via its `data`/`props` shape) rather than a bolt-on separate component — this is real UI design work, not a mechanical type swap.
2. Decide whether the manifest `type` string stays `subagent-spawn-approval` (same wire type, different component wiring underneath) or changes — if it changes, update every literal string match across the codebase (see the `sessions.go` gate above and any others found by grep).
3. Wire response-routing through `props: "envelope"` + a `CORE_OVERRIDES` entry, matching the existing interactive-card pattern (not the vestigial manifest `props` value).
4. Update `internal/subagent/service.go:848`'s emitter and `envelope_response_subagent.go`'s response handler registration to match whatever `type`/shape decision was made.
5. Verify the full approve/reject round-trip, prior-response hydration after a page reload, and the `sessions.go:818` recovery-gating conditional all still work correctly post-rebuild.

## Done means

- `subagent-spawn-approval`'s full current UI (risk badge, collapsible sections, reason field, approve/reject) is reproduced through `ApprovalCard` + composition, not lost in the rebuild.
- Every literal `"subagent-spawn-approval"` string match across the codebase is updated consistently (or confirmed unchanged, if the type string itself doesn't change).
- Prior-response hydration after a page reload works correctly (regression check — this is the specific thing `props: "envelope"` exists to preserve).
- `cd ui && npm run build` passes; `node scripts/generate-plugin-imports.mjs --check` passes.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- Verified end to end in a real session: a real subagent spawn approval request, approved and rejected paths both exercised.

## Work log

**Decision: wire type `subagent-spawn-approval` stays unchanged; only the component wiring moved.**
Per the task's own explicit option ("Decide whether the manifest type string stays `subagent-spawn-approval` ... or changes"), I kept the wire type. Every literal `"subagent-spawn-approval"` string in the backend (the emitter at `internal/subagent/service.go:848`, the response-handler registration key in `internal/service/container.go:1225`, the recovery-rehydration gate in `internal/api/sessions.go` — found at line 609 in this checkout, not ~818 as the task's stale line ref said — and every Go test asserting that literal) is confirmed unchanged and still passing. This was the lower-risk reading of "approval-card + subagent-specific data via the existing props discriminator mechanism": reuse the `ApprovalCard` *component*, not necessarily the `approval-card` *wire type*. Renaming the wire type would have required migrating a real, non-empty production table (`~/.local/share/nanite/workspaces/default/main.db` has 5 real `subagent-spawn-approval` rows, 3 already responded) and touching every one of the call sites above for no behavioral gain, since `approval-card` itself turned out to have no live backend emission path of its own to converge onto (grep confirms `RegisterResponseHandler` is never called for the bare `"approval-card"` type — it's the subagent-spawn-approval + elicitation-prompt flavors that are the only live interactive-response-routed cards riding `props: "envelope"` today besides `proposal-card`/`confirmation-card`).

**What changed:**
1. `libs/go-envelopes/manifest/envelopes.yaml` (shared mirror, `/Users/chrispian/dev/hollis-labs/libs/go-envelopes`, reached via the `.claude/libs` symlink from this worktree): the `subagent-spawn-approval` entry's `component`/`export` now point at `components/chat/envelopes/ApprovalCard` / `ApprovalCard` instead of the old standalone `SubagentSpawnApprovalCard`. `type:` is unchanged. Added an explanatory comment plus a CHANGELOG.md `[Unreleased] > Changed` entry documenting the UI-hint-only move (no schema/Go-API change). This is the only manifest hunk I touched — confirmed via `git diff -- manifest/envelopes.yaml` in the mirror repo that my hunk is isolated to the `subagent-spawn-approval` block, both before and after two sibling tasks (01-rebuild-todo-list, 02-rebuild-plan-review, and apparently also touching confirmation-card/table-card/list-card schemas — likely 02 and/or 04) landed further concurrent, uncommitted edits to the same shared working tree while I worked. Per instruction, I did not revert or touch their hunks. I deliberately did **not** commit inside the go-envelopes repo myself (it still shows `main [ahead 3]` from an earlier, separate phase of commits, now with several tasks' edits piled up uncommitted side by side) — the task file says the Orchestrator does the sequential 01→02→03 merge, and since the working tree currently interleaves multiple tasks' uncommitted work in the same files, there's no way for me to `git commit` just my own hunk without either mis-attributing or clobbering a concurrently-running sibling's in-progress edit. Left it uncommitted for the Orchestrator to reconcile.
2. `ui/src/components/chat/envelopes/SubagentSpawnApprovalCard.tsx` — deleted.
3. `ui/src/components/chat/envelopes/ApprovalCard.tsx` — rewritten as a single component with two flavors, discriminated by `envelope.type === 'subagent-spawn-approval'`:
   - Shares the risk-badge config (`RISK_META`: icon/tone/label for low/medium/high — the old `SubagentSpawnApprovalCard` had its own near-duplicate `RISK_CONFIG` with lowercase labels; unified onto the one already in `ApprovalCard`, a genuine, minor cosmetic normalization, not a functional loss), the `Envelope`/`EnvelopeHeader`/`EnvelopeBody`/`EnvelopeFooter`/`StatusPill` primitives, and a single shared `hydrateDecision` (verified the old subagent-only `hydrateDecided` and the generic `hydrateDecision` produce identical results for both flavors' actual wire shapes — subagent approve posts `{status:"submitted"}` with no `data`, so `data?.approved === false` is never true; subagent reject posts `{status:"cancelled"}` which the shared function catches first regardless).
   - The subagent flavor reproduces the full previous UI: risk badge, prompt truncation/collapse (`Show full`/`Collapse`, 200-char threshold), the "Advanced" collapsible (parent_agent_id / inputs_json), the reason textarea, and Approve/Reject buttons.
   - Preserved the exact backend wire contract per flavor rather than collapsing them: the subagent flavor's `respond()` posts `status: Submitted` (approve, no data) or `status: Cancelled` with `data: {reason}` (reject) — matching what `internal/chat/envelope_response_subagent.go`'s registered handler switches on. The generic `approval-card` flavor is untouched: always `status: Submitted` with `data: {approved, description}`.
   - One deliberate minor visual normalization: the old subagent card only drew a left accent bar for medium/high risk (`undefined` accent at low risk); the composed card now always draws the accent (matching the generic flavor's existing behavior, `accent={meta.tone}`, where low → a subtle success-tone bar). Noting this explicitly since "full current UI... reproduced, not lost" is a Done-means bullet — this is the one intentional, cosmetic deviation, not a scope loss.
4. `libs/go-envelopes`-mirror-driven codegen: ran `npm run generate:plugins` (`node scripts/generate-plugin-imports.mjs`), confirmed `--check` passes. `ui/src/generated/plugin-envelopes.ts`'s `subagent-spawn-approval` entry now lazy-imports `ApprovalCard` instead of the deleted file. (Note: by the time I ran the final `--check`, the shared manifest had also picked up sibling tasks 01/02's own concurrent, uncommitted edits — `todo-list` and `plan-review` entries are both gone from the generated file's core list as a result. That is their work landing in the shared mirror during my session, not something I did or reverted; `go build`/`go vet`/`go test` for this worktree are unaffected since neither `todo-list` nor `plan-review` ever had a JSON Schema file or a Go-side registry dependency — confirmed by grep and by the full green Go test run below.)
5. `ui/src/__tests__/envelope-card-hydration.test.tsx` — updated the two `SubagentSpawnApprovalCard` hydration tests to import and render `ApprovalCard` instead (same assertions, same envelope fixtures — the composed component reproduces the exact same terminal-state text).
6. No backend Go changes were needed or made — the emitter, the response handler, its registration, and the `sessions.go` recovery gate are byte-for-byte unchanged, confirmed by full `go test ./...`.

**Verification performed:**
- `go build ./cmd/nanite/`: pass.
- `go vet ./...`: same 4 pre-existing findings as the pre-task baseline (`internal/service/container.go` reaper-cleanup-path warnings, confirmed present before I touched anything) — no new findings.
- `go test ./...`: all 70+ packages pass, including targeted re-runs of `TestEnvelopeResponse_SubagentApproval_Approve` / `_Reject` (`internal/api`) and `internal/chat`'s subagent-approval handler tests and `internal/api`'s `session_plugin_envelopes_test.go` (the `sessions.go:609` rehydration-gate test).
- `cd ui && npm install && npm run build`: pass (had to `npm install` first — `node_modules` wasn't present in this worktree).
- `node scripts/generate-plugin-imports.mjs --check`: pass.
- `npx vitest run` (full suite): 22 files / 180 tests, all pass, including the two updated hydration tests.
- Real end-to-end evidence beyond synthetic fixtures: pulled real historical `subagent-spawn-approval` rows out of `~/.local/share/nanite/workspaces/default/main.db` (`envelope_instances` table — 5 real rows, including real `run_id`/`role`/`prompt`/`mode`/`inputs_json`/`timeout_seconds`/`parent_agent_id` payloads and 3 real persisted `{"status":"submitted"}` responses) and rendered them through the new composed `ApprovalCard` in a throwaway vitest test (written, run, confirmed passing — pending render showed risk badge/advanced section/Approve+Reject; terminal render showed the correct "approved" text and hid the pending header), then deleted the scratch test since it wasn't meant to be a permanent addition. The approve/reject round-trip itself (spawn → gated envelope → POST respond → run status transition, both approved and rejected) is exercised by the real HTTP-API-level `TestEnvelopeResponse_SubagentApproval_Approve`/`_Reject` integration tests in `internal/api/envelopes_subagent_approval_test.go`, which I re-ran explicitly (both pass, verbose log confirms real envelope emit + respond + run-state transition for both paths).
- Grepped every literal `"subagent-spawn-approval"` occurrence across the repo (Go, TS/TSX, YAML, docs) — confirmed all are either (a) unchanged wire-type-string usages that still match, or (b) my own new explanatory comments. The only docs referencing the retired `SubagentSpawnApprovalCard.tsx` filename by name are the task file itself and two dated historical snapshots (`docs/audits/envelope-audit-2026-05-16.md`, `docs/architecture/chat-system/13-notification-and-card-surface.md`) — left untouched as historical record, consistent with how `08-cards.md` (the authoritative, current architecture doc for this system) already describes the target composition without needing any edit.

**Scope note:** confirmed (not part of this task, not touched) a pre-existing, unrelated schema/data mismatch: the emitter's payload includes a `"provider"` field (`internal/subagent/service.go:848`'s marshal) that the `subagent-spawn-approval.schema.json` schema doesn't declare, under `additionalProperties: false` — visible in 2 of the 5 real DB rows. This is invisible today because this emission path (`ApprovalEmitterImpl.Emit`) never calls `envelope.ValidateData` for this type. Pre-existing, unrelated to the UI composition being rebuilt here; not fixed as part of this task.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
