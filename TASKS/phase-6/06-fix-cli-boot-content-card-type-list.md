# Fix CLI-agent boot content to source the Card type list dynamically instead of hardcoded/stale content

**Phase:** 6
**Status:** reviewed
**Depends on:** none, but should land after `01`-`04` (the primitive-composition rebuilds) so the dynamically-sourced list reflects the final, post-rebuild type set rather than needing a second pass
**Touches:** `internal/runtime/agent/sandbox_content_envelope.go` (`envelopeSchemaContent` — planted `.sandbox/envelope-schema.md`), `internal/runtime/agent/sandbox_content_claude.go` (`claudeMDBody` — the CLAUDE.md addendum planted into Claude-CLI boot dirs), `internal/chat/envelope.go` (`EnvelopeRegistry()`/`registeredTypes` — the live, in-process registry to query instead of hardcoding), `internal/plugin/builtin/adapter-claude/plugin.go:168` (the planting call site)

## Context

Architecture doc `08-cards.md`: *"CLI-launched agents are currently told to consult files for 'the current card type list' that don't exist in this repo, and separately given a stale, incomplete list directly in their boot prompt. Fix as part of the CLI boot-directory content work — don't hardcode a static type list into planted content again; source it from the manifest the codegen check already validates against."*

### Two separate stale-content problems, not one — verified, both real

1. **`.sandbox/envelope-schema.md`** — content: `envelopeSchemaContent` const (`internal/runtime/agent/sandbox_content_envelope.go:8-155`), planted via `internal/plugin/builtin/adapter-claude/plugin.go:168` into every Nanite-managed boot dir regardless of provider. Contains a hardcoded "Registered Envelope Types" table of **exactly 7 types** — `giphy-modal`, `document-viewer`, `report-card`, `kb-result`, `ticket-confirmation`, `ticket-form`, `resolution-capture` — with the line *"These are the only types the frontend can render. Using any other type causes the envelope to be silently dropped."* **5 of these 7 are cut in Phase 0** (`giphy-modal`, `kb-result`, `ticket-confirmation`, `ticket-form`, `resolution-capture` — items 15a/15c). Post-Phase-0, this file describes 5 nonexistent types and is missing all ~17-20 real remaining ones (the 8 Phase-7 primitives, `approval-card`/`proposal-card`/`document-viewer`/`report-card`/`error-report`, `artifact-mini`, `elicitation-prompt`, plus this phase's composed replacements) — a materially worse mismatch than before Phase 0 even landed.
2. **`claudeMDBody`** (`internal/runtime/agent/sandbox_content_claude.go:43-59`) — the CLAUDE.md addendum planted into every Claude-CLI boot dir. Lines 48-49 point agents at `config/envelopes.yaml` and `internal/envelope/schemas/*.schema.json` for "the current registered envelope types and per-type schemas" — **both paths confirmed not to exist anywhere in this repo.** Per current root `CLAUDE.md`, the real manifest is external (`github.com/hollis-labs/go-envelopes` module's `manifest/envelopes.yaml`).

**Broader staleness, same two nonexistent paths, found beyond the two planted-content files**: live doc-comments in `internal/mcp/self_tools.go:270,293`, `internal/mcp/elicitation.go:20`, `internal/mcp/self_tools_describe_test.go:254-255`, `internal/service/chat_loop_terminated.go:12-13,18`, `internal/service/chat_loop_budget_soft_warning.go:15-16` (note: this file is cut by Phase 0 item 12, confirm before touching), `internal/service/chat_loop_state.go:103`, `internal/chat/envelope_test.go:10`. Not all of these are boot-planted content, but they're the same underlying doc-drift — sweep them alongside the two primary fixes.

### The fix does not need new codegen — a live in-process registry already exists

`internal/chat/envelope.go` already maintains `registeredTypes map[string]bool` (populated via `InitCoreTypes(types []string)`, called once at startup from `envelopes.LoadCore`'s output) and a richer `EnvelopeRegistry() *envelopes.Registry` accessor. Since boot-dir content planting happens server-side at agent-launch time (not compile time), `sandbox_content_envelope.go`/`sandbox_content_claude.go` can query `chat.EnvelopeRegistry()` directly when building the planted string, rather than hardcoding a table or pointing at a stale path — **no new manifest-sync mechanism is needed, the data is already loaded in memory by the time a boot dir gets planted.**

## What to do

1. Rewrite `envelopeSchemaContent`'s generation to build its "Registered Envelope Types" table from `chat.EnvelopeRegistry()` at plant time, not a hardcoded Go string literal.
2. Rewrite `claudeMDBody`'s pointer text to describe the real, current manifest source (the external `go-envelopes` module) instead of the two nonexistent paths — or, better, also derive the type list dynamically here rather than pointing an agent at a path it would need to separately resolve.
3. Sweep the other doc-comment sites listed above referencing the same two nonexistent paths — update or remove, confirming each file's Phase 0 disposition first (e.g. `chat_loop_budget_soft_warning.go` may already be deleted by Phase 0 item 12 by the time this task runs).
4. Verify a freshly-launched CLI agent's planted boot content reflects the real, current type list (post this phase's `04`-`07` rebuilds) — not a stale snapshot from planning time.

## Done means

- No planted CLI boot content hardcodes a static envelope-type list or points at a nonexistent file path.
- A freshly-booted CLI agent's `.sandbox/envelope-schema.md` and CLAUDE.md addendum both accurately reflect the live, current type registry.
- The broader doc-comment sweep (the additional file list above) is completed, each confirmed still-live before editing.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.

## Work log

**Added an exported accessor, not just a bigger struct query.** The task suggested querying `chat.EnvelopeRegistry()` directly. In practice two catalogs exist in `internal/chat/envelope.go`: `registeredTypes` (the bare-name set `ValidateEnvelope` actually checks — core types plus every plugin type, whether or not the plugin shipped a schema) and `envelopeRegistry` (the `*envelopes.Registry`, used for schema compilation — core types always land here via `LoadCore`, but a plugin type registered via `Host.RegisterEnvelope` without a matching `RegisterPluginEnvelopeSchema` call never does). Using `EnvelopeRegistry()` alone would under-report schema-less plugin types. Added `chat.RegisteredEnvelopeTypeNames()` (sorted, read-locked) as the complete/authoritative name list, and use `EnvelopeRegistry().Lookup()` only to enrich each name with a "core"/"plugin" Source column where available. This matches the task's explicit "Touches" note that `registeredTypes` itself was fair game to touch.

**`internal/plugin/builtin/adapter-claude/plugin.go:168`** — the task's cited line number was stale (a repo-history artifact); at HEAD, line 168 sits inside `SyncProjectRoot` (unrelated: writes the project-root CLAUDE.md's "available agents" managed section) and is not a planting call site for envelope content — `PopulateSandbox` (the actual old planting hook, lines 141-152) is a documented no-op since Phase 4c.6. The real, live planting call site for `.sandbox/envelope-schema.md` is `internal/runtime/agent/bootdir_plant.go`'s `sandboxFiles()`, which is what got the `envelopeSchemaContent` → `envelopeSchemaContent()` call-site update. Per instruction, checked plugin.go anyway per the task's ask and did find one real, separate problem there worth fixing: its `PopulateSandbox` doc comment claimed the content builders were "exported as `runtimeagent.BuildCLAUDEMD / BuildAgentContext / BuildEnvelopeSchema`" — no `BuildEnvelopeSchema` export has ever existed (grepped the whole repo); corrected the comment to name the real unexported `envelopeSchemaContent` function and its real call site instead of a symbol that was never real.

**What changed:**
1. `internal/chat/envelope.go` — added `RegisteredEnvelopeTypeNames() []string` (sorted, mutex-protected read of `registeredTypes`).
2. `internal/runtime/agent/sandbox_content_envelope.go` — `envelopeSchemaContent` converted from a hardcoded `const` to a function. The static wire-format/field-reference/examples sections stay as literals (unrelated to the staleness this task targets — describe the envelope wire shape itself, which hasn't changed); the "Registered Envelope Types" table is now built at call time from `chat.RegisteredEnvelopeTypeNames()` + `chat.EnvelopeRegistry()` for the Source column. Verified via a throwaway preview test (deleted before commit) that with the real go-envelopes manifest loaded it renders exactly the 18 live core types, sorted, all marked "core", and none of the 5 Phase-0-cut types (`giphy-modal`/`kb-result`/`ticket-confirmation`/`ticket-form`/`resolution-capture`).
3. `internal/runtime/agent/bootdir_plant.go` — updated the one call site (`sandboxFiles`) from `envelopeSchemaContent` to `envelopeSchemaContent()`.
4. `internal/runtime/agent/sandbox_content_claude.go` — `claudeMDBody` converted from a `const` (pointing at the two nonexistent paths) to a function. Took the task's "better" option explicitly: rather than just pointing at the correct-but-unreachable external `go-envelopes` module path (unreachable because it lives in a different repo, not inside the agent's boot dir), the registered-type names are now listed inline in the CLAUDE.md addendum itself (`registeredTypesParagraph()`, sourced from the same `chat.RegisteredEnvelopeTypeNames()`), alongside a pointer to `.sandbox/envelope-schema.md` (already planted alongside CLAUDE.md, and itself now dynamic per #2) for full per-type field docs.
5. `internal/plugin/builtin/adapter-claude/plugin.go` — corrected the stale `PopulateSandbox` doc comment (see above); no behavior change (still a no-op).
6. Doc-comment sweep of the broader stale-path list from the task: `internal/mcp/elicitation.go`, `internal/service/chat_loop_terminated.go`, `internal/service/chat_loop_state.go`, `internal/chat/envelope_test.go`, `internal/selftools/self_tools.go` (2 sites — this is where `internal/mcp/self_tools.go` and `self_tools_describe_test.go` actually live now; both were renamed into the `internal/selftools` package by an earlier phase, the task's file paths were stale), `internal/selftools/self_tools_describe_test.go`. All corrected to either name the real source (external `github.com/hollis-labs/go-envelopes` module's `manifest/envelopes.yaml`/`manifest/schemas/*.schema.json`, confirmed present at `libs/go-envelopes/manifest/`) or, for the two LLM-facing tool-description strings in `self_tools.go`, drop the false path claim entirely rather than assert an external-repo path an agent has no way to open. `internal/service/chat_loop_budget_soft_warning.go` confirmed already deleted by Phase 0 item 12 (`git log` shows the deleting commit `093e8b8b`) — nothing to do there, exactly as the task's own caveat anticipated.
7. Added `internal/runtime/agent/sandbox_content_envelope_test.go` (new file): pins the nil-registry fallback (no panic, static sections still render, no phantom type rows) and, against a real `envelopes.LoadCore()`-populated registry, asserts every live core type appears in both `envelopeSchemaContent()` and `claudeMDBody()`, that none of the 5 Phase-0-cut types appear, and that neither of the two nonexistent paths appear anywhere in `claudeMDBody()`'s output.

**Scope boundary held:** left the "Interactive envelopes" (`kind: question|action|approval`, `type: "nanite"`) pattern in `envelopeSchemaContent`'s static header/examples untouched. Noticed while reading the code that `"nanite"` is not itself a registered type anywhere (`ValidateEnvelope` would reject it as `unregistered_type` if actually validated) — a possibly-separate staleness in that legacy pattern. Neither the task file nor `08-cards.md` calls this out, and it's outside this task's explicit scope (the type-list table + the two nonexistent-path pointers), so left as-is rather than scope-creeping; flagging here for visibility only.

**Baseline:** `go build ./cmd/nanite/` clean. `go vet ./...` reports the same 2 pre-existing findings in `internal/service/container.go` (`stopReaper`/`stopRuntimeReaper` context-leak warnings) that exist on this branch before any of this task's changes (confirmed via `git stash` + re-run) — unrelated to this task, not touched. `go test ./...` — all packages pass, zero FAIL.

## Review notes
**2026-08-21, fresh Reviewer (no shared context with the implementing worker): PASS.** All four claims in the Work Log independently verified: the stale call-site correction (`bootdir_plant.go`'s `sandboxFiles()` is the real planting site), the `RegisteredEnvelopeTypeNames()` vs `EnvelopeRegistry()` reasoning (traced through `internal/plugin/registrations.go` and the external `go-envelopes` module), the generated content itself (executed live against a real `envelopes.LoadCore()` registry — exactly 18 sorted core types, no stale/cut types), and the `BuildEnvelopeSchema` doc-comment bug fix (confirmed via `git log -S` that symbol never existed). The five files outside this task's originally-cited list are legitimate narrow doc-comment corrections, not scope creep. Fresh (non-cached) `go test -count=1 ./...` clean; `go vet ./...` shows only the 2 pre-existing `container.go` findings. Two non-blocking, pre-existing observations logged for awareness: a stale `"type": "nanite"` example in the planted boot-content header, and `08-cards.md`'s "known live bug" section now being stale (neither introduced by this task). No findings, no fix needed.
