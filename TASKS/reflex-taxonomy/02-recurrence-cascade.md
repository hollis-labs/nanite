# Recurrence cascade — system default → kind override → reflex override

**Phase:** 1 — Core Engine (`TASKS/reflex-taxonomy`)
**Status:** implemented
**Depends on:** `01-taxonomy-schema-foundation.md` (needs `reflex_action_kinds.default_recurrence_seconds` and `agent_reflexes.recurrence_override_seconds`)
**Touches:** `internal/agent/reflexes/engine.go` (`recentlyFired`, `EvaluateState`'s debounce call at line ~141), `internal/service/chat_reflex_dispatch.go` (`attemptReflexDispatch`, its header-comment design note 2), `internal/mcp/self_tools_dispatch.go` (`matchDispatchToAgentReflex`).

## Context

`docs/engineering/architecture/10-reflex-action-taxonomy.md`, "Facet 4 — Recurrence": cascading, least-to-most-specific — **system default → per-action-kind override → per-reflex override.** Today's system default (15 minutes) and `dispatch_to_agent`'s override (none) both exist, but as *code*, not data, and in two different places:

- `internal/agent/reflexes/engine.go:141` — `if recentlyFired(r, now, 15*time.Minute) { continue }`, a bare literal inside `EvaluateState`'s loop. Applies to all five non-`dispatch_to_agent` kinds (Phase 4 #09 already excludes `dispatch_to_agent` from this loop entirely — see the comment at `engine.go:101-128`).
- `internal/service/chat_reflex_dispatch.go`'s header comment (design note 2, lines ~26-42) documents `attemptReflexDispatch` deliberately **not** going through `EvaluateState`/`recentlyFired` at all, specifically to avoid the 15-minute debounce — a routing decision needs fresh re-evaluation every turn, not a cooldown built for a nudge. `internal/mcp/self_tools_dispatch.go`'s `matchDispatchToAgentReflex` (lines ~296-410) does the same, independently, for the same reason.

The design doc's exact framing: *"Once that's data instead of code, the only remaining reason for `dispatch_to_agent` to have its own invocation points is the import-cycle constraint below — not a recurrence-semantics difference."* This task makes it data. It does **not** collapse the two `dispatch_to_agent` call sites into one — that's explicitly out of scope; the doc is clear their separate `State`-building (from live, not-yet-persisted classification data) is a legitimate, permanent reason to keep them distinct. Only the cooldown-bypass *justification* goes away.

**Open, not this task's job to decide:** whether recurrence needs a third "fire once ever, for the life of the session" mode (e.g. for `task_complete_self_terminate`/`task_timeout`-style lifecycle kinds). Build the two-level duration-or-none cascade only; leave the third mode as a documented future extension if you notice a concrete need, don't build it speculatively.

## What to do

1. Add an exported constant in `internal/agent/reflexes` — e.g. `DefaultReflexCooldown = 15 * time.Minute` — replacing the bare literal at `engine.go:141`. This is the system-level default; it stays a Go constant, not a DB row (only kind- and reflex-level overrides need to be data — the design doc's cascade language doesn't require the system default itself to be editable).

2. Add an exported cascade-resolution function, e.g.:
   ```go
   func EffectiveCooldown(kindDefaultSeconds, reflexOverrideSeconds *int64) time.Duration
   ```
   Semantics: `reflexOverrideSeconds` non-nil wins (including `0` → `time.Duration(0)`, meaning "no cooldown"); else `kindDefaultSeconds` non-nil wins (same zero-means-none rule); else `DefaultReflexCooldown`. Unit-test all three levels explicitly, including the zero-override case (must NOT be treated as "unset" — a nil pointer is unset, a pointer to `0` is an explicit override).

3. Replace `engine.go`'s hardcoded `recentlyFired(r, now, 15*time.Minute)` call with one that resolves the reflex's actual kind (via `01`'s new lookup) and calls `EffectiveCooldown` with that kind's `default_recurrence_seconds` and the reflex row's own `recurrence_override_seconds`. Since kind metadata changes rarely, load `reflex_action_kinds` once (e.g. at `Engine` construction, alongside `NewEngine`) into a small in-memory map rather than querying per-reflex-per-turn — consistent with `docs/engineering/architecture/00-overview.md`'s guiding principle ("hot-reload is worth deliberately considering for anything that changes with meaningful frequency... fine to skip for things that rarely change"). If you want a manual refresh hook for when an operator edits `reflex_action_kinds` (not currently editable via any API — `01` deliberately didn't build CRUD for it), a simple `Engine.RefreshActionKindCache(ctx)` method is enough; don't over-build this.

4. Wire an actual (currently-always-zero, but real) cooldown check into `attemptReflexDispatch` and `matchDispatchToAgentReflex` using the same `EffectiveCooldown` function, so a future non-zero `recurrence_override_seconds` set on a specific `dispatch_to_agent` reflex row genuinely takes effect at both call sites, not just in theory. Concretely: before treating a fired `dispatch_to_agent` candidate as a winner, check `EffectiveCooldown(...)` against `LastFiredAt` the same way `recentlyFired` does — reuse that helper (export it if it isn't already, or factor a small shared function both `engine.go` and the two call sites can call) rather than reimplementing the time-window comparison a third time.

5. Update `chat_reflex_dispatch.go`'s header comment (design note 2) to reflect the corrected reasoning: the debounce-bypass is no longer "we skip the pipeline to dodge its behavior," it's "the kind-level default is seeded at `0`, and this call site now applies that same cascade explicitly." Keep the real remaining reason (import-cycle + live in-flight `State` construction `StateCollector` can't do) intact and don't remove or weaken that part of the comment — it's still true and still the reason two call sites exist.

6. Do not touch `fired_count`/`last_fired_at` bump *timing* or telemetry emission in this task — that's `06-unified-reflex-telemetry.md`. This task only changes *eligibility* (does a fired trigger get suppressed by cooldown), not what happens once something is allowed to fire.

## Done means

- `EffectiveCooldown` (or equivalently named/shaped function) is exported from `internal/agent/reflexes`, unit-tested for all three cascade levels including the explicit-zero-override case.
- `EvaluateState`'s debounce reads from the cascade; a fixture test confirms the five non-`dispatch_to_agent` kinds still debounce at the system default (15 min) unchanged, and a kind- or reflex-level override actually changes the observed debounce window in a test.
- `attemptReflexDispatch` and `matchDispatchToAgentReflex` both apply a real cooldown check via the same shared function. A regression test proves: setting a non-zero `recurrence_override_seconds` on a specific `dispatch_to_agent` reflex row suppresses re-firing within that window, at both call sites independently.
- No change in observed behavior for any existing reflex under default settings (every current row's `recurrence_override_seconds` is `NULL` post-migration, so nothing's cooldown actually changes) — confirm via existing test suite passing unmodified plus a new before/after comparison note in the Work Log.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.

## Work log

Implemented as specified, no deviations from the What-to-do instruction. Summary:

1. **`internal/agent/reflexes/recurrence.go`** (new file) — `DefaultReflexCooldown = 15 * time.Minute` (the system-level default, a Go constant, matching the task's explicit call not to make it a DB row); `EffectiveCooldown(kindDefaultSeconds, reflexOverrideSeconds *int64) time.Duration` implementing the cascade (reflex override wins if non-nil, including 0; else kind default wins if non-nil, including 0; else `DefaultReflexCooldown`); `RecentlyFired(r store.AgentReflex, now time.Time, window time.Duration) bool` — the exported form of `engine.go`'s former unexported `recentlyFired`, now the one shared time-window comparison all three call sites use (a non-positive window always returns false — zero cooldown means "no suppression").

2. **`internal/agent/reflexes/engine.go`** — added an `actionKinds map[string]*store.ReflexActionKind` cache (guarded by `actionKindsMu sync.RWMutex`) to `Engine`, loaded once in `NewEngine` via a new `RefreshActionKindCache(ctx) error` method (also exposed as a manual refresh hook per the task's suggestion) rather than queried per-reflex-per-turn. Added `ActionKindDefaultRecurrenceSeconds(actionKind string) *int64` as the cache's public accessor — used by `EvaluateState`'s own debounce check and by both `dispatch_to_agent` call sites (avoids a 3rd, 4th, Nth reimplementation of "look up the kind row"). `EvaluateState`'s debounce line changed from the hardcoded `recentlyFired(r, now, 15*time.Minute)` to `cooldown := EffectiveCooldown(e.ActionKindDefaultRecurrenceSeconds(r.ActionKind), r.RecurrenceOverrideSeconds); if RecentlyFired(r, now, cooldown) { continue }`. The old unexported `recentlyFired` function body moved to `recurrence.go` as exported `RecentlyFired`; a short pointer comment was left in its old spot in `engine.go` for anyone still looking for it there.

3. **`internal/store/reflex_taxonomy.go`** — added `ListReflexActionKinds(ctx) ([]ReflexActionKind, error)`, a small addition beyond the task's literal file list (not in "Touches") needed to load the cache in one round trip rather than six separate `GetReflexActionKind` calls at `Engine` construction. This is a pure read addition consistent with `01`'s own file and comment ("no CRUD surface... GetReflexActionKind is the minimum read primitive 02... needs" — this is the same category of read-only lookup support, not a CRUD surface).

4. **`internal/service/chat_reflex_dispatch.go`** — wired a real cooldown check into `attemptReflexDispatch`'s candidate loop, right before a fired candidate is allowed to become `winner`: `kindDefault := s.reflexEngine.ActionKindDefaultRecurrenceSeconds(store.ReflexActionDispatchToAgent); cooldown := reflexes.EffectiveCooldown(kindDefault, r.RecurrenceOverrideSeconds); if reflexes.RecentlyFired(r, now, cooldown) { continue }`. Rewrote the header comment's design note 2 per the task's exact instruction — reframed from "we skip the pipeline to dodge its 15-minute debounce" to "the cascade is now data, dispatch_to_agent's kind-level default is seeded at 0, and this call site applies that same cascade explicitly" — while keeping the still-true remaining reason (import-cycle constraint + live in-flight `State` construction `StateCollector` can't do) intact, moved into its own paragraph rather than removed or weakened.

5. **`internal/mcp/self_tools_dispatch.go`** — wired the equivalent check into `matchDispatchToAgentReflex`, resolving `dispatch_to_agent`'s kind-level default once per call via `st.Store.GetReflexActionKind(ctx, store.ReflexActionDispatchToAgent)` (a single query per `task_execute` invocation — not per-candidate — chosen over threading a `*reflexes.Engine` reference through `SelfToolsTransport`/`container.go` to keep this task's footprint to the file the task's own "Touches" list named, since this call site only ever evaluates one action kind and the extra per-turn query is negligible; `internal/mcp` already had no cycle-free way to reach `Engine`'s cache without adding new wiring surface that wasn't asked for here). Same `reflexes.EffectiveCooldown` + `reflexes.RecentlyFired` functions as the other two call sites — the loop `continue`s past a cooldown-suppressed candidate rather than returning, so the search correctly proceeds to the next fired candidate instead of giving up.

**Deviation from plan:** none against the What-to-do instruction. One implementation choice not explicitly dictated by the task file: added `ListReflexActionKinds` to the store package (a small read-only addition) rather than calling `GetReflexActionKind` six times inside `NewEngine`, to genuinely load the cache "once" (one query) as the task's wording asked for, not six queries at startup.

**Before/after behavior comparison (Done-means requirement):** before this task, every reflex's debounce window was a single hardcoded `15*time.Minute` literal inside `EvaluateState`, and `dispatch_to_agent`'s two call sites had zero cooldown concept at all (any fired trigger always won). After this task: the five non-`dispatch_to_agent` kinds still resolve to exactly 15 minutes today, because every one of their `reflex_action_kinds.default_recurrence_seconds` values is `NULL` (seeded by migration 124) and every existing `agent_reflexes.recurrence_override_seconds` is `NULL` post-migration (also migration 124's own backfill, confirmed in `01`'s Work Log and by `01`'s regression test `TestMigrate124SeedsTaxonomyLookupTables`) — `EffectiveCooldown(nil, nil)` returns `DefaultReflexCooldown` unchanged. `dispatch_to_agent`'s two call sites now have a real (not hardcoded-absent) cooldown check, but it resolves to `EffectiveCooldown(&0, nil) = 0`, and `RecentlyFired` treats a non-positive window as "never suppress" — so observed behavior for `dispatch_to_agent` is also unchanged (fires fresh every turn, exactly as before). This is confirmed both by the full existing test suite passing unmodified (see below) and by the two new tests in this task explicitly asserting a *non-zero* override is required to observe any suppression — with `recurrence_override_seconds` left at its default `NULL` for every real seeded reflex, no suppression is observed anywhere today.

**New tests added:**
- `internal/agent/reflexes/recurrence_test.go` — unit tests for `EffectiveCooldown` at all three cascade levels including both the kind-level and reflex-level explicit-zero-override cases (`TestEffectiveCooldown_*`); unit tests for `RecentlyFired`'s window boundary behavior; `EvaluateState`-level integration tests proving the cascade is actually wired into the generic pass's debounce (`TestEvaluateState_DefaultCooldown_StillDebouncesAt15Minutes`, `TestEvaluateState_ReflexLevelZeroOverride_BypassesCooldown`, `TestEvaluateState_ReflexLevelPositiveOverride_ShortensWindow`); cache-loading tests (`TestEngine_ActionKindDefaultRecurrenceSeconds_LoadsSeededDispatchToAgentZero`, `TestEngine_RefreshActionKindCache_PicksUpChange`).
- `internal/service/chat_reflex_dispatch_cooldown_test.go` — `TestAttemptReflexDispatch_RecurrenceOverride_SuppressesRefire`: a `dispatch_to_agent` reflex with `recurrence_override_seconds=3600` fires turn 1, is suppressed turn 2 (immediate re-run of the same trigger), fires again on turn 3 once `last_fired_at` is backdated past the window — asserted both via `Matched`/`AgentSlug` and via the fake `ToolService.Execute` call count.
- `internal/mcp/self_tools_dispatch_cooldown_test.go` — `TestMatchDispatchToAgentReflex_RecurrenceOverride_SuppressesRefire`: the same fire/suppress/resume pattern, independently, at the `matchDispatchToAgentReflex` call site.

**Verification run:** `go build ./cmd/nanite/` — ok. `go vet ./...` — only the two pre-existing, task-file-acknowledged `internal/service/container.go` findings (`stopReaper`/`stopRuntimeReaper` possible context leak), unchanged by this task, confirmed by diffing the vet output against a check before any of this task's edits. `go test ./...` — all packages pass, including the full pre-existing `internal/agent/reflexes`, `internal/service`, and `internal/mcp` suites unmodified (no existing test file was edited, only new ones added).

## Review notes

<!-- Reviewer fills in. -->
