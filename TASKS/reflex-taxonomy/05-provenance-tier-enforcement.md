# Provenance tier — per-kind declare allow-list, enforced at write time

**Phase:** 2 — Provenance & Telemetry (`TASKS/reflex-taxonomy`)
**Status:** implemented
**Depends on:** `01-taxonomy-schema-foundation.md` (needs `reflex_provenance_tiers`, `reflex_action_kinds`, `agent_reflexes.provenance_tier`). Parallel-safe with `02`/`03`/`04` — this task's file surface (`internal/api/reflexes.go`, a new join table, store insert/update paths) barely overlaps the engine-internal files those three touch; confirm no overlap on `internal/store/agent_reflexes.go`'s specific functions before running truly concurrently.
**Touches:** `internal/store/migrations/` (new migration for the allow-list join table), `internal/store/agent_reflexes.go` (`InsertAgentReflex`, `UpdateAgentReflex`, `ApprovePendingReflex`), `internal/api/reflexes.go` (`validateReflexDefinition`).

## Context

`docs/engineering/architecture/10-reflex-action-taxonomy.md`, "Facet 3 — Provenance / authority tier": *"used first for a security purpose: which tiers may even declare a given action kind."* Live tiers: `system` (seeded, may be marked required/opt-out-immune — the three `halt_session` seeds already are, via the existing `opt_out_allowed` column from `internal/store/migrations/115_agent_reflex_opt_out.sql`), `operator` (authored through the CRUD API), `plugin` (plugin-contributed — no live caller exists yet). `halt_session` and `dispatch_to_agent` are named as "the obvious candidates to restrict away from `plugin`-tier" — **not locked values**, the mechanism is decided, the specific policy isn't. The operator's explicit call on this (recorded in `TASKS/phase-4/10-reflex-architecture-review.md`'s Work Log, point 7): *"acceptable to leave open and let real usage over the next couple weeks inform the answer, rather than force a decision now."* Seed the doc's own named candidates as a sane, documented-as-adjustable default — do not treat this as final security policy requiring further sign-off before landing.

**`agent_proposed` is confirmed not a fourth live tier.** `store.ApprovePendingReflex` (`internal/store/agent_reflexes.go:555-577`) already collapses an approved pending reflex's `created_by` to `"operator:" + reviewedBy` on approval — "active in `agent_reflexes` ⇒ operator-approved" is already true today by construction. This task's job is to make sure the new `provenance_tier` column preserves that same collapse (→ `'operator'`), not to invent new approval-time behavior.

## What to do

1. **New table `reflex_action_kind_provenance_allow`**:
   ```sql
   CREATE TABLE reflex_action_kind_provenance_allow (
       kind_name TEXT NOT NULL REFERENCES reflex_action_kinds(name),
       tier_name TEXT NOT NULL REFERENCES reflex_provenance_tiers(name),
       PRIMARY KEY (kind_name, tier_name)
   );
   ```
   Presence of a `(kind, tier)` row means that tier may declare that action kind. Seed: every one of the 6 kinds × 3 tiers = 18 combinations, **except** `(halt_session, plugin)` and `(dispatch_to_agent, plugin)` — 16 allowed rows, 2 denied by omission. Document in this file's Work Log that this default is explicitly adjustable per the operator's call above, not a locked policy — a future operator decision to loosen or tighten it is a data change (`INSERT`/`DELETE` on this table), not a code change.

2. **Enforce at write time in `internal/api/reflexes.go`'s `validateReflexDefinition`** (and any other real insert path into `agent_reflexes` you find — grep for callers of `InsertAgentReflex` and `ApprovePendingReflex` first; as of this task's authoring, `internal/api/reflexes.go:108` hardcodes `CreatedBy: "operator"` for the one live CRUD create path, and no plugin-registration insert path exists). Given the reflex's `action_kind` and the resolved provenance tier for this write (see step 3), reject the write if no `reflex_action_kind_provenance_allow` row exists for that pair — clear, specific error message naming both the kind and the tier.

3. **Resolve "the caller's provenance tier"** the same way `created_by` is resolved today at each real call site — this is mostly already fixed by construction (the one live CRUD path is always `'operator'`; seeded rows in `seeds.go`/`loom_pilot_seeds.go` are always `'system'`). If you find a call site this reasoning doesn't cover, treat it as a genuine unknown per `EXECUTION-PROCESS.md`'s escalation rule (stop, document, don't guess) rather than inventing a tier-resolution heuristic.

4. **`ApprovePendingReflex`** (`internal/store/agent_reflexes.go:555-577`): the resulting live row's `provenance_tier` must be `'operator'` regardless of the pending reflex's original proposer, mirroring the existing `created_by` → `"operator:" + reviewedBy` collapse. Add this explicitly — don't let it fall through to whatever default the column happens to have.

5. Do **not** build an actual `plugin`-tier insert path in this task. No concrete plugin caller exists today (design doc: "no concrete plugin need exists today" for the plugin-registered-action-kind seam more broadly). This task's job is to make sure the *gate* is real and load-bearing wherever a write does happen — not to invent a caller for it to gate.

## Done means

- `reflex_action_kind_provenance_allow` seeded with exactly 16 allowed rows (18 combinations minus the two named exceptions), migration applies cleanly against a real backup DB copy.
- A test: attempting to declare a `halt_session` reflex at `plugin` tier is rejected with a clear error; the same attempt at `system` or `operator` tier succeeds.
- A test: every one of the real pre-existing seeded/operator-created `agent_reflexes` rows still validates successfully post-migration (no false-positive rejection of already-live data — spot check all 14+ real rows from a backup, not just a synthetic fixture).
- A test: `ApprovePendingReflex` on a pending reflex produces a live row with `provenance_tier='operator'`, independent of whatever tier (if any) the pending row itself carried.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.

## Work log

**2026-08-19 — implemented.**

**Migration:** `internal/store/migrations/125_reflex_action_kind_provenance_allow.sql` (latest existing at authoring time was `124_reflex_action_taxonomy.sql`, confirmed by listing the migrations directory directly). No table rebuild needed — `reflex_action_kind_provenance_allow` is a brand-new table with no pre-existing rows to migrate, so this is a plain transactional `CREATE TABLE` + `INSERT`, unlike 124's `NO TRANSACTION` rebuild pattern (documented in the migration's own header comment as a deliberate style choice). Schema exactly as specified in this task file: `(kind_name TEXT NOT NULL REFERENCES reflex_action_kinds(name), tier_name TEXT NOT NULL REFERENCES reflex_provenance_tiers(name), PRIMARY KEY (kind_name, tier_name))`. Seeded with exactly 16 rows — all 18 (kind, tier) combinations except `(halt_session, plugin)` and `(dispatch_to_agent, plugin)`, listed explicitly (not generated via a `CROSS JOIN`/`WHERE NOT` query) so the seed is trivially auditable against the design doc's two named candidates.

**Adjustability documented explicitly, per this task's own instruction:** the migration's header comment states this default is not locked security policy — "a future operator decision to loosen or tighten it is a plain `INSERT`/`DELETE` against this table, not a code or migration change." Repeating that note here in the Work Log per the task's explicit ask.

**Go-side lookup:** `internal/store/reflex_taxonomy.go` gained `func (s *Store) ActionKindAllowsProvenanceTier(ctx, kindName, tierName string) (bool, error)` — a plain existence check against the new table. An unrecognized `kindName`/`tierName` returns `false, nil` (a miss), not an error — the caller's own action-kind validation is responsible for rejecting an unknown kind name before this check is reached, which is exactly how `internal/api/reflexes.go`'s `validateReflexDefinition` uses it (see below).

**Enforcement — `internal/api/reflexes.go`'s `validateReflexDefinition`:** grepped for every real caller of `InsertAgentReflex` and `ApprovePendingReflex` first, per the task's instruction. Confirmed exactly what the task's own Context already stated: the only two real `InsertAgentReflex` callers besides the CRUD create handler are `internal/agent/reflexes/seeds.go` and `internal/agent/reflexes/loom_pilot_seeds.go` (both always `CreatedBy: "system"` → resolves to `system` tier, which is unrestricted for all 6 kinds — no possible violation, so no gate added there), and no plugin-registration insert path exists. `validateReflexDefinition` was converted from a free function to a method (`func (a *API) validateReflexDefinition(ctx context.Context, row store.AgentReflex) []string`) so it can call `a.Services.Store.ActionKindAllowsProvenanceTier`; all three real call sites updated (`handleCreateAgentReflex`, `handlePatchAgentReflex`, and the dry-run `handleValidateReflex` preview endpoint — deliberately included, since it shares the same shape-validation function and previewing what a write would do should reflect what the write would actually reject). The gate only runs when `action_kind` already passed its own switch validation, to avoid a spurious/duplicate "no allow-list row" error stacked on top of an already-reported "invalid action_kind" error.

**Provenance-tier resolution for the gate (task step 3):** `row.ProvenanceTier` is used directly when already set (non-empty) — this covers `handlePatchAgentReflex`, which carries the existing row's real persisted tier forward (`updated := *current`), and lets a synthetic test row exercise a tier with no live insert path (e.g. `"plugin"`) by setting it explicitly. When unset, it falls back to the *exact* same rule `store.InsertAgentReflex` already applies for its own default (added in task 01): `CreatedBy == "system"` → `"system"`, else → `"operator"`. This covers `handleCreateAgentReflex` (hardcoded `CreatedBy: "operator"` → resolves to `operator`, matching what `InsertAgentReflex` will actually persist a moment later) and `handleValidateReflex` (no `CreatedBy` set at all → falls to `"operator"`, the correct default for an operator-facing preview endpoint with no plugin caller). No new tier-resolution heuristic was invented — every real call site's resolution was traceable to construction as the task's Context predicted; no genuine unknown was hit, so no escalation was needed.

Error message shape (task's "clear, specific error message naming both the kind and the tier"): `provenance tier %q may not declare action_kind %q` (both values interpolated via `%q`).

**`ApprovePendingReflex`** (`internal/store/agent_reflexes.go`): the `INSERT INTO agent_reflexes` this function runs now explicitly includes `provenance_tier` in its column list, hardcoded to the literal `'operator'` — independent of `pending.ProposedBy` or `reviewedBy`, mirroring the existing `createdBy := "operator:" + reviewedBy` collapse immediately above it in the same function. Before this change the column would have silently fallen through to its own DB `DEFAULT 'operator'` (migration 124) and happened to produce the same value — made explicit per the task's instruction ("don't let it fall through to whatever default the column happens to have") so the behavior doesn't silently depend on that default staying `'operator'` forever.

**Not built, per task step 5:** no concrete `plugin`-tier insert path. The gate is exercised for `plugin` tier only via a synthetic test row (`ProvenanceTier: "plugin"` set directly), since no real caller produces that tier today.

**Tests added:**
- `internal/store/migration_125_reflex_action_kind_provenance_allow_test.go`:
  - `TestMigrate125SeedsProvenanceAllowList` — exact 16-row seed content (byte-for-byte against the design's 18-minus-2 set), round-trips all 18 (kind, tier) combinations through `ActionKindAllowsProvenanceTier` (16 true, 2 false), confirms an unrecognized kind is a plain `false, nil` miss, confirms goose has nothing pending and a second `s.migrate()` is a clean no-op.
  - `TestApprovePendingReflexCollapsesProvenanceTierToOperator` — three pending reflexes proposed under different `proposed_by` values (a plain agent id, `"system"`, and an already-`"operator:..."`-shaped string) all produce a live row with `provenance_tier = "operator"` after approval, verified both from `ApprovePendingReflex`'s own return value and an independent `GetAgentReflex` re-fetch.
  - `TestRealBackupAgentReflexesValidateAgainstProvenanceAllowList` — the real-backup spot-check the Done-means requires. Copies `~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726` into `t.TempDir()` (an absolute, per-test scratch path — the original backup file is never opened in place, only `os.Open`/`io.Copy`'d from), opens the *copy* via `store.New` (applies every pending migration including this task's 125), then checks every real `agent_reflexes` row's `(action_kind, provenance_tier)` pair against `ActionKindAllowsProvenanceTier`. All 14 real rows in this backup validate successfully (all pre-existing rows in this particular backup happen to be `provenance_tier='system'`, per task 01's own backfill finding — `system` tier is unrestricted for every kind, so this run doesn't exercise a real historical denial, but it does confirm the "no false-positive rejection of already-live data" requirement against the actual production-shaped dataset, not a synthetic fixture). Skips (not fails) if the backup file isn't present on the machine running the test.
- `internal/api/reflexes_test.go`: `TestValidateReflexDefinition_ProvenanceTierGate` — calls `a.validateReflexDefinition` directly with a `halt_session` row carrying `ProvenanceTier` set to `"plugin"`, `"system"`, and `"operator"` in turn. `"plugin"` produces an error string containing both `"plugin"` and `"halt_session"`; `"system"` and `"operator"` produce no `"provenance tier"`-prefixed error.

**Bug caught and fixed during verification, not left in:** the first draft of `TestRealBackupAgentReflexesValidateAgainstProvenanceAllowList` issued a second query (`ActionKindAllowsProvenanceTier`, itself a `QueryRowContext`) *while iterating* an outer, still-open `*sql.Rows` cursor over `agent_reflexes` on the same `*Store`. `sqlitekit.OpenSingle` gives every `Store` a single-connection pool, so the nested query blocked forever waiting for a connection the outer cursor couldn't release until fully drained — a real deadlock, not a slow query: `go test -run 'Reflex|Provenance|125|124' ./internal/store/...` hit Go's 10-minute test-suite alarm and panicked with the two goroutines' stacks pointing exactly at this. Fixed by fully draining and closing the outer `*sql.Rows` into a local slice first, then running the allow-list check against each collected row afterward — no more overlapping queries on the single connection. Confirmed fixed: the same test now completes in well under a second.

**Verification:**
- `go build ./cmd/nanite/`, `go vet ./...` (same two pre-existing, unrelated `internal/service/container.go` findings noted by task 01 — confirmed still unrelated to this task's files), and `go test ./...` (full suite, all packages) all pass.
- `git status --short` run immediately after the real-backup verification (per this task's own safety note and `EXECUTION-PROCESS.md`'s live-verification rule): only this task's own intended file changes plus pre-existing dirty state already present in this shared working tree from tasks 01-04 of this same batch (all still uncommitted) and the pre-existing `data/artifacts/...` directory/doc edits noted in task 01's own Work Log — nothing this task touched or authored outside its intended surface, and the real backup file itself was never opened in place (only copied via `os.Open`/`io.Copy` into `t.TempDir()`).
- No managed-agent file-write path (`AgentConfigService`/`writeManaged`) was exercised by this task's verification — consistent with this being a pure schema/validation task, per the task's own safety note.

## Review notes

<!-- Reviewer fills in. -->
