# Unified reflex telemetry — one trace record, one sink, full plugin-hook coverage

**Phase:** 2 — Provenance & Telemetry (`TASKS/reflex-taxonomy`)
**Status:** implemented
**Depends on:** `03-shared-decision-engine.md` (needs `Resolve()` as the single decision point to hook emission into), `04-halt-turn-synchronicity.md` (both touch `chat_generate.go`/`chat_reflexes.go`'s reflex call site — land after `04` to avoid re-churning the same lines twice)
**Touches:** `internal/agent/reflexes/` (`Resolve()` or the executor path, wherever emission gets centralized), `internal/service/chat_reflexes.go` (`evaluateAndInjectReflexes`'s `event_log` write), `internal/service/chat_reflex_dispatch.go` (`attemptReflexDispatch`'s existing `event_log`/`alternatives_considered` write), `internal/mcp/self_tools_dispatch.go` (`matchDispatchToAgentReflex`'s `playbook_match_log` write via `ReflexLogger`/`LogReflexMatch`), `internal/api/reflexes.go` or wherever reflex telemetry is read back for the operator UI (`GET /api/agents/{id}/reflexes`'s `fired_count`/`last_fired_at`).

## Context

`docs/engineering/architecture/10-reflex-action-taxonomy.md`, "Telemetry" section — three confirmed gaps, all from the same root cause as the call-site duplication `03` already fixed for *selection* logic: three code paths independently implementing "a reflex fired," never unified for *telemetry* either.

1. **Split sinks.** `dispatch_to_agent` firings from `attemptReflexDispatch` (chat-turn-loop path) write to `event_log` (`internal/service/chat_reflex_dispatch.go`, event_type `dispatch_to_agent`, category `reflex`). The same kind of event from `matchDispatchToAgentReflex` (the `task_execute` self-tool path, `internal/mcp/self_tools_dispatch.go:405`) instead writes to `playbook_match_log` via `ReflexLogger.LogReflexMatch` (`store.ReflexMatchLogEntry`, `internal/store/migrations/032_playbook_match_log.sql`) — a different table, different schema, no shared shape.
2. **`fired_count`/`last_fired_at` blind spot.** `Engine.EvaluateState` and `attemptReflexDispatch` both call `Store.BumpAgentReflexFired`; `matchDispatchToAgentReflex` never does. A `dispatch_to_agent` reflex firing mainly through the self-tool path shows `fired_count: 0`/`last_fired_at: null` in the operator UI's reflex list while actively routing turns.
3. **Plugin hooks blind to two of three paths.** `PluginHooks.EmitReflexFired`/`EmitReflexActionStaged` (`internal/agent/reflexes/engine.go:15-16`) are invoked exclusively from inside `Engine.EvaluateState`'s loop. Both `dispatch_to_agent` call sites bypass `EvaluateState` by design and call `Executor.Apply` directly — any plugin instrumented against those hooks is structurally blind to every `dispatch_to_agent` firing.

Design doc's resolution: *"this isn't separate work from the shared-decision-engine fix — once `reflexes.Resolve(...)` is the single place a firing gets decided, telemetry emission belongs there too, so the three gaps above close as a side effect of that fix rather than needing three separate patches later."*

**What the trace record should carry**, beyond what any single path captures today: `reflex_id`/`name`/`action_kind` (existing everywhere), the resolved **category** and **combining algorithm** applied (new — from `01`'s lookup), **why this one won** when a combining algorithm suppressed competitors (generalize `attemptReflexDispatch`'s existing `alternatives_considered` list — every candidate evaluated that turn, with whether it fired — to every kind under `first_applicable`/`deny_overrides`, not just `dispatch_to_agent`), **provenance tier** (new — from `05`), and **recurrence outcome** (did the trigger fire true but get suppressed by cooldown, and at which cascade level — system/kind/reflex — per `02`).

**Sink choice is explicitly an implementation call, not an architecture one** (design doc's own words) — pick `event_log` (already the majority path, general-purpose table) or promote `playbook_match_log` (currently narrowly named/shaped for `dispatch_to_agent` only). Recommendation: `event_log`, since it's already general-purpose and already the majority path — but document whichever you choose and why; this isn't a blocking decision to escalate.

## What to do

1. **Decide and document the unified sink** (event_log vs. widening playbook_match_log vs. a new dedicated table) — pick one, don't build two.

2. **Centralize emission.** Move the event_log/trace write, the `fired_count`/`last_fired_at` bump, and the `EmitReflexFired`/`EmitReflexActionStaged` plugin hook calls into `Resolve()` itself (or a thin wrapper every caller of `Resolve()` goes through immediately) — one implementation, not three call sites each independently patched to add whatever they're currently missing. Check what each of the three currently does before centralizing, so nothing regresses:
   - `Engine.EvaluateState` today: bumps `fired_count` + fires plugin hooks, but the actual `event_log` write happens one layer up in `chat_reflexes.go:evaluateAndInjectReflexes` (line 35), not inside the engine itself.
   - `attemptReflexDispatch` today: writes `event_log` (with `alternatives_considered`) + bumps `fired_count`, but never fires plugin hooks.
   - `matchDispatchToAgentReflex` today: writes only to `playbook_match_log`, does none of the other two.

   Land on one consistent set of side effects that happens exactly once per real firing, regardless of which of the three call sites produced it.

3. **Retire `matchDispatchToAgentReflex`'s dedicated `playbook_match_log` write** in favor of the unified sink — but first grep for any real reader of `playbook_match_log`/`ReflexMatchLogEntry`/`LogReflexMatch` (an operator UI, a CLI report, an admin export) before removing the write path. If a real reader exists, either keep writing both during a transition and note the follow-up to migrate the reader, or migrate the reader in this same task if it's small — don't silently starve a real consumer.

4. **Generalize `alternatives_considered`.** `attemptReflexDispatch`'s existing pattern (every `dispatch_to_agent` candidate evaluated that turn, fired or not) should become the standard shape emitted for every kind under `first_applicable`/`deny_overrides` (where "why did this one win over that one" is a meaningful question — `all_applicable` doesn't need it, everything that fired was applied). `03`'s `Resolve()` should already be returning enough per-candidate detail for this — use it, don't re-evaluate candidates a second time just to build the telemetry record.

5. **Add the new fields** — category, combining_algorithm, provenance_tier, recurrence outcome (fired-but-cooldown-suppressed + which cascade level set the effective cooldown) — to the emitted record.

## Done means

- One trigger of each of `inject_reminder`, `force_tool_choice`, `halt_session`, and `dispatch_to_agent` (via **both** the chat-turn-loop path and the `task_execute` self-tool path) produces one consistent-shape trace record in the one chosen sink, with `fired_count`/`last_fired_at` bumped and plugin hooks fired in every case — verified by a test exercising all four kinds across both dispatch-to-agent entry points, not just the paths that already worked before this task.
- **Verified fix for gap 2, live**: a `dispatch_to_agent` reflex fired exclusively via the `task_execute` self-tool path shows a non-zero `fired_count`/non-null `last_fired_at` via `GET /api/agents/{id}/reflexes` — reproduce the exact previously-broken scenario and confirm it's fixed, not just unit-tested.
- `matchDispatchToAgentReflex`'s dedicated `playbook_match_log` write is either removed (with confirmation no real reader was orphaned) or explicitly kept with documented reasoning — either outcome recorded in this file's Work Log.
- A test confirms `alternatives_considered`-style data is now present for a `force_tool_choice`/`halt_session` firing, not just `dispatch_to_agent`.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.

## Work log

**Sink choice: `event_log`.** Went with the design doc's own recommendation — already the majority path (`Engine.EvaluateState` via `chat_reflexes.go`, and `attemptReflexDispatch` both already used it), already general-purpose, and no new table/migration needed. Did not widen `playbook_match_log` (see below) or add a new dedicated table.

**Centralized emission: `reflexes.EmitFirings`, a thin wrapper, not a `Resolve()` signature change.** New file `internal/agent/reflexes/telemetry.go`. Chose the "thin wrapper every caller of `Resolve()` goes through immediately" option over folding emission into `Resolve()` itself, because `Resolve()`'s decision logic is independently, heavily unit-tested (`resolve_test.go`) and adding caller-specific emission concerns (a store handle, plugin hooks, per-caller extra metadata like the raw/sent-text audit pair) to its parameter list would entangle "deciding what fired" with "recording that it fired" — two things that don't need to be entangled. `EmitFirings(ctx, TraceStore, PluginHooks, AppliedActions, []CandidateOutcome, State, FiringContext, *slog.Logger)` does, for every fired action: one `event_log` write (`event_type` = the fired `action_kind`, `category` = `"reflex"`, `detail` = the reflex name), one `Store.BumpAgentReflexFired` call, and (when hooks non-nil) one `EmitReflexFired`/`EmitReflexActionStaged` pair. All three real callers now go through it immediately after their own `Resolve()` call:
- `Engine.EvaluateState` (`engine.go`) — replaced its inline per-action bump+hook loop; the event_log write itself *moved into the engine* from one layer up (`chat_reflexes.go:evaluateAndInjectReflexes`'s own per-action `LogEvent` loop, now removed — it would have double-written otherwise).
- `attemptReflexDispatch` (`chat_reflex_dispatch.go`) — replaced its own manual `event_log`/`alternatives_considered`/`BumpAgentReflexFired` block. This is also the fix for gap 3's `attemptReflexDispatch` half: it never fired plugin hooks before; now it does (new test assertion below).
- `matchDispatchToAgentReflex` (`self_tools_dispatch.go`) — replaced its dedicated `playbook_match_log` write entirely (see below). This is gap 2's fix (`BumpAgentReflexFired` now actually gets called from this path) and gap 3's other half (plugin hooks now fire here too, via a new `SelfToolsTransport.Plugins reflexes.PluginHooks` field wired in `main.go` from the same `pluginHost` instance `internal/service`'s container wires onto `Engine.Plugins`).

One correction made along the way: `matchDispatchToAgentReflex`'s `State{}` literal never set `SessionID` (not needed pre-task — no trigger predicate reads it). `EmitFirings` needs it to attribute the event_log row to the right session, so I added `SessionID: sessionID` to that literal — safe (confirmed `evaluator.go` never reads `State.SessionID`) and it's exactly the same field `attemptReflexDispatch`'s own `State{}` already populated.

**`CandidateOutcome` extended with a `Category` field** (`resolve.go`), populated in the same per-kind lookup loop that already resolves `CombiningAlgorithm`, from the same `*store.ReflexActionKind` row — no second lookup. `ProvenanceTier` did **not** need a `CandidateOutcome` field: it's already on the fired `store.AgentReflex` row itself (`AppliedActions.FiredReflexes[i].ProvenanceTier`, live since task 05), so `EmitFirings` reads it straight off that.

**`alternatives_considered` generalized** to every kind under `first_applicable`/`deny_overrides` (not just `dispatch_to_agent`), grouped by `ActionKind` (not `CombiningAlgorithm`) — deliberately, because a candidate whose trigger never fired never entered `Resolve()`'s `eligibleByKind` and so never got a `CombiningAlgorithm` assigned; grouping by algorithm would have silently dropped exactly the "fired or not" competitors the task asks to keep. One accepted simplification, noted in `telemetry.go`'s own doc comment: for `deny_overrides`, Facet 2's algorithm technically unions candidates *across every* `deny_overrides` kind present in a pass, but grouping by same-`ActionKind` is what's cheap to do without a second per-candidate kind lookup, and today `halt_session` is the only seeded `deny_overrides` kind, so same-kind and same-algorithm coincide in practice. Flagged as a documented scope-limitation, not silently wrong.

**Recurrence-outcome field: settled for less than the full ask.** The design doc/task ask for "which cascade level (system/kind/reflex) set the effective cooldown" for a cooldown-suppressed candidate. `CooldownFunc`'s signature (`func(store.AgentReflex) bool`) only returns a bool, and none of the three real `cooldownFn` closures expose which level of `EffectiveCooldown`'s cascade actually won — recovering it cheaply would mean either widening `CooldownFunc`'s return type (touching all three closures + `resolve_test.go`'s fixtures) or a second per-candidate kind lookup inside `Resolve()`'s hot per-candidate loop just for suppressed rows. Per the task's own "don't over-engineer this if the cascade level itself isn't cheaply recoverable" allowance: shipped `CandidateOutcome.CooldownSuppressed` (already existed, task 03) surfaced into `alternativeOutcome.CooldownSuppressed` in the trace record — "fired but suppressed" is captured; "at which cascade level" is not. Noted here as the deliberate gap, not silently dropped.

**`matchDispatchToAgentReflex`'s dedicated `playbook_match_log` write: retired.** Grepped the whole tree for `playbook_match_log`/`ReflexMatchLogEntry`/`LogReflexMatch`/`ReflexMatchLogger`/`ListReflexMatchLog` before touching anything: no API handler, no UI reference (`ui/src` grep empty), no CLI report, no admin export — the only callers were the writer itself, its own tests, and `ListReflexMatchLog` (itself only ever called from tests, "used in tests and future analytics queries" per its own doc comment). Confirmed no real reader existed to starve. Removed: the `ReflexMatchLogger` interface and the `LogReflexMatch` call site (`self_tools_dispatch.go`), the `ReflexLogger` field (`self_tools_transport.go`, replaced by the new `Plugins` field) and its `main.go` wiring, and the now-100%-dead `internal/store/reflex_log.go` (`LogReflexMatch`/`ReflexMatchLogEntry`/`ListReflexMatchLog`/`ReflexMatchLog`) plus its test file. **Left untouched, matching this project's own precedent** (`TASKS/adhoc/02`'s identical "leave the column/table in place, dead, unread" call): migrations `032_playbook_match_log.sql` and `094_playbook_match_log_raw_sent_text.sql`, and the `playbook_match_log` table itself — dropping a table via a new migration is a bigger, less-reversible move than this task asked for, and no clean need for it surfaced. The one real *data* value the retired write carried — the CW-20260816-0068 raw-vs-sent audit-trail pair, plus the matched-input excerpt — was not dropped: it's folded into the unified `event_log` row's metadata via `FiringContext.ExtraMetadata`, with the same "collapse to absent when raw==sent" bloat-avoidance rule the old writer used.

**Tests updated/added:**
- `internal/agent/reflexes/telemetry_test.go` (new) — `EmitFirings` end-to-end via `Engine.EvaluateState` for `inject_reminder`, `force_tool_choice`, and `halt_session`: unified record shape (`category`/`combining_algorithm`/`provenance_tier`/`spec`), `fired_count`/`last_fired_at` bump, plugin hooks. `TestEmitFirings_ForceToolChoice_AlternativesConsideredPresent` and the `halt_session` test's own assertion are the Done-means bullet "alternatives_considered-style data is now present for a force_tool_choice/halt_session firing."
- `internal/mcp/self_tools_dispatch_audit_test.go` (rewritten) — `TestCallExecuteTask_ReflexMatch_EmitsUnifiedTraceRecord` replaces the old `ReflexLogger`-capturing test; proves the `task_execute` self-tool path now lands one `event_log` row with the unified shape, and that raw==sent collapses to absent.
- `internal/mcp/self_tools_dispatch_cooldown_test.go` (updated) — **this is the gap-2 reproduction**: removed the test's own manual `BumpAgentReflexFired` call (its comment used to say the test "must do this step itself" because `matchDispatchToAgentReflex` never did) and replaced it with an assertion that `FiredCount`/`LastFiredAt` are already non-zero/non-empty immediately after the bare `matchDispatchToAgentReflex` call — the exact previously-broken scenario (fired exclusively via the self-tool path), now fixed.
- `internal/service/chat_reflex_dispatch_integration_test.go` (updated) — added a `recordingReflexPluginHooks` fake wired onto the engine, asserting `attemptReflexDispatch` now fires `EmitReflexFired`/`EmitReflexActionStaged` (gap 3's other half); updated the `event_log` metadata assertions to the new nested-`spec` shape (`agent_slug` moved from a flat top-level key to `spec.agent_slug`, since different action kinds carry different spec fields and a truly unified shape can't flatten all of them to the top level) and added `category`/`combining_algorithm`/`provenance_tier` assertions.
- `internal/service/chat_reflex_dispatch_generic_pass_leak_test.go` (updated) — its "no redundant `reflex_action`-typed row" checks were keyed to the now-retired literal `"reflex_action"` event_type string; switched to a shape-agnostic check by reflex name (`Detail`) instead, since event_type is now the fired action_kind, not a fixed literal. Confirmed the test's actual intent (no redundant generic-pass row for a `dispatch_to_agent` reflex) still holds and still asserts something real, not vacuous.

**Verification:** `go build ./cmd/nanite/`, `go vet ./...` (only the two pre-existing, task-file-acknowledged `container.go` findings), and `go test ./...` all pass — full sweep, not just the touched packages. Did not do a live check against the real deployed `nanite-api-service` — the task marks that explicitly as a bonus, not required, for the gap-2 verification; the unit test above reproduces the exact previously-broken scenario and confirms the fix.

**Deviation from the task file's `Touches` line:** no change was needed in `internal/api/reflexes.go` — `GET /api/agents/{id}/reflexes` already just returns the stored `AgentReflex` rows (`FiredCount`/`LastFiredAt` fields), so once all three paths call `BumpAgentReflexFired` (via `EmitFirings`), that endpoint reflects the fix with zero code changes there.

## Review notes

<!-- Reviewer fills in. -->
