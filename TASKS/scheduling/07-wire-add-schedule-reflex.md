# Wire the `add_schedule` reflex hook — closes CW-20260819-0006's loop

**Phase:** 2 — Observability & producers (`TASKS/scheduling`)
**Status:** implemented
**Depends on:** `02-store-adapter.md` (needs `InsertAgentSchedule` to accept the new `01`-added columns correctly — `job_type`/`job_payload`/`max_retries`/`on_fail`/`next_run` — so a reflex-inserted row is a genuinely valid, dispatchable schedule, not just schema-valid).
**Touches:** `internal/service/container.go` (assigns `reflexEngine.Executor.Schedule`, mirroring the existing `Executor.Halt` assignment at `container.go:921`).

## Context

`docs/engineering/architecture/12-scheduling.md`'s "Producers" section, item 3, and — more directly — the doc's own "Where Nanite already stood" section: `add_schedule` is legal in schema, seeded into `reflex_action_kinds` by migration 124, but **entirely dormant**. Confirmed directly against this checkout (not assumed):

- `internal/agent/reflexes/executor.go:19-22` declares `ScheduleHook` and `:34` declares the `Executor.Schedule` field.
- `internal/agent/reflexes/executor.go`'s `Apply` method already has a real, correct dispatch case: `case store.ReflexActionAddSchedule: if e.Schedule != nil { e.Schedule(ctx, reflex.AgentID, spec) } else { /* staged only, logged */ }` — the *call site* has always been correct. Only the hook assignment is missing.
- `internal/service/container.go:921` shows the exact pattern to mirror: `reflexEngine.Executor.Halt = func(ctx, sessionID, reason, evidence) error { ... }`, assigned right after `reflexEngine := reflexes.NewEngine(...)` is constructed and plugin hooks are wired.

**This is the concrete fix that closes `CW-20260819-0006`'s loop** — the design doc's own finding that `add_schedule` "doesn't duplicate a scheduler and isn't a separate concern needing reconciliation — it was designed as a thin producer into [`agent_schedules`], and simply never got its wire connected."

## What to do

1. In `internal/service/container.go`, immediately alongside the existing `reflexEngine.Executor.Halt = ...` assignment (`:921`), add `reflexEngine.Executor.Schedule = func(ctx context.Context, agentID string, spec map[string]interface{}) error { ... }`.
2. The hook's body: translate `spec` (the reflex's `action_spec` JSON, already unmarshaled by `Executor.Apply` before the hook is called — `internal/agent/reflexes/executor.go`'s `Apply` method, `spec := map[string]interface{}{}`) into a `store.AgentSchedule` and call `cfg.Store.InsertAgentSchedule(ctx, ...)`. Decide the exact `spec` field names a reflex author would write in `action_spec` JSON to describe a schedule (e.g. `{"name": "...", "schedule_kind": "cron", "schedule_spec": "0 9 * * *", "job_type": "durable_agent_wake", "body": "...", "max_retries": 3, "on_fail": "retry"}`) — document your chosen field names, since nothing existing pins this down (no reflex today declares `action_kind='add_schedule'`, so there's no existing `action_spec` shape to match against).
3. Compute `next_run` at insert time (reuse `02`'s `gosched.NextRun`-based helper for `cron`-kind specs, or the equivalent one-shot due-time parsing) — an inserted row with no `next_run` would never fire under the new CAS-claim engine, silently defeating the whole point of wiring this hook.
4. Apply reasonable defaults for any `spec` field the reflex author omits (e.g. `max_retries`/`on_fail` falling back to `01`'s column defaults if not explicitly overridden in `spec`) — don't require every reflex-authored schedule to specify every retry-policy field explicitly.
5. Log/event a failure the same way the existing `Executor.Schedule hook failed` warning already does (`internal/agent/reflexes/executor.go`'s existing `e.Logger.Warn("reflex schedule hook failed", ...)` on hook error) — no new error-handling shape needed here, the call site already handles a hook error correctly; this task only needs to make the hook itself correct.

## Done means

- A regression test proves: a reflex firing with `action_kind='add_schedule'` and a well-formed `action_spec` produces a real `agent_schedules` row with a correctly-computed `next_run`, discoverable by `02`'s `ListDueSchedules` on the next tick.
- A regression test proves a malformed/incomplete `action_spec` fails cleanly (hook returns an error, logged per the existing warning path) rather than inserting a broken row.
- A live dogfeed: fire a real `add_schedule` reflex against a scratch instance (a reflex with a near-future `cron`/`one_shot` target), confirm the resulting `agent_schedules` row is picked up and actually fires through the engine built by `01`-`05`.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass.
- Your `action_spec` field-name shape (step 2) is documented in this file's Work Log.

## Work log

**Phase 1 (`01`-`05`) re-confirmed merged/reviewed clean before starting; stale line numbers corrected.** `internal/service/container.go`'s `reflexEngine.Executor.Halt = ...` assignment is at line 944 in this checkout (task file's own cited `:921` and Context's `:921` are both stale — `05` added `Engine *gosched.Engine` and its construction/doc-comment block earlier in the same file, shifting everything after it). `reflexEngine.Executor.Schedule` is now assigned immediately after `Executor.Halt`, same pattern, same block.

**Files touched:**
- `internal/service/container.go` — added `reflexEngine.Executor.Schedule = NewReflexScheduleHook(cfg.Store)` right after the existing `Executor.Halt` assignment (8 lines total, comment + one statement). No other change to this file.
- `internal/service/reflex_schedule_hook.go` (new) — `NewReflexScheduleHook(st *store.Store) reflexes.ScheduleHook` (the hook constructor wired above) and `buildReflexAgentSchedule(agentID string, spec map[string]interface{}, now time.Time) (store.AgentSchedule, error)` (the pure, DB-free `action_spec` → `store.AgentSchedule` translation, kept as a separate function specifically so it's testable without a real store), plus three small unexported helpers (`specString`, `specInt64`, `specJobPayload`) for reading typed fields out of the unmarshaled `map[string]interface{}`.
- `internal/service/reflex_schedule_hook_test.go` (new) — regression tests (below).

**Package-level naming note:** `NewReflexScheduleHook` is exported (capital N), not container.go-local, specifically so a same-module, cross-package end-to-end verification (below, "live dogfeed") could construct the *exact* production hook together with the real `go-scheduler` engine — `internal/scheduler` (which needs to be on the other side of that wiring) already imports `internal/service` for `RunnerAdapter`'s narrow dependency types (`service.ToolResult`, `service.DurableAgentWakeRequest`, etc.), so `internal/service` cannot import `internal/scheduler` back without a cycle. Exporting the hook constructor was the smallest change that let a genuinely real (not hand-simulated) `gosched.Engine` fire a genuinely real (not hand-built) hook-inserted row in the same process, without restructuring either package.

**`action_spec` field-name shape (this task's own design call — nothing existing pinned this down, no reflex anywhere declared `action_kind='add_schedule'` before this task):**

| field | type | required? | notes |
|---|---|---|---|
| `name` | string | required | → `Row.Name` |
| `schedule_kind` | string | required | `"cron"` \| `"one_shot"` (`store.ScheduleKindCron`/`ScheduleKindOneShot` — the two live values per migration 127's narrowed CHECK) |
| `schedule_spec` | string | required iff `schedule_kind="cron"` | standard 5-field cron expression, e.g. `"0 9 * * *"`. Optional/ignored for `one_shot` (a `one_shot` row has no independent target-time encoding today, per `backfillScheduleNextRun`'s own confirmed finding) |
| `body` | string | required | → `Row.Body`, the agent-facing prompt/instruction text. For `job_type=durable_agent_wake` (the default), `internal/scheduler`'s `StoreAdapter` forwards this straight through as `WakePayload.Prompt` |
| `job_type` | string | optional, default `durable_agent_wake` | must be one of the four seeded job types if given explicitly (`store.ScheduleJobType*`) |
| `job_payload` | object or JSON string | optional | only meaningful for job types other than `durable_agent_wake` — `internal/scheduler`'s `RunnerAdapter` decodes this directly (`02`'s confirmed "pass-through-as-is, not parse-and-repack" contract) into that job type's own `*Payload` struct (`AgentWorkflowRunPayload`/`CommandRunPayload`/`ReflexDispatchPayload`, `internal/scheduler/runner_adapter.go`), so the reflex author is responsible for matching that job type's own field names. Given as a nested JSON object it's re-marshaled to a string; given as an already-encoded JSON string it's validated (`json.Valid`) and used as-is |
| `max_retries` | number | optional | omitted (or `<=0`) → `InsertAgentSchedule`'s own column default (3) applies — deliberately left unset rather than hard-coded a second time |
| `on_fail` | string | optional | one of `retry`\|`disable`\|`notify` if given; omitted → `InsertAgentSchedule`'s own `"retry"` default |
| `priority` | number | optional, default 0 | |
| `session_id` | string | optional | empty means "applies to all sessions of this agent," per `AgentSchedule.SessionID`'s own doc comment |
| `expires_at` | string | optional, RFC3339 | |

Example: `{"name":"nightly-audit","schedule_kind":"cron","schedule_spec":"0 3 * * *","body":"run the nightly audit"}`.

**`next_run` computation:** via the exported `store.ComputeAgentScheduleNextRun(kind, spec, now)` helper (`01`/`05`) — no second hand-rolled cron parse. This was flagged explicitly in the dispatch brief (`TASKS/ESCALATIONS.md`'s 2026-08-20 note on task `05` already committing this exact anti-pattern once) and confirmed correct against the real precedent: `managed_durable_configs.go`'s `syncManagedDurableAgentSchedule` calls the identical helper the identical way for its own new-row case.

**ID generation — design call, documented since nothing pinned it down:** each firing mints a fresh `"reflex-schedule-" + uuid.New().String()` row rather than deriving a deterministic ID from `agentID+name` (the pattern `managed_durable_configs.go`'s `managedDurableAgentScheduleID` uses for its own idempotent-boot-resync producer). Rejected the deterministic-ID route for this hook specifically: replicating that producer's own "preserve `fired_count`/`last_fired_at`/`next_run`/`max_retries`/`on_fail`/`job_type`/`job_payload` across every re-insert" logic (a real bug `05`'s own Work Log found and fixed for the boot-resync case) would be solving a problem this call site doesn't have — a reflex fires occasionally (governed by the existing 15-minute cooldown/debounce, `internal/agent/reflexes/recurrence.go`), not on every process boot, so "reflex fires again → a fresh schedule row" is the simpler and more defensible default matching the doc's own "inserts a row" language (not "upserts").

**Validation design — deliberately shallow where `store.ComputeAgentScheduleNextRun` already has documented, correct fallback behavior:** the hook rejects (returns an error, no insert attempted) for: missing `name`/`body`, `schedule_kind` outside `{cron,one_shot}`, `schedule_kind="cron"` with an empty `schedule_spec`, an unrecognized `job_type`/`on_fail`, or invalid `job_payload` JSON. It does *not* independently re-validate cron syntax for a non-empty `schedule_spec` — a malformed-but-non-empty cron expression falls through to `ComputeAgentScheduleNextRun`'s own documented "due now" fallback (matching `backfillScheduleNextRun`/`managed_durable_configs.go`'s established precedent for the exact same helper), rather than a second cron-parse call here duplicating what that helper already owns.

**Regression tests (`internal/service/reflex_schedule_hook_test.go`):**
- `TestBuildReflexAgentSchedule_CronSpec_ComputesNextRun` / `_OneShot_NextRunIsNow` / `_Defaults` / `_ExplicitOverrides` / `_JobPayloadAsPreEncodedString` — pure, DB-free translation tests covering the field mapping and default-deferral behavior above.
- `TestBuildReflexAgentSchedule_MalformedSpecs` — table-driven, 8 cases (missing `name`, missing `schedule_kind`, invalid `schedule_kind` — including the retired `every_n_ticks` value, `cron` with no `schedule_spec`, missing `body`, invalid `job_type`, invalid `on_fail`, invalid `job_payload` JSON) — each asserts `buildReflexAgentSchedule` returns a non-nil error.
- `TestNewReflexScheduleHook_InsertsDiscoverableRow` — real `*store.Store` (real agent profile row, satisfying the schema's `agent_id REFERENCES agent_profiles(id)` FK under this codebase's `PRAGMA foreign_keys=1`): calls the real hook, asserts exactly one row via `ListAgentSchedules`, asserts `next_run` is non-empty, and asserts the row is present in `st.ListDueAgentSchedules` (the low-level query `02`'s `scheduler.StoreAdapter.ListDueSchedules` is built directly on top of) once `next_run` has elapsed — the literal "Done means" bullet.
- `TestExecutorApply_AddSchedule_FiresRealHook` — same, but fired through the real `reflexes.Executor.Apply` call site (not the hook function directly), proving the wiring end to end through the actual dispatch surface every `add_schedule` reflex fires through in production.
- `TestExecutorApply_AddSchedule_MalformedSpec_NoRowInserted` — asserts the hook itself returns an error for a malformed spec directly, and separately that `Executor.Apply` (which swallows/logs a hook error rather than propagating it, matching `Halt`/`SendMessage`'s existing behavior in `executor.go` — confirmed by reading it, not assumed) leaves zero `agent_schedules` rows behind. Confirmed the existing `e.Logger.Warn("reflex schedule hook failed", ...)` path fires (visible in `go test -v` output: `WARN reflex schedule hook failed reflex=malformed-schedule err="reflex add_schedule: action_spec.name is required"`).

**Live dogfeed (per Done-means, run once, not committed):** a throwaway `cmd/dogfeed07/main.go` package (deleted after use, `git status --short` confirmed clean afterward — same convention `02`'s Work Log documents for its own "Real-data verification" section) that: opened a scratch `*store.Store` at an isolated `os.MkdirTemp` path (never the real tracked workspace/DB); created a synthetic `agent_profiles` row directly via `st.CreateAgent` (never a real `.nanite/agents/*.md` file, per this project's own two logged incidents about relative `source_ref` writes); built a real `reflexes.Executor{Schedule: service.NewReflexScheduleHook(st)}` and fired a real `add_schedule` reflex (`schedule_kind=one_shot`, `job_type=command_run` — chosen to avoid also needing a `durable_agent_instances` row, which `durable_agent_wake`'s dispatch path additionally requires) through `Executor.Apply`; then built the *real* Phase 1 engine — `scheduler.StoreAdapter` + `scheduler.RunnerAdapter` (with only `Commands` wired, to a small fake `CommandExecutor` capturing calls — the one narrow interface boundary faked, everything else the identical production types `cmd/nanite/main.go` wires into `container.Engine`) + `scheduler.RetryingRunner` — and called `gosched.Engine.TickNow(ctx)` (a real, synchronous single tick the library's own doc comment calls "primarily useful in tests"). Result: the reflex fire produced exactly one `agent_schedules` row with a non-empty `next_run`; the engine tick reported `dispatches=1, worker_errors=0`; the fake `CommandExecutor` was called exactly once with the `agent_id`/`command`/`args` from the reflex's own `job_payload`; and the `one_shot` schedule's status flipped to `expired` (the real `StoreAdapter.DisableSchedule` mapping) after firing — full output captured above this Work Log entry's authoring session, confirming the resulting row is genuinely picked up and dispatched by the real Phase 1 engine, not just schema-valid.

**Verification:** `go build ./cmd/nanite/` clean. `go vet ./...` shows the same two pre-existing `internal/service/container.go` warnings (`stopReaper`/`stopRuntimeReaper` "not used on all paths") task `02`'s Work Log already flagged as predating and being unrelated to this batch — confirmed via `git diff --stat` that this task's own diff touches only 8 added lines near the `Executor.Halt` block, nowhere near those functions. `go test ./...` passes clean across every package, including the new tests above.

## Review notes

**FAIL (2026-08-20, fresh Reviewer, Phase 2 section review covering 06-09) — one real, reproduced bug.** Wiring itself confirmed correct (`Executor.Schedule` assigned unconditionally in `NewContainer`, right after `Executor.Halt`); malformed-spec rejection confirmed correct for every field this hook *does* validate; the fresh-UUID-per-firing ID strategy confirmed reasonable given the real 15-minute reflex cooldown backstop.

**The bug**: `buildReflexAgentSchedule` never validates cron syntax for a non-empty `schedule_spec`, relying on `store.ComputeAgentScheduleNextRun`'s documented "fall back to due-now" behavior. The reviewer proved this is worse than the documented "one off-schedule immediate fire" worst case by constructing the real production pipeline (`internal/scheduler.StoreAdapter` + a real `gosched.Engine`) and inserting a `schedule_kind=cron` row with a syntactically invalid `schedule_spec`: after 3 ticks, `Dispatches=0`, `WorkerErrors=3`, `next_run` never advances, the row is never claimed and never fires — silently, forever, with only a coarse aggregate counter as any operator-visible signal. Root cause: `go-scheduler`'s own `tick()` (`libs/go-scheduler/engine.go:130-140`) computes `NextRun(sch.CronExpr, now)` *before* the CAS claim and skips the row entirely on a parse error — it never reaches `ClaimAndUpdateScheduleRun`. This directly contradicts this task's own Done-means bullet ("a malformed/incomplete `action_spec` fails cleanly... rather than inserting a broken row") — none of the 8 existing malformed-spec test cases used a syntactically-invalid-but-non-empty `schedule_spec`, so this gap shipped untested.

**Fix required, scoped and precedented**: add a `cron.ParseStandard` pre-check to `buildReflexAgentSchedule` for `schedule_kind=cron`, mirroring what tasks `08` (`schedule_create`) and `09` (`/api/schedules`) already do for exactly this reason — both validate cron syntax before insert rather than trusting `ComputeAgentScheduleNextRun`'s fallback for front-door input. Plus a regression test covering a malformed-but-non-empty `schedule_spec`. See `TASKS/ESCALATIONS.md`'s entry for the fix dispatch and re-review status.
