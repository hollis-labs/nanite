# `StepKindFlex` — the one real engine-level schema change the design doc's own ledger names

**Phase:** 1 — Schema & storage foundation (`TASKS/teams`)
**Status:** implemented
**Depends on:** none directly (independent migration/const addition; parallel-safe with `01`/`02`/`04`/`05` — coordinate migration numbering only, see `01`'s numbering note)
**Touches:** `internal/agentworkflow/types.go` (new `StepKindFlex` constant), `internal/store/migrations/` (new migration — `workflow_run_steps.kind` CHECK widening, table-rebuild), `internal/service/workflow_engine.go` (confirm/adjust the step-kind dispatch switch does not error on an unrecognized-but-now-valid `flex` kind before task `06` gives it real behavior).

## Context

`docs/engineering/architecture/15-teams.md`'s "New mechanism vs. reuse" ledger names this explicitly: *"Fluid coordination stretches | **New** step kind (`StepKindFlex`) — real engine-level work."* But the design doc's own text never names the specific schema obstacle this creates — found directly by this planning session's research, not assumed:

**`internal/agentworkflow/types.go:9-18`** defines `StepKind` as a `string` type with exactly three constants today: `StepKindLLM = "llm"`, `StepKindTool = "tool"`, `StepKindGate = "gate"`. Adding a fourth Go constant is trivial — but **`internal/store/migrations/051_agent_workflows.sql`** hardcodes a matching DB-level constraint: `workflow_run_steps.kind` has `CHECK (kind IN ('llm','tool','gate'))`. SQLite cannot `ALTER` a `CHECK` constraint in place — this needs the same table-rebuild pattern already used elsewhere in this codebase (`internal/store/migrations/119_agent_reflex_dispatch_to_agent.sql` is the cited precedent for exactly this kind of CHECK-widening migration in the scheduling batch, `TASKS/scheduling/01-schema-schedule-kind-collapse-and-retry-columns.md`). Without this migration, any code inserting a `workflow_run_steps` row with `kind='flex'` (task `06`'s job) fails at the DB layer regardless of how correct the Go-level engine logic is.

**`StepDefinition`** (`types.go:221-241`) is `{ID string; Kind StepKind; DependsOn []string; Config map[string]any; Verify *VerifySpec}` — `Config` is already untyped `map[string]any`, so a flex step's `{active_slots, exit_trigger}` config (per the design doc's illustrative compiled-`WorkflowDefinition` example) needs **no struct change** here, only the new constant and the schema fix above.

**Secondary, judgment-call item:** `durable_agent_instances.launch_source_type` (migrations `083`/`084`) has `CHECK (... IN ('api_chat','cli_harness','boot_profile','durable_advisor','process_tick','task_template_run'))` — no `team_run` value. Whether a TeamRun-launched durable slot member needs a distinct `launch_source_type` tag, or can correctly use an existing generic value (`process_tick`/`task_template_run` are both plausible fits), is genuinely task `08`'s call once it builds the real launch path — **this task should confirm with `08` (or make and document its own default) rather than add an unused CHECK value speculatively.**

## What to do

1. Add `StepKindFlex StepKind = "flex"` to `internal/agentworkflow/types.go`, alongside the existing three constants, with a doc comment: a flex step is a phase boundary where N active Team Slot members self-organize, not a single prescribed executor action — cite `15-teams.md`'s own framing verbatim ("A flex step is a phase boundary, not a single execution action").

2. New migration (confirm the actual next-available number at dispatch time — see `01`'s numbering note): table-rebuild `workflow_run_steps` (same rename-recreate-copy pattern as `119_agent_reflex_dispatch_to_agent.sql` — `PRAGMA foreign_keys = OFF` / `BEGIN` / `CREATE ... _new` / `INSERT ... SELECT` / `DROP` / `RENAME` / recreate indexes / `END` / `PRAGMA foreign_keys = ON`), widening the CHECK to `IN ('llm','tool','gate','flex')`. Preserve every existing column and the `UNIQUE (workflow_run_id, step_id)` index exactly.

3. Confirm no other CHECK constraint or Go-side exhaustive `switch`/`case` over step-kind string literals needs a matching update — grep `"llm"`, `"tool"`, `"gate"` as string literals in `internal/agentworkflow/` and `internal/service/workflow_engine.go`. The engine's real per-kind execution switch (whichever function that turns out to be — confirm the exact name) will need a `case agentworkflow.StepKindFlex` eventually, but **this task's job is only to make that switch not silently fall through or panic on an unrecognized-but-now-schema-valid kind** — leave the actual flex-step execution body to task `06`. Document exactly what placeholder behavior you leave in place (e.g. a clear "not yet implemented" error is fine and expected here; a silent no-op is not) so `03` landing alone doesn't quietly break anything for existing `llm`/`tool`/`gate` steps or leave a `flex` step hanging with no diagnostic.

4. Resolve the `launch_source_type` question above: either add `'team_run'` to that CHECK in the same migration (if you judge it's clearly needed and low-risk to add now) or explicitly punt to task `08` with a one-line note here documenting which you chose and why.

## Done means

- Migration applies cleanly against a real backup copy of the database, not just an empty fixture.
- Regression test: inserting a `workflow_run_steps` row with `kind='flex'` succeeds where it previously violated the CHECK; existing `llm`/`tool`/`gate` inserts are unaffected.
- `go build ./cmd/nanite/`, `go vet ./...`, `go test ./...` pass — including a test proving a `flex`-kind step reaching the engine's dispatch switch produces a clear, non-crashing "not yet implemented" result rather than an unhandled-case failure, pending task `06`.

## Work log

**Migration number used: `130`.** Re-verified immediately before writing the file (`ls internal/store/migrations/ | sort -t_ -k1 -n | tail -10`, done twice — once against the shared checkout while reading the batch context, once against this worktree's own copy of the tree right before creating the file): latest on disk in this worktree was `127_schedule_runs_and_retry_policy.sql`, `128`/`129`/`131` were not present (this worktree was branched before tasks `01`/`02`/`05` landed their migrations), and `130` was free. Used `130` as pre-assigned.

**Worktree note (environment, not scope):** this worktree (`.claude/worktrees/agent-ac9d230d8c4288890`) was branched from `main` before `TASKS/teams/` and `docs/engineering/architecture/15-teams.md` were committed there, so neither existed in the worktree's working tree at dispatch time (they were only present, uncommitted, in the orchestrator's own shared checkout). Read the design doc, `GLOSSARY.md`, and `EXECUTION-PROCESS.md` from the shared-checkout absolute paths (Read tool has no worktree restriction; only Edit/Write do). Recreated this task file (`TASKS/teams/03-stepkindflex-schema.md`) inside the worktree with its original content before editing it, so the Work Log below lands in the same file the orchestrator dispatched — the recreated content was a byte-for-byte copy of the original before any of these edits.

**1. `StepKindFlex` constant** — added to `internal/agentworkflow/types.go` alongside the existing three, with a doc comment citing `15-teams.md`'s "A flex step is a phase boundary, not a single execution action" verbatim, per the task instruction.

**2. Migration `130_workflow_run_steps_flex_kind.sql`** — table-rebuild of `workflow_run_steps` following `119_agent_reflex_dispatch_to_agent.sql`'s exact rename-recreate-copy pattern (`PRAGMA foreign_keys = OFF` / `-- +goose NO TRANSACTION` / `BEGIN` / `CREATE ... _new` / `INSERT ... SELECT` / `DROP` / `RENAME` / recreate both indexes / `END` / `PRAGMA foreign_keys = ON`), widening `kind`'s CHECK to `IN ('llm','tool','gate','flex')`. Confirmed the live column shape directly against a real backup copy before writing the CREATE TABLE (see testing section below) rather than reconstructing it from the migration ledger alone: `051_agent_workflows.sql`'s original 13 columns plus `gate_input` (`091_workflow_gate_input.sql`, appended via `ALTER TABLE ADD COLUMN`, so it's physically last in the real table even though `workflowRunStepColumns` in `workflow_runs.go` lists it earlier — doesn't matter functionally since every query/insert in that file names columns explicitly, but the migration's CREATE TABLE matches the real physical layout for clarity). Grepped the full `migrations/` directory for `workflow_run_steps` — confirmed only `051` (create) and `091` (add column) ever touch its shape; `100_cut_dead_storage_and_config.sql` only mentions it in a comment. Confirmed no FK anywhere in the ledger references `workflow_run_steps`, so the `PRAGMA foreign_keys = OFF` window has no referential-integrity exposure to reason about (unlike `119`'s `agent_reflex_opt_outs` case). Both indexes (`idx_workflow_run_steps_run_step` UNIQUE on `(workflow_run_id, step_id)`, `idx_workflow_run_steps_status` on `(workflow_run_id, status)`) recreated identically and covered by a dedicated regression test (below).

**Deviation caught by testing, fixed:** my first draft of the migration omitted `-- +goose NO TRANSACTION` on the Up section. `119`'s Up section has it (required because SQLite can't run `PRAGMA foreign_keys` inside a transaction, and goose wraps every migration in its own transaction by default); I missed it on the first pass and `go test ./internal/service/... -run WorkflowEngine` immediately caught it with `SQL logic error: cannot start a transaction within a transaction (1)` at `goose up: partial migration error (type:sql,version:130)`. Added the directive and re-ran — clean. Noted for the record: `119`'s (and `105`'s) *Down* sections use the identical `PRAGMA`+`BEGIN` shape but do **not** carry a matching `-- +goose NO TRANSACTION` under `-- +goose Down` — this looks like it would hit the same error if a Down were ever actually executed via goose. Did not touch this in other files (out of scope for this task and not something `03`'s instructions asked me to audit), but mirrored the existing convention exactly in my own Down section (NO TRANSACTION only under Up, matching `119`) rather than introduce a one-off inconsistency of my own; flagging here since it's a real latent gap in the established pattern this task's migration was told to copy, and the next person writing a Down-testing task for a CHECK-widening migration should know to add the directive there too.

**3. Dispatch-switch placeholder** — confirmed the actual per-kind execution switch is `BuiltinWorkflowEngine.executeLLMOrTool` (`internal/service/workflow_engine.go`), called from `runStep` for every step that isn't `StepKindGate` (gate is special-cased earlier in `runStep` and never reaches this switch). Before this change, the switch had cases only for `StepKindLLM`/`StepKindTool`; an unhandled kind would fall through leaving `sr` as a zero-value `agentworkflow.StepResult{}` — `IsError: false`, empty `Output` — which is exactly the silent-no-op shape the task explicitly forbids (indistinguishable from a step that ran and legitimately produced nothing). Added an explicit `case agentworkflow.StepKindFlex:` that sets `sr` to `IsError: true` with `Output: "agentworkflow: flex step execution not yet implemented (see TASKS/teams/06-stepkindflex-executor.md)"`, and updated the doc comments on both `runStep` and `executeLLMOrTool`'s call sites to say plainly that this is a synchronous placeholder, not the real gate-like pause/resume behavior task 06 will build. **Placeholder behavior, stated exactly as requested:** a flex step reaching the engine completes immediately (not a pause) with a clear, typed failure result — the step and run both land in a terminal `failed` state with a diagnostic message, never `waiting`/`running` forever and never a silent empty success. Existing `llm`/`tool`/`gate` steps are unaffected — verified by the full pre-existing `workflow_engine_test.go` suite still passing unchanged, plus a new test that puts a flex step and an independent tool step in the same run and confirms the tool step still completes normally.

Also found, and fixed, a second exhaustive-switch site the task's own grep instruction (item 3) surfaced beyond `workflow_engine.go`: **`internal/agentworkflow/validate.go`'s `Validate` function** has its own `switch s.Kind { case StepKindLLM, StepKindTool, StepKindGate: default: return error }`. `BuiltinWorkflowEngine.Run` calls `agentworkflow.Validate(wf)` as its very first line — so without updating this switch too, a `WorkflowDefinition` containing a flex step would be rejected at Validate time with "unknown kind", and would *never reach* the dispatch switch described above at all. That would fail the Done-means requirement directly ("a test proving a flex-kind step reaching the engine's dispatch switch produces a clear... result") — a flex step needs to actually reach that switch for this task to be verifiable end-to-end, not be turned away one layer earlier. Added `StepKindFlex` to `Validate`'s known-kinds case. This is a real, necessary consequence of item 3's own instruction ("grep `\"llm\"`, `\"tool\"`, `\"gate\"` as string literals in `internal/agentworkflow/`"), not scope creep — leaving it out would have made the schema migration's own regression test (a flex step reaching the dispatch switch) impossible to write.

**4. `launch_source_type` — punted to task `08`, not touched.** Confirmed (grepped `TASKS/teams/*.md` for `launch_source_type`) no other task file in this batch currently claims this decision either — task `08` (the real TeamRun launcher) doesn't mention `launch_source_type` in its own context section as currently written. Per this task's own framing ("this task should confirm with `08` ... or make and document its own default"), and since `08` hasn't made this call yet, I deliberately did not add `'team_run'` to `durable_agent_instances.launch_source_type`'s CHECK (migrations `083`/`084`) — adding an unused CHECK value speculatively is explicitly what the task file warns against. Documented this explicitly in migration `130`'s own header comment as well, so the reasoning travels with the code, not just this Work Log.

**Testing — real backup DB, not just an empty fixture, per `EXECUTION-PROCESS.md`.** Copied `~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726` to an isolated scratch path under the session scratchpad directory first (never opened in place), inspected its real `workflow_run_steps` schema/data directly with `sqlite3` before writing any code: confirmed the live physical column order (`gate_input` last, appended via `ALTER TABLE`), confirmed both indexes' exact `CREATE INDEX` text, confirmed no FK references, and confirmed 2 real pre-existing rows (`kind='gate'` x1, `kind='llm'` x1) — real seeded data, not an empty table. Deleted that manual scratch copy after use. Then wrote three permanent regression tests in `internal/store/migration_130_workflow_run_steps_flex_kind_test.go`, following this package's established real-backup-test convention (`migration_125_reflex_action_kind_provenance_allow_test.go`'s `TestRealBackupAgentReflexesValidateAgainstProvenanceAllowList` as the direct precedent — copies into `t.TempDir()`, skips rather than fails if the backup isn't present on the machine):
- `TestMigrate130WidensWorkflowRunStepsKindCheck` — inserts all four kinds (`llm`/`tool`/`gate`/`flex`) into a fresh migrated test store and confirms all four persist; confirms a genuinely unknown kind (`"bogus"`) still violates the CHECK (the widening is exactly 4 values, not "anything goes"); confirms `assertGooseHasNothingPending` and that a simulated-restart re-`migrate()` is a clean no-op with all rows intact.
- `TestMigrate130PreservesWorkflowRunStepsIndexes` — reads `sqlite_master` directly and confirms both `idx_workflow_run_steps_run_step` (UNIQUE, `workflow_run_id`+`step_id`) and `idx_workflow_run_steps_status` survive the rebuild with their defining columns intact.
- `TestRealBackupWorkflowRunStepsSurviveFlexKindMigration` — copies the real backup into `t.TempDir()`, runs it through the real `store.New` (full migration pipeline including 130), confirms every one of the real pre-existing rows (2, as found above) still has a valid `kind`/non-empty `status` after the rebuild, then inserts a new `kind='flex'` row against that same real, migrated backup copy to prove the new capability actually works end-to-end against real data, not just a synthetic fixture.

All three pass. Also added `TestBuiltinWorkflowEngine_FlexStepReachesDispatchNotYetImplemented` in `internal/service/workflow_engine_test.go` (engine-level, per Done-means' explicit ask): runs a real `WorkflowDefinition` with one flex step and one independent tool step through the real `BuiltinWorkflowEngine` (no mocking of the engine itself, only the `StepExecutor`), and asserts: the flex step's `StepResult` is `IsError: true` with an Output containing "not yet implemented"; neither `ExecuteLLMStep` nor `ExecuteToolStep` was ever called for it (proving it took its own switch case, not an accidental fallthrough into llm/tool); the sibling tool step still completed normally; and the persisted `workflow_run_steps` row for the flex step landed with `kind='flex'`, `status='failed'`, `is_error=1` — proving the DB-level CHECK widening and the Go-level dispatch placeholder work together end-to-end, not just in isolated unit tests of each side.

**Baseline checks:** `go build ./cmd/nanite/` clean. `go vet ./...` reports exactly the same 4 pre-existing warnings in `internal/service/container.go` (`stopReaper`/`stopRuntimeReaper` possible context leak) that `TASKS/ESCALATIONS.md` already documents as pre-existing and unrelated (confirmed via `git diff --stat -- internal/service/container.go` showing no changes from me to that file) — no new vet findings from this change. `go test ./...` — all packages pass, including the new/updated tests above.

**No escalation needed.** Both judgment calls the dispatching brief flagged as expected (not escalation-worthy) were resolved as documented above: the placeholder dispatch behavior (synchronous, clear `IsError` "not yet implemented" result, not a pause) and the `launch_source_type` question (punted to task `08`, documented in both the migration file and here).

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
