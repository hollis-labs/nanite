# Team authority — real enforced grants, not a copy of `parent_dispatch_allowlist`'s advisory pattern

**Phase:** 1 — Schema & storage foundation (`TASKS/teams`)
**Status:** implemented
**Depends on:** `01` (Team Slot vocabulary — this table's `from_slot`/`to_slot` values should correspond to real `TeamSlotDefinition.Name` values, though no FK is proposed since slots live inside `teams.slots_json`, not a normalized table)
**Touches:** `internal/store/migrations/` (new migration), `internal/store/team_authority.go` (new — `TeamAuthorityGrant` struct, CRUD, `AuthorizedForVerb` check function).

## Context

**This task exists to correct a real, load-bearing misreading in the design doc — read this before writing any code.** `docs/engineering/architecture/15-teams.md`'s "Authority: generalize, don't invent" section claims: *"`internal/dispatch`'s `agent_parent_dispatch_allowlist` (migration 060) already governs 'which *tools* may a dispatching parent authorize a spawned child to use.' A Team's `may_spawn`/`may_message`/`may_not_review` is the same shape one level up — *role*-level rather than *tool*-level authorization... This should generalize that existing mechanism... rather than be built as an unrelated Team-native permission system."*

Verified directly against the real code (this planning session's research, not assumed): `agent_parent_dispatch_allowlist` is **not a table** — it is a real column, `agent_profiles.parent_dispatch_allowlist TEXT NOT NULL DEFAULT '[]'` (migration `060_agent_parent_dispatch_allowlist.sql`), a JSON array of **role slugs** (seeded default: `["researcher","planner","worker"]`). It is **already role-slug-level**, not tool-level — the design doc's "generalize from tool-level to role-level" framing is backwards. More importantly: **it is advisory only.** Its sole real consumer is `internal/service/tool.go`'s `parseParentDispatchAllowlist` (lines ~246/862), which feeds `describer.CallerAgent.DispatchAllowlist` — rendered into the `task_execute` self-tool's LLM-facing description. No enforcement call site anywhere rejects a `task_execute` call whose target role isn't on the allowlist. This is consistent with `docs/engineering/architecture/00-overview.md`'s own guiding principle, quoted in full: *"Hints, not control. Steering nudges; it doesn't gate with a deterministic pre-decision layer. Hard-gating experiments in this codebase produced dead-end conversations — this isn't a style preference, it's an observed failure mode."*

**The consequence for this task:** the design doc explicitly wants Team authority to be a real, load-bearing gate — its own text says it "should compose with the existing `dispatch.TrustTier`/`ErrUntrustedRole` enforcement point" for approval-bypass cases. `dispatch.TrustTier` (`internal/dispatch/trust.go`) is a genuine hard gate, confirmed: three tiers (`untrusted`/`normal`/`trusted`), resolved via `TrustResolver.ResolveTrust(ctx, agentProfileID)` from `agent_profiles.default_trust_tier` (`internal/store/trust.go:22`), enforced at `internal/subagent/service.go:785-787` (`if tier == TrustUntrusted { return "", dispatch.ErrUntrustedRole }`, comment: "must NOT be bypassed"), caught cleanly at `internal/selftools/self_tools_transport.go:1839`. **This task must build something with `TrustTier`'s enforcement shape (a real `if`-gate at the actual dispatch/message call site), not `parent_dispatch_allowlist`'s shape (a hint folded into an LLM-facing tool description).** Do not copy the wrong precedent because the design doc names it as the thing to generalize.

**Verb set is deliberately not final** — the design doc is explicit: *"`may_message` is one illustrative verb, not the root authority primitive... 'May exchange messages at all' (transport access) and 'may direct/command/approve' (semantic authority) are different questions... No verb set is locked by this session; this is a warning against letting the one convenient example ossify into the whole mechanism."* Build the schema so a future verb (`may_delegate`/`may_approve`/`may_signal`) is a CHECK-constraint widening, not a new column/migration per verb.

**This task does not wire enforcement into real call sites** — that's task `08` (spawn-time, `may_spawn`) and task `09` (message-time, `may_message`/`may_not_review`), once slot resolution and routing exist to check against. This task builds the storage and a pure, directly-testable check function.

## What to do

1. New migration (confirm the actual next-available number at dispatch time — see `01`'s numbering note):
   ```sql
   CREATE TABLE team_authority_grants (
       id            TEXT PRIMARY KEY,
       team_id       TEXT NOT NULL REFERENCES teams(id),
       from_slot     TEXT NOT NULL,
       verb          TEXT NOT NULL CHECK (verb IN ('may_spawn','may_message','may_not_review')),
       to_slot       TEXT NOT NULL,
       created_at    TEXT NOT NULL DEFAULT (datetime('now'))
   );
   CREATE INDEX idx_team_authority_grants_team_slot ON team_authority_grants(team_id, from_slot, verb);
   ```
   `to_slot = 'self'` is a legitimate value (per the design doc's `reviewer.may_not_review: self` example — a slot excluding itself as a valid target, not addressing another slot literally named "self"). Document your call if you widen the `verb` CHECK now for a verb beyond the three named — the design doc doesn't lock this, so adding one speculatively is a judgment call, not a violation, as long as you document why.

2. **Go types + store CRUD**, `internal/store/team_authority.go`:
   - `TeamAuthorityGrant` struct + `CreateTeamAuthorityGrant`, `ListTeamAuthorityGrants(teamID string)`, `DeleteTeamAuthorityGrant`.
   - A real check function: `AuthorizedForVerb(ctx context.Context, teamID, fromSlot, verb, toSlot string) (bool, error)` — this is the actual enforcement primitive tasks `08`/`09` call at their real dispatch/message call sites, modeled on `TrustResolver.ResolveTrust`'s "resolve, then a hard `if`-gate at the real call site" shape — **not** `parseParentDispatchAllowlist`'s "resolve, then only render into an LLM-facing description" shape. **Fail closed, not fail open**: an unknown `team_id`/`from_slot`/no matching grant row must return `false`, never `true` by default — this codebase has a documented, real history of exactly this class of bug (`TASKS/reflex-taxonomy/08-fix-resolve-fail-open-visibility.md`, a fail-open regression in `Resolve()`'s combining-algorithm lookup found by a fresh reviewer) — write an explicit regression test for the fail-closed case, don't just assume the implementation gets it right.

## Done means

- Migration applies cleanly against a real backup copy of the database, not just an empty fixture.
- Round-trip test for `TeamAuthorityGrant` CRUD.
- `AuthorizedForVerb` regression tests covering: a granted verb returns `true`; an ungranted verb/slot pair returns `false`; `to_slot='self'` correctly excludes self-targeting for `may_not_review`; an unknown team ID or slot name returns `false` (explicit fail-closed test, not incidental).
- No call site in `internal/dispatch`, `internal/selftools`, or `internal/service` is modified by this task — enforcement wiring is tasks `08`/`09`'s job. `go build`/`vet`/`test` clean.

## Work log

**Pre-work verification (per dispatch instructions):** confirmed task `01`'s artifacts (`internal/store/teams.go`, `internal/store/migrations/128_teams.sql`) are present on this branch, along with tasks `02`/`03`/`05` (`129_team_run_members.sql`, `130_workflow_run_steps_flex_kind.sql`, `131_agent_reflexes_workflow_run_scoping.sql`) — `git log --oneline -5` showed all four already committed to this worktree's history. Re-ran `ls internal/store/migrations/ | sort -t_ -k1 -n | tail -3` immediately before writing the migration file, as instructed: `131_agent_reflexes_workflow_run_scoping.sql` was still the latest on disk, so **migration 132** was used, exactly as the dispatch anticipated — no renumbering needed.

Read `internal/dispatch/trust.go` and `internal/subagent/service.go` (lines 750-819, covering the 785-787 gate) directly before writing any code, per the dispatch's explicit instruction, to confirm the enforcement shape firsthand rather than trusting the task file's paraphrase. Confirmed: `TrustResolver.ResolveTrust` resolves a tier, then `subagent/service.go` has a real, unconditional `if tier == TrustUntrusted { return "", dispatch.ErrUntrustedRole }` — no LLM-facing rendering step, a hard refusal at the actual call site. `AuthorizedForVerb` (below) is modeled on that shape: resolve (query the grants table), then return a plain bool the caller must act on with a real `if`-gate at its own call site (tasks `08`/`09`) — not rendered into any tool description anywhere in this task's own code.

**Migration (`internal/store/migrations/132_team_authority_grants.sql`):** created `team_authority_grants` exactly per the task file's own DDL (`id`, `team_id REFERENCES teams(id)`, `from_slot`, `verb CHECK IN ('may_spawn','may_message','may_not_review')`, `to_slot`, `created_at`), plus the specified `idx_team_authority_grants_team_slot` index. No FK from `from_slot`/`to_slot` to a normalized slots table, per the task's own "Depends on" note (Team Slots live in `teams.slots_json`, a JSON blob — nothing to reference). Brand-new table, plain transactional `CREATE TABLE` (goose `+goose Up`/`+goose Down`), matching `126_selftool_reactions.sql`/`128_teams.sql`/`129_team_run_members.sql`'s precedent — no PRAGMA-foreign-keys rebuild dance needed since there's no pre-existing data in this table.

**Judgment call — `verb` CHECK not widened:** kept the CHECK to exactly the three verbs the task file's own DDL specifies (`may_spawn`/`may_message`/`may_not_review`). The design doc explicitly leaves the verb set open ("`may_delegate`/`may_approve`/`may_signal`... No verb set is locked by this session") and the dispatch instructions call adding a speculative verb "a judgment call, not a violation, as long as you document why" — but also state plainly not to add one "unless you have a real, documented reason." No such reason exists yet: this batch's only two consumers (tasks `08`/`09`, per this task's own Context) only ever need `may_spawn`/`may_message`/`may_not_review`. Widening the CHECK later is exactly the one-line-migration shape this task's own "What to do" section asks the schema to support ("a future verb... is a CHECK-constraint widening, not a new column/migration per verb") — documented in the migration file's own header comment so a future task doesn't have to re-derive this reasoning.

**Judgment call — `to_slot='self'` handling:** resolved entirely inside `AuthorizedForVerb`, not at the DB layer (no CHECK/trigger). The stored value `'self'` is a literal string in `to_slot`; `AuthorizedForVerb` treats a grant row with `to_slot == "self"` as matching a query only when the queried `toSlot` argument equals the queried `fromSlot` argument (i.e., true self-targeting) — never as a literal slot named "self". This matches the task file's explicit instruction verbatim ("a slot excluding itself as a valid target, not addressing another slot literally named 'self'"). Verified by `TestAuthorizedForVerb_SelfSlotExcludesSelfTargeting`, which also checks a `to_slot='self'` grant for one `from_slot` does not leak into a different `from_slot`'s self-targeting check.

**Go types + CRUD (`internal/store/team_authority.go`):** `TeamAuthorityGrant` struct (mirrors the table 1:1) + `CreateTeamAuthorityGrant` (validates required fields and the verb enum in Go before insert, mirroring `team_run_members.go`'s `UpdateTeamRunMemberStatus` precedent so callers get a typed error instead of a raw SQL CHECK-violation; lets the column default populate `created_at` then reads the row back, same pattern `InsertTeamRunMember` uses for `resolved_at`), `ListTeamAuthorityGrants(ctx, teamID)` (ordered, returns an empty non-nil slice for a team with zero grants — a Team declaring no authority rules is a legitimate starting shape, same reasoning `15-teams.md` applies to gate-free Teams), `DeleteTeamAuthorityGrant` (returns `ErrTeamAuthorityGrantNotFound` on no match, matching this package's existing `ErrTeamNotFound`/`ErrTeamRunMemberNotFound` convention).

**The real enforcement primitive — `AuthorizedForVerb(ctx, teamID, fromSlot, verb, toSlot) (bool, error)`:** a pure SQL lookup (`SELECT to_slot FROM team_authority_grants WHERE team_id=? AND from_slot=? AND verb=?`), scanned in Go rather than folded into one giant `WHERE ... OR to_slot = 'self'` clause specifically so the self-slot semantics stay explicit, readable Go logic rather than SQL that's easy to get subtly wrong. Fails closed in every case the task's "Critical correctness requirement" and "Done means" enumerate:
- Empty `teamID`/`fromSlot`/`verb`/`toSlot` argument → rejected before the query runs, `false, nil`.
- Unknown `teamID`, unknown `fromSlot`/`toSlot`, or a team with zero grants at all → the `WHERE` clause matches zero rows → loop never sets `true` → falls through to `return false, nil`.
- A real DB error → `false` is returned alongside the wrapped error; there is no code path anywhere in the function that returns `true` on an error.
- There is no `default: return true` or equivalent anywhere in the function — verified by reading the final code, not just assumed.

This function does not itself decide what `true`/`false` *means* for `may_not_review` (an allow-verb like `may_spawn` treats `true` as "proceed"; a deny-verb like `may_not_review` needs its caller to invert the match into a block) — the task file's own scope explicitly defers that call-site policy to task `09`; this task only had to build "a pure, directly-testable check function," which is what's delivered. Documented explicitly in the function's own doc comment so task `09`'s author doesn't have to re-derive it.

**Tests (`internal/store/team_authority_test.go`, 11 tests, all passing):** `TestTeamAuthorityGrant_RoundTrip` (create 3 grants incl. a `to_slot='self'` row, list, cross-team isolation check, delete, delete-again/delete-unknown → `ErrTeamAuthorityGrantNotFound`), `TestTeamAuthorityGrant_ListEmpty`, `TestTeamAuthorityGrant_Validation` (5 invalid-input cases incl. an invalid verb), `TestTeamAuthorityGrant_UnknownTeamID_ForeignKeyRejects` (confirms the real DB-level FK — this codebase runs with `PRAGMA foreign_keys=1` — actually rejects a grant against a nonexistent team, not just that Go-side validation happens to catch it), and the `AuthorizedForVerb` suite specifically required by "Done means": `TestAuthorizedForVerb_GrantedReturnsTrue`, `TestAuthorizedForVerb_UngrantedReturnsFalse` (wrong `toSlot`, wrong `verb`, and wrong `fromSlot`, three sub-cases), `TestAuthorizedForVerb_SelfSlotExcludesSelfTargeting` (self-targeting matches; non-self `toSlot` does not; a different `fromSlot`'s self-exclusion grant doesn't leak), and three explicit, non-incidental fail-closed regression tests per the task's "Critical correctness requirement" — `TestAuthorizedForVerb_FailClosed_UnknownTeamID`, `TestAuthorizedForVerb_FailClosed_UnknownSlot`, `TestAuthorizedForVerb_FailClosed_NoGrantsAtAll` — plus `TestAuthorizedForVerb_FailClosed_EmptyArguments` as defense-in-depth beyond what was strictly asked.

**Migration tested against a real backup DB copy (not just an empty fixture), per `EXECUTION-PROCESS.md`'s requirement:** copied `~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726` to an isolated scratch path under this session's scratchpad directory (never touched the real backup file in place — confirmed its mtime is still `Aug 18 13:27:26 2026`, unchanged, after the run). That backup predates this repo's goose adoption entirely (no `goose_db_version` table, but a real, populated `sessions` table with 343 rows and 28 `agent_profiles` rows) — i.e. it exercised the pre-goose-cutover path (`seedLegacyLedger`, seeding versions 1-94) *and* the real replay of every migration 95 through 132 (including this task's own 132) against a genuine production-shaped dataset, not a synthetic empty DB. Ran via a throwaway `internal/store` test (`store.New` against the scratch copy, gated behind an env var, deleted immediately after use — never committed) that confirmed: `team_authority_grants` exists after migration with the exact expected schema (`sqlite3 .schema` output matches the migration verbatim), pre-existing `sessions` rows survived (343, unchanged), `teams` (migration 128) is present (confirms the whole 95-132 chain replayed correctly, not just 132 in isolation), a real `CreateTeamAuthorityGrant`/`AuthorizedForVerb`/`ListTeamAuthorityGrants` round-trip works against the migrated real DB, and reopening the same file a second time (`store.New` again) is a clean idempotent no-op. `goose_db_version` showed `max(version_id) = 132` after the run. Deleted the throwaway test file and the scratch DB copy afterward; `git status --short` immediately after confirms only this task's own three new files are present, no stray writes anywhere else.

**Baseline:** `go build ./cmd/nanite/` — ok. `go vet ./...` — only the two pre-existing, already-documented-as-known-unrelated `internal/service/container.go` findings (`stopReaper`/`stopRuntimeReaper` not used on all paths), same findings flagged as unrelated in `TASKS/reflex-taxonomy/08-fix-resolve-fail-open-visibility.md`; not touched by this task. `go test ./...` — full repo, every package `ok`, zero `FAIL` (re-ran `go test ./internal/store/... -count=1` uncached after deleting the manual-verification file, to rule out a stale cache masking a real failure).

**No enforcement call sites touched**, per the task's own explicit scope boundary: `internal/dispatch`, `internal/selftools`, and `internal/service` are untouched by this task — confirmed via `git status --short` (only the migration file, `team_authority.go`, and `team_authority_test.go` are new/modified). Wiring `AuthorizedForVerb` into a real spawn/message call site is tasks `08`/`09`'s job, as stated.

**Nothing escalated.** No genuine unknown was hit — the two judgment calls above (verb-CHECK non-widening, `to_slot='self'` resolution) are exactly the kind of expected, document-your-reasoning calls the dispatch instructions named up front, not stop-worthy ambiguities.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
