# Run-scoped `agent_reflexes` — the design doc's own named "real gap," at both real call sites

**Phase:** 1 — Schema & storage foundation (`TASKS/teams`)
**Status:** implemented
**Depends on:** none directly (independent column addition; parallel-safe with `01`/`02`/`03`/`04` — coordinate migration numbering only, see `01`'s numbering note)
**Touches:** `internal/store/migrations/` (new migration — nullable `agent_reflexes.workflow_run_id` column), `internal/store/agent_reflexes.go`, `internal/agent/reflexes/` (candidate-set lookup feeding `Resolve()`), `internal/service/chat_reflex_dispatch.go` (`attemptReflexDispatch`), `internal/selftools/self_tools_dispatch.go` (`matchDispatchToAgentReflex`).

## Context

`docs/engineering/architecture/15-teams.md`'s "Routing: real reuse, and one real gap" section names this precisely: *"`agent_reflexes.agent_id` today is only `NULL` (class-bound/global) or a specific agent... there is no third scoping dimension for 'this reflex exists only for the lifetime of this run, and its candidate set is this run's resolved members.' That's genuine new schema/engine work... not configuration over what exists."* Confirmed directly against the real migration chain (through `119_agent_reflex_dispatch_to_agent.sql`, plus `124_reflex_action_taxonomy.sql`/`125_reflex_action_kind_provenance_allow.sql`'s later `ProvenanceTier`/`RecurrenceOverrideSeconds` additions) — the design doc's claim holds exactly.

**A real, non-obvious cost the design doc's single-paragraph description doesn't surface:** `dispatch_to_agent` evaluation happens at **two independent real call sites**, split by a genuine import-cycle constraint (`internal/selftools` cannot import `internal/service`) — `attemptReflexDispatch` (`internal/service/chat_reflex_dispatch.go`) and `matchDispatchToAgentReflex` (`internal/selftools/self_tools_dispatch.go`). Both intentionally run, not collapsed into one (explicit comment at `self_tools_dispatch.go` lines ~45-53 — `task_execute`'s own dispatch runs a second, independent evaluation downstream of the upstream one). **Both need the run-scoped candidate-set lookup** — fixing only one and assuming symmetry will produce a Team whose routing works from one entry point and silently doesn't from the other.

**The reflex-taxonomy work has already landed on `main`, ahead of what `15-teams.md`'s own text assumes is still informal** — a shared decision engine, `reflexes.Resolve(candidates, state)`, is called from all three real evaluation sites (the two above, plus `Engine.EvaluateState`'s generic per-turn pass). Build on `Resolve()` directly rather than reimplementing the candidate-list/evaluate/pick loop a third time. **Do not regress** `TASKS/reflex-taxonomy/09-fix-duplicate-kind-lookup-warn-logging.md`'s prior fix, which correctly excludes `dispatch_to_agent` rows from `Engine.EvaluateState`'s generic pass entirely (that pass has no live dispatch hook for this action kind) — a run-scoped row must stay excluded from that third site too, not newly leak into it.

**Combining algorithm** for `dispatch_to_agent` is `first_applicable` (highest-priority firer wins, ties broken by `created_at ASC`) — already the live algorithm, no new combining logic needed here; this task only widens the *candidate set* a run-scoped session sees, using the existing algorithm as-is.

**Provenance tier requirement, real, easy to miss:** migration `125`'s `reflex_action_kind_provenance_allow` table gates which `provenance_tier` values are valid for which `action_kind` — confirmed `(halt_session, plugin)` is explicitly absent/denied for at least one kind, and Team-authored routing rules are not operator-authored, so whatever `provenance_tier` this task's run-scoped rows (or task `09`'s, which actually creates them) carry must pass `ActionKindAllowsProvenanceTier` for `dispatch_to_agent` — confirm which tier is appropriate (`operator` is the safest default if Team routing rules are conceptually "configured by whoever created the Team," but document your actual reasoning) rather than assuming.

**Real seed example to model the shape against** (not hypothetical): `internal/agent/reflexes/seeds.go:334-352`, `dispatch_to_agent_open_subagent` — `ClassTag: "advisor"`, `TriggerKind: "predicate"`, `Priority: 10`, `ActionKind: "dispatch_to_agent"`, `ActionSpec: {"agent_slug":"planner","confidence":0.75,"reason":"..."}` (shape documented `internal/store/agent_reflexes.go:45-47`).

## What to do

1. New migration (confirm the actual next-available number at dispatch time — see `01`'s numbering note): add nullable `agent_reflexes.workflow_run_id TEXT REFERENCES workflow_runs(id)`. `NULL` = global/class-bound, exactly as today — this codebase's and `go-scheduler`'s existing convention for "unscoped." No table rebuild needed if a plain `ALTER TABLE ADD COLUMN` suffices for a nullable FK-referencing column with no CHECK — confirm SQLite allows this directly (it generally does for a nullable column with no new CHECK); if it doesn't in practice, use the standard rebuild pattern instead and document why.

2. **Candidate-set lookup**: find and extend whatever function currently loads `dispatch_to_agent` candidates feeding `Resolve()` at both call sites, so that when evaluation happens inside a session that is itself a `team_run_members`-resolved session (i.e., its `session_id` maps to a real `team_run_members` row — task `02`'s table), the candidate set includes `agent_reflexes` rows where `workflow_run_id` matches that run **or** `workflow_run_id IS NULL` (global rules still apply; run-scoped rules layer on top, don't replace). **Document the priority ordering you land on** when both a global and a run-scoped rule could fire for the same predicate — the design doc's routing section establishes semantic-rule-before-coordinator-fallback ordering explicitly but doesn't address global-vs-run-scoped precedence at all; your documented call here is real design work, not a mechanical translation.

3. Apply this at **both** `attemptReflexDispatch` and `matchDispatchToAgentReflex` — write one test per call site proving run-scoped isolation (a rule scoped to run A does not fire for a session in unrelated run B, or for a non-Team session at all).

4. Confirm `Engine.EvaluateState`'s existing `dispatch_to_agent` exclusion still holds unchanged after this column addition — add a regression test if one doesn't already directly cover "a `dispatch_to_agent` row with a non-NULL `workflow_run_id` is still excluded from the generic per-turn pass," since this is exactly the kind of column-addition-induced regression this project's history shows happens easily.

## Done means

- Migration applies cleanly against a real backup copy of the database, not just an empty fixture.
- Regression tests at **both** `attemptReflexDispatch` and `matchDispatchToAgentReflex` proving a run-scoped rule fires only for sessions belonging to its own run and is invisible elsewhere; a global (`workflow_run_id IS NULL`) rule is unaffected and still fires everywhere it did before.
- Documented, tested global-vs-run-scoped priority ordering.
- Documented `provenance_tier` choice, confirmed via a direct test that it passes `ActionKindAllowsProvenanceTier` for `dispatch_to_agent`.
- `Engine.EvaluateState`'s dispatch_to_agent exclusion confirmed unregressed. `go build`/`vet`/`test` clean.

## Work log

**Environment note (not a task deviation, but load-bearing for how this session worked):** this worker's worktree (`agent-a6afc8e55b4657f67`) was branched before `TASKS/teams/*` and `docs/engineering/architecture/15-teams.md` were committed to the main checkout — they existed only as uncommitted files in the primary `apps/nanite` working tree, not in this worktree at all. Read the task file and the full architecture doc directly from the primary checkout path (`/Users/chrispian/dev/hollis-labs/apps/nanite/TASKS/teams/05-agent-reflexes-run-scoping.md`, `/Users/chrispian/dev/hollis-labs/apps/nanite/docs/engineering/architecture/15-teams.md`) since the Read tool isn't worktree-restricted, but all code edits and this Work Log itself had to land inside the worktree (the Edit/Write tools refuse to touch the shared-checkout path from an isolated worktree) — this copy of the task file is therefore new in this worktree, mirrored from the primary checkout with this Work Log filled in.

Also confirmed `internal/mcp/self_tools_dispatch.go` (the path `TASKS/reflex-taxonomy/09-fix-duplicate-kind-lookup-warn-logging.md` names) has since moved to `internal/selftools/self_tools_dispatch.go` — this task file's own "Touches" line already has the corrected path; no discrepancy to resolve, just confirming it before editing.

**Migration number used: `131_agent_reflexes_workflow_run_scoping.sql`**, exactly as pre-assigned by the dispatch instructions. Re-verified via `ls internal/store/migrations/ | sort -t_ -k1 -n | tail -10` immediately before writing the file — `127_schedule_runs_and_retry_policy.sql` was still the latest on disk in this worktree, `128`/`129`/`130` were unclaimed (reserved for tasks `01`/`02`/`03`), so `131` was genuinely free.

**1. Migration.** `ALTER TABLE agent_reflexes ADD COLUMN workflow_run_id TEXT REFERENCES workflow_runs(id);` plus a supporting index, no table rebuild, no `PRAGMA foreign_keys` toggling, no `NO TRANSACTION` marker — following `112_add_consumers_table.sql`'s and `117_agent_profiles_composition_columns.sql`'s already-shipped precedent for a nullable FK column added via plain `ADD COLUMN` (confirmed by reading both files first; this is not a novel pattern for this codebase). Confirmed the "no rebuild needed" claim two ways before committing to it, per the dispatch instructions:
   - Direct `sqlite3` CLI against a copy of the real backup (`~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726`, copied to an isolated scratch path first, never touched in place): `ALTER TABLE agent_reflexes ADD COLUMN workflow_run_id TEXT REFERENCES workflow_runs(id);` succeeded with no error against the table's 14 pre-existing rows.
   - The real Go/goose migration path: wrote a temporary `internal/store/zzz_scratch_migration_verify_test.go` (deleted before finishing, never committed) that opened a fresh copy of the same real backup via `store.New()` — i.e., ran the *entire* migration chain up through 131, not the ALTER statement in isolation — and confirmed all 14 pre-existing `agent_reflexes` rows come back with `workflow_run_id IS NULL`, plus a second `st.migrate()` call as a clean-no-op restart check. Re-ran this same verification once more after all other code/tests were finished, as a final end-to-end check, then deleted the scratch test file again (confirmed via `git status --short` showing no stray file afterward).

**2. Candidate-set lookup — three new pieces in `internal/store/agent_reflexes.go`/new files, none in `internal/agent/reflexes/`:**
   - `AgentReflex.WorkflowRunID` field, `agentReflexColumns`/`scanAgentReflex`/`InsertAgentReflex` updated to persist/read it. Deliberately **not** added to `UpdateAgentReflex`'s SET clause — treated as an immutable scope binding, same rationale as `AgentID`/`ClassTag`.
   - `Store.ListAgentReflexesForWorkflowRun(ctx, runID, agentID, classTag)` (new) — the run-scoped counterpart of the existing `ListAgentReflexesForAgent`, same class-bound/opt-out/agent-specific filtering, plus `WHERE workflow_run_id = ?`. Returns `nil, nil` for an empty `runID`.
   - `Store.ResolveWorkflowRunIDForSession(ctx, sessionID)` (new file, `internal/store/session_team_run.go`) — resolves a session to its `team_run_members` row's `workflow_run_id`, fail-open (returns `found=false, err=nil`, not an error) both for "no row" and for "`team_run_members` table doesn't exist yet on this database" (detected via a `"no such table"` substring match on the driver error — confirmed exact error text, `"SQL logic error: no such table: team_run_members (1)"`, against `modernc.org/sqlite` directly before relying on the substring check).
   - **Deliberately did not touch `ListAgentReflexesForAgent`'s SQL.** It stays scope-agnostic (returns rows regardless of `workflow_run_id`, exactly as before this column existed) so every *other* caller of it — most importantly `Engine.EvaluateState`'s generic per-turn pass, which has no run context of its own — sees byte-identical behavior. This is also why `internal/agent/reflexes/` needed **no changes at all**: `EvaluateState`'s existing `r.ActionKind == store.ReflexActionDispatchToAgent` skip already excludes every `dispatch_to_agent` row regardless of scope, so it was already correct against this new column with zero new code — see item 4 below for the regression test proving this holds.
   - Because `ListAgentReflexesForAgent` is scope-agnostic, filtering to "global only" (`WorkflowRunID == ""`) plus `ActionKind == dispatch_to_agent` is each of the two dispatch call sites' **own** responsibility, additional to their pre-existing `ActionKind` filter — documented inline at both sites. **This was caught by my own test, not reasoned out in advance**: my first implementation pass widened the candidate set by appending `ListAgentReflexesForWorkflowRun`'s result to the *unfiltered* `ListAgentReflexesForAgent` result, then filtered only by `ActionKind`. The isolation test (`sess-in-run-b`/`sess-no-run` sub-tests) immediately failed — the run-scoped reflex fired for both, because `ListAgentReflexesForAgent`'s query has no `workflow_run_id` predicate at all and was already returning the run-scoped row (it matches on `class_tag`/`agent_id` alone) regardless of which session was asking. Fixed by requiring `WorkflowRunID == ""` from the global list before merging in the run-scoped list. Left in the Work Log as a concrete instance of exactly the gap the design doc's own text warned about ("no third scoping dimension" — without this extra filter, adding the column alone does nothing to prevent the leak the whole task exists to close).

**Documented priority ordering (my judgment call, as flagged):** global and run-scoped candidates are merged into **one flat candidate slice** before calling `reflexes.Resolve()` — no two-tier fallback, no automatic run-scoped-over-global (or reverse) boost. `dispatch_to_agent`'s existing `first_applicable` combining algorithm (`priority DESC, created_at ASC`, unchanged) is the sole arbiter of which candidate wins, regardless of which scope it came from. Landed on this specifically because the task file's own text says the combining algorithm is "already the live algorithm, no new combining logic needed here; this task only widens the candidate set... using the existing algorithm as-is" — a two-tier "try run-scoped first, fall back to global" scheme would itself be new combining logic, which that sentence reads as a fairly direct instruction against. The practical consequence: a Team wanting its run-scoped routing rule to reliably beat the generic class-bound fallback for the same session needs to author it with a higher `priority` value — the same discipline the design doc's own "coordinator fallback = the lowest-priority row in the same scoped set" convention already assumes of Team-authored rows. (I considered the alternative — "more specific wins," mirroring the design doc's `role → agent → task` cascade language elsewhere — and think it's a defensible reading too, but the task's explicit "no new combining logic" instruction is the more direct signal, so I followed it literally rather than substituting my own judgment for it.)

**3. Both call sites updated and tested:**
   - `internal/service/chat_reflex_dispatch.go`'s `attemptReflexDispatch` — widened as described above; `internal/service/chat_reflex_dispatch_run_scoping_test.go` (new): one test with sub-tests proving (a) a run-scoped reflex fires for a session resolved to its own run, (b) is invisible to a session resolved to a *different* run, (c) is invisible to a session with no `team_run_members` row at all, (d) a global reflex fires unaffected in all three of those sessions; plus a second test proving the "`team_run_members` table doesn't exist yet" fail-open path doesn't break ordinary (non-Team) dispatch.
   - `internal/selftools/self_tools_dispatch.go`'s `matchDispatchToAgentReflex` — identical widening and identical fix for the same leak, applied independently (this file cannot import `internal/store.Store`'s sibling helpers any differently than `internal/service` does, but it's a separate package so the fix had to be written twice, not shared). `internal/selftools/self_tools_dispatch_run_scoping_test.go` (new): the same test shape, calling `matchDispatchToAgentReflex` directly via `NewSelfToolsTransport`.
   - Both test files stand up a **minimal, test-local** `team_run_members` table (`session_id`, `workflow_run_id` only — the two columns `ResolveWorkflowRunIDForSession` actually reads) via raw `CREATE TABLE` in the test itself, not a real migration. `TASKS/teams/02-team-run-members-table.md` (the task that owns this table for real) is an independent, parallel sibling not present in this worktree — deliberately did **not** add a competing `CREATE TABLE team_run_members` to this task's own migration, to avoid a real collision risk (two migrations creating the same table, in whichever order they eventually merge, with a real chance of a "table already exists" failure if task 02's own migration doesn't use `IF NOT EXISTS`). Instead, `ResolveWorkflowRunIDForSession`'s fail-open behavior means this task's production code is correct **today** (table absent → no run scoping, ordinary dispatch unaffected) and will start actually working the moment task 02's migration lands, using the same column names (`session_id`, `workflow_run_id`) both this task's migration comment and task 02's own task file's illustrative DDL specify — read task 02's task file directly (not just the design doc) to confirm those exact column names before committing to this shape.

**4. `Engine.EvaluateState` exclusion — confirmed unregressed, with a new explicit regression test.** No code change was needed in `internal/agent/reflexes/engine.go` (see item 2 above for why). Added `TestEvaluateState_DispatchToAgent_RunScopedRowAlsoExcludedFromGenericPass` to the existing `internal/agent/reflexes/dispatch_to_agent_generic_pass_test.go` (the file `TASKS/phase-4/09-fix-dispatch-to-agent-generic-pass-leak.md`'s original fix lives in) — inserts a `dispatch_to_agent` row with a real, non-empty `WorkflowRunID` (a genuine `workflow_runs` row is required first, since the new column is FK-enforced and `foreign_keys` is ON by default in this codebase's SQLite DSN options) and confirms `EvaluateState` still produces zero actions, zero fired-count bump, and zero plugin-hook calls for it — exactly the same assertions the pre-existing global-row test makes, proving the exclusion is scope-agnostic (keyed purely on `ActionKind`, as it always was).

**Documented `provenance_tier` choice: `operator`.** Reasoning: Team routing rules are conceptually "configured by whoever created the Team," not the base reflex seeder (`system`, reserved for `seeds.go`/`loom_pilot_seeds.go`) and not a live plugin-authored path (migration `125` explicitly denies `plugin` for `dispatch_to_agent` — confirmed by reading `125_reflex_action_kind_provenance_allow.sql` directly). `operator` is the same tier the codebase's existing convention already gives every human/operator-originated reflex (migration `124`'s own backfill rule: any `created_by` other than `"system"` collapses to `provenance_tier = "operator"`), so this is consistent with existing practice rather than a new category. This task itself creates no write/CRUD path for run-scoped rows (that's task `09`'s job per this task file's own Context section) — the choice is recorded here as guidance for that task, and confirmed directly in this task's own tests: `internal/store/agent_reflexes_workflow_run_scoping_test.go`'s `TestActionKindAllowsProvenanceTier_DispatchToAgent_OperatorTier` calls `ActionKindAllowsProvenanceTier(ctx, "dispatch_to_agent", "operator")` directly and asserts `true` (plus a sanity check that `"plugin"` correctly resolves `false`, so the test isn't vacuously true), and `TestAgentReflex_WorkflowRunID_RoundTrips` in the same file inserts and round-trips a `WorkflowRunID`+`ProvenanceTier: "operator"` row shaped like a real Team routing rule end to end. Note this overlaps with, but doesn't replace, `internal/store/migration_125_reflex_action_kind_provenance_allow_test.go`'s pre-existing `TestMigrate125SeedsProvenanceAllowList`, which already covers this exact `(dispatch_to_agent, operator)` pair as part of its full 18-combination table — added this task's own direct, self-contained test anyway so the connection between the judgment call and its enforcement is explicit in this task's own test surface rather than relying on a separate task's coverage to imply it.

**Baseline checks (as implemented in the worktree):** `go build ./cmd/nanite/` clean. `go vet ./...` — only the two pre-existing, known-unrelated `internal/service/container.go` findings (`stopReaper`/`stopRuntimeReaper` possible context leak — the same two `TASKS/reflex-taxonomy/09-fix-duplicate-kind-lookup-warn-logging.md`'s own Work Log already flagged as known-unrelated); no new findings. `go test ./...` — full suite green in the worktree, all packages `ok`. `git status --short` confirms only the intended files changed: `internal/store/migrations/131_agent_reflexes_workflow_run_scoping.sql` (new), `internal/store/agent_reflexes.go` (modified), `internal/store/session_team_run.go` (new), `internal/store/agent_reflexes_workflow_run_scoping_test.go` (new), `internal/service/chat_reflex_dispatch.go` (modified), `internal/service/chat_reflex_dispatch_run_scoping_test.go` (new), `internal/selftools/self_tools_dispatch.go` (modified), `internal/selftools/self_tools_dispatch_run_scoping_test.go` (new), `internal/agent/reflexes/dispatch_to_agent_generic_pass_test.go` (modified) — no dangling references, no stray scratch/temp files left behind.

**No escalations.** No instruction in this task file was ambiguous, nothing touched had zero coverage in `docs/engineering/*`, and nothing here contradicted another decided part of the design — the two judgment calls the task file explicitly left to the worker (priority ordering, `provenance_tier`) are documented above with reasoning, per the task's own framing of them as expected judgment calls rather than escalation triggers.

**Orchestrator merge note (2026-08-20):** by the time this task merged into `main`, task `02` (`team_run_members-table`) had already landed there for real (migration `129`, the actual 7-column/FK schema) — so `chat_reflex_dispatch_run_scoping_test.go`'s and `self_tools_dispatch_run_scoping_test.go`'s own test-local `CREATE TABLE team_run_members` stand-ins (item 3 above) collided at merge time with the now-real table (`SQL logic error: table team_run_members already exists`). Fixed at merge: removed both `createTestTeamRunMembers` helpers; `insertTestTeamRunMember` now inserts a real row into the real table via `Store.InsertTeamRunMember` (task `02`'s own CRUD), creating a minimal fixture `agent_profiles` row and a minimal `sessions` row inline to satisfy the table's real `agent_id`/`session_id` FKs (this test only cares about `session_id`/`workflow_run_id`, same as the original scope note said). `TestAttemptReflexDispatch_NoTeamRunMembersTable_DegradesGracefully`/`TestMatchDispatchToAgentReflex_NoTeamRunMembersTable_DegradesGracefully` no longer literally exercise `ResolveWorkflowRunIDForSession`'s "no such table" branch (both migrations now always coexist on a fresh store) — left in place since they still cover the real, adjacent "no row for this session" fail-open path, with an honest doc-comment update noting the gap rather than a false claim of continued coverage. Re-ran `go build ./cmd/nanite/`, `go vet` (same 4 pre-existing `container.go` findings only), and `go test ./...` (full repo) after the fix — all clean, no failures anywhere.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
