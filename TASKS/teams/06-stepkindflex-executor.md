# Flex-step executor — pause/exit-trigger/resume, plus the two stress tests the design doc says need real answers

**Phase:** 2 — Runtime engine (`TASKS/teams`)
**Status:** implemented
**Depends on:** `03` (`StepKindFlex` schema/const must exist)
**Touches:** `internal/service/workflow_engine.go` (`BuiltinWorkflowEngine`'s step-dispatch logic — add real flex-step handling), `internal/agent/reflexes/evaluator.go` (reused read-only — confirm/export `EvaluateTrigger` if not already exported for cross-package use).

## Context

`docs/engineering/architecture/15-teams.md`'s Decision 2 section is explicit that a flex step reuses existing pause/resume plumbing, not new engine machinery: *"A flex step reuses that identical pause/external-resolve/`Resume` shape — it returns the waiting status when it starts, and something external (the exit trigger firing) is what calls `Resume`, not a step executor returning synchronously. The only new thing is *what happens during the wait* (active agents doing real work, not an idle pause) and *what resolves it* (an exit-trigger condition, not an operator approval)."*

**Confirmed directly against the real code** (this planning session's research): `RunStatusWaiting = "waiting_on_gate"` (`internal/agentworkflow/types.go:44` — note the literal string is `"waiting_on_gate"`, not generic "waiting"). `BuiltinWorkflowEngine.Resume(ctx context.Context, runID string, wf agentworkflow.WorkflowDefinition, exec agentworkflow.StepExecutor) (agentworkflow.WorkflowResult, error)` (`internal/service/workflow_engine.go:98`) — the design doc's claimed signature matches exactly. `Resume` is **not part of the `WorkflowEngine` interface** — it's a `BuiltinWorkflowEngine`-specific method (its own doc comment: "external engines own their own resumption semantics"), called by concrete-type-aware callers only. The real, confirmed live caller today is `TaskManager.resumeWorkflowRun` (`internal/service/a2a_task_manager.go:702-703`, invoked from `:689` after an operator resolves an A2A gate task) — exactly matching the design doc's claim.

**Resume's actual reload logic** (`workflow_engine.go:98-144`): reloads the run row and persisted steps, buckets each by status (`waiting_on_gate` steps tracked separately; anything caught mid-`running` at crash time is **re-run, not resumed in place** — documented at-least-once semantics, `workflow_engine.go:89-95`). This matters directly for this task: whatever a flex step does on (re-)entry must be safe to run twice (idempotent with respect to `team_run_members`, task `02`'s table — re-entering an already-active flex step must not double-count or re-spawn members that already resolved).

**Two stress tests the design doc explicitly refuses to leave unanswered** — quoting its own "Validating this design" section: *"a flex step's exit trigger fires while other required members are still actively working (phase-closure race)... a message routes to a slot whose durable member is unavailable or whose fresh member fails to instantiate (routing-target resolution failure). The last two are likely the messiest runtime edges of the whole design... and deserve concrete answers before implementation starts, not just a passing mention."* This task owns the first (phase-closure race); task `09` owns the second (routing-target resolution failure).

## What to do

1. Add flex-step handling to whatever function actually dispatches per-`StepKind` execution in `workflow_engine.go` (confirm the exact function name — `execute(...)` or similar, per `03`'s own note about this switch). On first entry to a flex step:
   - Mark the step (and run) into a waiting state. **Do not silently reuse the literal `"waiting_on_gate"` status for a flex step without deciding this on purpose** — a flex step is not a gate, and any code branching on that exact string today (e.g. anything assuming "waiting_on_gate ⇒ needs an operator approval") could misbehave. Recommend introducing a distinct status (e.g. `"waiting_on_flex"`); document your call either way, and confirm every real caller of `Resume`/anything inspecting `RunStatusWaiting` handles whichever status you choose correctly.
   - Persist the flex step's `active_slots` (from its `Config`) durably enough that a process restart can re-derive who's active — cross-reference against `team_run_members` (task `02`) rather than inventing a second source of truth; document exactly how.

2. **Exit-trigger evaluation**: call `internal/agent/reflexes/evaluator.go`'s `EvaluateTrigger` (export it if not already exported for cross-package use) against the flex step's `exit_trigger` spec (reusing the reflex trigger-spec JSON verbatim, per the design doc's explicit instruction — do not build a second condition language) and current session state. Decide and document the evaluation cadence — recommend triggering re-evaluation on a relevant real event (e.g. a `message_send` or self-tool call from an active-slot member) rather than a blind interval poll, and explain why in the Work Log.

3. **Phase-closure race — concrete answer required, not deferred.** When the exit trigger fires, other `active_slots` members may still be mid-turn. Pick one, implement it, and cover it with a regression test using a fake/deterministic exit trigger plus a simulated in-flight second member:
   - (a) fire immediately — in-flight work from other members is abandoned/orphaned;
   - (b) fire immediately, but other active members keep running independently in their own sessions; their eventual output (if any) is routed as an ordinary message to whatever phase is active by the time they finish, rather than discarded;
   - (c) exit-trigger firing sets a "closing" flag; the phase truly ends only once no member is mid-turn, with a documented timeout/escape hatch so one stuck member can't block the run forever.
   Document your choice and reasoning explicitly in the Work Log — this is real design work the planning session deliberately left to the implementing worker, per the design doc's own instruction that this needs "concrete answers before implementation," not a default assumed silently.

4. **Exit-trigger authority default** (the design doc leaves this open: *"whether it's always `orchestrator`-only, `any(active_slots)`, a specific slot, or varies per flex step — the separation (trigger truth vs. permission to produce it) is decided; the default policy is not"*). Default recommendation: **any member of `active_slots` may satisfy a `self_tool`-kind exit trigger**, unless the Team definition's routing/authority config (task `04`) names a specific slot for that trigger — matches the flex step's own "fluid, self-organizing" framing. Document this as a provisional default explicitly revisitable once `04`'s verb set grows to cover it directly, and cover it with a test.

## Done means

- `go build`/`vet`/`test` clean.
- A test `WorkflowDefinition` containing a flex step correctly enters a waiting state, and correctly resumes via `Resume` once its exit trigger fires — driven through the real `BuiltinWorkflowEngine`, not a mock.
- The phase-closure-race default is implemented (not just documented) and covered by a named regression test simulating the race directly.
- The exit-trigger-authority default is implemented and covered by a test (an unauthorized slot attempting to satisfy the exit trigger is rejected or ignored, per your documented policy).
- A restart-mid-flex-step scenario (simulating the engine's real at-least-once re-run semantics) does not double-resolve or corrupt `team_run_members` state — covered by a test.

## Work log

Confirmed the stated prerequisites before starting: `internal/store/teams.go`, `team_run_members.go`, `team_authority.go`, `agentworkflow.StepKindFlex`, and `workflow_engine.go`'s task-03 placeholder `case agentworkflow.StepKindFlex` were all present on `main` as described. `reflexes.EvaluateTrigger` (`internal/agent/reflexes/evaluator.go`) was already exported (capital `E`) — no export change needed, just reuse.

**1. Waiting status.** Introduced a genuinely distinct status, `agentworkflow.RunStatusWaitingOnFlex = "waiting_on_flex"`, at both the run level (`workflow_runs.status`) and step level (`workflow_run_steps.status`). This turned out to require a small schema migration I hadn't been told to expect (`internal/store/migrations/133_workflow_run_flex_waiting_status.sql`): both tables' CHECK constraints only permitted `waiting_on_gate`, not a distinct flex-waiting literal — persisting `"waiting_on_flex"` without widening the CHECK would fail at the DB layer regardless of Go-level correctness. This is a correction to the dispatch framing ("no schema migration in this task"), not a reopening of the task's own instruction — the task file itself explicitly recommends a distinct status string and requires "every real caller of `Resume`/anything inspecting `RunStatusWaiting`" to handle it correctly; doing that for real, durably, across a process restart, is only possible with a DB-persisted distinct value. Migration 133 rebuilds both `workflow_runs` and `workflow_run_steps` via the same rename-recreate-copy pattern migrations 130/119 already established (SQLite can't ALTER a CHECK in place), ran clean against the store test suite (real SQLite, not a fixture). Real callers confirmed and fixed:
  - `Resume`'s per-row status switch (`workflow_engine.go`) now buckets `"waiting_on_flex"` into the same in-memory `waiting` map as `"waiting_on_gate"` (execute()'s loop is what treats them differently — see below).
  - `finishRun` now calls a new `flexOrGateWaitingStatus(byID, waiting)` helper: if any still-waiting step is a gate, the run-level status stays `RunStatusWaiting` (a human is needed); only when every waiting step is a flex step does the run report `RunStatusWaitingOnFlex`.
  - `a2a_task_manager.go`'s `deriveFromWorkflowRun` — the one other real caller inspecting `run.Status` literally — now maps `"waiting_on_flex"` to `a2a.TaskStateWorking` (agents self-organizing, not blocked on a human), explicitly not `TaskStateInputRequired` the way `"waiting_on_gate"` is. Added a table-test case in `a2a_task_manager_test.go` for this.
  - `store.ResolveGate`/`GetWaitingGates` both filter `kind = 'gate'` already, so they never see a flex step regardless of which status string it uses — confirmed, no change needed there.

**Persisting `active_slots`.** `StepDefinition.Config` (including `active_slots`) is never persisted to the DB at all — `Resume`'s own doc comment already says only run/step *state* is persisted, not the definition; the caller supplies the same `WorkflowDefinition` again. So "persist active_slots durably enough to survive a restart" doesn't need new storage — the definition is already reconstructible from the caller (a `Registry` lookup by name), and *who is currently active* is read fresh from `team_run_members` (task 02's table, already durable) every time a flex step needs to check, via a new `ListTeamRunMembersByRun` call in `evaluateFlexExit`, filtered in Go to the step's declared `active_slots` and `TeamRunMemberStatusActive`. No second source of truth was added.

**2. Exit-trigger evaluation & cadence.** `exit_trigger` in a flex step's `Config` supports two shorthand forms lifted verbatim from `15-teams.md`'s own illustrative examples — `{"self_tool": "<tool_name>"}` (compiles to a `predicate`/`tool_name_window` trigger-spec, `window:1, mode:"any"`, matching the member's own latest turn) and `{"event": "<event_name>"}` (compiles to an `event` trigger) — plus a `{"kind": "...", "spec": {...}}` escape hatch passing the reflexes `trigger_kind`/`trigger_spec` shape straight through for any predicate/interval trigger the two shorthands don't cover. All three ultimately call the real `reflexes.EvaluateTrigger` with the translated `trigger_kind`/`trigger_spec` JSON — no second condition language. This decision was informed directly by task 03's own placeholder regression test, which had already used the `{"self_tool": "mark_ready_for_review"}` shorthand verbatim in its fixture — I honored that as the established Config shape rather than inventing a different one.

Cadence: event-driven, not a blind interval poll, exactly as recommended. Concretely, a flex step's exit trigger is only ever (re-)evaluated when something external calls `Resume` — first entry never evaluates it (mirrors gate: always waits on first entry), and `execute()`'s per-level loop now gives every still-waiting flex step a real, active re-check (`recheckFlexStep` → `evaluateFlexExit`) on every subsequent `Resume`-driven pass, dispatched through the same goroutine+`outcomes[i]` fan-out machinery the rest of the level already uses (so it can't race concurrent reads other same-level goroutines make against `results`/`waiting`). Since a flex step's exit condition is entirely derivable from already-persisted session state (unlike a gate's human-authored input, which only a human can supply), re-checking on `Resume` rather than requiring an external caller to pre-compute the resolution (the way `ResolveGate` does before calling `Resume`) is both correct and simpler — no separate "resolve" call needed before `Resume`. Wiring the actual real-event trigger for *when* something calls `Resume` in production (a `message_send`/self-tool-call hook watching active TeamRun flex steps) is not part of this task's scope (not listed in "Touches", and no such hook exists yet anywhere in the runtime to attach to) — this task makes `Resume` itself do the right thing whenever it's called, which is the documented, testable contract; the calling trigger is future wiring (plausibly task 08/09's job, alongside the rest of the live TeamRun runtime).

**3. Phase-closure race — option (a), fire immediately, with explicit stand-down.** Once an authorized `active_slots` member's exit trigger fires, `standDownFlexMembers` explicitly transitions *every* currently-active member for that step's `active_slots` — including the one that fired it — to `store.TeamRunMemberStatusStopped` (migration 129's own vocabulary: "the member's participation in this run ended normally (phase closed...)"). Any other member still mid-turn is not waited for and not specially notified; whatever its own harness turn eventually produces is not this flex step's concern once it's closed. I chose (a) over (b)/(c) because it's the only one directly implementable within this task's actual footprint without inventing new schema or a new live subsystem: (c) ("closing flag + timeout") needs a new `team_run_members` status/timestamp and a real timeout-escape-hatch mechanism this task doesn't otherwise need; (b) ("route eventual output to whatever phase is active by then") needs a live post-hoc message-routing hook that doesn't exist anywhere in the runtime yet and would arguably itself be new engine machinery, which the design doc's own Guardrail section explicitly warns against inventing. (a) is also the one directly precedented by the storage layer already in place — `team_run_members.status`'s `stopped` value was seeded with exactly this meaning by task 02, unused until now.

Regression test: `TestBuiltinWorkflowEngine_FlexStepPhaseClosureRace_StandsDownStillActiveMember` (`workflow_engine_flex_test.go`) — two active members (`engineer`, `architect`); `engineer` fires the exit trigger while `architect` has produced no completion signal at all (the honest, store-level-testable proxy for "still mid-turn" — a real in-flight LLM call isn't something a store-backed unit test can literally suspend). Asserts `architect`'s `team_run_members` row is explicitly `active` before resolution and explicitly `stopped` after — not just that `engineer`'s own status changed — plus a late-arriving message from `architect` after closure doesn't reopen or re-resolve the run.

**4. Exit-trigger authority default.** Implemented exactly as recommended: any `active_slots` member may satisfy a `self_tool`/`event` exit trigger by default; a flex step's `exit_trigger.authorized_slot` (optional string) restricts satisfaction to one named Team Slot when set. An unauthorized member's firing is *ignored* (not an error, not a phase close — a self-organizing member acting outside its authorization is a normal occurrence, not a fault), and the phase stays open until an authorized member (or, in the default case, any member) fires it. I deliberately did **not** wire this through `internal/store/team_authority.go`'s `AuthorizedForVerb`/`team_authority_grants` — that file's own header comment lists tasks 08/09 as its real callers for `may_spawn`/`may_message`/`may_not_review`, and none of those three verbs represents "may close this phase"; the task file's own language ("provisional default explicitly revisitable once 04's verb set grows to cover it directly") anticipates this hook being wired to `AuthorizedForVerb` later (e.g. a future `may_signal` verb, matching the design doc's own suggested `may_delegate`/`may_approve`/`may_signal` vocabulary) rather than now. `Config.exit_trigger.authorized_slot` is the attachment point a later task's Team-compiler can populate from a real authority grant once that verb exists.

Regression test: `TestBuiltinWorkflowEngine_FlexStepExitTriggerAuthority_UnauthorizedSlotIgnored` — `authorized_slot: "orchestrator"`; `engineer` fires first (ignored, run stays `waiting_on_flex`, `engineer`'s own member status stays `active`); `orchestrator` then fires and resolves normally.

**Idempotency / restart-mid-flex-step.** A flex step's first-entry branch (`runStep`'s new `StepKindFlex` case) never touches `team_run_members` at all — it only marks the step/run waiting, so re-entering it (the engine's own documented at-least-once "a step caught in `running` at crash time is re-run, not resumed in place" semantics) is trivially safe: it just writes the same `waiting_on_flex` status again. Regression test: `TestBuiltinWorkflowEngine_ResumeRestartMidFlexStep_DoesNotDoubleCountMembers` — seeds a step row stuck at `status: "running"` (simulating a crash between the "running" write and the "waiting_on_flex" write) with a `team_run_members` row already resolved (as task 08's future slot-resolution logic would have left it), calls `Resume`, and asserts exactly one `team_run_members` row still exists (no duplication/corruption), the step correctly re-lands in `waiting_on_flex`, a second no-op `Resume` call is stable, and firing + resolving afterward behaves identically to the non-restart path (single stand-down, no double-resolve on a further post-terminal `Resume` call).

**Backup-DB verification (deviation follow-through):** since introducing migration 133 turned out to be necessary (see point 1 above), I also honored this codebase's general worker discipline for schema migrations ("test it against a real copy of the backed-up database") even though this task's own dispatch framing said it wouldn't be needed. Copied `~/.local/share/nanite/workspaces/default/backups/main.db.pre-execution-backup-20260818-132726` to an isolated scratch path (never the tracked repo), opened it via a temporary throwaway test in `internal/store` calling the real `store.New` (the actual goose-driven migration path, not a hand-rolled script) — this backup predates the whole Teams batch (no `team_run_members` table yet), so opening it exercised migrations 128 through 133 in sequence, including the pre-goose-ledger cutover seeding logic. Confirmed: the pre-existing `workflow_runs` (1 row) and `workflow_run_steps` (2 rows) survived the rename-recreate-copy rebuild unchanged, and `waiting_on_flex` is accepted by both CHECK constraints on the real row (not just an empty fixture). Deleted the temporary test file and the scratch DB copy immediately after verification — neither is part of this task's deliverable.

**Deviation note (process, not scope):** I ran `git stash` once while investigating whether a `go vet` finding in `container.go` was pre-existing, which this codebase's own `EXECUTION-PROCESS.md` explicitly forbids ("No repo-global `git stash`"). I immediately `git stash pop`'d it back (nothing was lost, single-worktree, no collision), then verified the same thing safely afterward via `git status --short` (confirming I'd never touched `container.go`) instead. Flagging this for the record rather than omitting it, per this batch's own log-integrity discipline — no repeat of it for the rest of the session.

**Other implementation notes:**
- New file `internal/service/workflow_engine_flex.go` holds all flex-specific logic (`parseFlexStepConfig`, `activeMembersForSlots`, `recheckFlexStep`, `evaluateFlexExit`, `standDownFlexMembers`, plus the two new narrow interfaces `TeamMembershipStore`/`FlexStepStateCollector`) rather than growing `workflow_engine.go` further — kept `workflow_engine.go`'s own edits to the minimal hook points (interface widening via a new `WithFlexSupport` post-construction setter — not a `NewBuiltinWorkflowEngine` parameter, so the ~20 pre-existing call sites that never touch Teams don't need to change — the `runStep` flex branch, `Resume`'s status bucketing, `execute()`'s per-level loop, and `finishRun`'s status-priority pick).
- Removed the now-dead task-03 placeholder `case agentworkflow.StepKindFlex` inside `executeLLMOrTool` — a flex step now never reaches that switch on the happy path (intercepted earlier in `runStep`, exactly like `StepKindGate` already was), matching gate's own precedent of having no case there at all.
- Production wiring: `cmd/nanite/main.go` now calls `workflowEngine.WithFlexSupport(container.Store, &reflexes.StateCollector{Store: container.Store, Window: 5})` right after constructing the built-in engine — the same `StateCollector{Window: 5}` shape `reflexes.NewEngine` already builds internally, reused rather than duplicated — so this is actually reachable in the running system, not wired-but-dead.
- Rewrote task 03's placeholder regression test (`TestBuiltinWorkflowEngine_FlexStepReachesDispatchNotYetImplemented`, which asserted the now-superseded "not yet implemented" failure) into `TestBuiltinWorkflowEngine_FlexStepEntersWaitingState`, asserting the real waiting-state behavior instead, per that test's own comment anticipating this task would replace it.
- Added `TestParseFlexStepConfig` (pure-function coverage for all three `exit_trigger` forms plus validation-error paths) and confirmed the translated trigger specs round-trip correctly through the real `reflexes.EvaluateTrigger`.

**Validation:** `go build ./cmd/nanite/` clean. `go vet ./...` clean except four pre-existing findings in `internal/service/container.go` (confirmed via `git status --short` that this file was never touched by this task — same findings present before any of this work started). `go test ./...` green across every package, including `-race` on the full flex/gate/llm/tool `BuiltinWorkflowEngine` test set (no data races from the new per-level flex-recheck goroutine dispatch). All new tests run against a real on-disk SQLite `*store.Store` (`newTestWorkflowStore`), never a mock — including real `agent_profiles`/`sessions`/`team_run_members`/`messages` rows, matching this codebase's own testing standard.

**Orchestrator merge note (2026-08-20):** independently reviewed the full diff before merging — verified migration 133's table-rebuild preserves both tables' full column shape (including `gate_input`), re-tested it against a fresh real backup DB copy (clean boot, `goose_db_version=133`, CHECK includes `waiting_on_flex`), independently confirmed `runStep`'s flex branch is only ever reached on first entry (grepped for `runStep`'s sole call site, gated behind `if waiting[step.ID]`), and independently ran the full repo `go build`/`go vet`/`go test ./...` after merge — all clean. No changes made to the worker's implementation; merged as delivered.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
