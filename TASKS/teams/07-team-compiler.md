# The Team compiler — Team definition → `WorkflowDefinition`, the design's own guardrail piece

**Phase:** 2 — Runtime engine (`TASKS/teams`)
**Status:** implemented
**Depends on:** `01` (Team definition + phase sub-structure), `02` (`team_run_members` shape), `03` (`StepKindFlex` exists), `04` (authority grants — referenced, not enforced, by the compiled definition), `05` (run-scoped reflex routing — installed alongside compilation), `06` (flex-step semantics the compiled `Config` must satisfy)
**Touches:** `internal/service/team_compiler.go` (new).

## Context

This task builds the literal center of `docs/engineering/architecture/15-teams.md`'s "Guardrail" section — quoted in full because every line of this task exists to protect it:

```
Team definition
    ↓ resolve slots
    ↓ install run-scoped routing/reflex policy
    ↓ compile phase/gate sequence into a WorkflowDefinition
    ↓ launch an ordinary WorkflowRun
    ↓ existing agents + messaging + lifecycle do the actual work
```

*"Protect that framing during implementation. If building this starts to require a `TeamExecutionEngine`, `TeamMessageBus`, `TeamScheduler`, `TeamMemory`, or `TeamGateManager` — anything that duplicates a subsystem this doc already named as reused — that's a signal the design has drifted from what's written here, not a natural extension of it."* This task **is** the "compile phase/gate sequence into a `WorkflowDefinition`" step in that diagram — nothing more. Slot resolution (the step before it) is task `08`'s job; launching the resulting `WorkflowRun` (the step after it) is also task `08`'s job, via the existing `WorkflowLauncher`. This task's function should be callable in isolation, given already-resolved inputs, and produce a plain `agentworkflow.WorkflowDefinition` value — no side effects, no I/O beyond what's needed to read the Team definition itself.

**Forward-compat requirement, from the design doc's own explicit instruction** (its "What this session did not decide" list, third bullet): *"Implementation should keep the compile-Team-phase-sequence-into-WorkflowDefinition step as one clean function/module boundary with the phase sequence treated as its own addressable sub-structure within the Team definition, even though authored together for v1 — so a later split is 'accept a phase sequence from a second source' rather than a rewrite of the compiler itself."* Concretely: this function's signature should take the phase sequence as its own distinct input (task `01`'s `phases_json` sub-structure), not the whole `Team` blob undifferentiated — so a future "one Team runs different phase-sequence shapes" split is a call-site change, not a rewrite.

**Confirmed real target shape** (`internal/agentworkflow/types.go:270-293`): `WorkflowDefinition{Name string; Engine string; Steps []StepDefinition}`. `Engine` selects `EngineBuiltin` (the default, consumes `Steps`) versus an external engine (LangGraph/CrewAI/GoogleADK/AutoGen/LangChain via `internal/workflowrunner`, which ignores `Steps` entirely). **A compiled Team definition must always set `Engine` to empty/`EngineBuiltin`** — routing a Team through an external engine would be meaningless, since none of them execute `Steps` at all; make this a hard, not-configurable choice in the compiler, not an option a caller could accidentally get wrong.

**The design doc's own illustrative compile example** (`15-teams.md`'s "Illustrative shape" section) is the concrete acceptance target:
```
scope_work (flex, active_slots: [orchestrator, engineer, architect], exit_trigger: self_tool)
  → review_gate (gate, approver_slot: reviewer)
    → address_feedback (flex, active_slots: [engineer, reviewer, architect], exit_trigger: event)
      → merge_gate (gate, approver_slot: operator — human, not a Team Slot)
```

## What to do

1. `func CompileTeam(phases []store.TeamPhase, resolvedMembers map[string][]store.TeamRunMember) (agentworkflow.WorkflowDefinition, error)` (exact signature is your call — the key constraint is that phases and resolved-member data are separate, explicit inputs, not one opaque blob) in `internal/service/team_compiler.go`.

2. Walk the phase sequence, emitting one `StepDefinition` per phase: `kind: gate` for a gate phase (`Config: {approver_slot: ...}`, matching `StepKindGate`'s existing real config shape — confirm it against `internal/service/workflow_engine.go`'s actual gate handling rather than assuming the design doc's illustrative YAML is the literal Go `Config` shape), `kind: flex` for a flex phase (`Config: {active_slots: [...], exit_trigger: {...}}`, per task `06`'s real implementation). Wire `DependsOn` in sequence order, matching the illustrative example's linear chain — a Team's phase sequence is not assumed to be non-linear in this v1, document if you find a reason it needs to be.

3. Thread resolved member identity into a flex step's `Config.active_slots`: decide and document precisely whether this holds slot *names* (resolved again against `team_run_members` at flex-step-entry time, by task `06`'s executor) or already-resolved concrete `(agent_id, session_id)` tuples baked in at compile time. Recommend slot names only, resolved live at flex-step entry — keeps the compiled `WorkflowDefinition` reusable/inspectable independent of a specific run's member churn (a `replaced`-status member from `02`'s status vocabulary shouldn't require recompiling the whole definition) — but this is a real design call, document your reasoning either way.

4. Explicitly support a **fully fluid Team** — the design doc states this is a legitimate, gate-free shape (*"A Team is not required to declare any gates at all — a fully fluid team is a legitimate, gate-free shape"*): a phase sequence of one or more flex steps with no gate steps at all must compile correctly.

## Done means

- Unit test compiling the SME example above into a `WorkflowDefinition` matching it exactly (`kind`s, `DependsOn` chain, `Config` keys present and correctly typed) — a direct, literal reproduction of the design doc's own illustrative shape, not just "something reasonable."
- Unit test compiling a fully fluid (gate-free) Team, confirming it produces a valid `WorkflowDefinition` with only `flex` steps.
- Unit test confirming the compiled definition always has `Engine` set to the builtin default, never left to a caller-suppliable value.
- This task performs no store I/O beyond reading its inputs and no slot resolution — a pure(ish) function, testable without a live database. `go build`/`vet`/`test` clean.

## Work log

Confirmed prerequisites before starting: `main` at `1214a2fd` ("TASKS/teams: task 06 reviewed clean — PASS"), tasks 01-06 all present and reviewed clean (`git log --oneline` shows the full 01-06 landing + review chain). Read `15-teams.md` in full, `internal/store/teams.go`, `internal/agentworkflow/types.go`/`validate.go`/`dag.go`, `internal/service/workflow_engine.go` (`runStep`'s gate/flex branches, `configString`/`configStringSlice`/`configMap` helpers), and `internal/service/workflow_engine_flex.go` (`parseFlexStepConfig`, `evaluateFlexExit`) directly before writing any code, per the task's own required-reading list.

**`Team.PhasesJSON` decoding design.** Added `TeamPhase` (+ `validTeamPhaseKinds`/`validateTeamPhases`/`Phases()`/`SetPhases()`) to `internal/store/teams.go`, mirroring `TeamSlotDefinition`/`Slots()`/`SetSlots()`'s exact existing pattern rather than inventing a new one or defining the type only inside `internal/service` — `teams.go`'s own `PhasesJSON` doc comment explicitly anticipated this ("task 07 defines and consumes its real shape"), and keeping decode/encode logic next to the storage type it decodes is this file's established convention. `TeamPhase` has `ID`, `Kind` ("flex"|"gate"), `ActiveSlots []string` + `ExitTrigger map[string]any` (flex-only), and `ApproverSlot string` (gate-only) — **deliberately no `depends_on`/`DependsOn` field**: per this task's own instruction ("a Team's phase sequence is not assumed to be non-linear in this v1, document if you find a reason it needs to be"), I found no such reason — the SME example is a strict linear chain, and `CompileTeam` derives each compiled step's `DependsOn` from slice order (previous phase's ID) rather than an authored field.

Deviation/correction worth recording: task 01's own merged `TestTeam_RoundTrip` fixture (`internal/store/teams_test.go`) already contains a raw `PhasesJSON` string with an explicit `depends_on` key per phase-object (`{"id":"review_gate","kind":"gate","depends_on":["scope_work"]}`, etc.) — but that string was authored pre-task-07 purely as a plausible round-trip byte string (task 01 explicitly left `PhasesJSON` undecoded/unvalidated) and is never run through any decode/validate/compile path in that test. Verified directly (scratch decode) that unmarshaling it into my `TeamPhase` (no `depends_on` field) silently drops the unknown key per Go's default `json.Unmarshal` behavior — no conflict, and `TestTeam_RoundTrip` still passes unmodified. I did **not** change that fixture or reopen task 01; noting the discrepancy per this batch's own documentation discipline, not as a defect.

Because that old fixture would fail `validateTeamPhases` (no `active_slots`/`exit_trigger`/`approver_slot`), I deliberately did **not** wire `validateTeamPhases`/`SetPhases` into `CreateTeam`/`UpdateTeam` the way `validateTeamSlots`/`SetSlots` already are — doing so would retroactively break that already-reviewed-clean, merged test outside this task's own scope. `Phases()` (decode-only, no validation, matching `Slots()`) and `SetPhases()` (validates + encodes, matching `SetSlots()`) are both implemented and available for a future caller (e.g. task 10's Team CRUD API) to opt into `SetPhases`'s validation; `CreateTeam`/`UpdateTeam` remain exactly as permissive of `PhasesJSON` as before. Confirmed by running `go test ./internal/store/...` before and after — no behavior change to any existing test.

**`CompileTeam` (`internal/service/team_compiler.go`, new).** Signature: `func CompileTeam(name string, phases []store.TeamPhase, resolvedMembers map[string][]store.TeamRunMember) (agentworkflow.WorkflowDefinition, error)`. Deviated from the task's exact suggested signature by adding a `name string` parameter (not in the task's illustrative signature) — required because `agentworkflow.Validate` (which `CompileTeam` calls internally) rejects an empty `WorkflowDefinition.Name`, and a plain string input keeps the function from needing to read any `store.Team` field beyond what it's explicitly handed (the task's own "not one opaque blob" constraint). Walks `phases` in order, emitting one `StepDefinition` per phase with `DependsOn` set to `[]string{previousPhaseID}` (empty for the first phase) — no manual duplicate-ID/unknown-reference/cycle checking added, since `agentworkflow.Validate`→`Levels` (called at the end of `CompileTeam`) already covers all three; confirmed via a dedicated duplicate-ID test that this delegation actually catches it. `Engine` is hard-set to `agentworkflow.EngineBuiltin` with **no corresponding input parameter at all** — not just a default a caller could override, but structurally impossible to set otherwise from this function's signature.

**Gate step Config shape — confirmed, not assumed.** Read `runStep`'s `StepKindGate` branch (`workflow_engine.go`) directly: on entry it unconditionally writes `status = "waiting_on_gate"` and returns — it reads **zero** keys from the gate step's `Config`. The only real resolution path today is `store.ResolveGate(runID, stepID, input)` (`internal/store/workflow_runs.go`), called exclusively by `internal/service/a2a_task_manager.go`'s `ProvideTaskInput` (A2A "input-required" flow) with an arbitrary string `input` and no approver-identity check of any kind; `GetWaitingGates` similarly filters only on `kind='gate' AND status='waiting_on_gate'`, nothing approver-related. Grepped the whole tree for `approver_slot`/`approver_role`/`GateApprover` — the only occurrences anywhere outside this task's own new code are the design doc's illustrative YAML and this task file's own prose. **Finding: `approver_slot` is not, today, read or enforced by anything in the running engine.** I still compile it into `Config["approver_slot"]` verbatim (matching the design doc's illustrative shape, and it costs nothing since `Config` is `map[string]any` and untyped by design) — documented explicitly, both in `TeamPhase.ApproverSlot`'s doc comment and `CompileTeam`'s own doc comment, as forward-compatible metadata for a not-yet-built gate-approval-enforcement mechanism, not a claim that the mechanism exists yet. This is a genuine, reportable finding, not a guess resolved silently.

**`active_slots` design call.** Chose slot **names**, not resolved `(agent_id, session_id)` tuples, exactly as the task file recommends — and confirmed this is not just the smaller-surface-area choice but the only shape that actually interoperates with task 06's already-merged `evaluateFlexExit`/`activeMembersForSlots`, which re-resolves declared slot names against a live `ListTeamRunMembersByRun` call at flex-step-entry (and on every subsequent `Resume`-driven re-check), filtering by `TeamRunMemberStatusActive` — there is no code path anywhere in the merged executor that would consume a baked-in tuple. `resolvedMembers map[string][]store.TeamRunMember` is accepted as an explicit, separate input (satisfying "phases and resolved-member data are separate, explicit inputs, not one opaque blob") but is **deliberately unconsumed** — `CompileTeam`'s compiled `Config["active_slots"]` comes only from `TeamPhase.ActiveSlots` (the phase's own authored slot names), never from `resolvedMembers`'s values. This isn't an oversight: the mere presence of `resolvedMembers` in the signature, combined with the documented decision not to bake its contents into `Config`, is what makes the design call concrete and testable rather than just asserted in prose — proven by `TestCompileTeam_ResolvedMembersUnconsumed`, which compiles the same phases with `nil` vs. a fully populated `resolvedMembers` map (including concrete `agent_id`/`session_id` values) and asserts byte-for-byte identical compiled `Config["active_slots"]` output, with an explicit check that no `agent_id`/`session_id` string ever leaks into the compiled slot-name list.

**`parseFlexStepConfig` round-trip — genuinely verified, not just shape-asserted.** `CompileTeam` itself calls `parseFlexStepConfig(step.Config)` (same package, direct call, no reimplementation) immediately after building every flex step's `Config`, and returns a compile-time error if it fails — so every `WorkflowDefinition` `CompileTeam` successfully returns is *guaranteed* parseable by task 06's real executor, not just plausible-looking. Additionally wrote `TestCompileTeam_SMEExample_FlexStepsRoundTripParseFlexStepConfig`, which independently re-parses every compiled flex step's `Config` through `parseFlexStepConfig` from the test itself and asserts `ActiveSlots`/`TriggerKind`/`TriggerSpecJSON` are populated correctly. Also added negative tests (`flex phase missing active_slots`/`missing exit_trigger`) confirming a malformed phase fails compilation via this exact round-trip path, not a separate hand-rolled check.

**Fully fluid Team support.** `CompileTeam` has no gate-related requirement anywhere in its logic — a phase sequence of only `"flex"` phases compiles cleanly. Covered by `TestCompileTeam_FullyFluidTeam` (two chained flex phases, no gates) and `TestCompileTeam_SingleFlexPhaseTeam` (the minimal one-phase case), both asserting every step is `StepKindFlex`, `Engine == EngineBuiltin`, and every flex step's `Config` round-trips through `parseFlexStepConfig`.

**Testing.** `internal/service/team_compiler_test.go` (new): `TestCompileTeam_SMEExample` (the task's literal acceptance target — reproduces 15-teams.md's SME example exactly: kinds, `DependsOn` chain, `Config` keys and types for all four steps), `TestCompileTeam_SMEExample_FlexStepsRoundTripParseFlexStepConfig`, `TestCompileTeam_FullyFluidTeam`, `TestCompileTeam_SingleFlexPhaseTeam`, `TestCompileTeam_EngineAlwaysBuiltin`, `TestCompileTeam_Errors` (7 subtests: empty name, empty phase sequence, unknown phase kind, gate missing `approver_slot`, flex missing `active_slots`/`exit_trigger` via the real round-trip, duplicate phase ids caught by `agentworkflow.Validate`), and `TestCompileTeam_ResolvedMembersUnconsumed`. No live database anywhere in this file — every test constructs `[]store.TeamPhase` literals directly, matching the task's "testable without a live database" requirement. All pass (`go test ./internal/service/... -run TestCompileTeam -v -count=1`).

**No schema migration.** Confirmed: this task touches only `internal/store/teams.go` (new Go types/methods over the existing `phases_json` TEXT column — no new column, no new table, no CHECK-constraint change) and two new files in `internal/service`. No migration added, matching the task's own expectation this time (unlike task 06, which found a real gap).

**Baseline.** `go build ./cmd/nanite/` clean. `go vet ./...` clean except the same four pre-existing, unrelated `internal/service/container.go` findings task 06's own Work Log already documented (confirmed via `git status --short internal/service/container.go` showing no changes from this task). `go test ./...` — every package green, no failures, full repo (`internal/store`, `internal/service`, `internal/agentworkflow` explicitly re-run with `-v -count=1` for the new/touched tests; `TestTeam_RoundTrip` and the rest of `internal/store`'s Team suite re-confirmed unaffected).

**Escalations:** none. No instruction ambiguity, no undocumented-anywhere gap, and no contradiction with another decided part of the design was hit — the one real "correction to the dispatch framing" (gate's `Config` shape not being enforced today) was explicitly anticipated by this task's own instruction to confirm it rather than assume it, and is documented above and in code, not escalated.

**Orchestrator merge note (2026-08-20):** independently reviewed the full diff before merging — confirmed `TestTeam_RoundTrip`'s pre-existing fixture (with its stray `depends_on` key) genuinely still passes with `TeamPhase` decoding it, confirmed the SME-example and `parseFlexStepConfig` round-trip tests are real (not vacuous), and independently ran `go build`/`go vet`/`go test ./...` after merge — all clean. No changes made to the worker's implementation; merged as delivered. The worker's own edit to `TASKS/INDEX.md` was not used — the Orchestrator's own INDEX.md update (below) supersedes it, per this project's standing "workers don't edit INDEX.md" convention.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
