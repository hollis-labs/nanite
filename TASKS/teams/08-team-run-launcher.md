# TeamRun launcher — slot resolution, `resolution: durable`'s corrected meaning, and the real launch call

**Phase:** 2 — Runtime engine (`TASKS/teams`)
**Status:** reviewed
**Depends on:** `07` (needs the compiler), `04` (`may_spawn` enforcement at resolution time)
**Touches:** `internal/service/team_run_launcher.go` (new).

## Context

**A real, load-bearing correction to the design doc — read before writing any resolution logic.** `docs/engineering/architecture/15-teams.md`'s illustrative slot config states: *"`resolution: durable | fresh` and `activation_mode: singleton | fresh-per-wake | concurrent` are not new vocabulary — they're `agents.durable`/`agents.activation_mode` as they exist today, referenced by a slot rather than redefined by one."*

This is **half right**. Confirmed directly against the real code (this planning session's research): `agent_profiles.activation_mode` (migration `074`, the file's own internal header comment mislabels itself `073` — a pre-existing, harmless drift, not something to fix here) genuinely has exactly the three claimed values — `singleton | fresh-per-wake | concurrent` — enforced in Go (`validateAgentMultiAgentFields`, `internal/store/agents.go:220-227`), not a DB CHECK. **This half of the claim holds.**

`agent_profiles.durable` (migration `073_agent_profiles_durable.sql`) is real, but its actual documented purpose is unrelated to what a Team slot's "durable" resolution needs: it **exempts a profile row from migration `061`'s reingest/eject predicate** (`source != 'internal' AND durable = 0`) — a data-retention/survival flag on a static profile record, not a runtime signal about whether an active `durable_agent_instances` row exists to wake. Treating `resolution: durable` as "read `agent_profiles.durable`" would be a category error: a slot needs to know *whether to call `DurableAgentService.Start`/`Resume` against a named instance versus construct a brand-new session via the ordinary cascade* — an operational choice about **this launch**, not a static property of the referenced profile.

**Concretely, this task must treat `resolution: durable|fresh` as new Team-Slot-level launch-time configuration** (task `01`'s `TeamSlotDefinition.Resolution` field), not a literal read of `agent_profiles.durable`. `DurableAgentService` (`internal/service/durable_agents.go:92-109`) is the real mechanism to call for a `durable` slot — no single "wake" method exists; the real methods are `Start(ctx, id, req DurableAgentStartRequest)` and `Resume(ctx, id, req DurableAgentStartRequest)` (confirm which applies given the target instance's current `status`), plus `RequestStart`/`RequestStop`/`RequestPause`/`RequestResume` for state-transition requests. Real confirmed callers of this same service today: `WorkflowLauncher` (constructor dependency, `internal/service/workflow_launch.go:86,94`) and A2A's `TaskManager.CancelTask` (`internal/service/a2a_task_manager.go:378-420`, `TargetKind == "instance"`) — exactly matching the design doc's claim that both already call it. `DurableAgentStore.EnsureSessionAgent(sessionID, agentID, mode string, isPrimary bool) error` is the real function that would back a resolved slot's `(agent_id, session_id)` tuple binding into `session_agents`-adjacent state.

For a `fresh` slot: construct via the ordinary Agent Construction cascade — `internal/service/role_cascade.go`'s `RoleOverrideConfig`/`AgentOverrideConfig`, merged into `internal/agent/override.OverrideConfig` (the real role → agent → task closest-wins cascade, `docs/engineering/architecture/01-agent-construction.md`).

**`min`/`max`/elastic resolution — deliberately narrowed for this batch, with a concrete, current reason, not just caution.** The design doc leaves open *"Whether `min`/`max`/elastic slot resolution... needs a Team-level self-tool, or reuses `nanite_execute_task`'s existing dispatch surface with a slot-name argument."* Confirmed: that self-tool's real name is `task_execute` (not `nanite_execute_task`; `internal/selftools/self_tools.go:1174`, dispatched via `internal/selftools/self_tools_dispatch.go:54`), and it is **hard-capped at recursion depth 0** — only a root/non-subagent session may call it. An orchestrator Team Slot is itself very often a dispatched (non-root) session, so the design doc's own speculated reuse path is **currently blocked** for exactly the caller who would need it to grow a slot mid-run. **This batch resolves `min` members eagerly at launch (or an invocation-time override count up to `max`, via the existing runtime-override cascade) and does not build mid-run elastic growth** — a deliberate v1 scope cut, not an oversight; a future batch revisits this once either `task_execute`'s recursion cap changes or a dedicated Team-level self-tool is built.

**`agent_id` slot-config-to-instance mapping**: the design doc's YAML example (`agent_id: nanite-architect`) reads like a `durable_agent_instances.slug`, not a raw `agent_profiles.id` — confirm and document which this task resolves against.

**Launch call**: `WorkflowLauncher`/`WorkflowLaunchRequest` (`internal/service/workflow_launch.go`) — real struct: `{WorkflowName string; Params map[string]any; ProjectID string; AgentProfileID string /* required — durable_agent_instances.profile_id is NOT NULL FK */; ParentSessionID string; TimeoutSeconds int}`. Its own doc comment (`workflow_launch.go:63-79`) confirms the design doc's "TeamRun IS a WorkflowRun" claim exactly: *"a workflow run IS a template-class durable agent, reusing durable_agents.go's instance/event-log machinery... No dedicated FK links a `workflow_runs` row back to the durable_agent instance — the link lives in the instance's `metadata_json` (`workflow_run_id`), stamped after `Run()` returns."* **This link is metadata-JSON-based, not a foreign key** — there is no existing FK pattern for this task to piggyback on for linking `team_run_members` rows to the eventual `workflow_runs.id`. Since a `team_run_members` row (task `02`) requires a non-null `workflow_run_id`, and the run doesn't exist until `WorkflowLauncher` actually runs, **document your resolved ordering explicitly** — e.g. resolve slots into a provisional/local structure first, call the launcher, then backfill `team_run_members` rows once the real `workflow_runs.id` is known, rather than assuming an ID is available up front.

## What to do

1. `func LaunchTeamRun(ctx context.Context, teamID string, overrides TeamRunOverrides) (*agentworkflow.WorkflowResult, error)` in `internal/service/team_run_launcher.go`.

2. Load the Team (task `01`), resolve each `TeamSlotDefinition`:
   - `required: false` slots (the design doc's "architect, normally dormant" example) resolve **lazily** — not at launch, but the first time a flex step's `active_slots` (task `06`) actually names them. Document precisely how this lazy trigger is implemented (a callback the flex executor invokes, a check-and-resolve-on-read at flex-step entry, etc.).
   - `durable` slots: call `DurableAgentService.Start`/`Resume` per the corrected semantics above (not a literal `agent_profiles.durable` read).
   - `fresh` slots: construct via the Agent Construction cascade.
   - `concurrent`-activation-mode, `min`/`max` slots: resolve `min` members eagerly (or an invocation-time override count between `min` and `max`, via `overrides`) — **no mid-run elastic growth in this batch**, per the concrete `task_execute` recursion-cap constraint above.

3. Insert one `team_run_members` row (task `02`) per resolved member, once the real `workflow_run_id` is known (see the ordering note above).

4. **`may_spawn` enforcement**: before resolving any slot beyond a Team's `required: true` baseline (i.e., an elastic-but-still-launch-time resolution triggered implicitly by `min`/`max` configuration, if such a case exists at launch), check task `04`'s `AuthorizedForVerb` at this actual resolution call site — not merely documented as a future integration point.

5. Call task `07`'s compiler with the resolved phase sequence, then call `WorkflowLauncher`'s real launch method (confirm its exact name/signature) with `WorkflowLaunchRequest.Params` populated from the Team's own stored defaults merged with this call's `overrides` — the same closest-wins cascade shape already standardized elsewhere in this codebase (role → agent → task; Team defaults → saved config → invocation overrides).

## Done means

- An end-to-end test launching the SME example Team (against real or fixture-seeded fresh/durable-capable test agents — never against a real tracked `.nanite/agents/*.md` file or a real production `durable_agent_instances` row, per `EXECUTION-PROCESS.md`'s live-verification-write discipline) produces a real `workflow_runs` row, correct `team_run_members` rows for every resolved slot, and reaches the first flex step in a waiting state.
- A test covering `min`-eager multi-member resolution for a `concurrent` slot.
- The corrected `resolution: durable` semantics (and the `agent_id`-to-instance mapping you resolved) are documented explicitly in the Work Log — this is the task's central de-risking finding and must not be buried in code comments alone.
- `may_spawn` enforcement is exercised by a test (an unauthorized elastic resolution attempt is rejected).
- `go build`/`vet`/`test` clean.

## Work log

**Pre-work verification.** Confirmed `01`-`07` are present and reviewed clean on this branch before starting (`internal/store/teams.go`, `team_run_members.go`, `team_authority.go`, migration 128-133, `agentworkflow.StepKindFlex`, `internal/service/workflow_engine_flex.go`, `internal/service/team_compiler.go`'s `CompileTeam`). Read `internal/service/durable_agents.go` (`DurableAgentService.Start`/`Resume`, `durableAgentInstanceFromProfile`, `reconcileProfileBackedInstances`) and `internal/service/workflow_launch.go` (`WorkflowLauncher.Launch`, `WorkflowLaunchRequest`) directly before writing any code, per the task's own instruction not to trust its paraphrase.

**What was built:**
- `internal/service/team_run_launcher.go` (new) — `TeamRunLauncher` struct (deviation from the task's illustrative bare-function signature, documented inline: four real collaborators — store, registry, `WorkflowLauncher`, `DurableAgentService` — make a struct+method the same shape `WorkflowLauncher` itself already uses, not a stylistic choice), `NewTeamRunLauncher`, `TeamRunOverrides`, `LaunchTeamRun`, `ResolveLazySlot`, plus the internal resolution machinery (`planEagerResolution`, `authorizedForElasticResolution`, `resolveMemberCount`, `resolveSlotMember`/`resolveDurableMember`/`resolveFreshMember`, `mergeTeamRunParams`, `defaultLaunchAgentProfileID`).
- `internal/agentworkflow/registry.go` — added `Registry.Register(wf WorkflowDefinition) error` plus a `sync.RWMutex` guarding `definitions` (deviation beyond the task's declared `Touches`, necessary and documented: `WorkflowLauncher.Launch` requires `l.registry.Get(req.WorkflowName)` to succeed, and the pre-existing `Registry` had no way to add a dynamically-compiled, no-YAML-file `WorkflowDefinition` at runtime — only `NewRegistry`/`LoadRegistryDir`, both load-once/read-only. `Register` is the minimal additive seam letting a compiled TeamRun definition reach `WorkflowLauncher.Launch` through the exact same `WorkflowName` lookup every YAML-authored workflow already uses. The mutex is real, not defensive-only: more than one TeamRun can plausibly launch concurrently against the one shared `*Registry` `cmd/nanite/main.go` constructs (also read by `a2a_task_manager.go`/`a2a_agent_card.go`), so `Register`/`Get`/`Names` all needed to become safe against each other — covered by `TestRegistry_Register_ConcurrentWithGet` (run under `-race`).
- `internal/store/agents.go` — added `ListAgentsByRoleID(roleID string) ([]AgentProfile, error)` (small, necessary addition: a `resolution: fresh` Team Slot's `RoleSlug` resolves to a `store.Role`, and there was no existing store function to find the concrete `agent_profiles` row(s) bound to that role via `role_id` — `CountAgentsByRoleID` existed but only returns a count, used elsewhere as a plugin-unload deletion guard).
- `internal/store/durable_agents.go` — added `GetDurableAgentInstanceByProfileID(profileID string) (*DurableAgentInstance, error)` (small, necessary addition: `DurableAgentService.Start`/`Resume` take a `durable_agent_instances.id`, but `TeamSlotDefinition.AgentID` names an `agent_profiles.id` — this is the bridge, deliberately not gated on `agent_profiles.durable`/tags, unlike `reconcileProfileBackedInstances`'s own gate).
- `internal/service/team_run_launcher_test.go` (new) and `internal/agentworkflow/registry_test.go` (extended) — regression coverage, see below.

**Corrected `resolution: durable` semantics — confirmed and implemented exactly as the task's Context describes.** `resolveDurableMember` never reads `agent_profiles.durable`. Given a Team Slot's `AgentID` (an `agent_profiles.id`, per task `01`'s own already-reviewed `TeamSlotDefinition.AgentID` doc comment — "the concrete agent_profiles.id to wake"), it: (1) confirms the profile exists (`GetAgent`); (2) looks up the bound `durable_agent_instances` row via the new `GetDurableAgentInstanceByProfileID` — **not** gated on `profileIsDurableCandidate`/`agent_profiles.durable`/`"durable-agent"` tag, deliberately, since that gate is `reconcileProfileBackedInstances`' own unrelated auto-reconcile sweep and applying it here would silently reintroduce the exact category error the task's Context warns against; (3) if no instance row exists yet, bootstraps one via the *same* `durableAgentInstanceFromProfile` shape `internal/service/durable_agents.go`'s own reconcile sweep uses (same-package reuse, not reimplemented) and calls `DurableAgentService.Create` then `Start`; (4) if an instance row already exists, calls `Resume`, falling back to `Start` on `ErrDurableAgentNoResumableSession` (an existing-but-never-launched instance) — this is the "confirm which applies given the target instance's current status" the task's Context asks for, made concrete rather than guessed. Regression: `TestResolveDurableMember_NewInstance_CallsStart_NotGatedOnDurableFlag` (profile has `Durable=false` and no tag, resolution still succeeds; a second resolve call against the same slot correctly `Resume`s the *same* session, not a new one) and `TestResolveDurableMember_ConcurrentMultiMember_Rejected` (see below).

**`agent_id`-to-instance mapping, resolved and documented as required.** `TeamSlotDefinition.AgentID` is an `agent_profiles.id` (task `01`'s own settled, already-reviewed doc comment — not relitigated here, per this batch's "distinguish decision from rationale" discipline). `resolveDurableMember` is the bridge from that profile id to the `durable_agent_instances.id` `Start`/`Resume` actually require, via the new `GetDurableAgentInstanceByProfileID` lookup (auto-create-if-missing, as above). Documented in-code (both this file's package-level doc comment and `resolveDurableMember`'s own) and here per the task's explicit instruction not to bury this finding in code comments alone.

**Concurrent multi-member durable resolution — explicitly rejected, not silently wrong.** A durable slot wakes one existing identity; `DurableAgentService.Resume`'s own `latestAttachedSession` logic would hand back the *same* already-attached session on a second call for most `LifecycleClass` session policies, silently collapsing N requested concurrent members onto one shared session. `resolveDurableMember` fails closed with a clear error (`"resolution=durable does not support concurrent multi-member resolution"`) when `ActivationMode == "concurrent" && Max > 1` rather than produce that wrong behavior — 15-teams.md's own SME example never needs this (its one durable slot, architect, is singleton), so this is a documented, deliberate scope boundary, not an oversight. Covered by `TestResolveDurableMember_ConcurrentMultiMember_Rejected`.

**Lazy resolution mechanism for `required: false` slots — a check-and-resolve-on-read callback (`ResolveLazySlot`), not a live hook into task `06`'s flex executor.** `LaunchTeamRun` classifies each Team Slot at launch: `required:true` slots resolve unconditionally; `activation_mode == "concurrent" && Min > 0` slots resolve eagerly (min, or an authorized override count) subject to `may_spawn`; every other slot (the "architect, normally dormant" shape) is left **entirely unresolved** at launch — no `team_run_members` row, nothing. `ResolveLazySlot(ctx, workflowRunID, teamID, slotName, overrides)` is the documented lazy trigger: an idempotent (existing active rows short-circuit, no double-resolve), authorization-checked (same `may_spawn` gate as launch-time elastic resolution — a lazy slot is by definition "beyond the required:true baseline") resolve-on-demand function. This task's own `Touches` scope is `team_run_launcher.go` alone, and task `06`'s already-merged, already-reviewed flex executor is deliberately **not** modified here — `evaluateFlexExit`/`activeMembersForSlots` already degrade correctly without a live hook (a lazy slot with zero `team_run_members` rows contributes zero active members, "legitimately still waiting," not an error, confirmed by re-reading `workflow_engine_flex.go` directly). `ResolveLazySlot` is this task's documented answer to "how is the lazy trigger implemented" — intended to be invoked by whichever real event actually names a dormant slot (most concretely task `09`'s routing layer: "a thin resolver from slot name to the team_run_members tuple," the first time something addresses/messages a slot with no active member yet), with wiring a live call into task `06`'s executor left as a real, flagged, but out-of-this-task's-scope follow-up. Covered by `TestResolveLazySlot_ResolvesDormantSlotAndIsIdempotent` (resolves, then confirms idempotency — same row/session on a second call) and `TestResolveLazySlot_UnauthorizedDormantSlot_Rejected`.

**A real, worth-flagging finding surfaced while building the lazy-resolution test fixture:** 15-teams.md's own SME authority example (`orchestrator.may_spawn: [engineer, reviewer]`) never names `architect` — so under this implementation's `may_spawn`-based gate (applied uniformly to every non-required resolution, eager or lazy, per the task's own item 4 instruction), the SME example's dormant architect slot is **not** actually authorized to be lazily woken by anything, as literally authored. This reads as consistent with "waking a dormant durable identity is a `may_message` concern (task `09`), not a `may_spawn` one" — the design doc's own `engineer.may_message: [architect, orchestrator, engineer]` grant is what would functionally let someone reach the architect — but it means `may_spawn` alone, as this task implements it, is not sufficient to lazily resolve the SME's own architect slot without an additional, explicitly-authored `may_spawn` grant naming it. Documented in `TestResolveLazySlot_*`'s own fixture comments (a separate, minimal Team with an explicit `may_spawn` grant for its own dormant slot) rather than silently assumed against the SME's literal authority list. Not something this task's own scope resolves further — flagging for task `09` (which owns `may_message` semantics) and any future authority-verb work (`may_signal`, per 15-teams.md's own suggested vocabulary) to pick up.

**`team_run_members`-insertion-ordering problem — solved, exactly per the task's own required resolved-ordering documentation.** `LaunchTeamRun`'s four steps, in order: (1) resolve every eagerly-planned Team Slot member into a purely local `[]resolvedTeamMember` slice — no `team_run_members` row exists yet; (2) compile the phase sequence via `CompileTeam` (needs no `workflow_run_id` either, since a flex step's `Config["active_slots"]` carries Team Slot *names*, per task `07`'s own documented design call, re-resolved live against `team_run_members` by task `06`'s executor, never a baked-in id); (3) register the compiled definition under a fresh per-launch unique name (`team-run:<team-name>:<ulid>`) and call `WorkflowLauncher.Launch` — the one call that actually produces a real `workflow_runs.id`, running the engine synchronously to completion or its first genuine wait before returning; (4) only now, with a real `WorkflowLaunchResult.RunID` in hand, backfill one `InsertTeamRunMember` row per provisionally-resolved member. This ordering is safe specifically because task `06`'s own merged implementation confirms a flex step's *first* entry never reads `team_run_members` at all (`runStep`'s flex branch only marks the step/run waiting) — `team_run_members` is only ever consulted later, on a `Resume`-driven recheck, by which point this function has already backfilled every row. Verified directly by the SME end-to-end test: the run reaches `RunStatusWaitingOnFlex` with a real `workflow_runs` row and correct `team_run_members` rows for every resolved slot.

**`may_spawn` enforcement — real, at the actual resolution call site, tested.** `planEagerResolution` runs a full authorization pre-pass (`authorizedForElasticResolution`, calling task `04`'s real `AuthorizedForVerb`) for every slot beyond the Team's `required:true` baseline **before** `LaunchTeamRun`'s Step 1 resolves a single member — a two-pass design specifically so an unauthorized Team definition never creates so much as one session/durable wake. `authorizedForElasticResolution` checks whether *any* of the Team's own `required:true` slots holds a `may_spawn` grant naming the target slot (documented reasoning in-code: nothing in the design/schema hardcodes a single "orchestrator" slot name as the sole legitimate spawner, and a Team's `required:true` baseline is exactly the set of identities guaranteed to exist at launch time to have granted such permission). Fails closed on a DB error and on zero matching grants, never defaults to true (mirrors `AuthorizedForVerb`'s own fail-closed discipline). Tested: `TestLaunchTeamRun_ElasticSlotWithoutAuthorityGrant_Rejected` (end-to-end — an elastic slot with zero authority grants at all is rejected, wrapping `ErrTeamRunElasticResolutionUnauthorized`, and zero sessions are created anywhere, confirmed by a direct `sessions` row count), `TestAuthorizedForElasticResolution_FailsClosed` (unit-level: zero grants, and a grant for the wrong verb, both fail closed), `TestResolveLazySlot_UnauthorizedDormantSlot_Rejected` (same enforcement at the lazy-resolution call site).

**End-to-end SME example test.** `TestLaunchTeamRun_SMEExample_ReachesFirstFlexStepWaiting` builds 15-teams.md's Feature Development Team verbatim (four Team Slots — architect: durable/dormant, orchestrator: fresh/required, engineer: fresh/concurrent min:1/max:4, reviewer: fresh/required — and the same four-phase sequence, `scope_work` flex → `review_gate` gate → `address_feedback` flex → `merge_gate` gate, plus the SME's own literal `orchestrator.may_spawn: [engineer, reviewer]` grants) against synthetic fixture agents/roles, launches it, and asserts: a real `workflow_runs` row exists with `status = "waiting_on_flex"`; the persisted `scope_work` step is `status = "waiting_on_flex"`; `team_run_members` has exactly one row each for orchestrator/engineer/reviewer (correct `workflow_run_id`, non-empty `agent_id`/`session_id`, `status = "active"`) and **zero** rows for architect (lazy, never resolved at launch, exactly as designed).

**Min-eager multi-member resolution test.** `TestLaunchTeamRun_ConcurrentSlot_MinEagerMultiMemberResolution` covers: default launch resolves exactly `slot.Min` (1) engineer member; an invocation-time `TeamRunOverrides.SlotMemberCounts` override between `Min` and `Max` (3, out of 1..4) resolves exactly that many `team_run_members` rows, all sharing the *same* resolved `agent_id` (a concurrent fresh slot reuses the one Agent composed from its Role, per `resolveFreshMember`'s own documented design call) with distinct, non-duplicate `session_id`s; an override above `Max` or below `Min` is rejected with a clear error, not silently clamped.

**`resolution: fresh` construction — "spawn through the ordinary Agent Construction cascade" means binding a new session to an already-composed Agent (a `role_id`-bound `agent_profiles` row), not materializing a brand-new profile row per launch.** `resolveFreshMember` resolves `RoleSlug` → `store.Role` (`GetRoleBySlug`) → the concrete `agent_profiles` row bound to it via `role_id` (`ListAgentsByRoleID`, new, first match), computes `ResolveAgentCascade(role, &profile, nil)` to seed the new session's `Provider`/`Model`, creates the session (`ContextType: "team_slot"`), and calls `EnsureSessionAgent`. The same role→agent→task scalar cascade this codebase already applies automatically, per-turn, once a session's primary agent is set (`internal/service/agent.go`'s `applyScalarCascade`, keyed off `profile.RoleID`) continues to apply on every subsequent turn without this function needing to re-invoke or persist it — documented in-code so a reviewer doesn't read this as a partial/one-shot cascade application.

**`WorkflowLaunchRequest.Params` cascade.** Confirmed and documented in-code: `teams` (task `01`'s schema) has no dedicated "default params" sub-structure distinct from the Team row itself — so the real cascade here is two layers, not the three `role→agent→task` has (`mergeTeamRunParams`'s own doc comment states this explicitly rather than silently assuming three tiers exist). Base = `{team_id, team_name}` derived from the Team row (the one real "Team default" this schema has); `TeamRunOverrides.Params` layers on top, closest-wins per key, including the ability to override `team_id`/`team_name` themselves if a caller supplies them.

**`WorkflowLaunchRequest.AgentProfileID` default.** When `overrides.AgentProfileID` is empty, defaults to the first eagerly-resolved `required:true` slot's own resolved `agent_id` (the SME's "orchestrator") — documented as the Team's own primary/organizing identity being the natural default owner of the TeamRun's audit-trail `durable_agent_instances` row when no caller-specific identity is supplied.

**No schema migration.** Confirmed: this task touches only Go code (one new file, three small additive changes to existing store/registry files) — no new table, no new column, no CHECK-constraint change. Matches the task's own expectation.

**Live-verification safety.** Every test in `team_run_launcher_test.go` and the `Registry.Register` tests constructs synthetic fixture Roles/AgentProfiles/DurableAgentInstances via the real store API against a fresh `t.TempDir()`-rooted SQLite database (`newTestWorkflowStore`, already established by task `06`'s own test file, reused directly) — no test anywhere in this diff touches a real tracked `.nanite/agents/*.md` file (no filesystem write of any kind happens in any test here) or a real production `durable_agent_instances` row. `git status --short` immediately after running the full suite confirmed only this task's intended files changed.

**Baseline checks.** `go build ./cmd/nanite/` — clean. `go vet ./...` — clean except the same four pre-existing, already-documented `internal/service/container.go` findings every prior task in this batch has flagged (`stopReaper`/`stopRuntimeReaper` possible-context-leak warnings; confirmed via `git status --short internal/service/container.go` showing no changes from this task). `go test ./...` (full repo, `-count=1`) — every package `ok`, zero `FAIL`. `go test ./internal/service/... -run "TestLaunchTeamRun|TestResolveLazySlot|TestResolveDurableMember|TestAuthorizedForElasticResolution" -race` and `go test ./internal/agentworkflow/... -run TestRegistry -race` — both clean (a full-repo `-race` run across `internal/service`/`internal/store` together hit the default 5-minute per-*package* timeout on unrelated, pre-existing tests unconnected to this diff — `TestAttemptReflexDispatch_RecurrenceOverride_SuppressesRefire`, `TestUpdateUserSettings_ToolClassifierTimeoutDefault` — a known characteristic of running the whole suite's SQLite-migration-heavy tests under `-race` instrumentation at once, not a regression from this change; the same tests pass individually without `-race` and this task's own new/touched tests pass cleanly under `-race` on their own).

**Not built here, per this task's own scope boundary (task `09`'s job):** "install run-scoped routing/reflex policy" (the Guardrail diagram's own middle step) is not touched — no `agent_reflexes.workflow_run_id` rows are created by this task, and `may_message`/`may_not_review` enforcement is not wired anywhere here. `TeamRunLauncher` is not wired into `cmd/nanite/main.go`/`container.go` — no HTTP surface calls it yet (task `11`'s job, `TASKS/teams/11-team-run-launch-api.md`), matching task `07`'s own precedent of shipping a callable-but-unwired function awaiting its real caller.

**Nothing escalated.** No instruction in this task file was ambiguous, nothing touched had zero coverage in `docs/engineering/*`, and nothing found here contradicted another decided part of the design. The judgment calls the task's own Context/What-to-do flagged as expected, real design work (lazy-resolution mechanism, `agent_id`-to-instance mapping, `may_spawn`'s "any required slot" generalization, the Params two-layer-not-three-layer finding, the durable-concurrent rejection) are documented above and in code, not escalated.

**Orchestrator merge note (2026-08-20):** independently reviewed the full diff before merging — traced `resolveDurableMember`'s corrected semantics, the `team_run_members`-insertion ordering, the `may_spawn` two-pass authorization gate, and independently ran `go build`/`go vet`/`go test ./...` (full repo, fresh/uncached) after merge — all clean. One additional concern flagged for the fresh reviewer to independently assess, not resolved here: `Registry.Register` has no corresponding `Unregister`/eviction, so every `LaunchTeamRun` call permanently grows the shared, process-lifetime `*Registry` by one entry (a per-launch-unique-named compiled `WorkflowDefinition`) that is never removed — a slow, unbounded memory-growth pattern over a long-running service's life once `TeamRunLauncher` is wired into real traffic (task `11`). Not necessarily a Phase 2 blocker (no live traffic path calls this yet), but worth a real reviewer judgment call on severity/urgency rather than silently accepting it.

## Review notes

**PASS (2026-08-20).** Fresh reviewer, no shared context with the implementing worker or the Orchestrator's own merge review. Independently re-verified: the corrected `resolution: durable` semantics never read `agent_profiles.durable`; the `may_spawn` two-pass gate genuinely creates zero sessions when unauthorized (confirmed via a direct `sessions` row count in the test, not just an error return); the `team_run_members`-insertion ordering is safe (independently re-read task `06`'s `runStep` flex branch and confirmed it never touches `team_run_members` on first entry); concurrent durable resolution is genuinely rejected; `ResolveLazySlot`'s sequential idempotency holds (same row/session on a repeat call); the SME end-to-end test's assertions are real; the architect/`may_spawn` gap finding is accurate against the design doc's own text; `Registry`'s new mutex correctly guards `Get`/`Names`/`Register` together (confirmed via an independently-reproduced `-race` run). Independent `go build`/`go vet`/`go test ./... -count=1` (full repo) and targeted `-race` runs on every new/touched test — all clean.

**Finding 1, escalated above the Orchestrator's own framing — real, reportable, actioned:** the registry-growth concern isn't just an in-memory leak — `cmd/nanite/main.go` wires the same shared `*Registry` into `AgentCardGenerator`, which enumerates `registry.Names()` into the **public** `/.well-known/agent-card.json` A2A discovery response. Once task `11` wires `LaunchTeamRun` into real traffic, every single TeamRun launch permanently pollutes an externally-visible discovery API from the very first call, not eventually. Reviewer's explicit recommendation: this must be a required "Done means" item for task `11`, not a footnote. **Actioned**: added directly to `TASKS/teams/11-team-run-launch-api.md`'s own Context and Done-means sections.

**Finding 2, non-blocking, real, threaded forward:** `ResolveLazySlot`'s idempotency claim (its own doc comment: protects against "two messages addressed to architect in close succession") is a plain check-then-act with no transaction or lock — migration `129` deliberately has no uniqueness constraint on `(workflow_run_id, slot_name)`, so nothing at the DB layer would catch two goroutines racing to lazily resolve the same singleton dormant slot at once. Currently unreachable (no caller exists yet — task `09`'s job). **Actioned**: added an explicit warning to `TASKS/teams/09-team-routing.md`'s own Context section, since task `08`'s own Context names `09`'s routing layer as the most concrete intended caller.

**Process note, self-disclosed by the reviewer, closed with zero impact:** the reviewer briefly ran `git stash -u` + `git checkout <ref> -- .` during investigation (against this project's standing rule), caught it immediately, and restored the tree cleanly — see `TASKS/ESCALATIONS.md`'s corresponding 2026-08-20 entry for the full record and independent Orchestrator confirmation of zero impact.
