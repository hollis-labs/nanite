# Team routing — explicit addressing, semantic/fallback routing, and the resolution-failure stress test

**Phase:** 3 — Routing & messaging (`TASKS/teams`)
**Status:** reviewed
**Depends on:** `05` (run-scoped `agent_reflexes`), `08` (`team_run_members` must be populated to resolve `@slot` addresses against)
**Touches:** `internal/service/team_routing.go` (new — explicit addressing resolver, semantic-rule seeding).

## Context

**Corrected self-tool name, real, easy to get wrong from the design doc's own prose:** the actual agent-facing self-tool for sending a message is **`message_send`**, not `send_message` (`internal/selftools/self_tools_transport.go:481`, `case "message_send":`, builds `messaging.SendInput`, calls `st.Messaging.SendMessage(ctx, msg)`). `send_message` is the name of the *reflex `action_kind`* (a different, related but distinct vocabulary item) — do not conflate the two when wiring explicit addressing to a real tool call.

**Corrected tuple-addressing model.** The design doc's "Slot resolution" section describes "the same tuple `internal/messaging` already addresses `agent_messages` by," implying one `(agent_id, session_id)` tuple. Confirmed directly: `agent_messages` (`internal/store/migrations/090_agent_messages_subagent_result_kind.sql`) actually has **two independent tuples** — `(from_session_id, from_agent_id)` and `(to_session_id, to_agent_id)` — each validated independently by `SendMessage` ("validates both ends of the address tuple," `internal/messaging/service.go:122`). This doesn't change what this task builds, but get the mental model right: explicit `@slot` addressing resolves the **`to_*`** tuple only; the sender's own `from_*` tuple is already known from the calling session.

**Real seed example to model a Team-scoped semantic routing rule against** (already cited in task `05`'s Context, repeated here since this is the task that actually writes these rows): `internal/agent/reflexes/seeds.go:334-352`'s `dispatch_to_agent_open_subagent` — `ActionKind: "dispatch_to_agent"`, `ActionSpec: {"agent_slug":"planner","confidence":0.75,"reason":"..."}` (shape documented `internal/store/agent_reflexes.go:45-47`). A Team's `architecture_question -> architect` routing rule (the design doc's own SME example) becomes one such row, with `workflow_run_id` set (task `05`) and `agent_slug` resolved to the concrete Team Slot's currently-resolved member.

**Multi-member addressing default — the design doc explicitly leaves this open, on purpose:** *"`@engineer` when the slot resolved to three concrete members — route-to-one, broadcast, or address-a-specific-member are all plausible; no syntax or default is chosen here, deliberately, since real usage should inform it."* This task must still make **a** concrete, working default (a Team can't ship with `@engineer` simply undefined) — treat it as provisional and flag it as such, not as a final answer to the design doc's open question.

**Provenance-tier requirement** — task `05` already established that run-scoped `dispatch_to_agent` rows must carry a `provenance_tier` that passes `ActionKindAllowsProvenanceTier` (migration `125`'s allow-list; `plugin` tier is confirmed denied for at least one kind). This task is the one that actually inserts these rows — apply task `05`'s documented choice, or make and document your own if `05` left it open.

**The second of the design doc's two explicitly-unresolved stress tests** (task `06` owns the first, phase-closure race): *"a message routes to a slot whose durable member is unavailable or whose fresh member fails to instantiate (routing-target resolution failure)... deserve concrete answers before implementation starts, not just a passing mention."*

**Routing provenance** — the design doc's "why did this message go to Architect" trace: *"the delivered message (`agent_messages`) → the reflex firing that produced it → the resolved slot → the concrete `(agent_id, session_id)` in `team_run_members`."* `EmitFirings`'s `alternatives_considered` telemetry is already general (not `dispatch_to_agent`-specific) per the reflex-taxonomy work already on `main` — this task should confirm it's queryable filtered by `workflow_run_id` (task `05`'s column), not build new persistence for it.

**`AuthorizedForVerb`'s `to_slot='self'` sharp edge, flagged by Phase 1's fresh reviewer, real for you specifically — this task is `AuthorizedForVerb`'s first real caller for `may_message`/`may_not_review`.** `internal/store/team_authority.go`'s `AuthorizedForVerb(ctx, teamID, fromSlot, verb, toSlot)` matches a stored `to_slot='self'` grant row whenever the literal string `"self"` is passed as the `toSlot` *argument* — the function's actual self-targeting check is `grantToSlot == toSlot`, which is trivially true if you ever call it with `toSlot == "self"` literally, regardless of what `fromSlot` really is. The function's real self-check (`grantToSlot == TeamAuthoritySelfSlot && fromSlot == toSlot`) only correctly detects genuine self-targeting when you pass `toSlot` as the *real resolved slot name*, not the sentinel string. **When this task calls `AuthorizedForVerb` to enforce `may_not_review`, always resolve `toSlot` to the real Team Slot name being addressed before calling — never pass the literal string `"self"` as `toSlot`.**

**`ResolveLazySlot`'s idempotency has a real check-then-act race, flagged by task `08`'s own fresh reviewer — real for you specifically, since this task is the first real candidate caller.** Task `08`'s `TeamRunLauncher.ResolveLazySlot` (`internal/service/team_run_launcher.go`) is the documented mechanism for resolving a dormant Team Slot (e.g. the SME's `architect`) the first time something addresses it — task `08`'s own Context names this task's routing layer as the most concrete intended caller ("a thin resolver from slot name to the `team_run_members` tuple, the first time something addresses/messages a slot with no active member yet"). But `ResolveLazySlot`'s idempotency (documented as protecting against "two messages addressed to the architect in close succession") is a plain read-then-write with no transaction and no application-level lock — migration `129`'s `team_run_members` table deliberately has no uniqueness constraint on `(workflow_run_id, slot_name)` (needed for concurrent-mode multi-member slots), so nothing at the DB layer would catch two goroutines racing to lazily resolve the same singleton dormant slot at once. **If this task wires a concurrent-capable call path to `ResolveLazySlot` (e.g. two routing decisions that could plausibly race for the same dormant slot), add real locking/transactional protection at that call site — do not rely on `ResolveLazySlot`'s own doc comment's idempotency claim holding under true concurrency, since it currently doesn't.** If this task's own call pattern is genuinely sequential (no real race is possible given how/when routing invokes it), document that reasoning explicitly rather than silently assuming the underlying function already handles it.

## What to do

1. **Explicit `@slot` addressing**: resolve a Team Slot name to its `team_run_members` row(s) (task `02`), then call the real `message_send` self-tool (or its underlying `messaging.SendMessage`, whichever integration point is cleaner given this code's actual caller) with the resolved `to_session_id`/`to_agent_id`. **Multi-member default: broadcast to every `active`-status member of the slot.** Document this explicitly as a provisional v1 default per the design doc's own "no syntax or default is chosen here, deliberately" framing — not a closed decision.

2. **Semantic/fallback routing**: for each Team routing rule (task `01`'s `routing_json`), insert a run-scoped `agent_reflexes` row (`workflow_run_id` set per task `05`, `action_kind='dispatch_to_agent'`, `ActionSpec.agent_slug` resolved against the target slot's currently-resolved member) at TeamRun launch time (called from task `08`, or from this task's own function that `08` calls — your call on the exact wiring boundary, document it). Add one additional lowest-priority row per Team for the coordinator fallback (`* -> orchestrator`, matching the design doc's three-tier framing) — `first_applicable`'s existing priority/`created_at` tie-break (already the live combining algorithm for this kind, no new logic needed) naturally makes this the fallback once all higher-priority run-scoped rules are checked and none matched.

3. **Routing-target-resolution-failure — concrete answer required, not deferred.** When a resolved target's `team_run_members.status != 'active'` (a durable member's underlying instance isn't in a resumable state, or a fresh member's session construction previously failed), pick one, implement it, and cover it with a test:
   - (a) the send fails loudly — surfaced back to the sender as a real tool-result error, matching `message_send`'s existing per-call validation style (recommended: matches this codebase's "hints not control, but real gates stay real gates" posture, and avoids silently masking a real failure behind an automatic reroute);
   - (b) automatically fall through to the coordinator-fallback routing rule instead of failing.
   Document your choice and reasoning in the Work Log.

4. **Provenance trace test**: write a test proving the full walk — a delivered `agent_messages` row → the `event_log` firing (via `EmitFirings`) that produced it → the resolved slot → the `team_run_members` tuple — is reconstructable end to end, filtered by `workflow_run_id`, with no new persistence layer required.

## Done means

- Explicit addressing (including the broadcast-to-multi-member default), semantic routing, and coordinator-fallback routing are each covered by a real test against a launched TeamRun fixture (from task `08`).
- The routing-target-resolution-failure stress test's chosen behavior is implemented and tested, not just documented.
- Provenance-tier choice for inserted `agent_reflexes` rows is documented and confirmed valid via a direct test.
- The provenance trace test passes.
- `go build`/`vet`/`test` clean.

## Work log

**Preconditions confirmed before writing any code.** `git log --oneline` on this worktree's `main`-derived branch confirmed tasks `01`-`08` are all merged (`37a2896f` through `fb6a0b2c`, Phase 1 "reviewed clean," task `06`/`07`/`08` each "implemented"/"reviewed clean"). `ls internal/store/migrations/ | sort -t_ -k1 -n | tail` confirmed `133_workflow_run_flex_waiting_status.sql` is the latest migration at dispatch time — this task adds none, matching the "no schema migration expected" instruction, confirmed rather than assumed. Read `docs/engineering/architecture/15-teams.md` in full, `docs/engineering/GLOSSARY.md`, `docs/engineering/EXECUTION-PROCESS.md`, and all of `internal/store/agent_reflexes.go` (`ListAgentReflexesForWorkflowRun`), `internal/store/session_team_run.go` (`ResolveWorkflowRunIDForSession`), `internal/service/team_run_launcher.go` (`TeamRunLauncher`, `ResolveLazySlot`), `internal/store/team_run_members.go`, `internal/store/team_authority.go` (`AuthorizedForVerb`), `internal/selftools/self_tools_transport.go`'s `message_send` case, `internal/messaging/service.go`'s `SendMessage`, `internal/agent/reflexes/seeds.go`'s `dispatch_to_agent_open_subagent` seed and the migrated-promptrouter seeds (for the real `user_regex_window` phrase-match trigger shape), `internal/agent/reflexes/resolve.go` (`Resolve`'s `first_applicable` tie-break), `internal/agent/reflexes/evaluator.go` (predicate kinds), `internal/agent/reflexes/telemetry.go` (`EmitFirings`), and migration `125`'s provenance-tier allow-list — all before writing code, per the task's own required-reading list.

**What was built.**

1. `internal/store/teams.go` (existing file, extended — a real, documented deviation from the task's own "Touches" line, which lists only `internal/service/team_routing.go` as new; see "Deviation" note below): `TeamRoutingRule` (`Name`, `Phrases []string`, `TargetSlot`, `Priority int64`) and `TeamRouting` (`Rules []TeamRoutingRule`, `CoordinatorSlot string`) types, decoded/encoded from/to `Team.RoutingJSON` via `Team.Routing()`/`Team.SetRouting()` — the exact shape task `01` explicitly deferred to this task ("Task `09` owns the real routing-rule shape"). Mirrors `TeamPhase`/`Phases()`/`SetPhases()`'s own precedent exactly, including the same "not wired into `CreateTeam`/`UpdateTeam`, `SetRouting` validates directly" choice, for the same reason `SetPhases`' own doc comment gives (task `01`'s already-merged `TestTeam_RoundTrip` fixture saved an unvalidated placeholder `RoutingJSON` string before this type existed).

2. `internal/service/team_routing.go` (new): `TeamRoutingService`, constructed via `NewTeamRoutingService(store, messagingService, teamRunLauncher)`. Two real entry points:
   - `SendToSlot(ctx, SendToSlotRequest) (*SendToSlotResult, error)` — explicit `@slot` addressing. Resolves `ToSlot` via `resolveActiveMembers` (below), enforces `may_message` via `authorizedForMessage` (below), then calls the real `messaging.Service.SendMessage` (the same machinery `message_send`'s `callMessageSend` calls) once per active member of the resolved slot — **broadcast to every `active`-status member**, the chosen provisional v1 multi-member default, documented explicitly in the file's own package doc comment as provisional per the design doc's own "no syntax or default is chosen here, deliberately" framing, not a closed answer.
   - `InstallTeamRunRouting(ctx, runID, teamID) ([]string, error)` — semantic + coordinator-fallback routing. For each Team-authored `TeamRoutingRule` (from `team.Routing().Rules`) and one additional structural coordinator-fallback rule, inserts one run-scoped (`workflow_run_id` set) `dispatch_to_agent` `agent_reflexes` row per currently-active, deduplicated `team_run_members.agent_id` in the run — see "Wiring boundary" and "Why per-agent_id rows" below for the reasoning.
   - Supporting: `resolveActiveMembers` (routing-target-resolution-failure + `ResolveLazySlot` concurrency fix, see below), `authorizedForMessage` (`may_message` enforcement), `resolveAgentSlugForSlot` (agent_slug resolution for a routing rule's target, live-resolved-member-preferred with a static-config fallback for a dormant target), `insertTeamRoutingReflex`, `dedupeActiveAgentIDs`, `anyPhraseRegex` (a deliberate small local copy of `seeds.go`'s own unexported helper of the same shape — not worth exporting across a package boundary for one caller).

3. `internal/service/team_routing_test.go` (new): regression coverage for every "Done means" item — see "Test coverage" below.

**Wiring boundary (task's own required decision).** `InstallTeamRunRouting` is a standalone function, NOT called from inside `TeamRunLauncher.LaunchTeamRun` — task `08`'s own file (`team_run_launcher.go`) is untouched by this task. `InstallTeamRunRouting` must be called AFTER `LaunchTeamRun` returns a real `workflow_runs.id` (it reads `team_run_members` rows that only exist post-launch, per task `08`'s own documented ordering). The intended real caller is task `11`'s future TeamRun-launch HTTP API handler (`TASKS/teams/11-team-run-launch-api.md`, not yet built), which will call `LaunchTeamRun` then `InstallTeamRunRouting` in sequence — this task's own tests use exactly that two-step composition. This is flagged explicitly, per the task file's own instruction, as a real documented follow-up need for task `11` to pick up, not a silent retrofit into task `08`'s already-reviewed file.

**Multi-member addressing default: broadcast to every active member (provisional v1).** Implemented in `SendToSlot` — every currently-`active` `team_run_members` row for the resolved slot receives its own `messaging.SendMessage` call; `SendToSlotResult.Recipients` reports a per-recipient outcome (message or error) so a partial-failure broadcast is visible to the caller rather than silently collapsed. Documented in the file's package doc comment, explicitly flagged provisional per the design doc's own "no syntax or default is chosen here, deliberately" text — not treated as this task closing that open design question. Tested by `TestSendToSlot_BroadcastsToEveryActiveMember` (3-member concurrent `engineer` slot, confirms 3 distinct sessions each receive a message with the correct `(session_id, agent_id)` tuple).

**Routing-target-resolution-failure: chose option (a), fail loudly — implemented and tested with a real simulated failure, not just documented.** `resolveActiveMembers` is the one implementation this policy runs through for both `SendToSlot` (directly) and, structurally, every future explicit-addressing caller. Behavior:
- Target slot has ≥1 `team_run_members` row but none `status='active'` → returns `ErrTeamRoutingTargetUnavailable` immediately, no retry, no fallback. **Tested with a real simulated failure**, exactly as the task's own Done-means demands: `TestSendToSlot_TargetUnavailable_NoActiveMember_FailsLoudly` launches the real SME TeamRun, takes the reviewer's real, launch-resolved `team_run_members` row, calls the real `UpdateTeamRunMemberStatus(..., "failed")` against it, then asserts `SendToSlot` returns `ErrTeamRoutingTargetUnavailable` and that no new member was silently conjured to route around the failure.
- Target slot has zero rows at all (a genuinely dormant lazy slot, e.g. the architect SME) → attempts exactly one real lazy-resolution pass via `TeamRunLauncher.ResolveLazySlot` (task `08`'s own documented mechanism, this task is its first real caller as task `08`'s own Context predicted). If that attempt itself errors (a role with no bound agent, an unauthorized elastic resolution, a durable wake failure) → same `ErrTeamRoutingTargetUnavailable`, wrapping the underlying cause. **Tested**: `TestSendToSlot_TargetUnavailable_LazyResolutionFails` builds a Team Slot (`ghost`) whose `role_slug` has zero bound `agent_profiles` rows (with a real `may_spawn` grant in place so the failure reached is genuinely the resolution failure, not an authorization rejection), confirms `SendToSlot` returns `ErrTeamRoutingTargetUnavailable`.
- The happy lazy-resolution path (dormant slot resolves successfully on first touch) is separately tested (`TestSendToSlot_DormantDurableSlot_LazilyResolvesThenSends`) so the failure tests aren't the only coverage of this code path.

Chose (a) over (b) for the reason the task file itself recommends: matches this codebase's "hints not control, but real gates stay real gates" posture, and avoids silently masking a real failure behind an automatic reroute the sender never asked for. Also noted explicitly in the file's own doc comment: this resolution-failure question is structurally specific to explicit addressing (`SendToSlot`, which resolves against a real `team_run_members` tuple at send time) — semantic/coordinator-fallback routing's own `dispatch_to_agent` firing mechanism spawns a fresh `task_execute` subagent independent of `team_run_members` state entirely (confirmed by reading `chat_reflex_dispatch.go`'s `attemptReflexDispatch` in full), so it has no equivalent "target unavailable" failure mode to apply this policy to.

**`ResolveLazySlot` concurrency race: real locking added at this call site, not assumed away.** `resolveActiveMembers` only ever reaches `ResolveLazySlot` after re-checking `team_run_members` under a real, in-process `*sync.Mutex` keyed per `(workflow_run_id, slot_name)` (`lockSlotResolution`, a `sync.Map`-backed lock table) — a genuine double-checked-locking pattern: check unlocked (fast path for the common "already active" case), then check again under the lock immediately before calling `ResolveLazySlot`, so two goroutines racing to address the same dormant slot can never both observe "zero active members" and both proceed to resolve. This call pattern IS genuinely concurrent (two `SendToSlot` calls from two different in-flight chat turns addressing the same dormant slot), so per the task's own instruction this needed real protection, not a documented "this call pattern happens to be sequential" exemption. **Tested with `-race`**: `TestResolveActiveMembers_ConcurrentLazyResolution_OnlyResolvesOnce` fires 8 concurrent goroutines at the same dormant `architect` slot and asserts exactly one `team_run_members` row (not 8) exists afterward; run via `go test ./internal/service/... -run TestResolveActiveMembers_ConcurrentLazyResolution_OnlyResolvesOnce -race` — passes clean, no data race reported.

**`AuthorizedForVerb`'s `to_slot='self'` sharp edge: resolved correctly at this task's one real call site.** `authorizedForMessage` (this task's real `may_message` enforcement, called from `SendToSlot`) always passes `toSlot` as the real, resolved Team Slot name the sender is addressing — never the literal string `"self"` anywhere in this file. **Tested directly**: `TestAuthorizedForMessage_SelfSentinel_ResolvesCorrectly` stores a `to_slot='self'` grant, confirms it correctly matches only true self-targeting (`reviewer`→`reviewer`) and does NOT accidentally match a different real target (`reviewer`→`engineer`) — the exact bug shape Phase 1's reviewer flagged. `may_not_review` has **no real call site built in this task**: routing/messaging has no natural "review" operation to gate (that's gate-approval-enforcement territory — task `07`'s own doc comment on `ApproverSlot` confirms nothing currently reads or enforces it yet, a not-yet-built mechanism). This is a deliberate, reasoned scope decision, not an oversight: the task's own "What to do"/"Done means" sections never require a `may_not_review` call site, only that *if* this task calls `AuthorizedForVerb`, it must handle the sentinel correctly — which the one real call site (`may_message`) does, tested above.

**`may_message` enforcement design (a real judgment call, documented per the task's own latitude):** opt-in allow-listing, mirroring `team_run_launcher.go`'s own `authorizedForElasticResolution` posture for `may_spawn` — presence of *any* declared `may_message` grant for the sending slot means messaging from that slot is now restricted to exactly its grant-listed targets (a real Team-authoring governance choice); absence of any declared grant for that slot means unrestricted. Chosen because the design doc is explicit that "the transport (`internal/messaging`) can and should stay generic" and an ungoverned Team is a legitimate, gate-free shape by the same document's own framing extended from gates to authority. Tested both directions: `TestAuthorizedForMessage_UngovernedSlot_Allowed` (no grants at all → allowed) and `TestAuthorizedForMessage_GovernedSlot_DeniesUnlistedTarget` (a declared grant list → only listed targets pass) plus `TestSendToSlot_NotAuthorized_RejectsSend` (confirms `SendToSlot` itself, not just the helper, surfaces `ErrTeamRoutingNotAuthorized` and sends nothing).

**Semantic/coordinator-fallback wiring detail: one reflex row per (rule × distinct active asking `agent_id`), not one global row per rule.** Read `ListAgentReflexesForWorkflowRun`'s own WHERE clause closely (`internal/store/agent_reflexes.go`): a run-scoped row only becomes a candidate for a given turn if `agent_id IS NULL AND class_tag` matches the caller's own class, OR `agent_id` equals the caller's own agent_id. Since Team Slot members can span different classes and a Team routing rule needs to apply regardless of which member is asking, the only shape that reliably satisfies "any Team member's turn sees this Team's own routing rules" is an `agent_id`-scoped row per currently-active, deduplicated asking identity (`dedupeActiveAgentIDs` — a concurrent slot's members share one `agent_id` across distinct sessions, per `resolveFreshMember`'s own doc comment, so this doesn't produce redundant rows for a concurrent slot). Documented as a known limitation in the file's own doc comment: a Team Slot resolved lazily *after* `InstallTeamRunRouting` runs (e.g. the architect SME, first addressed mid-run) does not retroactively get its own asking-side routing rows — out of this task's scope (not required by "What to do"), a real, flagged follow-up rather than a silent gap.

**Dormant target slots still get a valid `agent_slug` at install time.** `resolveAgentSlugForSlot` prefers an actually-resolved, active `team_run_members` row for the rule's target slot ("the target slot's currently-resolved member," this task's own item 2 wording) and falls back to the Team Slot's own static configuration (the durable slot's `AgentID`'s profile, or the first agent bound to a fresh slot's `RoleSlug` — the exact same lookups `team_run_launcher.go`'s `resolveDurableMember`/`resolveFreshMember` make, read-only here, no session/durable-wake side effect) when the target has no active member yet. This is what lets `architecture_question -> architect` install correctly even though the architect SME is "normally dormant" and never eagerly resolved — tested by `TestInstallTeamRunRouting_DormantTargetSlot_StillResolvesAgentSlug`, which confirms the installed row carries a real, non-empty `agent_slug` while the architect slot remains genuinely unresolved (`team_run_members` still empty) after install.

**Priority scheme (documented judgment call):** semantic rules default to `500 - 10*declaration_index` (first-declared rule wins ties, overridable per-rule via `TeamRoutingRule.Priority`); the coordinator fallback is fixed at `100`. Both are set well above the highest priority any existing global (non-run-scoped) `advisor`-class `dispatch_to_agent` seed uses today (`seeds.go`'s migrated-promptrouter seeds top out at 60), so once a session is inside a TeamRun, the Team's own three-tier routing (explicit / semantic / coordinator fallback) reliably governs instead of a coincidental global class-bound phrase match — matches task `05`'s own documented "author a higher priority value" guidance for exactly this global-vs-run-scoped tie-break question. The coordinator fallback's own trigger is `{"kind":"user_regex_window","window":1,"pattern":".*"}` — reuses the existing evaluator's `user_regex_window` predicate kind unconditionally-true rather than inventing a new "always fire" trigger kind, since `chat_reflex_dispatch.go` always populates a synthetic single-entry `State.UserMessages` window for every dispatch_to_agent evaluation, so this pattern always finds something to match against.

**Provenance tier: `operator`, applying task `05`'s own already-documented choice** (not re-deciding it) — `teamRoutingProvenanceTier = "operator"` in `team_routing.go`. Confirmed valid via a direct test: `TestInstallTeamRunRouting_ProvenanceTierIsValid` calls `store.ActionKindAllowsProvenanceTier(ctx, "dispatch_to_agent", "operator")` directly (asserts `true`) and additionally reads back every row `InstallTeamRunRouting` inserted via `GetAgentReflex`, confirming each one's persisted `ProvenanceTier` is actually `"operator"`, not just that the constant is theoretically valid.

**Provenance trace test: passes, full walk, no new persistence layer.** `TestTeamRouting_ProvenanceTrace_EndToEnd` proves the complete chain the design doc's routing section describes: (1) evaluates the real run-scoped candidate set via `ListAgentReflexesForWorkflowRun` and the real `reflexes.Resolve`, (2) emits the real unified telemetry sink via `reflexes.EmitFirings` (the identical function every production `dispatch_to_agent` call site uses) — this is the `event_log` firing, (3) sends the real delivered message via `SendToSlot` (exercising lazy resolution of the dormant architect target in the same test), (4) walks backward from the `event_log` row (`category='reflex'`, filtered by the addressing session, itself confirmed run-scoped via `ResolveWorkflowRunIDForSession`) through its `metadata.spec.team_target_slot`/`agent_slug` fields to the concrete `team_run_members` tuple via `ListTeamRunMembersBySlot`, and (5) confirms the delivered `agent_messages` row's own `(to_session_id, to_agent_id)` matches that exact tuple — closing the loop message → firing → slot → tuple → (back to) message. Every table involved (`event_log`, `agent_messages` via `internal/messaging`, `team_run_members`) already existed before this task; nothing new was persisted to make this walk possible, matching the task's own "no new persistence layer required" instruction. The one small addition that makes the walk convenient (not required, but avoids a second lookup by `agent_slug`) is `team_target_slot`, an extra key folded into `ActionSpec` alongside the standard `{agent_slug, confidence, reason}` shape — inert to every existing `dispatch_to_agent` consumer (`chat_reflex_dispatch.go`/`self_tools_dispatch.go` only ever read `agent_slug`/`confidence`/`reason`) and passed straight through into `EmitFirings`' own `traceRecord.Spec`.

**Deviation from the task's "Touches" line, documented:** `internal/store/teams.go` was extended (114 lines: `TeamRoutingRule`, `TeamRouting`, `validateTeamRouting`, `Team.Routing()`/`Team.SetRouting()`) in addition to the new `internal/service/team_routing.go` the task's "Touches" line names explicitly. This was necessary and anticipated by task `01`'s own Context ("Task `09` owns the real routing-rule shape") — `Team.RoutingJSON` needed a real decoded Go shape to build against, and `internal/store/teams.go` is where every sibling shape (`TeamSlotDefinition`, `TeamPhase`) already lives, mirroring `Phases()`/`SetPhases()`'s own precedent exactly rather than inventing a parallel decode path inside `internal/service`. No schema/migration change — purely a Go-level decode/encode of the already-existing `routing_json` column.

**Test coverage summary (`internal/service/team_routing_test.go`):** `TestSendToSlot_BroadcastsToEveryActiveMember`, `TestSendToSlot_DormantDurableSlot_LazilyResolvesThenSends`, `TestSendToSlot_TargetUnavailable_NoActiveMember_FailsLoudly`, `TestSendToSlot_TargetUnavailable_LazyResolutionFails`, `TestAuthorizedForMessage_UngovernedSlot_Allowed`, `TestAuthorizedForMessage_GovernedSlot_DeniesUnlistedTarget`, `TestSendToSlot_NotAuthorized_RejectsSend`, `TestAuthorizedForMessage_SelfSentinel_ResolvesCorrectly`, `TestResolveActiveMembers_ConcurrentLazyResolution_OnlyResolvesOnce` (run with `-race`), `TestInstallTeamRunRouting_UnknownTargetSlot_Errors`, `TestInstallTeamRunRouting_SemanticRuleAndCoordinatorFallback_InstalledAndPrioritizedCorrectly` (both a matching-phrase and a non-matching-phrase subtest, confirming the semantic rule wins on match and the coordinator fallback wins otherwise), `TestInstallTeamRunRouting_DormantTargetSlot_StillResolvesAgentSlug`, `TestInstallTeamRunRouting_ProvenanceTierIsValid`, `TestTeamRouting_ProvenanceTrace_EndToEnd`. All reuse task `08`'s own already-reviewed fixtures (`newTeamRunLauncherTestFixtures`, `createTestRoleBoundAgent`, `createTestDurableCandidateAgent`, `buildSMETeam`) unmodified, plus a small `newTestMessagingService`/`storeAgentResolver` adapter (mirrors `container.go`'s own real production wiring: `messaging.NewSQLiteStore` + `messaging.NewService`, `*store.Store` as both resolver and registrar) — no mocks below that.

**Live-verification safety.** Every test builds its own `t.TempDir()`-rooted SQLite store (`newTestWorkflowStore`, already the shared convention across this whole batch); nothing in this task ever touches a real tracked file or a real production database. `git status --short` checked after every verification pass — clean except the intended new/modified files.

**Baseline checks.** `go build ./cmd/nanite/` — clean. `go vet ./...` — clean for every file this task touches; the only findings (`internal/service/container.go`'s `stopReaper`/`stopRuntimeReaper` possible-context-leak warnings) are pre-existing and unrelated, confirmed via `git status --short` showing `container.go` untouched by this task (same pre-existing findings task `01`'s Work Log already documented). `go test ./...` — full suite passes, `internal/service` and `internal/store` both re-run with `-count=1` (bypassing cache) to confirm cleanly: `ok github.com/hollis-labs/nanite/internal/service 72.321s`, `ok github.com/hollis-labs/nanite/internal/store 11.041s`. A targeted `go test ./internal/service/... -run '<this task's new tests>' -race` run also passed clean (no data race, including the concurrency-fix regression). One diagnostic-only attempt to run `-race` across the ENTIRE (very large, heavily `t.Parallel()`) `internal/service` package hit Go's default 10-minute test-binary timeout — traced the stuck goroutines to pre-existing, untouched `TestBootDirAdapter_Untrack`/`TestBootDirAdapter_NoEntry` (`agent_bootdir_adapter_test.go`, not part of this task), confirming this is a pre-existing characteristic of running the whole package under `-race` together, not a regression introduced here; the required baseline (`go test ./...`, no `-race`) already passes cleanly and does not depend on that diagnostic run.

**Nothing escalated.** No instruction in this task file was ambiguous, nothing touched had zero coverage in `docs/engineering/*`, and nothing here contradicted another decided part of the design. The multi-member-addressing default, the routing-target-resolution-failure choice, the exact launch-time wiring boundary, the `may_message` opt-in-governance shape, and the coordinator-fallback slot-name default are all documented judgment calls the task file itself framed as expected worker latitude, not escalations.

**Orchestrator merge note (2026-08-20):** independently reviewed the full diff before merging — traced the priority-floor guard ensuring the coordinator fallback stays lowest-priority in the Team-scoped set regardless of rule count, confirmed the double-checked-locking pattern in `resolveActiveMembers`/`lockSlotResolution` is correct, and independently ran `go build`/`go vet`/`go test ./...` (full repo, fresh/uncached) plus a targeted `-race` run on every new test — all clean. No changes made to the worker's implementation; merged as delivered.

**Correction to the merge note above (2026-08-20), for the record, per this project's log-integrity discipline:** the priority-floor guard claim above was wrong — it only held for the derived-default path, not an explicit `TeamRoutingRule.Priority` override, and the fresh reviewer dispatched afterward found and reproduced this gap directly (see the "Fix addendum" section below). My own pre-merge trace missed this because I read the guard's placement inside the `priority <= 0` branch and confirmed *that* branch was correct, without separately checking whether an explicit override could bypass it entirely — a real gap in my own review, not just the implementation's. Noted here rather than silently letting the earlier claim stand uncorrected.

## Fix addendum (2026-08-20): explicit `TeamRoutingRule.Priority` override could sink to/below the coordinator-fallback floor, making the rule permanently unreachable

**Bug, confirmed by a fresh reviewer via direct reproduction (a throwaway test, since deleted).** The Orchestrator merge note above says the priority-floor guard was traced and confirmed correct — that check missed a real gap: `InstallTeamRunRouting`'s floor guard (`if priority <= coordinatorFallbackPriority { priority = coordinatorFallbackPriority + 1 }`) only ran *inside* the `priority <= 0` (derived-default) branch:

```go
priority := rule.Priority
if priority <= 0 {
    priority = semanticRulePriorityBase - int64(i)*semanticRulePriorityStep
    if priority <= coordinatorFallbackPriority {
        priority = coordinatorFallbackPriority + 1
    }
}
```

`TeamRoutingRule.Priority`'s own doc comment (`internal/store/teams.go`) explicitly invites a Team author to set it directly ("Priority optionally overrides `InstallTeamRunRouting`'s own derived default... for this rule's installed `agent_reflexes` row"), and `validateTeamRouting` never bounds it. So a Team author setting `Priority: 50` (or any value `<= coordinatorFallbackPriority`, 100) bypassed the guard entirely — the explicit value was stored as-is. Since both the semantic rule row and the structural coordinator-fallback row share `ActionKind = dispatch_to_agent`, `reflexes.Resolve`'s `first_applicable` combining algorithm (`internal/agent/reflexes/resolve.go`) groups same-`ActionKind` eligible candidates and selects the single highest-priority one (tie-break `created_at` ASC) — confirmed directly by reading `Resolve`'s `first_applicable` branch (`sorted := append([]int(nil), idxs...); sort.SliceStable(...); selected[sorted[0]] = true`), not assumed. The coordinator fallback's trigger (`{"kind":"user_regex_window","window":1,"pattern":".*"}`) always fires, so it was always eligible; at a fixed priority of `coordinatorFallbackPriority` (100), it would always outrank an explicit-priority semantic rule set at or below 100 — even on a genuine phrase match, that rule could never win. This directly contradicted this design doc's own stated invariant ("Coordinator fallback... the lowest-priority row in the same scoped set," `docs/engineering/architecture/15-teams.md`) and is exactly the "wired but never actually reachable" pattern `docs/engineering/EXECUTION-PROCESS.md`'s review criteria calls out by name.

**The fix.** Moved the floor-guard clamp so it runs unconditionally on the *final* `priority` value, after both the derived-default computation and any explicit `rule.Priority` override:

```go
priority := rule.Priority
if priority <= 0 {
    priority = semanticRulePriorityBase - int64(i)*semanticRulePriorityStep
}
if priority <= coordinatorFallbackPriority {
    priority = coordinatorFallbackPriority + 1
}
```

Kept minimal and fully localized to `InstallTeamRunRouting` in `internal/service/team_routing.go` — no other function touched, no schema change, no restructuring beyond relocating this one guard.

**Behavior chosen for an explicit-but-too-low override: silently clamp to `coordinatorFallbackPriority + 1`, matching the derived-default path's own pre-existing behavior — not a hard validation error at install time.** Both were genuinely defensible (a hard install-time rejection would surface a Team author's mistake more loudly, arguably "better" in the abstract), and this was a real judgment call, not a forced choice. Chose silent clamp for four concrete reasons, all documented in the code's own comment at the guard site:

1. **True minimality.** This is exactly what "the floor guard should apply unconditionally" means read literally — one guard, one behavior, applied uniformly to the final value regardless of which branch produced it, rather than introducing a second, install-time validation/rejection code path with its own error shape and its own test surface.
2. **Symmetry between the two priority-derivation paths.** The derived-default path already silently clamps a too-low *computed* value (e.g. many declared rules could in principle push the derived priority at/below the floor). Treating an *explicit* too-low value differently — reject instead of clamp — would make the function's two priority-derivation paths diverge in observable behavior at exactly the boundary condition that matters most (the floor itself), which is its own kind of surprising asymmetry.
3. **Consistent with this file's own established tone elsewhere.** `InstallTeamRunRouting` already silently completes other under-specified Team-authored routing input rather than hard-rejecting it (e.g. an empty `TeamRouting.CoordinatorSlot` silently defaults to `"orchestrator"`, a few lines below the fix). Hard validation in this function is reserved for a genuinely *unresolvable* reference with no sensible default or clamp available at all — a rule naming a `TargetSlot` that doesn't exist on the Team, which has no clamp equivalent (there's no "closest valid slot" to fall back to). A too-low priority, by contrast, has an obvious, well-defined, and already-precedented clamp target.
4. **It practically achieves what the Team author actually wanted.** The author's real intent in setting a high-ish explicit priority is "this rule should beat the coordinator fallback" — silently raising it to `coordinatorFallbackPriority + 1` delivers exactly that outcome. The literal number differs from what was typed, but the functional result (the rule is now genuinely reachable and wins the correct matches) is what the author was actually asking for.

**Documented trade-off, not free:** a Team author who explicitly writes `Priority: 50` believing it will win outright is not told their literal value was raised — it is silently normalized to `coordinatorFallbackPriority + 1` (101) instead. This is a real, acknowledged cost of the chosen behavior; the hard-validation-error alternative would have surfaced this to the author immediately at install time instead. Flagging here for anyone considering tightening this later (e.g. a future `agent_reflexes` inspection/audit surface could report "was this rule's priority silently adjusted at install time" as a diagnostic, without needing to revisit the underlying clamp-vs-reject choice).

**Required regression test, added:** `TestInstallTeamRunRouting_ExplicitPriorityAtOrBelowFloor_IsFloorClamped` (`internal/service/team_routing_test.go`), mirroring `TestInstallTeamRunRouting_SemanticRuleAndCoordinatorFallback_InstalledAndPrioritizedCorrectly`'s own install-then-`Resolve` approach exactly, but with an **explicit** `TeamRoutingRule.Priority: 50` (at/below `coordinatorFallbackPriority`) instead of that test's derived-default path. Confirms, against the real launched-TeamRun fixture stack (`newTeamRoutingTestFixtures`/`launchSMETeamRun`/`buildSMETeam`, unmodified):
1. The install succeeds and the semantic rule's stored `agent_reflexes.priority` is now strictly greater than `coordinatorFallbackPriority` (floor-clamped, not the raw `50` that was set on the rule).
2. The real end-to-end proof the reviewer's own reproduction used: a matching user message (`"I have an architecture question about the schema"`), run through the *actual* `reflexes.Resolve` combining logic (not a re-implementation or a stubbed decision) against the real installed candidate rows, correctly fires the semantic rule (`architecture_question`) and NOT the coordinator fallback — `applied.FiredReflexes[0].Name` and `applied.Actions[0].Spec["team_target_slot"] == "architect"` are both asserted directly.

**Test genuinely exercises the bug — verified both directions.** Before writing the final fix, the guard's relocation was temporarily reverted in place (the derived-default-only structure restored, the corrected unconditional guard disabled) and the new regression test was run against that reverted code: it failed exactly as expected (`semantic rule priority = 50, want strictly greater than coordinatorFallbackPriority (100)`), confirming the test is a real, load-bearing regression check and not a tautology. The guard was then restored to its unconditional final form (the only change kept) and the same test now passes cleanly.

**Baseline checks (this fix).** `go build ./cmd/nanite/` — clean. `go vet ./...` — clean for every file this fix touches; the only findings are the same pre-existing, unrelated `internal/service/container.go` `stopReaper`/`stopRuntimeReaper` warnings this task's own original Work Log already documented (`container.go` confirmed untouched by `git status --short`). `go test ./...` — full repo, fresh/uncached, every package `ok`, including `internal/service` (`74.923s`) and `internal/store` (`11.811s`). Targeted re-run of the existing suite named in this fix's own instructions (`TestSendToSlot|TestResolveActiveMembers|TestAuthorizedForMessage|TestInstallTeamRunRouting|TestTeamRouting`, `-count=1`) — all pass, including every pre-existing test unchanged, confirming this fix did not regress anything already reviewed clean. `gofmt -l` on both modified files — clean, no output. `git status --short` — clean except the two intended files (`internal/service/team_routing.go`, `internal/service/team_routing_test.go`; this addendum is a third).

**Scope discipline.** Fix is fully localized to `InstallTeamRunRouting`'s priority-derivation block in `internal/service/team_routing.go` (the floor-guard relocation) plus the one new regression test in `internal/service/team_routing_test.go` — no other function, file, or behavior touched. `Status:` left as `implemented` (not `reviewed`) — this fix is itself now subject to the same re-review discipline as any other worker deliverable in this batch.

**Orchestrator re-merge note (2026-08-20):** the fix worker was cut off mid-task by a transient API connection error, right after confirming its regression test correctly failed (red) against the buggy code with the real fix temporarily disabled behind a guard block — no data lost, resumed the same agent (which retained full context) to restore the fix and complete verification. Independently re-verified after resumption: read the final fix directly (the guard now applies unconditionally, exactly as described above), read the new regression test and confirmed it genuinely exercises the real `reflexes.Resolve` combining path (not just the stored priority value), and independently ran `go build`/`go vet`/`go test ./...` (full repo, fresh/uncached) after merging — all clean.

**Re-review addendum (2026-08-20): PASS, plus one small follow-up applied.** A fresh reviewer (no shared context with the fix worker or the Orchestrator) independently reproduced the bug and the fix from scratch — surgically reverted the guard to its pre-fix placement as a throwaway uncommitted change, confirmed the regression test failed red with the exact reported symptom, restored via `git checkout -- <file>`, confirmed green — then independently ran `go build`/`go vet`/`go test ./...` (all clean at the documented baseline) and grepped the repo for any other `InstallTeamRunRouting`/`TeamRoutingRule.Priority` call sites that might be affected by the behavior change (none found beyond this file). Verdict: **PASS**, fix is correct and complete.

The reviewer flagged one real, non-blocking finding: the silent-clamp path (rationale #3 in the fix comment above) logs nothing when it actually overrides an author's explicit value, which sits in tension with `docs/engineering/standards/code-quality.md`'s explicit standard against "a default that silently substitutes for the real thing, with nothing surfacing that it happened" — and this same function already has a directly analogous precedent (the coordinator-slot-not-found case, `slog.Warn` a few lines below) for surfacing exactly this class of under/mis-specified input loudly rather than silently. The reviewer correctly distinguished this from rationale #3's original comparison (silently defaulting an *omitted* field, e.g. empty `CoordinatorSlot`) — overriding an *explicitly-set* author value is the closer match to the anti-pattern the standard warns about.

Applied directly (small, low-risk, mirrors the existing `slog.Warn` precedent exactly): a `slog.Warn("team routing: explicit rule priority at or below coordinator fallback floor, raised to stay reachable", "team", ..., "rule", ..., "requested_priority", ..., "applied_priority", ...)` now fires at the clamp site, gated on `rule.Priority > 0` so it only logs when an author-supplied value was actually overridden — the derived-default path flows through the same clamp on every install and would otherwise log unconditionally for no reason. Verified the log fires with the correct fields (confirmed via `-v` test output: `team="Feature Development" rule=architecture_question requested_priority=50 applied_priority=101`) and re-ran full `go build`/`go vet`/`go test ./...` (fresh, full repo) — all clean, identical baseline. `gofmt -l` clean. The underlying judgment call (silent clamp, not a hard validation error) is unchanged and still stands on its original 4-reason rationale — this follow-up only adds observability, it does not change behavior.

## Review notes
<Reviewer fills this in: pass/fail, what was checked, anything fixed and how.>
