Invoke the blueprint-builder skill. Generate a Hadron blueprint from a natural language description.

Usage: /blueprint-builder <name> — <description>

$ARGUMENTS

If no arguments provided, ask the user for a blueprint name and description.