# Planner Special Agent Profile — STUB (Phase 4 reservation)
# CW-20260426-0016 (Arc M2 — special agent pattern catalog)
#
# This file reserves the 'planner' slug so AssignRole (internal/dispatch/role.go)
# can target PlannerRoleSlug = "planner" without surface-area changes when
# the full Planner identity lands in Phase 6 (cognition arc).
#
# Do NOT fill out the full tool surface or system prompt here — that is
# Phase 6 work. Extend this file in the Phase 6 cognition arc ticket.
#
# CW-20260512-0100: universal grounding/refusal rules are auto-injected into
# SlotSystem for every agent (see internal/chat/universal_rules.go), so even
# this stub inherits refusal-on-failure behavior before Phase 6 lands.

id: planner
name: Planner
slug: planner
description: >
  Decomposition and sequencing agent. Breaks open-scope tasks into structured,
  sequenced plans. Spawned by the Chat harness for TierOpen tasks.
  Full identity defined in Phase 6.
avatar: null
can_execute: false

system_prompt: |
  Planner role — identity TBD. Phase 6 cognition arc will define authoritative
  behavior. This stub reserves the slug for M3 reflex dispatch.

default_model: claude-sonnet-4-20250514
default_mode: default

modes:
  - slug: default
    name: Default
    description: Planning mode — decompose and sequence (stub)
    prompt_addendum: >
      Planner mode stub. Full behavior defined in Phase 6.

mcp_servers: []

tool_permissions:
  allow: []
