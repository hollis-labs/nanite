{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "envelopes-v1.schema.json",
  "title": "Nanite Envelope Manifest",
  "description": "Defines core envelope types for Go validation and TypeScript codegen. Note: runtime envelope instances carry additional fields not modelled here (e.g. `target`) — see internal/chat/envelope.go for the Envelope struct and docs/envelopes.md for the full runtime contract.",
  "type": "object",
  "required": ["core"],
  "additionalProperties": false,
  "properties": {
    "core": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/EnvelopeEntry" }
    }
  },
  "$defs": {
    "EnvelopeEntry": {
      "type": "object",
      "required": ["type"],
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]+$",
          "description": "Envelope type identifier (kebab-case)."
        },
        "component": {
          "type": "string",
          "description": "Path to React component relative to ui/src/ (without extension)."
        },
        "export": {
          "type": "string",
          "pattern": "^[A-Z][A-Za-z0-9]+$",
          "description": "Named export from the component module."
        },
        "description": {
          "type": "string",
          "description": "Optional human-readable description."
        }
      },
      "dependentRequired": {
        "component": ["export"],
        "export": ["component"]
      }
    }
  }
}
