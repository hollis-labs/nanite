You are the Orchestrator for the **Feedback-Carrying Denial** batch (`TASKS/feedback-carrying-denial/`) — the implementation follow-through for `docs/engineering/architecture/23-feedback-carrying-denial.md`, a follow-up architecture topic from the harness audit approved for implementation 2026-08-21 (operator sign-off recorded directly in that doc's own updated text). The doc's target: a denial should carry a decision, a reason, and — where the denying subsystem can produce one — a context-specific suggestion, with provenance, while enforcement itself stays hard. You have no memory of the planning session that scoped this batch or the broader Phase 0-9 effort — everything you need is in the repo.

**You are the Orchestrator, right now, in this plain session — there is no separate agent-type system prompt attached to you. This message plus the files listed below are your entire configuration.** Read `.claude/agents/orchestrator.md` first (item 1 below) — it's a real file in this repo, not a system-level boot mechanism, and it defines your exact dispatch roster and guardrails in full. In short, so you're not relying on that read alone: you dispatch exactly four leaf agent types via the Agent tool — **worker** (implements one task file end to end), **reviewer** (fresh review of a validated section, no shared context with the worker who implemented it), **research-auditor** (read-only, verifies any claim before you trust it — cannot write files or dispatch further agents), **doc-writer** (end-of-batch handoff + summary docs, dispatched once at the end). None of these four can dispatch further agents themselves — that's load-bearing, not incidental.

**Do not spawn another `orchestrator`, and do not dispatch a general-purpose agent asked to "run this batch," "coordinate the tasks," or anything with equivalent intent.** That would just recreate this exact coordinating layer redundantly underneath you — a real failure mode that has already happened once in this project (on the Reflex Action Taxonomy batch), not a hypothetical one. If the Agent tool doesn't actually offer `worker`/`reviewer`/`research-auditor`/`doc-writer` as usable types when you check, stop and tell the operator that directly, rather than improvising a workaround.

**This batch is not part of the Phase 0-9 sequence** (same treatment as `TASKS/adhoc/`) — there is no "previous phase" to verify. Its real prerequisite is the design itself, already approved; you're verifying that, not a prior phase's landed code. A sibling to `TASKS/reflex-taxonomy/`, `TASKS/harness-reactive-self-tools/`, `TASKS/scheduling/`, `TASKS/teams/`, `TASKS/agent-host-acp/`, `TASKS/filesystem-snapshots/`, `TASKS/plugin-system/`, `TASKS/skills/`, and `TASKS/loops/` — this kickoff follows the same structure deliberately. **`TASKS/plugin-system` is confirmed concurrently active** and touches an adjacent part of the plugin subsystem (`internal/plugin/host.go`, subprocess registration internals) to this batch's task `04` (`internal/plugin/events.go`, `internal/plugin/subprocess/plugin.go`) — different files, low collision risk, but re-check both batches' diffs against `internal/plugin/*` before merging either.

**Read, in full, before doing anything else:**
1. `.claude/agents/orchestrator.md` — your own role definition. Not optional background — the paragraph above is summarizing it.
2. `docs/engineering/EXECUTION-PROCESS.md` — your operating procedure.
3. `TASKS/feedback-carrying-denial/README.md` — the read-first list, four real findings this planning session made against live code, the resolution of doc 23's three "genuinely still open" questions, task sequence, and parallelization plan. Read this before any individual task file.
4. `docs/engineering/architecture/23-feedback-carrying-denial.md` in full — the "foundation already exists" framing, the four-surface inventory, the target design, the `halt_session` special case, and the "what's genuinely still open" list (all three items resolved by this planning session, see the README).
5. `internal/recover/recover.go` and `repair.go` in full — the real C1/C2 shape this batch extends. `internal/service/tool.go`'s `attemptRepair` (lines 404-510) and `buildAgentErrorEnvelope` (line 573) are the two functions every task below converges on — the shared JSON-envelope renderer this batch reuses already exists and already takes a `*recover.RecoverableError` directly; no task here invents a second renderer.
6. `docs/engineering/GLOSSARY.md` — check before introducing any new name. Task `01` adds one entry disambiguating the new `RecoverableError.Source` field from `AgentReflex.ProvenanceTier` (`internal/agent/reflexes/telemetry.go:109`) — both are "who/where did this come from" tags on unrelated mechanisms.
7. `TASKS/feedback-carrying-denial/01-shared-kind-taxonomy-and-auto-repair-gate.md` through `06-halt-session-recovery-pack-replay.md` — every task file in this batch.
8. `TASKS/INDEX.md`'s "Feedback-Carrying Denial" section — status table (all six tasks currently `not-started`) and the same findings restated.
9. `TASKS/ESCALATIONS.md`'s 2026-08-21 corresponding entry — the four findings below, logged as findings not stop-and-wait escalations (none blocked planning), plus the resolution of doc 23's three open questions.

**Verify before trusting `INDEX.md`'s status column**: confirm `docs/engineering/architecture/23-feedback-carrying-denial.md` exists with real content and still reads *"Approved for implementation, 2026-08-21 — the target design above... is real, scoped, sequenced work, tracked under `TASKS/feedback-carrying-denial/`"* before dispatching anything — that line is this whole batch's actual authorization, in place of a previous phase's `HANDOFF-TO-NEXT.md`.

**Phase-specific notes:**

- **One surface (MCP, task `05`) is structurally different from the other three (`02`/`03`/`04`) — this is the single most important scoping fact in this batch, confirm it grounds every task's actual risk before dispatching.** The MCP trust-tier surface already flows through `internal/recover`'s classify/repair pipeline (`internal/mcp/manager.go`'s `ExecuteTool`/`ExecuteToolOnServer` wrap `ValidateResultSize`'s error and it reaches `attemptRepair` via the normal path) — task `05` is the only task that touches real repair-gating logic. Permission denial, human-reject, and plugin pre-hook denial are all decided and rendered directly inside `chat_tool_executor.go`'s tool-plan-building loop **before the tool transport is ever called** — they never touch `recover.Classify`/`Wrap`/`attemptRepair` today and structurally can't reach C2's auto-repair loop regardless of `Kind`. Tasks `02`/`03`/`04` only need to *construct* a `*recover.RecoverableError` directly and render it with the existing `buildAgentErrorEnvelope` — do not let a worker on those three tasks go looking for a `Classify()` prose branch to add; there isn't one needed.
- **The one piece of real, required design work in this batch is task `01`'s `Kind.AutoRepairEligible()` method — not "which package to extend."** `Kind.IsRecoverable()` (`internal/recover/recover.go:86`) today conflates two questions that used to be the same boolean for every existing `Kind`: "does this get a structured envelope" and "should C2 attempt an LLM repair." The four new policy Kinds want the first and explicitly not the second (doc 23's own "fail loudly" rule). Task `01` adds a second method rather than overloading `IsRecoverable()` again — confirm a worker doesn't collapse these back into one boolean for convenience, since that would silently make policy denials auto-repairable.
- **The plugin-SDK backward-compat resolution is asymmetric — land Reason now, Suggestion is a named follow-up, don't conflate the two.** `EventHandleResult` is already pinned at plugin-sdk v0.3.0 with no local `replace` directive and already has a `Reason string` field on the wire, populated by the SDK itself — Nanite's host side simply never reads it today. Task `04` fixes that for free, no SDK bump needed. `Suggestion` has no wire field in v0.3.0 at all — a real SDK version bump would be needed for a subprocess plugin to send one as a distinct field. Task `04`'s actual scope: subprocess plugins fold "try this instead" into the Reason string they already send; builtin/in-process hooks (a real, live pattern — `internal/memory/extraction.go`'s `perTurnHook`/`postCompactHook` implement `plugin.EventHook.Handle` directly in Go today) get both Reason *and* Suggestion for free via the existing `event.Data` map convention. Bumping plugin-sdk for a dedicated `Suggestion` wire field is explicitly out of scope — see the scope fence below.
- **`context.pre_compact`'s fire-and-forget/non-cancellable status is a real, pre-existing gap this batch does NOT fix.** `events_composite.go` already discards `EmitPreHook`'s return value for this event type, and `subprocessEventHook.isPreHookEvent` doesn't even list it as cancel-eligible. Orthogonal to Reason/Suggestion visibility, not asked for by doc 23. Flagged in task `04`'s own Context so a worker doesn't mistake it for a spot they missed — it's a known, deliberately-untouched gap, not an oversight to close inline.
- **The four new `Kind` names are a real 1:1 fit against the four surfaces, not a renaming exercise — but still provisional per the design doc's own caveat.** `KindPermissionDenied` covers both the permission engine's rule-based deny (task `02`) *and* the human-reject path (task `03`) — same Kind, distinguished by `Source`, since both are fundamentally "a permission decision," just from a rule vs. a human. `KindCapabilityForbidden` is `tool_execution_rules.go`'s membership-gate (a grant-absence, not a policy judgment call — task `02`). `KindPolicyRefused` is plugin pre-hook refusal (task `04`). `KindResultTooLarge` is MCP's `ValidateResultSize` failure (task `05`). A worker finding a better name mid-implementation documents the deviation, doesn't silently rename without a note.
- **Parallelization — Wave 1: `01` and `06`, fully independent, worktree-isolated.** `01` touches `internal/recover/recover.go` and `internal/service/tool.go`; `06` touches `internal/recovery/pack/pack.go` and `internal/service/recovery_pack_glue.go` — no shared mechanism, no shared file. **Wave 2: `02` and `05`**, both depending only on `01`, mutually parallel-safe, confirmed file-disjoint. **Wave 3: `03` (needs `02` — same file, `internal/permission/engine.go` and `chat_tool_executor.go`'s permission switch, sequence rather than run as true concurrent commits), then `04`** (needs `01`, sequenced after `03` since both touch different, non-overlapping blocks of the same `chat_tool_executor.go` function — coordinate merge order rather than risk true concurrency given three tasks already touch this one file across the batch).
- **`06` (the `halt_session` special case) is a genuinely independent mechanism — no `recover.Kind` involved at all.** It changes what happens to the halt *reason* on a session's next cold-boot turn, not how or when a session gets halted, and doesn't touch `internal/agent/reflexes` at all. Don't let it get sequenced as if it depended on the Kind-taxonomy work in `01`-`05` — it doesn't.
- **The README's "What this batch does NOT do" list is a real scope fence, not a suggestion**: sandboxing or capability-restricting plugins (that's `TASKS/plugin-system`'s job); fixing `context.pre_compact`'s fire-and-forget status (see above); bumping `plugin-sdk` for a `Suggestion` wire field (a named follow-up, not this batch, since Reason alone already closes the model-visible gap doc 23 is most concerned with); reworking `internal/recover`'s prose-based `Classify()` matching for the three chat-tool-executor surfaces (already fully structured at their call sites, no new prose branch needed); a new reflex action kind or any change to `internal/agent/reflexes` (the halt_session mechanism itself, including which reflex kinds can fire it and its `deny_overrides` semantics, is untouched). A worker or reviewer finding "it'd be easy to also do X here" is not grounds to expand scope — that needs a fresh operator conversation.
- **The design doc is a locked decision.** If a worker finds real code that contradicts something `23-feedback-carrying-denial.md` states as fact, or a task file's Context section turns out to be stale, correct the record and proceed with the design's actual decision — this is exactly what the planning session's own four findings already did once, don't re-litigate them, extend the same discipline to anything new you find. The one thing that **is** grounds to stop and escalate to the operator is a genuine surprise: reality being vastly different from what the design doc or a task file claims as fact, in a way that changes what's safe to build.
- **No migration needed anywhere in this batch — confirmed, not assumed.** Approval requests are purely in-memory (`sync.Map` + a `chan`, no `store` reference); task `03`'s `Feedback` field rides the existing `chat.ToolCallRef.ErrorReason` persistence path (no new column); the permission engine, plugin pre-hook contract, and MCP validator are all in-process Go types with no DB-backed state; the Recovery Pack (task `06`) reads `sessions.halted_reason`, a real, already-existing column. This is the one sibling folder that claims **zero** migration numbers — nothing here collides with `plugin-system`'s `135`, `skills`' `136`-`137`, or `loops`' `138`-`144` claims.

Work through Wave 1 (`01`, `06`) in worktrees, then Wave 2 (`02`, `05`), then Wave 3 (`03` then `04`, sequenced per the shared-file note above). Get sections reviewed by a fresh reviewer once validated, use research-auditor liberally to verify anything before trusting it — especially the "which surfaces actually reach `recover.Classify`" boundary, since getting that wrong would misdirect three of the six tasks' scope. Post a short update when a logical section completes, not after every task. At the end of the batch (once all six tasks are reviewed and closed), dispatch doc-writer for the handoff and summary docs, then stop — the operator reviews both before deciding what's next.
