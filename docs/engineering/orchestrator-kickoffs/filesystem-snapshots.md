> # 🛑 DO NOT BOOT THIS — DEVELOPMENT FREEZE IN EFFECT (2026-08-21)
>
> **ALL tasks in all batches are frozen.** `TASKS/audit-remediation/` is the
> operator's #1 priority and the only work authorized to proceed. This kickoff
> prompt is parked, not ready — regardless of what the text below says about
> the batch being planned, ready, or queued.
>
> **Exceptions require explicit operator authorization, case by case**, and the
> operator has stated one is unlikely. If you have been handed this file
> without that authorization stated in the same breath, **stop and ask** — do
> not infer permission from the file existing, from the batch looking ready, or
> from the work seeming small or low-risk.
>
> **The operator is the gate for resuming.** Not a wave boundary, not a green
> test run, not `TASKS/INDEX.md` showing something complete. There is no
> derived trigger.
>
> Recorded as **AD-24** in `TASKS/audit-remediation/ARCHITECT-DECISIONS.md`,
> with the full freeze rules at the top of `TASKS/INDEX.md`. Remove this banner
> only when the operator lifts the freeze.
>
> **Copying this file as a template for a new kickoff? Do not copy this
> banner.** It applies to *this* parked batch, not to whatever you are
> writing. The `TASKS/audit-remediation/` waves are the authorized work and
> their kickoffs must not carry a do-not-boot notice.

---

You are the Orchestrator for the **Filesystem Snapshots** batch (`TASKS/filesystem-snapshots/`) — the implementation follow-through for `docs/engineering/architecture/18-filesystem-snapshots.md`, an undo/audit primitive for the filesystem state an agent's granted paths hold (shadow-git capture/diff/preview/selective-restore, per-model-step cadence, never conflated with session/conversation state). You have no memory of the planning session that scoped this batch or the broader Phase 0-9 effort — everything you need is in the repo.

**STATUS CHECK FIRST — this batch is 1/3 complete, not fresh, and not finished. Read this before anything else.**

| Task | Status |
|---|---|
| `01-filesystem-snapshot-host-mechanism` (sibling repo `libs/go-agent-wrapper`) | **implemented** — commit `5c1a343`, landed 2026-08-21 |
| `02-nanite-capture-policy-and-wiring` (Nanite) | **not-started** — this is real, remaining work |
| `03-snapshot-diff-preview-restore-api` (Nanite) | **not-started** — this is real, remaining work |

**Your actual job is landing `02` and `03`.** `01` already shipped in a previous session and does not need to be redone, re-verified from scratch as "maybe not real," or re-planned — but you should still confirm it's genuinely there (see the verification step below) before building on top of it, the same discipline you'd apply to any prior phase's claimed-done work. Do not report this batch back to the operator as "done" or "mostly done" prematurely — two of three tasks, including the entire Nanite-side wiring and the entire consumer-facing API, remain to be built.

**Why `02`/`03` were held back, and why that reason no longer applies:** `02` touches Nanite's per-turn/per-step execution loop, which at the time overlapped with `TASKS/agent-host-acp/06`'s active rewrite of `internal/runtime/agent/agent.go` — a real merge-collision risk, so the previous Orchestrator run deliberately paused here rather than proceeding. **`agent-host-acp` is now fully complete — all 24 tasks reviewed and closed, landed and pushed to `origin/main`.** That blocker is cleared. But `agent.go`'s diff from that batch was substantial (real restructuring around `wrapper.Wrapper.Run`, not a no-op, per `TASKS/INDEX.md`'s own note) — this means task `02`'s own file/function guess about where the per-step boundary lives is now stale by definition. Treat it as a pointer to re-derive from, not a fact.

**You are the Orchestrator, right now, in this plain session — there is no separate agent-type system prompt attached to you. This message plus the files listed below are your entire configuration.** Read `.claude/agents/orchestrator.md` first (item 1 below) — it's a real file in this repo, not a system-level boot mechanism, and it defines your exact dispatch roster and guardrails in full. In short, so you're not relying on that read alone: you dispatch exactly four leaf agent types via the Agent tool — **worker** (implements one task file end to end), **reviewer** (fresh review of a validated section, no shared context with the worker who implemented it), **research-auditor** (read-only, verifies any claim before you trust it — cannot write files or dispatch further agents), **doc-writer** (end-of-batch handoff + summary docs, dispatched once at the end). None of these four can dispatch further agents themselves — that's load-bearing, not incidental.

**Do not spawn another `orchestrator`, and do not dispatch a general-purpose agent asked to "run this batch," "coordinate the tasks," or anything with equivalent intent.** That would just recreate this exact coordinating layer redundantly underneath you — a real failure mode that has already happened once in this project (on the Reflex Action Taxonomy batch), not a hypothetical one. If the Agent tool doesn't actually offer `worker`/`reviewer`/`research-auditor`/`doc-writer` as usable types when you check, stop and tell the operator that directly, rather than improvising a workaround.

**This batch is not part of the Phase 0-9 sequence** (same treatment as `TASKS/adhoc/`) — there is no "previous phase" to verify. A sibling to `TASKS/reflex-taxonomy/`, `TASKS/harness-reactive-self-tools/`, `TASKS/scheduling/`, `TASKS/teams/`, `TASKS/agent-host-acp/`, `TASKS/plugin-system/`, `TASKS/skills/`, `TASKS/loops/`, `TASKS/turn-vs-run/`, `TASKS/feedback-carrying-denial/`, and `TASKS/code-mode/`.

**This batch has no `README.md` — unlike every other sibling.** It was planned and handed off mid-flight while `agent-host-acp` was still executing, and `TASKS/INDEX.md`'s own "Filesystem Snapshots" section is the closest thing to one, carrying the sequencing history and scope fences a README would normally hold. Read that section in full in place of a README — item 3 in the read list below.

**Read, in full, before doing anything else:**
1. `.claude/agents/orchestrator.md` — your own role definition. Not optional background — the paragraph above is summarizing it.
2. `docs/engineering/EXECUTION-PROCESS.md` — your operating procedure.
3. `TASKS/INDEX.md`'s "Filesystem Snapshots" section in full — the sequencing history (why `02`/`03` were held, the "Update" note confirming `01` landed and `06` landed with a substantial `agent.go` diff), and the scope fences carried forward from the architecture doc (no universal filesystem rollback; no real conflict/merge resolution beyond a lightweight hash-check; no GUI/frontend; Nanite-first only, not portfolio-shared by default).
4. `docs/engineering/architecture/18-filesystem-snapshots.md` in full — the "what this is/isn't" framing, the two-independent-axes rule (filesystem state vs. session/conversation state, never coupled), the mechanism-vs-policy host boundary, OpenCode's shadow-git reference implementation, and the settled design calls.
5. `TASKS/filesystem-snapshots/01-filesystem-snapshot-host-mechanism.md` **in full, even though it's `implemented`** — it specifies the `FilesystemSnapshotProvider` interface (`Capture`/`Diff`/`Preview`/`Restore`) directly, which `02` and `03` both consume, and the required naming discipline ("snapshot," never "checkpoint" — three different things reachable by casual use of "snapshot" in this codebase, this feature owns none of the other two).
6. `docs/engineering/GLOSSARY.md`'s **Snapshot (filesystem)** entry — confirm it's real (it should already exist, added by task `01`) before either remaining task introduces any new terminology; it explicitly disambiguates from `checkpoint` and from Glass-4 handoff's own incidental use of the word "snapshot."
7. `TASKS/filesystem-snapshots/02-nanite-capture-policy-and-wiring.md` and `03-snapshot-diff-preview-restore-api.md` — the two real remaining tasks.
8. `internal/runtime/agent/agent.go` and `internal/runtime/agent/sandbox_profile.go` (specifically `buildSandboxProfile`, lines ~24-47 as of `02`'s own authoring — re-confirm the line numbers, this file has moved) — the current, post-`agent-host-acp` real code `02` wires into and derives snapshot targets from. Read the actual current file, not `02`'s own citations, given how much churn `agent.go` had.

**Verify before dispatching `02`/`03`**: confirm `docs/engineering/architecture/18-filesystem-snapshots.md` exists with real content, and separately confirm task `01`'s claimed "implemented" status is real, not just claimed — check `libs/go-agent-wrapper` for an actual `snapshot/` package (or equivalent) at or after commit `5c1a343`, and confirm the GLOSSARY.md entry above is genuinely present. This doc has no "## Status" section and no explicit "operator-signed-off" language of its own, unlike most siblings — but `01`'s real, landed, committed implementation is itself concrete evidence this batch was already approved and dispatched once; you are resuming an in-progress batch, not seeking fresh authorization to start one. No separate operator sign-off gate is needed before proceeding, only the code-level verification above.

**Phase-specific notes:**

- **Task `02`'s per-step execution boundary needs to be re-derived from current code, not assumed from the task file's own guess.** The task file itself says "confirm the exact real step boundary at implementation time, don't assume" and names `chat_generate.go` or `agent.go` as candidates written before `agent-host-acp/06`'s rewrite — that rewrite is exactly the kind of change that could have moved or restructured the real boundary. Have the dispatched worker (or a research-auditor first, your call) trace the actual current per-model-step execution point before wiring anything.
- **Task `02`'s target derivation is a settled design call, not open for a worker to re-decide**: snapshot targets derive from the sandbox `FS.Write` allowlist (`buildSandboxProfile`'s output — `opts.Workdir`, `workspaceDir`, `bootDir`, `naniteHomeDir()`, deduped), filtered to **exclude** the ephemeral entries (`bootDir`, `naniteHomeDir()`) before deriving `Target`s. The reasoning is explicit in the task file: avoiding two independently-drifting lists (what an agent may write vs. what gets snapshotted). Don't let a worker introduce a second, separately-configured target list.
- **Capture cadence is per-model-step** (immediately before the model call, after each cleanly-completed step) — matching OpenCode's own field-tested choice, enabling undo of part of a turn, not just the whole thing.
- **Capture must apply uniformly regardless of worktree isolation — a real, deliberate requirement, not an oversight to "optimize away."** `internal/worktree.Manager` already gives worker sessions a disposable `git worktree add` checkout — a second, independent safety mechanism at a different granularity (whole-branch-discard vs. per-path-per-turn undo). A worktree-isolated session still benefits from undoing one bad turn without discarding the whole worktree. Verify `internal/worktree.Manager`'s actual current shape before wiring — the task file flags it doesn't have a verified citation for this.
- **Capture must be best-effort and must never block a turn/step — a hard requirement, not a style preference.** Wire the capture call so its own error path can never propagate into the turn/step's success/failure determination. Task `02`'s Done means requires an explicit test that forces a capture failure and confirms the turn still completes normally — confirm a reviewer actually checks for this test, not just that capture code exists.
- **Task `02`'s retention-duration config surface is its own call** — a fixed sensible default with no per-agent override is acceptable v1 scope. Don't let a worker over-build a configurable-per-agent surface nothing has asked for yet.
- **Task `03` needs both `01` and `02`** — real capture data must exist to diff/restore against, so it cannot start until `02` is reviewed clean, not just merged.
- **Task `03`'s restore endpoint must be selective/path-scoped only — no whole-tree-restore code path should exist in the request shape at all.** This is a hard constraint on the API surface itself, not just documentation — a reviewer should confirm the request type structurally cannot express a whole-tree restore, not just that the handler currently doesn't call one.
- **Task `03`'s conflict check is a lightweight hash comparison, explicitly not real merge logic** — compare the current on-disk file's hash against the snapshot's stored hash before restoring that path; warn rather than silently overwrite; require an explicit confirm-anyway flag to proceed. Real conflict/concurrency resolution beyond this is deferred (per the architecture doc, revisit once Teams sees real multi-agent concurrent usage) — don't let a worker build more than the hash-check.
- **Task `03`'s session/turn correlation parameter must stay lookup-only — never a data-layer coupling.** The architecture doc is explicit: "the UI can still correlate the two axes without the underlying data model coupling them." A reviewer should verify there's no foreign key or implicit trigger between session/turn state and `SnapshotSet` state — an optional, non-enforced reference used purely for a listing filter is the ceiling of what's allowed.
- **Task `03`'s auth/permission gating must match this codebase's comparable existing endpoints** (e.g. `/api/agents/{id}/...`, `/api/projects/{id}/...`) — the shadow store holds full file contents indefinitely until cleanup and needs equivalent access-control treatment to other host-owned artifacts. The worker must cite the specific precedent followed in their Work Log, same discipline `TASKS/teams/10-team-crud-api.md`'s own Work Log used.
- **No frontend work in either remaining task** — both are backend-only, matching this project's standing no-frontend-in-any-phase discipline. The architecture doc names the eventual GUI surface explicitly as deferred.
- **No migration needed anywhere in this batch** — task `01` is a shadow-git-based host mechanism in a sibling repo with no Nanite DB involvement; tasks `02`/`03` wire into and expose that mechanism without any new Nanite schema. Confirmed directly from all three task files — nothing here touches the concurrent cross-batch migration sequence (`plugin-system` at `135`, `skills` at `136`-`137`, `loops` at `138`-`144`).
- **Scope fence, carried forward from the architecture doc, not to be silently expanded**: no universal filesystem rollback (only sandbox-`FS.Write`-derived targets are recoverable); no real conflict/merge resolution beyond `03`'s lightweight hash-check; no GUI/frontend surface; Nanite-first only — whether Tether/Torque adopt this is a separate, later portfolio-level call, not this batch's job.
- **The design doc is a locked decision.** If a worker finds real code that contradicts something `18-filesystem-snapshots.md` states as fact, or a task file's Context turns out to be stale (very possible for `02` given `agent.go`'s churn — see above), correct the record and proceed with the design's actual decision. The one thing that **is** grounds to stop and escalate to the operator is a genuine surprise: reality being vastly different from what the design doc or a task file claims as fact, in a way that changes what's safe to build.

Confirm `01`'s real, landed state first, then dispatch `02` (worktree-isolated), get it reviewed clean, then dispatch `03` once `02` is confirmed solid. Both remaining tasks are sequential, not parallel — `03` has a real data dependency on `02`, not just a sequencing preference. Use research-auditor to trace `agent.go`'s actual current per-step boundary before `02`'s worker starts, given how stale the task file's own guess is likely to be. Post a short update when each task completes. At the end of the batch (once `02` and `03` are both reviewed and closed), dispatch doc-writer for the handoff and summary docs — covering all three tasks, not just the two you ran, since `01` is part of this batch's real deliverable — then stop — the operator reviews both before deciding what's next.
