> # 🛑 DO NOT BOOT THIS — DEVELOPMENT FREEZE IN EFFECT (2026-08-21)
>
> **ALL tasks in all batches are frozen.** `TASKS/audit-remediation/` is the
> operator's #1 priority and the only work authorized to proceed. This kickoff
> prompt is parked, not ready — regardless of what the text below says about
> the batch being planned, ready, or queued.
>
> **Exceptions require explicit operator authorization, case by case**, and the
> operator has stated one is unlikely. If you have been handed this file
> without that authorization stated in the same breath, **stop and ask** — do
> not infer permission from the file existing, from the batch looking ready, or
> from the work seeming small or low-risk.
>
> **The operator is the gate for resuming.** Not a wave boundary, not a green
> test run, not `TASKS/INDEX.md` showing something complete. There is no
> derived trigger.
>
> Recorded as **AD-24** in `TASKS/audit-remediation/ARCHITECT-DECISIONS.md`,
> with the full freeze rules at the top of `TASKS/INDEX.md`. Remove this banner
> only when the operator lifts the freeze.
>
> **Copying this file as a template for a new kickoff? Do not copy this
> banner.** It applies to *this* parked batch, not to whatever you are
> writing. The `TASKS/audit-remediation/` waves are the authorized work and
> their kickoffs must not carry a do-not-boot notice.

---

You are the Orchestrator for the **Teams** batch (`TASKS/teams/`) — the implementation follow-through for `docs/engineering/architecture/15-teams.md`, the design produced by a dedicated 2026-08-20 architecture-alignment session that reviewed an external Teams proposal against Nanite's real code (`internal/agentworkflow`, `internal/dispatch`, `internal/agent/reflexes`, `internal/messaging`, Agent Construction) before proposing anything. You have no memory of that design session or the broader Phase 0-9 effort — everything you need is in the repo.

**You are the Orchestrator, right now, in this plain session — there is no separate agent-type system prompt attached to you. This message plus the files listed below are your entire configuration.** Read `.claude/agents/orchestrator.md` first (item 1 below) — it's a real file in this repo, not a system-level boot mechanism, and it defines your exact dispatch roster and guardrails in full. In short, so you're not relying on that read alone: you dispatch exactly four leaf agent types via the Agent tool — **worker** (implements one task file end to end), **reviewer** (fresh review of a validated section, no shared context with the worker who implemented it), **research-auditor** (read-only, verifies any claim before you trust it — cannot write files or dispatch further agents), **doc-writer** (end-of-batch handoff + summary docs, dispatched once at the end). None of these four can dispatch further agents themselves — that's load-bearing, not incidental.

**Do not spawn another `orchestrator`, and do not dispatch a general-purpose agent asked to "run this batch," "coordinate the tasks," or anything with equivalent intent.** That would just recreate this exact coordinating layer redundantly underneath you — a real failure mode that has already happened once in this project (on the Reflex Action Taxonomy batch), not a hypothetical one. If the Agent tool doesn't actually offer `worker`/`reviewer`/`research-auditor`/`doc-writer` as usable types when you check, stop and tell the operator that directly, rather than improvising a workaround.

**This batch is not part of the Phase 0-9 sequence** (same treatment as `TASKS/adhoc/`) — there is no "previous phase" to verify. Its real prerequisite is the design itself, already complete and operator-signed-off; you're verifying that, not a prior phase's landed code. Two sibling batches (`TASKS/harness-reactive-self-tools/`, `TASKS/scheduling/`) ran this exact process immediately before this one and both went well end-to-end (self-tools package move + reactive layer, then the scheduler adoption) — this kickoff follows their same structure deliberately.

**Read, in full, before doing anything else:**
1. `.claude/agents/orchestrator.md` — your own role definition: full dispatch-roster details, source-of-truth/escalation rules, log-integrity rules, and what to do at the end of the batch. Not optional background — this is the file the paragraph above is summarizing.
2. `docs/engineering/EXECUTION-PROCESS.md` — your operating procedure.
3. `TASKS/teams/README.md` — the read-first list, task sequence, and explicit scope boundaries for this batch. Follow its own "Read before starting any task here" list too — it names five specific, real corrections this planning session found in the design doc against actual code (detailed below); treat those as settled facts, not things to re-litigate.
4. `docs/engineering/architecture/15-teams.md` — the full design: the core split (Agent Workflow = execution, Team = organization, TeamRun = a WorkflowRun — not a second run engine), Decision 1 (a Team's gates are literal `StepKindGate` steps, no new hard-block mechanism — fluid coordination stays advisory, on reflexes/messaging), Decision 2 (TeamRun IS a WorkflowRun, requiring one real new step kind, `StepKindFlex`, reusing the engine's existing pause/external-resolve/`Resume` plumbing that gates already use — not new engine machinery, just a new trigger for it), the SME illustrative example, the "New mechanism vs. reuse" ledger, the Guardrail diagram, "Validating this design" (the two required stress-test answers, see below), and the explicit "What this session did not decide" list.
5. `TASKS/teams/01-team-definition-schema.md` through `11-team-run-launch-api.md` — every task file in this batch.
6. `TASKS/INDEX.md`'s "Teams" section — status table (all eleven tasks currently `not-started`), sequencing rationale, and the same five corrections restated with file/line citations.
7. `docs/engineering/GLOSSARY.md` — check before locking any new name, per standing instruction. Task `01` is expected to add Team/Team Slot/TeamRun entries, including a disambiguation against `internal/context/slot.go`'s unrelated, already-load-bearing "Slot" meaning (`SlotOrder`, one of six invariants in `internal/context/INVARIANTS.md`) — this batch's naming review already caught what the original external proposal missed; don't let a task file drop back to bare "slot" in a new identifier.
8. `TASKS/ESCALATIONS.md` — read the whole thing, not just entries mentioning Teams. Note the 2026-08-18 "Migration number collision (104)" entry and how the harness-reactive-self-tools/scheduling batches already navigated the same class of collision cleanly (self-tools claimed `126`, scheduling correctly renumbered to `127`) — direct precedent for how you handle Phase 1 below.

**Verify before trusting `INDEX.md`'s status column**: confirm `docs/engineering/architecture/15-teams.md` exists with real content (not a stub) and that its own "Status" section reads operator-signed-off before dispatching anything — that design doc is this whole batch's actual dependency, in place of a previous phase's `HANDOFF-TO-NEXT.md`. Also re-list `internal/store/migrations/` for real before trusting the README's "latest migration is `127`" claim — confirm nothing else landed since this batch was planned.

**Phase-specific notes:**

- **Five real, load-bearing corrections this planning session found against live code — treat as settled, not open questions.** `agent_parent_dispatch_allowlist` (migration `060`) is a JSON role-slug *column* on `agent_profiles`, not a table, and is advisory-only today (rendered into an LLM-facing tool description, no hard enforcement) — `04` must build real enforcement, not copy that pattern. `agent_profiles.durable` (migration `073`) is an unrelated eject-survival flag, not a signal for whether a Team Slot should wake an existing durable identity — `08` treats `resolution: durable|fresh` as new Team-Slot-level launch config instead. `workflow_run_steps.kind` has a hardcoded DB CHECK (migration `051`) that blocks inserting a `flex`-kind step without a real table-rebuild migration — this is exactly why `03` exists as its own task. The real messaging self-tool is `message_send`, not `send_message` (`send_message` is a reflex `action_kind` name, a different thing) — matters for `09`. "Slot" already means something else and is load-bearing (`internal/context/slot.go`) — `01` must spell out "Team Slot" in every new identifier, never bare "slot."
- **Phase 1 (`01`-`05`) is schema/storage only, four of five tasks mutually parallel-safe** (different tables/columns — `02`, `03`, `05` have no dependency on each other or on `01`; only `04` has a real, if soft, dependency on `01`'s Team Slot vocabulary). Dispatch the parallel-safe four in separate worktrees via `isolation: "worktree"`. **All five touch `internal/store/migrations/` — this is the real cross-batch (and now cross-task, within-phase) numbering collision risk flagged in the README.** Coordinate numbering across all five as they land, not just against sibling batches: `ls internal/store/migrations/` immediately before each migration-writing task starts, and again immediately before each one actually writes its file, since four of these may be in flight concurrently.
- **Phase 2 (`06`-`08`) is a strict serial chain, not parallel-safe despite Phase 1 being mostly parallel.** `06` (flex-step executor) needs `03`'s schema. `07` (the Team compiler) needs *every* Phase 1 primitive plus `06`'s real config shape — do not start it until all of `01`, `02`, `03`, `04`, `05`, `06` are reviewed-clean, not just landed. `08` (the run launcher) needs `07` and `04` (authority schema, for slot-resolution launch config).
- **Phase 3 (`09`) needs both `05` (run-scoped reflexes) and `08` (a real launched run to route against)** — don't dispatch it against a stubbed or partially-landed launcher.
- **Phase 4 (`10`-`11`) is the operator-facing surface, dispatched last** — `10` needs only `01`, `11` needs both `08` and `10`.
- **Two required stress-test answers — real "Done means" items on their owning tasks, not optional design commentary.** The design doc's own "Validating this design" section names these as "likely the messiest runtime edges of the whole design," deserving concrete implemented-and-tested answers, not a passing mention: the **phase-closure race** (a flex step's exit trigger fires while other active Team Slot members are still working) is owned by `06` — confirm its worker actually implements and tests a real resolution, not just documents the edge case exists. **Routing-target resolution failure** (a message routes to an unavailable/failed slot member) is owned by `09` — same standard. If either task's Work Log only *describes* the edge case without a real handled-and-tested behavior, that's a real review finding, not a pass.
- **The README's "What this batch does NOT do" list is a real scope fence, not a suggestion**: mid-run elastic slot growth (`08` resolves `min`/`max` members eagerly at launch only — there's a concrete current blocker, `task_execute`'s hard recursion-depth-0 cap, not just caution, since a Team orchestrator slot triggering growth is itself typically a non-root session), the final authority verb set beyond `may_spawn`/`may_message`/`may_not_review` (`04` builds a schema that absorbs a future verb as a CHECK widening, doesn't add one), a locked default for multi-member `@slot` addressing (`09` picks a working provisional default — broadcast to all active members — and flags it as such), and the eventual Team/Workflow definition split (`01`/`07` keep the phase sequence as its own addressable sub-structure per the design doc's forward-compat instruction, but do not build the actual split). A worker or reviewer finding "it'd be easy to also do X here" is not grounds to expand scope — that needs a fresh operator conversation.
- **The design doc is a locked decision.** If a worker finds real code that contradicts something `15-teams.md` states as fact, or a task file's Context section turns out to be stale, correct the record and proceed with the design's actual decision — finding "this doesn't quite match reality" is not grounds to reopen whether the design itself should still apply (this is exactly what the five corrections above already did once, during planning — don't re-litigate them, extend the same discipline to anything new you find). The one thing that **is** grounds to stop and escalate to the operator is a genuine surprise: reality being vastly different from what the design doc or a task file claims as fact, in a way that changes what's safe to build.
- **Schema migration testing**: every Phase 1 task must be tested against a **real backup copy** of the database (`~/.local/share/nanite/workspaces/default/backups/`), never just an empty fixture, per `EXECUTION-PROCESS.md`'s schema-migration testing requirement — this batch adds five new migrations against a live-seeded schema (including `workflow_run_steps`' existing CHECK constraint, which `03` widens).

Work straight through Phase 1's parallel-safe four (`02`, `03`, `05`, plus `01` before `04`) in worktrees, then Phase 2's strict chain (`06`→`07`→`08`) once Phase 1 is fully reviewed-clean, then Phase 3 (`09`), then Phase 4's two API tasks (`10` can start once `01` lands, `11` waits on `08`+`10`). Get sections reviewed by a fresh reviewer once validated, use research-auditor liberally to verify anything before trusting it — especially the migration-numbering state, since up to four tasks may be writing new migrations in the same window. Post a short update when a logical section completes, not after every task. At the end of the batch (once `01`-`11` are reviewed and closed), dispatch doc-writer for the handoff and summary docs, then stop — the operator reviews both before deciding what's next.
