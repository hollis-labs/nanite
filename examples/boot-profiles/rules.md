# Rules slot — example boot-profile catalog

This file is loaded by the `static` slot source in `claude-smoke.yaml`. It is
read verbatim at compile time and substituted into the rendered boot prompt
under the `## rules` section.

- Stay focused on the smoke task.
- Prefer minimal output.
- Do not fabricate tool results.
