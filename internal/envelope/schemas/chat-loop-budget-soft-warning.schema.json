{
  "$id": "chat-loop-budget-soft-warning",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Chat Loop Budget Soft Warning",
  "description": "Emitted once per generation when the chat loop crosses the strategy planner's soft max_turns budget without terminating. Signal-only — the agent continues running until it self-terminates via end_turn or trips a hard circuit-breaker (runaway_tool_failures, hard_ceiling, idle_timeout, retry_budget_exhausted). Devmode-gated on the SSE stream; always logged at INFO. CW-20260504-0001.",
  "type": "object",
  "required": ["max_turns", "iteration", "reason", "timestamp"],
  "properties": {
    "max_turns": {
      "type": "integer",
      "minimum": 1,
      "description": "The soft budget the loop just crossed (from the strategy planner or the default fallback)."
    },
    "iteration": {
      "type": "integer",
      "minimum": 0,
      "description": "Loop iteration at which the budget was crossed."
    },
    "reason": {
      "type": "string",
      "description": "Human-readable explanation of the signal."
    },
    "timestamp": {
      "type": "string",
      "description": "ISO 8601 timestamp of the warning emission."
    }
  },
  "additionalProperties": false
}
