[
  {
    "title": "Pre-flight a report-card payload (valid)",
    "args": {
      "tool_name": "card_show",
      "args": {
        "type": "report-card",
        "data": {
          "title": "Sprint summary",
          "metrics": [{"label": "Tickets done", "value": "5"}],
          "sources": []
        }
      }
    },
    "result": {"valid": true, "errors": []},
    "notes": "Returns valid=true when args match both the top-level input schema and (for card_show) the per-type envelope schema."
  },
  {
    "title": "Catch the wrong-shape report-card mistake from incident c107",
    "args": {
      "tool_name": "card_show",
      "args": {
        "type": "report-card",
        "data": {"sections": [{"heading": "x"}]}
      }
    },
    "result": {
      "valid": false,
      "errors": [
        {
          "path": "/data",
          "reason": "additional properties 'sections' not allowed",
          "suggestion": "allowed: title, metrics, summary, actions, generated_at"
        }
      ]
    },
    "notes": "Use this BEFORE calling card_show to avoid the c107-class failure mode (10 retries on the same wrong shape)."
  },
  {
    "title": "Unknown tool name",
    "args": {"tool_name": "nanite_does_not_exist", "args": {}},
    "result": {
      "valid": false,
      "errors": [{"path": "", "reason": "unknown tool 'nanite_does_not_exist'"}]
    },
    "notes": "When the tool isn't registered, validate fails with a single top-level error."
  }
]
