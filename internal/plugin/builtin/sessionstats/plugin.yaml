schema_version: 1
name: session-stats
id: session-stats
version: 1.0.0
description: Session statistics tracking and display plugin
author: Nanite
license: Apache-2.0
protocol: 1
runtime: builtin
# NOTE: session-stats registers UIComponents with Handler functions (HTTP
# routes). The yaml-authoritative loader does not yet express Handler, so
# these components stay in Load() via direct host.RegisterUIComponent calls
# until ComponentRegistration grows a handler / http_route surface.
# See backlog: BLG-20260413-013 (component display_name + handler gap).
