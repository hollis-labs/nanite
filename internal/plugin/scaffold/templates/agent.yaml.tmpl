id: {{.PluginID}}-001
name: {{.PluginName}}
slug: {{.PluginID}}
description: >-
  {{.Description}}
default_model: claude-sonnet-4-20250514
default_mode: default
can_execute: false

mcp_servers:
  - cortex

tool_permissions:
  allow:
    - "mcp__cortex__*"

system_prompt: |
  You are the {{.PluginName}} agent. Help users with tasks related to {{.PluginID}}.

  ## Rules

  - Be concise and helpful.
  - Use available tools when appropriate.
