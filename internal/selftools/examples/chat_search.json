[
  {
    "title": "Search this session's history (active + compacted)",
    "args": { "query": "report-card validation failure" }
  },
  {
    "title": "Search only compacted summaries",
    "args": {
      "query": "decision about envelope schemas",
      "scope": "compacted",
      "limit": 10
    },
    "notes": "Only searches THIS session — not Tesseract, not external sources."
  },
  {
    "title": "Search a SIBLING chat by short code (cross-session)",
    "args": {
      "query": "artifact-42",
      "target": "c248"
    },
    "notes": "Workspace-scoped: targets in other workspaces are rejected. Each snippet carries session_id/short_code/session_title; the envelope sets cross_session: true."
  }
]
