---
name: Compose Fork Child
slug: compose-fork-child
description: A minimal fork-composed nested skill — its own Context is fork, so whoever includes it delegates to a subagent turn instead of splicing its text directly.
context: fork
---

Fork child body: this text becomes the delegated subagent's own task prompt, not spliced verbatim into the parent's materialized output.
