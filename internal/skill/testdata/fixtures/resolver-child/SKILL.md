---
name: Resolver Child
slug: resolver-child
description: A minimal nested skill, installed independently so the resolver dependency-address test can resolve a real dependency against it.
---

This is the resolver-child skill's body. It declares no parameters and no
further dependencies — resolver-parent's own SKILL.md declares this
package as its one dependency.
