#!/bin/sh
# Fixture script — never executed by TASKS/skills/04's own tests (that's
# TASKS/skills/08's job). Exists only so the vendored/validated file tree
# has real content under scripts/.
echo "sample-skill: run"
