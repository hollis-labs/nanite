# Support Plugin — Moved

This plugin has been extracted to a standalone location:

    /plugins/support-ticket/

The Conduit build imports it via symlink at `conduit/plugins/support-ticket`.

See the plugin README at `../../plugins/support-ticket/README.md` for details.
