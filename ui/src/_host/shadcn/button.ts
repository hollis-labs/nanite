// Host re-export for @nanite/ui/button. Plugin bundles resolve this bare
// specifier via the importmap so they share the host's Button primitive
// instead of bundling their own copy.
export * from '@/components/ui/button'
